# RV3 — Code review: multi-hub phase 1 (node-side failover pool + hub-side active/standby)

You are a strict but pragmatic code reviewer with a security mindset. Read-only. Output the full review as your FINAL MESSAGE.

Repo: `/Users/konata/code/tmex-enhanced-wt-r13` (Bun monorepo). Diff under review: `/Users/konata/code/tmex-enhanced-wt-r13/prompt-archives/2026090104-round13-upgrade-multihub/sub/RV3-diff.patch` (commits `14f5f1dd` foundation, `d0d793cd` hub side, plus the uncommitted node side = `git diff HEAD -- apps/gateway/src/mesh` at the time the patch was produced). Specs: `sub/G1-prompt.md`, `sub/G2-prompt.md`, `sub/G3-prompt.md`; author reports: `sub/G1-result.md`, `sub/G2-result.md`, `sub/G3-result.md`; plan: `plan-00.md` §目标 3. Architecture and threat model: `docs/hub/2026082700-hub-node-architecture.md` (hub is NOT a trust root; compromise of any single point must only affect that point).

Design being implemented (phase 1): hubs are `hub,node` processes in `active` (single writer) or `standby` mode; nodes advertise their hub role in `node.status.hub`; the hub broadcasts `node.list.hubs[]` + `writerHubId/writerEpoch`; every node persists the set in `mesh_hubs` and an `UplinkPool` fails over in order (active by epoch desc → standby by priority), probes the preferred hub every 60 s and switches back make-before-break; a standby replicates the registry from the `node.list` it receives and rejects writes with `409 HUB_NOT_WRITER`; an active hub seeing a higher-epoch active demotes itself.

Review for:
1. **Security**: can a compromised ordinary node (which can send arbitrary `node.status`) inject itself as a hub / become writer / demote the real writer via a forged advertisement with a high `writerEpoch`? (This is the most important question — hub advertisements are accepted from any authenticated node; assess whether that violates the threat model and propose the minimal fix, e.g. only accept advertisements whose `hubNodeId` is already in the writer's configured/known hub set, or require the node's cert to carry hub capability, or restrict fencing to advertisements from hubs the writer itself previously listed.) Also: CA-pin bootstrap for a new standby URL (fetching `/api/tls/ca.crt` with `rejectUnauthorized:false` and verifying against a fingerprint that arrived over the authenticated uplink) — is the fingerprint comparison correct and is there any path where an unauthenticated fingerprint gets trusted? `HUB_NOT_WRITER` body leaks — fine or not?
2. **Failover correctness**: generation guard coverage (all inbound handlers?), make-before-break ordering, duplicate `node.status`, probe storms, backoff after all candidates fail, stale `node.list` from old link overwriting `mesh_hubs`, dual-role process attaching to itself, `switchTo` races with an in-progress connect, key-log catch-up cancellation, relay diagnostics.
3. **Replication correctness**: `applyReplicatedNodeList` — self-sourced lists ignored, cert gating, `replaceAll` not evicting the own row, online flags, absent nodes; standby accepting `key.log.append` (chain-extending) — any fork risk?
4. **Compatibility**: old 1.1.5 nodes (decoder ignores unknown keys — verified), old hub without `hubs[]` (legacy synthesis), `peer_cache` `node_id='hub'` sentinel still consistent, `/api/auth/mode` writer fields when store is empty.
5. **Tests**: are the important scenarios covered or only happy paths?

Classify findings as **blocker / should-fix / nit** with file:line and a concrete failing scenario. Be concise; if something is fine, say so briefly. No large refactors.
