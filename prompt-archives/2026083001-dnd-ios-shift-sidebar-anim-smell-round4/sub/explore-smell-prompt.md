You are a read-only code explorer for a Bun + TypeScript monorepo (tmex: apps/gateway = backend, apps/fe = React frontend, packages/* = shared libs). Do NOT modify files. Output a concise technical markdown report to stdout with file:line references.

Goal: plan a "round 4" code-smell cleanup. Three earlier rounds already ran; the retained-by-design hotspots that must NOT be proposed again are: `emitOsc`, `encodeMouseEvent`, `classifySshError`, control-mode `parse`, `dispatchPaneStreamByte`, `scan-managed-artifact`, `run-managed-smoke`, `sanitizeBunPath`, `runInit`, `gesture-machine`, `createWatchRuleDraft`, `parseIpv6ToBytes`, `decodeLegacyStateSnapshotDiff`, `DeviceRow`, and the files local-external-connection.ts, agent/supervisor.ts, canonical-feed-session.ts, external-tmux-core.ts, ws-client/state-machine.ts, api/agent.ts, messaging-routes.ts, tmux-command-handlers.ts, ws-client/client.ts, sidebar-agent-sessions.tsx, agent-session-actions.ts, tool-call-card.tsx.

A fresh complexity scan (McCabe per function, nested functions counted separately) is at `/Users/konata/code/tmex-enhanced/prompt-archives/2026083001-dnd-ios-shift-sidebar-anim-smell-round4/sub/cc-baseline.txt` — read it first. The newly merged hub/mesh code (apps/gateway/src/mesh/*, apps/gateway/src/hub/*, apps/gateway/src/auth/user-key-service.ts, packages/app/src/commands/{hub,enroll,init}.ts, packages/app/src/runtime/assemble.ts, apps/fe/src/auth/*, apps/fe/src/pages/settings/nodes/*, packages/ws-client/src/direct/*) was never cleaned and is where most of the CC>20 functions and 1000+ line files live.

The user's priorities: (a) reduce cyclomatic complexity of very large functions, (b) split oversized files (>900 lines) ONLY where a cohesive seam exists, (c) shorten very long functions, (d) fix real bugs found along the way, and (e) most importantly: REDUCE total line count — look for duplicated code across files (e.g. `apps/gateway/src/mesh/uplink-protocol.ts` vs `apps/gateway/src/hub/uplink-protocol.ts` both have `decodeUplinkCtl`; `apps/gateway/src/mesh/rtc/fragmenter.ts` vs `packages/ws-client/src/direct/fragmenter.ts`; `link-stream-carrier.ts pump` vs `websocket-link.ts pump`; copy-pasted validation/error-mapping helpers in mesh-routes/auth-routes/hub-runtime; repeated try/catch → error-shape mapping; dead exports; unused files). Use grep to detect exported symbols with zero importers outside their own file.

Deliver:
1. A ranked list (max ~25 items) of concrete refactor candidates. For each: file(s):line, function, current CC/lines, the smell, the proposed change in 1–3 sentences, estimated net line delta (negative preferred), risk (low/med/high), and which tests cover it (`*.test.ts` nearby). Group into BACKEND (apps/gateway, packages/app, packages/shared node-side) and FRONTEND (apps/fe, packages/panels, packages/ui, packages/stores, packages/ws-client browser side) so they can be assigned to separate coding agents whose file sets must not overlap. Mark items that touch the same file so they can be bundled.
2. A list of duplicate-code pairs with the shared abstraction you'd extract and where it should live.
3. Any bugs you notice while reading (wrong error handling, unawaited promises, leaked listeners, race conditions), with evidence.
4. Dead code: exported symbols / files with no importers (verify with grep, be careful about barrel `index.ts` re-exports and dynamic imports).

Keep the report under ~400 lines. Be concrete; no generic advice.
