- **major** — [device-folder-tree.tsx:151](/Users/konata/code/tmex-enhanced-wt-merge/packages/panels/src/device-folders/device-folder-tree.tsx:151)、[folder-tree-model.ts:158](/Users/konata/code/tmex-enhanced-wt-merge/packages/panels/src/device-folders/folder-tree-model.ts:158)：分组头注册了 `drop:folder:*` 放置区，碰撞检测又优先返回该放置区；但分组拖拽仅接受 `folder:*`，因此拖到另一个分组头时 `resolveDrop()` 返回 `null`。键盘传感器也会在嵌套节点、放置区和分组之间选目标，方向键很容易落到同样无效的 ID，导致分组键盘排序失效。修复：碰撞候选必须按 active 类型过滤；拖动分组时只保留根层 `folder:*` sortable，拖动节点时才启用节点和 `drop:*`。补充真实指针与键盘交互测试。

- **major** — [device-card-connect-toggle.tsx:30](/Users/konata/code/tmex-enhanced-wt-merge/packages/panels/src/device-management/device-card-connect-toggle.tsx:30)：离线态只把残留的 `connected` 改成 `disconnected`，残留的 `connecting` 或 `reconnecting` 仍映射为禁用的 `pending`。运行时被刻意保留后，这些状态可能一直存在，使离线卡片无法执行要求中的手动连接。修复：节点进入离线态时清理瞬时连接/重连状态，或显式区分“离线前残留状态”和“离线后用户发起的尝试”；前者统一显示可点击的 `disconnected`，后者才展示 `connecting/error/reconnecting`。

- **minor** — [device-card-host.tsx:33](/Users/konata/code/tmex-enhanced-wt-merge/packages/panels/src/device-management/device-card-host.tsx:33)：编辑或删除对话框在节点在线时打开后，若节点转为离线，`editing`/`deleteCandidate` 不会被清除；虽然菜单项已禁用，现有对话框仍能向离线节点提交更新或删除请求。[device-management-panel.tsx:403](/Users/konata/code/tmex-enhanced-wt-merge/packages/panels/src/device-management/device-management-panel.tsx:403) 的新增对话框还会在恢复在线后意外重新打开。修复：`offline` 变为 `true` 时关闭并清空所有增删改对话框，同时在提交入口再次校验离线状态。

- **minor** — [device-management-panel.tsx:283](/Users/konata/code/tmex-enhanced-wt-merge/packages/panels/src/device-management/device-management-panel.tsx:283)：`reorderDisabled` 只传给 `useSortable`，`handleDragEnd` 本身没有守卫。若拖拽开始后节点离线或已有排序请求进入 pending，拖拽结束仍会发送 `PUT /api/devices/order` 并执行乐观更新。修复：在 `handleDragEnd` 内检查 `offline || reorderMutation.isPending`，状态切换时最好同时取消当前拖拽。

- **minor（缺少测试）** — [device-management-panel.tsx:232](/Users/konata/code/tmex-enhanced-wt-merge/packages/panels/src/device-management/device-management-panel.tsx:232)：新增的面板级行为没有对应测试；现有测试只静态渲染卡片，未覆盖设备网格排序请求及回滚、离线缓存优先级、拖拽中离线竞态，以及 `animationend`/1.5 秒兜底移除 `tmex-stagger`。修复：增加带 DOM、fake timer、QueryClient 和 mock API 的 `DeviceManagementPanel` 测试，并覆盖嵌套 `DndContext` 下内层拖拽不会触发外层排序。