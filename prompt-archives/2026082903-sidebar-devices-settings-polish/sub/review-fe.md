You are a senior code reviewer (read-only; repo root is cwd, read any source you need). Review the diff in `prompt-archives/2026082903-sidebar-devices-settings-polish/sub/devices-fe.patch` together with the new untracked files apps/fe/src/pages/devices/device-snapshot-store.ts(.test.ts) and page-commands.ts(.test.ts). Context: the devices management page was reworked — folders became single-level "groups" whose only members are runtime nodes (placements are node-only, folders have no parent), a node's devices always render under their node and reorder only within it via PUT /api/devices/order, a migration 0025 flattens old data, a new POST /api/device-folders/reset endpoint restores the default layout; offline nodes now keep rendering device cards from a localStorage snapshot / server inventory with a manual connect action; the connect toggle got a stable pending state (connecting/disconnecting) to stop flicker; page width container unified. Focus: apps/fe pages/devices + global-device-provider + device-connection-status/intent-store. Check the snapshot store (what is persisted, quota, stale entries per node, nothing secret), the PendingConnectionRequests settlement timing hooks (min 350ms / max 8s) for leaks and stuck pending states, the NodeRuntimeScope kept mounted across ready↔offline, and the reset-layout mutation + confirm dialog.
Report only real defects: runtime bugs, data loss or migration hazards, validation bypasses, race conditions, broken DnD interactions, React hook misuse (stale closures, effects that loop), memory/storage leaks, credentials persisted to localStorage, i18n keys used but not defined in the locale JSON (check packages/shared/src/i18n/locales/zh_CN.json), and missing tests for changed behavior. For each finding: severity (blocker/major/minor), file:line, why, concrete fix. No style nits. Simplified Chinese, under 120 lines.
