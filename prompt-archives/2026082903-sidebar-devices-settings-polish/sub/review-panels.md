You are a senior code reviewer (read-only; repo root is cwd, read any source you need). Review the diff in `prompt-archives/2026082903-sidebar-devices-settings-polish/sub/devices-panels.patch`. Context: the devices management page was reworked — folders became single-level "groups" whose only members are runtime nodes (placements are node-only, folders have no parent), a node's devices always render under their node and reorder only within it via PUT /api/devices/order, a migration 0025 flattens old data, a new POST /api/device-folders/reset endpoint restores the default layout; offline nodes now keep rendering device cards from a localStorage snapshot / server inventory with a manual connect action; the connect toggle got a stable pending state (connecting/disconnecting) to stop flicker; page width container unified. Focus: packages/panels device-folders DnD model + device-management components. Check nested DndContext id collisions between group/node sorting and the per-node device grid, drop-zone resolution, keyboard sorting, the offline card rendering and disabled actions, the connect toggle pending state, and the tmex-stagger class removal.
Report only real defects: runtime bugs, data loss or migration hazards, validation bypasses, race conditions, broken DnD interactions, React hook misuse (stale closures, effects that loop), memory/storage leaks, credentials persisted to localStorage, i18n keys used but not defined in the locale JSON (check packages/shared/src/i18n/locales/zh_CN.json), and missing tests for changed behavior. For each finding: severity (blocker/major/minor), file:line, why, concrete fix. No style nits. Simplified Chinese, under 120 lines.
