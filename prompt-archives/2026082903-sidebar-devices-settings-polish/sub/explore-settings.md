You are a read-only code explorer for the tmex monorepo (Bun, React, TypeScript; packages under apps/fe, packages/panels, packages/ui, packages/theme, packages/shared, packages/stores, packages/gateway). Do NOT modify files. Produce a concise but precise technical map in Markdown: exact file paths, component names, relevant line ranges, the CSS classes / Tailwind utilities that control the concern, state/hook flows, existing tests that would need updating, and i18n keys (locale JSON paths). End with "Recommended change points" listing where each requested change should be made. Keep the answer under ~250 lines. 
Focus: the SETTINGS page (apps/fe/src/pages/settings/*) — specifically (a) the "nodes" tab label ("节点"/"Nodes") that should be renamed to "多节点互联" (multi-node interconnect) — list all locale keys and any other places (sidebar buttons, page titles, breadcrumbs) that show this label; and (b) the notification settings tab (notification-settings-tab.tsx): describe its layout — the 4 toggle rows at the top ("enable push notifications" etc.) and the 4 inputs below. Give the exact JSX structure and classes so they can be re-laid out as a 2-column grid for the toggles and a single row of 4 inputs (responsive: collapse on narrow widths). Note the form/state hooks used and tests to update. Also check whether shared form-row components in packages/ui/panels are used, to keep the change consistent.
