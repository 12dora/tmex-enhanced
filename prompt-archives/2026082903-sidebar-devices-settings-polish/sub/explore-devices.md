You are a read-only code explorer for the tmex monorepo (Bun, React, TypeScript; packages under apps/fe, packages/panels, packages/ui, packages/theme, packages/shared, packages/stores, packages/gateway). Do NOT modify files. Produce a concise but precise technical map in Markdown: exact file paths, component names, relevant line ranges, the CSS classes / Tailwind utilities that control the concern, state/hook flows, existing tests that would need updating, and i18n keys (locale JSON paths). End with "Recommended change points" listing where each requested change should be made. Keep the answer under ~250 lines. 
Focus: the DEVICES management page (apps/fe/src/pages/devices/*, packages/panels/src/device-folders/*, packages/panels device card components, packages/shared/src/device-folders.ts, gateway routes /api/device-folders, DB tables device_folders / device_folder_placements) and device connection state.
Questions to answer:
1. Device cards for remote runtime nodes: when the mesh link to a node goes down (offline), what happens to the cards — are they removed from the list? Trace the data source (stores: runtime nodes list, node status/online flag, remote devices fetched via node scope). Where would we keep showing the last known devices with an "offline" state? Is there any cached device list (device-name-cache.ts?) or server-side persisted list of remote devices?
2. The connect/disconnect toggle on a device card (useGlobalDevice().connection): why might the button "flash"/flicker when clicked? Look at optimistic state, loading spinner, motion/animate classes, key changes causing remount, and how connection state transitions.
3. Folder model: currently nested folders where nodes, devices and folders can be placed anywhere. Describe the data model (item_key node:<id>/device:<node>:<dev>/self, parent folder, order), the DnD implementation (collision, auto-expand, keyboard), and what would need to change to enforce: exactly one level of folders ("groups"), a device is always bound to its node (a node's devices render under the node; only whole nodes can be moved between groups/root; devices can only be reordered within their own node). Identify the validation points in packages/shared/src/device-folders.ts and the gateway that would reject an invalid layout.
4. Layout/width: the device page container width is sometimes "normal" and sometimes full-screen width. Find the container classes (max-w, w-full, grid) and any conditional that could cause it (e.g. loading vs loaded states, empty state, different wrappers). Also list where horizontal padding wastes space at wide/narrow widths.
5. i18n: where do the strings "文件夹"/"Folder", "节点 xxx 上的本机设备" (local device on node xxx), "本地设备" live (locale JSON key paths) and the runtime scope that decides local vs remote.
6. Is there an existing "reset layout"/delete-all API? Which API call would restore the default layout (e.g. PUT /layout with empty folders)?
