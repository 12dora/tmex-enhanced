# RV3 — Final review round (read-only). Output the full report as your final message.

Worktree `/Users/konata/code/tmex-enhanced-wt-r14`, branch `feat/round14-multihub-phase2-nodes-mgmt`. Review the work committed since the previous round (diffs, tests/i18n excluded: `/private/tmp/claude-501/-Users-konata-code-tmex-enhanced/c87e7d41-4167-4f04-b03f-99760894dfcc/scratchpad/rv3-backend.diff`, `/private/tmp/claude-501/-Users-konata-code-tmex-enhanced/c87e7d41-4167-4f04-b03f-99760894dfcc/scratchpad/rv3-frontend.diff`); open surrounding sources to verify. Threat model: any single compromised point affects only itself; hub is not a trust root; an ADMITTED hub is deliberately granted hub-control-plane power (documented trade-off — do not re-report it); node sessions equal full control of that node (documented — do not re-report). Also do not re-report: localStorage multi-tab TOCTOU (documented limitation), 2-hub auto-promote split-brain (documented, opt-in).

New work to review:
1. Hub-to-hub relay: attachment router, `hub.attachments`/`hub.forward` frames, `hub-relay` cross-hub streams, `attachedHubId` (apps/gateway/src/hub/attachment-router.ts, hub-relay.ts, uplink-server.ts, rtc/signaling.ts, shared codec).
2. RV2 fixes: write forwarding via authenticated uplink frame `hub.write-forward` (credentials stripped; rename uid asserted by authorized standby), retired-hub exclusion everywhere, hub.tokens writer-only + user-scoped + entry_sid stripped + paginated, force-keylog end-to-end, server-side epoch allocation in POST /api/hub/role.
3. G6: TLS-applied → immediate re-advertise (packages/app/src/tls/tls-service.ts, assemble.ts, mesh-runtime.ts, uplink-server updateSelfCaFingerprint); opt-in auto-promote with writerView quorum (hub-peer-poller.ts); RTT-preferred attach in uplink-pool.ts (EWMA, hysteresis, dwell, legacy exclusion).
4. FE role-switch flow (apps/fe/.../use-hub-role-switch.ts + dialog): server-allocated epoch, 4-phase sessionStorage resume, forced recovery dialog (retry/rollback), UUID fallback.

Focus: correctness bugs, protocol races (failover mid-relay, stale attachment routes, forwarded-write replay), frame-size/DoS limits, fencing/epoch correctness incl. auto-promote interactions with manual role switch, TLS event ordering, and FE/BE contract mismatches. Findings only (blocker/should-fix/nit, file:line, why, concrete fix). Terse.
