# Common rules for every coding agent on this worktree

- Worktree: `/Users/konata/code/tmex-enhanced-wt-r14` (branch `feat/round14-multihub-phase2-nodes-mgmt`). Runtime is **Bun** (`bun`, `bunx`); Node only for `packages/app` CLI. If `bun` is missing from PATH, source `~/.zshrc` PATH.
- **Other agents are editing this same worktree in parallel. Touch ONLY the files listed in your scope. Never run `git add/commit/stash/checkout/reset`.** The commander commits.
- Read `AGENTS.md` at the repo root first and follow it (Chinese comments only where logic is non-obvious; no unnecessary comments; no TODOs, no stubs, no "simple version first").
- **Never touch the production tmex**: do not read/write `~/Library/Application Support/tmex/`, do not curl port 9883, do not kill/restart launchd services, do not touch the tmux session named `tmex` or the default tmux socket. Tests must use temp dirs and free ports.
- Never lint/format generated files (`packages/shared/src/i18n/resources.ts`, `types.ts`, `resources/fe-dist/*`, `dist/*`).
- Look up library APIs in `node_modules` source before using them; do not guess.
- TDD: write/extend tests alongside the implementation. Before finishing run, in the package you changed: `bun test <dir>` (in `apps/fe` use `bun test src/...`, never bare `bun test`), `bunx tsc --noEmit -p .` (error count must not exceed the baseline given in your task), `bunx biome check <changed files>` (fix with `--write`). macOS has no `timeout` command; strip ANSI from bun test output with `sed 's/\x1b\[[0-9;]*m//g'`.
- When done, write a concise result report (what changed, file list, test/tsc numbers, anything left) to the absolute result path given in your task, **then exit**. Write the file only when finished.

# O1b — Frontend review fixes (nodes management + tunnel model)

Result file: `/Users/konata/code/tmex-enhanced-wt-r14/prompt-archives/2026090200-round14-multihub-phase2-nodes-mgmt/sub/O1b-result.md`

A code review (`prompt-archives/2026090200-round14-multihub-phase2-nodes-mgmt/sub/RV1-result.md`) of the committed FE work produced these findings; fix exactly these. Read `sub/O1-result.md` / `sub/O2-result.md` for what was built. Copy rules: `/Users/konata/code/tmex-copy-guidelines.md`; locale edits under `translation.nodes.*` / `translation.settings.remoteAccess.*`, then `bun run --filter @tmex/shared build:i18n`.

1. **The entry itself can no longer be batch-upgraded** (`apps/fe/src/pages/settings/nodes/management/nodes-management.tsx` bulk menu; the user requires the self row to stay non-selectable): make the「升级」menu item upgrade the selected rows **plus the entry itself** whenever the entry is upgrade-eligible (`upgradeBlockReason(selfRow, latest) === null`), so the persisted batch ordering (others → hub → self) and the entry-restart resume path stay reachable from the UI. Show it in the label/tooltip: 「升级（{{count}}，含本机）」 when self is included, plain count otherwise. The item is enabled when eligible count (incl. self) > 0 even if nothing is selected but self is upgradeable? — No: keep "须先勾选节点" when the selection is empty; self is only appended to a non-empty selection. Update `bulkMenuStates` + tests.
2. **Uninstall must require a writable hub** (`nodes-management.tsx` bulk menu state + `use-node-uninstall.ts`): the flow ends with a signed `revoke-node` through the writer; when `!hubOnline || writesBlocked` the menu item is disabled with the existing hint (`nodes.hubOffline` / `nodes.hubs.standbyNotice`), and `runUninstallBatch` re-checks writability before each node (abort the rest with a toast if the hub became unavailable mid-batch; already-uninstalled nodes keep their `uninstalling` operation record so the user can revoke later). Tests.
3. **Batch start when another tab owns a live plan** (`use-node-upgrade.ts` `startAll` + `upgrade-batch-storage.ts`): before starting a new batch, load the plan; if a plan exists whose `ownerTabId` differs and `updatedAt` is fresher than 30 s, refuse with an info toast 「另一个标签页正在批量升级。」 (new key `nodes.upgrade.allOtherTab`). Also make the resume check re-run when the `storage` event fires for our key (so a tab that saw `null`/foreign owner earlier can adopt after the owner disappears). Tests.
4. **`connector.reachable === false` must not read as "no connections"** (`apps/fe/src/pages/settings/remote-access/tunnel-model.ts` `connectorState`, `apps/fe/src/components/side-panels/connect-devices/host-status.ts`): `false` = metrics endpoint unreachable → `unknown`; only backend `process.state === 'degraded'` or `reachable === true && readyConnections === 0` count as degraded/noConnections. Adjust copy for the unknown state if needed (「无法探测」 already exists). Tests.
5. **api-client type**: add `authorization?: 'signed' | 'env' | 'self'` to the hub entries of `MeshHubsResponse` in `packages/api-client/src/auth/types.ts` (the backend `GET /api/mesh/hubs` now emits it; keep `HubEndpointInfo` from `@tmex/shared/uplink` untouched — use an intersection type for `hubs: Array<HubEndpointInfo & { authorization?: ... }>`), pass it through in `auth-api.ts` `listHubs()`, show it in `HubStrip` chip title as a third line 「授权：已签名 / 环境变量 / 本机」 (keys under `nodes.hubs.authorization.*`).

Baselines: `cd apps/fe && bun test src/` 1331 pass / 0 fail, tsc 0; `packages/api-client` `bun test` 140, tsc 5 pre-existing. Scope: `apps/fe/src/pages/settings/nodes/management/**`, `apps/fe/src/pages/settings/remote-access/tunnel-model.ts` (+test), `apps/fe/src/components/side-panels/connect-devices/host-status.ts` (+test), `apps/fe/src/node/mesh-hubs.ts` (+test) if needed, `packages/api-client/src/auth/{types.ts,auth-api.ts,auth-api.test.ts}`, locales. No git.
