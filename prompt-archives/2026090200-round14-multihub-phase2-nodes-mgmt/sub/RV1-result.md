## 审查结果

1. **blocker** — [apps/gateway/src/api/system.ts:79](/Users/konata/code/tmex-enhanced-wt-r14/apps/gateway/src/api/system.ts:79)、[packages/shared/src/contracts/system.ts:118](/Users/konata/code/tmex-enhanced-wt-r14/packages/shared/src/contracts/system.ts:118)：卸载接口没有会话、peer-only 或用户密钥签名校验；凭据弹窗只用于事后吊销。未启用 Access 时，任意能访问 gateway 的客户端可直接 `POST /api/system/uninstall` 删除服务、程序和数据；被攻陷的入口拿到节点会话后也能卸载其他节点。修复：直接请求必须拒绝或要求本地管理会话；远程请求携带绑定目标 nodeId、mode、operationId、过期时间和 nonce 的用户密钥签名，由目标节点验证并防重放，不能依赖卸载后的吊销。

2. **blocker** — [apps/gateway/src/tunnel/manager.ts:554](/Users/konata/code/tmex-enhanced-wt-r14/apps/gateway/src/tunnel/manager.ts:554)：新增 `degraded` 后，移除 Access/关闭 JWT 的“最后保护”检查仍只认 `running`。隧道断边进入 `degraded` 时，无需 `acknowledgeExposure` 即可移除保护；cloudflared 恢复连接后公网入口直接裸露。修复：把 `degraded` 视为存活，统一使用 `processUp()`，并补 degraded 状态下 `remove_access`、`set_access_enforce(false)` 的测试。

3. **blocker** — [apps/gateway/src/tunnel/connector-health.ts:225](/Users/konata/code/tmex-enhanced-wt-r14/apps/gateway/src/tunnel/connector-health.ts:225)、[apps/gateway/src/tunnel/redact.ts:4](/Users/konata/code/tmex-enhanced-wt-r14/apps/gateway/src/tunnel/redact.ts:4)：外部 cloudflared 的原始日志尾部被返回给状态 API/UI，而脱敏只处理长十六进制/base64 和 `token|aud` 查询参数。`password=hunter2`、URL userinfo、短 API token、其他查询参数都会原样泄露。修复：不要返回原始外部日志；只输出白名单结构化诊断。若必须保留日志，至少删除 URL userinfo/全部查询值及常见 secret 键，并覆盖短 token、base64url 等测试。

4. **should-fix** — [apps/fe/src/pages/settings/nodes/management/nodes-management.tsx:295](/Users/konata/code/tmex-enhanced-wt-r14/apps/fe/src/pages/settings/nodes/management/nodes-management.tsx:295)：入口自身不可选，而原“全部升级”按钮已被仅作用于选中行的菜单替代。因此批量升级永远不包含本机，旧行为回归；为本机重启设计的 batch 持久化也无法从 UI 触发。修复：允许 self 参与升级选择，并在移除/卸载动作内单独过滤 self，或恢复独立的“全部升级”。

5. **should-fix** — [apps/fe/src/pages/settings/nodes/management/nodes-management.tsx:385](/Users/konata/code/tmex-enhanced-wt-r14/apps/fe/src/pages/settings/nodes/management/nodes-management.tsx:385)、[apps/fe/src/pages/settings/nodes/management/use-node-uninstall.ts:168](/Users/konata/code/tmex-enhanced-wt-r14/apps/fe/src/pages/settings/nodes/management/use-node-uninstall.ts:168)：卸载不检查 writer 是否可写。hub 离线或当前挂在 standby 时，目标先接受并开始删除，随后吊销必然失败，留下仍有效的节点证书。修复：卸载前及每台开始前重新确认 writer 可用；更稳妥的是由服务端协调“受理卸载 + writer 确认吊销”的事务。

6. **should-fix** — [apps/fe/src/pages/settings/nodes/management/use-node-upgrade.ts:1047](/Users/konata/code/tmex-enhanced-wt-r14/apps/fe/src/pages/settings/nodes/management/use-node-upgrade.ts:1047)、[apps/fe/src/pages/settings/nodes/management/use-node-upgrade.ts:1058](/Users/konata/code/tmex-enhanced-wt-r14/apps/fe/src/pages/settings/nodes/management/use-node-upgrade.ts:1058)：localStorage owner 不是互斥锁。两个标签页可同时启动 batch 并互相覆盖计划；第二页把普通节点的 `UPGRADE_IN_PROGRESS` 当失败后可能提前升级 hub，破坏“普通节点全部完成后才动 hub”的硬顺序。另外，已缓存 `null` 的标签页不会在 owner 心跳过期后重新接管。修复：在后端实现带租约的 batch 互斥；前端启动前必须原子获取租约，并监听 storage/定时重试过期接管。

7. **should-fix** — [apps/fe/src/pages/settings/remote-access/tunnel-model.ts:29](/Users/konata/code/tmex-enhanced-wt-r14/apps/fe/src/pages/settings/remote-access/tunnel-model.ts:29)、[apps/fe/src/components/side-panels/connect-devices/host-status.ts:40](/Users/konata/code/tmex-enhanced-wt-r14/apps/fe/src/components/side-panels/connect-devices/host-status.ts:40)：`reachable=false` 只表示 metrics 端点不可达，不能证明边缘连接为零；前端却将其判为 `degraded/noConnections`，与后端保持 `running` 的语义冲突。阻断 metrics 端口但保持隧道在线即可误报断线。修复：`reachable=false` 映射为 `unknown`；只有后端 `process.state=degraded` 或已成功读取 `readyConnections===0` 才判 degraded。

8. **should-fix** — [apps/gateway/src/tunnel/connector-health.ts:48](/Users/konata/code/tmex-enhanced-wt-r14/apps/gateway/src/tunnel/connector-health.ts:48)、[apps/gateway/src/tunnel/external-detect.ts:394](/Users/konata/code/tmex-enhanced-wt-r14/apps/gateway/src/tunnel/external-detect.ts:394)：外部进程未显式带 `--metrics` 时扫描 20241–20245，并采用第一个响应，没有将 connector 与检测出的 PID/隧道绑定；同时 YAML 中的自定义 `metrics:` 未解析。运行两个 cloudflared 时会读取另一条隧道的健康状态，使 Access 检查产生错误结论。修复：解析配置中的 metrics 地址；无法唯一归属时返回 unknown，不能用任意默认端口宣称已验证。

9. **should-fix** — [apps/gateway/src/mesh/mesh-routes.ts:95](/Users/konata/code/tmex-enhanced-wt-r14/apps/gateway/src/mesh/mesh-routes.ts:95)、[apps/gateway/src/mesh/uplink-pool.ts:1243](/Users/konata/code/tmex-enhanced-wt-r14/apps/gateway/src/mesh/uplink-pool.ts:1243)：uplink 新增的 `rttMs/rttAt` 没有被 `/api/mesh/hubs` 序列化，api-client 永远收不到；同时失败/超时探测也被记录成 RTT，接通后会把超时时长冒充延迟。修复：序列化两个字段；仅成功探测时记录 RTT，失败时清空或增加明确的成功状态。

10. **should-fix** — [apps/gateway/src/tunnel/manager.ts:1303](/Users/konata/code/tmex-enhanced-wt-r14/apps/gateway/src/tunnel/manager.ts:1303)、[apps/gateway/src/tunnel/manager.ts:1358](/Users/konata/code/tmex-enhanced-wt-r14/apps/gateway/src/tunnel/manager.ts:1358)：停止轮询只递增 generation，不取消或隔离已在途的 connector probe。慢探测期间执行 stop/remove 后，完成的旧探测会覆盖刚写入的 `EMPTY_CONNECTOR`，停止状态仍显示旧连接器数据；切换隧道时也可能串入上一进程结果。修复：为探测绑定 generation/进程标识，提交结果前校验；或持有 AbortController，在 stop/remove/restart 时取消并等待在途探测。