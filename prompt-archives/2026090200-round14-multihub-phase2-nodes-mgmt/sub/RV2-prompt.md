# RV2 — Code review round 2 (read-only). Output the full report as your final message.

Same repo/branch as before (`/Users/konata/code/tmex-enhanced-wt-r14`). Review the NEW work committed since the last review. Diffs (tests excluded): `/private/tmp/claude-501/-Users-konata-code-tmex-enhanced/c87e7d41-4167-4f04-b03f-99760894dfcc/scratchpad/rv2-backend.diff`, `/private/tmp/claude-501/-Users-konata-code-tmex-enhanced/c87e7d41-4167-4f04-b03f-99760894dfcc/scratchpad/rv2-frontend.diff`. Open surrounding source to verify. Focus: correctness, races, security under the threat model (any single compromised point affects only itself; hub is NOT a trust root; a compromised ordinary node must not gain hub powers; standby forwarding must not become a confused deputy; no secrets in logs/UI), chain-integrity of the key log, epoch/fencing correctness, refresh-survivability of UI state, and backend↔frontend contract mismatches. Report findings only (severity blocker/should-fix/nit, file:line, why, concrete fix). Terse.

What the new work is:
1. Remote uninstall backend (`apps/gateway/src/system/uninstall.ts`, `api/system.ts`, `mesh/mesh-routes.ts` relay + gateway_kv node-operations, CLI `packages/app/src/commands/uninstall.ts` --delay-ms, detached self-copy).
2. Signed hub authorization: key-log records admit-hub/retire-hub (`packages/shared/src/auth/*`), projection `user_hub_authorizations`, merged `isAuthorizedHub` (`apps/gateway/src/hub/hub-authorization.ts`), compat gate KEYLOG_TYPE_UNSUPPORTED_BY_NODES (min version 1.1.13, force header).
3. Remote role switch `POST /api/hub/role` + `hub_role_transitions` + env write + self restart (`apps/gateway/src/hub/hub-role-routes.ts`, `packages/app/src/runtime/assemble.ts` injection), CLI parity.
4. Standby write forwarding to writer (HTTP with CA pin + credential pass-through + loop header; uplink key.log.append forwarded over writer uplink) and best-effort enrollment-token replication (`hub.tokens` frame, version-gated, revision-idempotent) — `apps/gateway/src/hub/writer-forward.ts`, `hub-tokens.ts`.
5. FE: hub role-switch flow (`apps/fe/src/pages/settings/nodes/management/use-hub-role-switch.ts`, admit-hub signing via POST /api/auth/keylog?hub=sync with force path, sessionStorage resume, restart-window polling), bulk upgrade incl. self, uninstall writability gating, multi-tab batch guard, connector-unknown semantics, api-client hub authorization field.

Pay special attention to: the standby HTTP forwarder passing caller cookies to the writer (scope? could a node session cookie be replayed elsewhere?), the force-keylog path, epoch computation in the FE (max of hubs list vs hubs the entry cannot see), the uninstall relay auth predicate, and `hub.tokens` trust boundaries.
