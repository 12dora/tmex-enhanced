# EX3 — Explore: remote uninstall, batch-upgrade orchestration persistence, hub candidates diagnostics (read-only, report as final message)

You are a read-only code explorer for the tmex monorepo (Bun runtime; `packages/app` = npm `tmex-cli` installer/upgrader, Node-compatible CLI in `packages/app/src/cli/**`, runtime server bundle; `apps/gateway` = server; `apps/fe` = React front-end). Output your COMPLETE report as your final assistant message (you cannot write files). Exact file paths + line numbers. Read; don't guess.

Tasks:

1. **Remote clean uninstall of a node's tmex**: map what uninstall means today. Is there a `tmex uninstall` CLI command in `packages/app/src/cli/**`? What does `init`/install create (launchd plist on macOS, systemd user/system unit on Linux, install dir `~/Library/Application Support/tmex/` or Linux equivalent with `versions/<v>`, `current` symlink, `journal`, `data/tmex.db`, `app.env`, shim `tmex` in PATH, `install-meta.json`)? Map the service-manager abstraction used by the crash-safe upgrader (`packages/app/src/**/upgrade*`, `service-manager*`, `applier`) and the runtime endpoints `POST /api/system/upgrade`, `GET /api/system/upgrade`, `DELETE ...`, `PUT /api/system/upgrade/package`, `GET /api/system/info` (`upgradeCapabilities`) in `apps/gateway/src/**/system*`. Propose `POST /api/system/uninstall {mode:'full'|'keep-data'?}` executed by the running node on itself: sequence (leave mesh first? the node should `hub leave`/revoke itself so the hub marks it gone — map `leave` API from `apps/gateway` `POST /api/mesh/leave` or similar; then spawn a detached uninstaller process from the CLI bundle that stops+unloads the service, removes install dir, shim, data, env; the HTTP response must return before the process dies; the entry calls it via `/n/<nodeId>/api/system/uninstall` or a dedicated `POST /api/mesh/nodes/:id/uninstall` like the upgrade relay). Also how the hub should treat the node afterwards (revoke cert + remove row — map `POST /api/hub/nodes/:id/revoke` and the FE "remove node" action that already exists). Where does the docker-node (manual deploy, no install-meta) fall — return a clear `UNINSTALL_NOT_ALLOWED` code like `UPGRADE_NOT_ALLOWED`. How can the FE observe progress after page refresh (the node will go offline; propose a `uninstalling` marker persisted on the hub row, e.g. in `nodes` table or entry-local store, cleared when the node is revoked/removed or after timeout)?

2. **Batch-upgrade orchestration persistence**: map the FE "upgrade all" orchestration in `apps/fe/src/pages/settings/nodes/management/**` and hooks (`use-node-upgrade*`?): ordering (normal nodes concurrency 3 → remote hub → local), per-row recovery after refresh (`GET /api/mesh/nodes/:id/upgrade` status read-back, AbortController per node), the summary toast, the cancel path. Propose how to persist the batch plan so a refresh resumes orchestration: e.g. `localStorage`/`sessionStorage` key per entry with `{batchId, order[], done[], startedAt}` restored on mount, resuming the not-yet-started stages and re-emitting the summary toast when everything is done; or a server-side batch record on the entry (`POST /api/mesh/upgrade-batch`) — evaluate which is simpler and robust (the entry itself may be the last to upgrade and restart!). Recommend one and list files.

3. **Per-row states that must survive refresh** (downloading, installing, cancelling, uninstalling, role-switching): enumerate current FE state machine for node rows (types, store, where it lives — React state vs a zustand store vs react-query), and the server-side sources of truth for each state. Identify what's missing for "uninstalling" and "switching hub role".

4. **`/api/mesh/hubs` `candidates[].lastError` in UI**: map the endpoint response shape in `apps/gateway/src/mesh/**` and the FE `HubStrip` (`apps/fe/src/pages/settings/nodes/management/hub-strip.tsx`) and its data hook. Propose where to show lastError/lastAttemptAt (tooltip / expandable row).

5. **Nodes table**: describe `nodes-table.tsx` + `nodes-management.tsx` structure (columns, row model, action buttons: upgrade, stop, remove, rename; the "upgrade all" button next to "add"), the shared UI kit available in `packages/ui` (`@tmex/ui/*`: is there a checkbox, dropdown-menu, tooltip, dialog? list exact exports), i18n key structure (`packages/shared/src/i18n/locales/*.json`? how keys for the nodes page are organized and the build step `bun run build:i18n`), and existing test files for this area with the current `cd apps/fe && bun test src/` pass count.

Deliver a structured report per task: "Current (files/lines)", "Proposed", "Files to touch", "Risks". Terse, concrete.
