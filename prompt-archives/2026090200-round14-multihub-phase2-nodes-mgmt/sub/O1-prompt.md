# Common rules for every coding agent on this worktree

- Worktree: `/Users/konata/code/tmex-enhanced-wt-r14` (branch `feat/round14-multihub-phase2-nodes-mgmt`). Runtime is **Bun** (`bun`, `bunx`); Node only for `packages/app` CLI. If `bun` is missing from PATH, source `~/.zshrc` PATH.
- **Other agents are editing this same worktree in parallel. Touch ONLY the files listed in your scope. Never run `git add/commit/stash/checkout/reset`.** The commander commits.
- Read `AGENTS.md` at the repo root first and follow it (Chinese comments only where logic is non-obvious; no unnecessary comments; no TODOs, no stubs, no "simple version first").
- **Never touch the production tmex**: do not read/write `~/Library/Application Support/tmex/`, do not curl port 9883, do not kill/restart launchd services, do not touch the tmux session named `tmex` or the default tmux socket. Tests must use temp dirs and free ports.
- Never lint/format generated files (`packages/shared/src/i18n/resources.ts`, `types.ts`, `resources/fe-dist/*`, `dist/*`).
- Look up library APIs in `node_modules` source before using them; do not guess.
- TDD: write/extend tests alongside the implementation. Before finishing run, in the package you changed: `bun test <dir>` (in `apps/fe` use `bun test src/...`, never bare `bun test`), `bunx tsc --noEmit -p .` (error count must not exceed the baseline given in your task), `bunx biome check <changed files>` (fix with `--write`). macOS has no `timeout` command; strip ANSI from bun test output with `sed 's/\x1b\[[0-9;]*m//g'`.
- When done, write a concise result report (what changed, file list, test/tsc numbers, anything left) to the absolute result path given in your task, **then exit**. Write the file only when finished.

# O1 — Nodes management: multi-select, bulk「更多」menu, remote uninstall UI, hub candidate diagnostics

Result file: `/Users/konata/code/tmex-enhanced-wt-r14/prompt-archives/2026090200-round14-multihub-phase2-nodes-mgmt/sub/O1-result.md`

## Context
Page: 「设置 → 多节点互联（nodes tab）→ 节点管理」 = `apps/fe/src/pages/settings/nodes/management/{nodes-management.tsx,nodes-table.tsx,hub-strip.tsx,types.ts,use-node-row-actions.ts}`, data hooks `apps/fe/src/node/{mesh-nodes.ts,mesh-hubs.ts}`, upgrade state machine `use-node-upgrade.ts` (another agent, O2, is editing `use-node-upgrade.ts`, `upgrade-batch.ts`, `upgrade-batch-storage.ts` and `types.ts` **upgrade-related** types in parallel — do not edit those four files; you only call the existing `NodeUpgradeController` API: `startAll(rows)`, `start(row)`, `cancel(row)`, `batch`, `eligibleCount(rows)`, `anyRunning`, `restoring`, `restoringIds`, `latest`). Backend agent G1 is adding the uninstall API in parallel (`apps/gateway`) — code against the contract below, mock in tests.
**Read `/Users/konata/code/tmex-copy-guidelines.md` before writing any copy.** zh_CN is the source; then en_US, ja_JP; run `bun run --filter @tmex/shared build:i18n` after locale edits (never hand-edit `resources.ts`/`types.ts`). Put new keys under `translation.nodes.*` (`nodes.actions`, `nodes.columns`, `nodes.uninstall` (new), `nodes.hubs`, `nodes.selection` (new)); do NOT edit `nodes.upgrade.*` (O2 owns it) except reading.

## Requirements

1. **Row multi-select**: first column = checkbox per row; the entry itself (`row.isSelf`) is not selectable (render a disabled checkbox). Header cell has one toggle button (icon-only, `aria-label`) that merges 全选 / 全不选: when not all selectable rows are selected it selects all selectable rows, otherwise it clears the selection. Selection lives in React state in `nodes-management.tsx` (a `Set<string>` of node ids), pruned when rows disappear. Add a standalone `Checkbox` component to the UI kit: `packages/ui/src/components/checkbox.tsx` exporting `Checkbox`, built on `@base-ui/react/checkbox` (the kit already uses `@base-ui/react/switch` and `/menu` — read `switch.tsx` for the styling pattern, check the base-ui version in `node_modules/@base-ui/react/package.json` and read its checkbox docs/types in `node_modules`), Tailwind classes consistent with `switch.tsx`; import it as `@tmex/ui/checkbox` (package.json wildcard export already maps `./*`).
2. **Bulk「更多」button** right of「添加」in the card header: icon `MoreHorizontal`/`Ellipsis` (lucide), opens `@tmex/ui/dropdown-menu` with three items acting on the **selected** rows: 升级 (`upgrade.startAll(selectedRows)` — the hook itself filters eligible rows/asks confirmation), 移除节点 (existing signed revoke flow — reuse the logic in `use-node-row-actions.ts` `revoke`; factor it into a reusable function that takes a row so the bulk path can run it sequentially for each selected row with one confirm listing the names; keep the per-row button working), 卸载 tmex (see 3). Items disabled with a `title` reason when the selection is empty, when hub writes are blocked (for 移除), or when a batch/uninstall is running. The menu item label for 升级 shows the count of eligible selected rows (e.g. 「升级（3）」). **Remove the `UpgradeAllButton`** and its export/tests (there is no separate "upgrade all" any more: select all → 更多 → 升级).
3. **Remote uninstall (new hook `use-node-uninstall.ts`, new `uninstall-dialog.tsx`)**:
   - Contract (`packages/shared/src/contracts/system.ts`, `MeshNode.operation` in `@tmex/api-client/auth`): entry endpoints `POST /api/mesh/nodes/:id/uninstall` → 202 `UninstallStatus {state:'scheduled'}`; errors JSON `{ code: MeshUninstallErrorCode }` with `UNINSTALL_SELF_BLOCKED` 409, `NODE_LOGIN_REQUIRED`, `NODE_UNREACHABLE`, `UNINSTALL_NOT_ALLOWED` 409 (container/manual deploy), `UNINSTALL_UNSUPPORTED` 501 (old target), `UPGRADE_IN_PROGRESS` 409; `GET /api/mesh/nodes/:id/operation` → `MeshNodeOperation | 404`; `DELETE /api/mesh/nodes/:id/operation` clears it. `GET /api/mesh/nodes` rows now carry `operation?: MeshNodeOperation | null` (`kind: 'uninstall'`, `phase: 'requested' | 'uninstalling' | 'failed'`) — map it into `NodeRow` (`apps/fe/src/node/mesh-nodes.ts`, add `operation: MeshNodeOperation | null`, default null; update `mesh-nodes.test.ts`).
   - Flow: confirm dialog (`@tmex/ui/alert-dialog`) listing the selected node names and stating the consequence (service, program, data on that machine are deleted; the node is then removed from the mesh). On confirm, for each node sequentially: POST uninstall → on 202 mark the row `uninstalling` (optimistic, and the server record arrives with the next nodes refresh) → then run the signed revoke for that node (the existing flow; one credential prompt for the whole batch if `useCredentialPrompt` supports reuse — read `apps/fe/src/auth/credential-prompt.ts`; otherwise prompt per node). If the POST fails, show the error toast with a localized message per code and skip the revoke. One summary toast at the end (「已卸载 X，失败 Y（名字）」).
   - Row rendering: when `row.operation?.kind === 'uninstall'`, show the status cell as 「卸载中」 (phase `uninstalling`/`requested`) or 「卸载失败」 with the error in `title` (phase `failed`, plus a small ✕ button that calls `DELETE .../operation`), disable rename/upgrade/select for that row; the 移除 button stays enabled so the user can finish the revoke after a refresh.
   - Blocked cases in the menu/dialog: rows that are self, offline, not logged in, or `version < 1.1.13` (use `compareSemver` from wherever `upgrade-batch.ts` gets it — import from that module's dependency, not from `upgrade-batch.ts` itself if that would couple you to O2's file; check `@tmex/shared` for a semver helper) are listed as skipped with the reason.
4. **Hub candidate diagnostics** (`/api/mesh/hubs.candidates[]` `{publicUrl, lastError, lastAttemptAt}` is already returned by `authApi.listHubs()` but dropped): add `candidates` to `MeshHubsState` in `mesh-hubs.ts` (keep in `refreshMeshHubs`, expose from `useMeshHubs`), pass to `HubStrip`; a chip whose hub `publicUrl` matches a candidate with `lastError` shows a small warning icon (`TriangleAlert`) and the chip's `title` gains two lines: 「最近尝试：<time>」 and 「最近错误：<lastError>」(truncate lastError to ~160 chars). Match URLs after normalizing trailing slash. Old backends without `candidates` → empty array, no change.
5. **Copy**: 「当前节点」 tag → the existing `nodes.self` key text must become 「当前」 / "Current" / 「現在」 (check the current value and its usages first — it is the tag next to the entry's own name).
6. Tests: extend `nodes-management.test.tsx` (selection toggle logic, header button label flips, 更多 menu items enabled/disabled, UpgradeAllButton removed, uninstall dialog → POST sequence with mocked fetch → revoke called → summary toast; row `uninstalling` rendering; failed op clear), `mesh-hubs.test.ts` (candidates kept), `mesh-nodes.test.ts` (operation mapping), `hub-strip` (warning icon + title), `packages/ui` checkbox smoke test if the kit has a test pattern. Baselines: `cd apps/fe && bun test src/` = 1275 pass / 0 fail (O2 will add more; only your own files matter), `bunx tsc --noEmit -p .` 0 errors; `packages/ui` `bun test` green.

## Scope — files you may edit
`apps/fe/src/pages/settings/nodes/management/{nodes-management.tsx,nodes-table.tsx,hub-strip.tsx,use-node-row-actions.ts,use-node-uninstall.ts (new),uninstall-dialog.tsx (new),nodes-management.test.tsx,hub-strip.test.tsx (new)}`, `apps/fe/src/pages/settings/nodes/management/types.ts` **only to add uninstall-related types** (append; O2 edits upgrade types in the same file — keep your edits to a clearly separate block at the end of the file to avoid overlapping lines), `apps/fe/src/node/{mesh-nodes.ts,mesh-nodes.test.ts,mesh-hubs.ts,mesh-hubs.test.ts}`, `packages/ui/src/components/checkbox.tsx` (+ test), i18n locales under `translation.nodes.*` except `nodes.upgrade`. Nothing else.
