# Common rules for every coding agent on this worktree

- Worktree: `/Users/konata/code/tmex-enhanced-wt-r14` (branch `feat/round14-multihub-phase2-nodes-mgmt`). Runtime is **Bun** (`bun`, `bunx`); Node only for `packages/app` CLI. If `bun` is missing from PATH, source `~/.zshrc` PATH.
- **Other agents are editing this same worktree in parallel. Touch ONLY the files listed in your scope. Never run `git add/commit/stash/checkout/reset`.** The commander commits.
- Read `AGENTS.md` at the repo root first and follow it (Chinese comments only where logic is non-obvious; no unnecessary comments; no TODOs, no stubs, no "simple version first").
- **Never touch the production tmex**: do not read/write `~/Library/Application Support/tmex/`, do not curl port 9883, do not kill/restart launchd services, do not touch the tmux session named `tmex` or the default tmux socket. Tests must use temp dirs and free ports.
- Never lint/format generated files (`packages/shared/src/i18n/resources.ts`, `types.ts`, `resources/fe-dist/*`, `dist/*`).
- Look up library APIs in `node_modules` source before using them; do not guess.
- TDD: write/extend tests alongside the implementation. Before finishing run, in the package you changed: `bun test <dir>` (in `apps/fe` use `bun test src/...`, never bare `bun test`), `bunx tsc --noEmit -p .` (error count must not exceed the baseline given in your task), `bunx biome check <changed files>` (fix with `--write`). macOS has no `timeout` command; strip ANSI from bun test output with `sed 's/\x1b\[[0-9;]*m//g'`.
- When done, write a concise result report (what changed, file list, test/tsc numbers, anything left) to the absolute result path given in your task, **then exit**. Write the file only when finished.

# G0b — Backend review fixes (tunnel connector health, uninstall auth, RTT serialization)

Result file: `/Users/konata/code/tmex-enhanced-wt-r14/prompt-archives/2026090200-round14-multihub-phase2-nodes-mgmt/sub/G0b-result.md`

A code review (`prompt-archives/2026090200-round14-multihub-phase2-nodes-mgmt/sub/RV1-result.md`) of the committed work in `apps/gateway/src/tunnel/**`, `apps/gateway/src/system/uninstall.ts`, `apps/gateway/src/api/system.ts`, `apps/gateway/src/mesh/mesh-routes.ts`, `apps/gateway/src/mesh/uplink-pool.ts` produced the findings below. Fix exactly these (the commander already judged which ones to take). Read each referenced spot before changing it.

1. **Last-protection ack ignores `degraded`** (`apps/gateway/src/tunnel/manager.ts` `requireLastProtectionAck` ~L554 and any other `state === 'running'` gate that decides whether the tunnel is "up"): a `degraded` tunnel is alive (the edge may come back any second), so removing Access / disabling JWT enforcement must still require `acknowledgeExposure`. Introduce one `processUp()` helper (running || degraded) and use it in every such gate. Tests for `remove_access` and `set_access_enforce(false)` under `degraded`.
2. **Log redaction too weak** (`apps/gateway/src/tunnel/redact.ts`, used by the ring buffer and by `readLogTail`): extend `redactSecrets` to also mask URL userinfo (`scheme://user:pass@host` → `scheme://***@host`), the VALUES of all URL query parameters (keep keys), and `key=value` / `"key":"value"` pairs whose key matches `/(pass(word)?|secret|token|api[-_]?key|authorization|cookie|session|jwt|bearer|credential)/i`, plus `Bearer <token>` and `Basic <b64>` headers; keep existing hex/base64 masking. Add tests covering short tokens, base64url, JSON-format cloudflared lines and text-format lines. Everything shown in `status.log` and `connector.lastError` must go through it (verify `extractLastError`).
3. **Metrics port attribution** (`apps/gateway/src/tunnel/connector-health.ts` `discoverMetricsAddr`, `apps/gateway/src/tunnel/external-detect.ts` config parsing): also parse `metrics:` from the cloudflared YAML config when a `--config` path is known (external detection already reads the config for ingress); when falling back to the 20241–20245 scan and **more than one** port answers `/ready`, return `reachable: null` with `lastError`-style note `multiple cloudflared metrics endpoints answered; cannot attribute` instead of picking the first. Tests.
4. **In-flight probe outlives stop/remove** (`manager.ts` connector poll ~L1303 / ~L1358): tag each probe with the poll generation (and process pid/startedAt for managed mode) and drop its result if the generation changed before it resolved; on `stop`/`remove`/`start` bump the generation and reset `lastConnector` to `EMPTY_CONNECTOR`. Test: slow probe resolving after stop must not overwrite the empty connector.
5. **RTT only on success + serialization** (`apps/gateway/src/mesh/uplink-pool.ts` ~L1243 and `apps/gateway/src/mesh/mesh-routes.ts` `serializeHubCandidate` ~L95): record `rttMs`/`rttAt` only when the `/healthz` probe succeeded (clear them on failure); serialize `rttMs`/`rttAt` in `GET /api/mesh/hubs` `candidates[]` (object branch pass-through, string branch `null`s, return type extended). Tests for both.
6. **Uninstall authorization** (`apps/gateway/src/api/system.ts` uninstall handlers, `apps/gateway/src/system/uninstall.ts`, `apps/gateway/src/mesh/mesh-routes.ts` uninstall relay): first determine how `/api/system/*` is protected today (session middleware around `/api/*`? read `apps/gateway/src/mesh/session-middleware.ts` and the route assembly). Then enforce, in the target handler itself (defense in depth, independent of middleware order): the request must carry an authenticated user session (same predicate the upgrade endpoints rely on; if they rely purely on middleware, add an explicit check using the request/mesh context helpers) — reject with 401 otherwise; log an audit line `[system] uninstall requested via=<self|peer nodeId> user=<uid>`. Document in the result what protects the direct path and the relayed path. Do not add a new signature scheme.

Baselines: `apps/gateway` full `bun test` green (≈3408), tsc 0. Scope: `apps/gateway/src/tunnel/**`, `apps/gateway/src/system/uninstall.ts` (+test), `apps/gateway/src/api/system.ts` (+test), `apps/gateway/src/mesh/mesh-routes.ts` (+test; **only** `serializeHubCandidate` and the uninstall relay — another agent G3 may touch `hub/**` and `mesh-runtime.ts` but not this file), `apps/gateway/src/mesh/uplink-pool.ts` (+test). Nothing else.
