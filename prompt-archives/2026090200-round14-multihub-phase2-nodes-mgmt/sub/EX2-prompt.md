# EX2 — Explore: hub authorization via signed key-log records + enrollment token replication (read-only, report as final message)

You are a read-only code explorer for the tmex monorepo (Bun runtime). Output your COMPLETE report as your final assistant message (you cannot write files). Use exact file paths and line numbers. Read the code; do not guess.

Context: see `docs/hub/2026082700-hub-node-architecture.md` (user key log: `user_key_log` strict chain `seq + prev_hash`, records signed by user keys; node certs; revocation), `docs/hub/2026090104-multi-hub-standby.md` (phase 1: `TMEX_HUB_PEERS` env allowlist of authorized hub node ids, `tmex hub allow/disallow`). Key code: `apps/gateway/src/auth/**` (key log service, record types, verification, catch-up), `apps/gateway/src/mesh/**` (HubRuntime `TMEX_HUB_PEERS` handling, `MeshHubStore`, enrollment token store & redeem, `HUB_NOT_WRITER` fencing), `packages/shared/src/auth/**` and `packages/shared/src/mesh/**` (record schemas/codecs), `packages/app/src/cli/**` (`hub allow/disallow/standby/promote/demote`, `hub join`, `enroll`), FE `apps/fe/src/pages/settings/nodes/**` and the account-security/signing flow in the browser (how does the browser sign a key-log record today? e.g. rename/revoke node — is it signed by the user's device key in the browser or by the hub?).

Tasks:

1. **`admit-hub` / `retire-hub` key-log records**: map every existing key-log record type (names, payload schemas, who signs, how verified, how replicated to nodes, how applied to local state on each node). Propose new record types `admit-hub {hubNodeId, publicUrl?, priority?}` and `retire-hub {hubNodeId}` signed by a user key, replicated via the existing chain, and applied on every node/hub to derive the authorized-hub set (replacing `TMEX_HUB_PEERS` as the source of truth; keep env as a bootstrap/override for backwards compat — say exactly how the two merge). Show where `isAuthorizedHub(nodeId)` checks live today and what they must read instead. Cover: how the current active writer appends these records (HTTP endpoint `POST /api/hub/hubs/:id/admit`? who signs — the browser user key, like other records?), how the FE would trigger it (the entry's "switch primary/standby" button will need to admit the target first), and how standby's "auto-authorize current writer" is subsumed. Backward compatibility with v1.1.x nodes that don't know the record type: does the verifier reject unknown types (breaking the chain) or skip them? Read the verifier to answer.

2. **Enrollment token replication to standby**: map the enrollment token lifecycle (table, create, redeem, `used_at`, expiry, `HUB_NOT_WRITER` fence on standby). Propose a design that lets a standby (a) at least REDEEM tokens the writer created before it went down and (b) ideally CREATE tokens while writer is down. Options to evaluate honestly: (i) replicate tokens through `node.list` or a new `hub.tokens` frame with LWW on `used_at` and accept the double-redeem race (bounded by token being single-use per node identity anyway — check what redeem actually issues: a node cert signed by whom?); (ii) tokens as signed key-log records; (iii) allow standby to redeem only after it has been auto-promoted (ties to election). Recommend one. Critical question: redeem issues a node cert — what key signs it? If it's a hub-local key, a cert issued by standby must be recognized by the writer when it comes back: trace `node_certs` replication and the trust path.

3. **Remote hub role switch (promote/demote) via API**: map what `tmex hub promote/demote/standby` do (env writes + restart via service manager), `POST /api/setup/*` (the hub wizard writes env and exits), and any existing "restart self" path in the gateway. Propose HTTP endpoints on each hub (`POST /api/hub/role {mode:'active'|'standby', writerEpoch?}`) callable from the entry through the existing `/n/<nodeId>/api/...` proxy so the FE can switch primary/standby from the node management table: exact sequence for "make node X the primary": (1) admit X if needed, (2) demote current writer, (3) promote X (epoch = max+1), (4) all nodes fail over via existing epoch logic. State what must persist (env vs `mesh_hubs`) and how the restart is done. Also the reverse (make X standby). How to make it idempotent and how to report progress to the FE (the FE must survive page refresh: recommend a status endpoint).

Deliver: structured report per task with "Current (files/lines)", "Proposed", "Files to touch", "Wire/DB/record changes", "Compat/Risks". Terse, concrete.
