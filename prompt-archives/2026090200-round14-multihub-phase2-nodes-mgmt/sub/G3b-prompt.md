# Common rules for every coding agent on this worktree

- Worktree: `/Users/konata/code/tmex-enhanced-wt-r14` (branch `feat/round14-multihub-phase2-nodes-mgmt`). Runtime is **Bun** (`bun`, `bunx`); Node only for `packages/app` CLI. If `bun` is missing from PATH, source `~/.zshrc` PATH.
- **Other agents are editing this same worktree in parallel. Touch ONLY the files listed in your scope. Never run `git add/commit/stash/checkout/reset`.** The commander commits.
- Read `AGENTS.md` at the repo root first and follow it (Chinese comments only where logic is non-obvious; no unnecessary comments; no TODOs, no stubs, no "simple version first").
- **Never touch the production tmex**: do not read/write `~/Library/Application Support/tmex/`, do not curl port 9883, do not kill/restart launchd services, do not touch the tmux session named `tmex` or the default tmux socket. Tests must use temp dirs and free ports.
- Never lint/format generated files (`packages/shared/src/i18n/resources.ts`, `types.ts`, `resources/fe-dist/*`, `dist/*`).
- Look up library APIs in `node_modules` source before using them; do not guess.
- TDD: write/extend tests alongside the implementation. Before finishing run, in the package you changed: `bun test <dir>` (in `apps/fe` use `bun test src/...`, never bare `bun test`), `bunx tsc --noEmit -p .` (error count must not exceed the baseline given in your task), `bunx biome check <changed files>` (fix with `--write`). macOS has no `timeout` command; strip ANSI from bun test output with `sed 's/\x1b\[[0-9;]*m//g'`.
- When done, write a concise result report (what changed, file list, test/tsc numbers, anything left) to the absolute result path given in your task, **then exit**. Write the file only when finished.

# G3b — Node-side uplink: immediate fail-back probe on hub state change + hub RTT diagnostics

Result file: `/Users/konata/code/tmex-enhanced-wt-r14/prompt-archives/2026090200-round14-multihub-phase2-nodes-mgmt/sub/G3b-result.md`

## Background
`apps/gateway/src/mesh/uplink-pool.ts` (1219 lines) is the node's ordered-failover uplink pool (multi-hub phase 1, see `docs/hub/2026090104-multi-hub-standby.md` §故障切换与切回). Fail-back today relies only on a 60 s ±20 % timer (`syncProbe()` ~L995, `probePreferred()` ~L1014 → `/healthz` → `switchTo()` ~L1039). When the writer comes back, the standby hub re-attaches to it and immediately rebroadcasts `node.list` with `hubs[]` (writer `online`, epoch) to its nodes — but `dispatchNodeList()` in the pool does not use that signal. Exploration notes: `prompt-archives/2026090200-round14-multihub-phase2-nodes-mgmt/sub/EX1-result.md` §2 and §4 (verify against code).

## Requirements
1. **Event-driven fail-back**: in the pool's `node.list` handling, after `mesh_hubs` is updated, compute whether (a) a hub that is ahead of the currently attached hub in `orderedEndpoints()` order flipped from offline/unknown to `online`, (b) `writerHubId`/`writerEpoch` changed, or (c) the attached hub is no longer the best candidate. If so, trigger `probePreferred()` **immediately** (debounced: at most once per 5 s; coalesce while a probe is in flight; still requires `/healthz` success + CA pin + generation guard before `switchTo`). Keep the 60 s timer as fallback. Log `[uplink] failback probe triggered by node.list` once per trigger (throttled like the other diag lines).
2. **RTT diagnostics**: measure the elapsed monotonic time of every `/healthz` probe the pool already makes (`probePreferred`, candidate connect attempts if there is a cheap hook) and keep per-URL `rttMs` / `rttAt` in the existing `diagByUrl` map; expose them on `UplinkCandidate` (`rttMs?: number|null; rttAt?: number|null`) next to `lastError/lastAttemptAt`. Additionally probe RTT of all authorized candidates every 5 minutes (±20 %, only when the pool has ≥2 candidates; skip in tests unless enabled) so the UI can show RTT even for hubs we are not attached to. Do NOT change candidate ordering or switching based on RTT in this task (attach-to-nearest needs hub-to-hub relay first; a later task will do it).
3. Serialize `rttMs`/`rttAt` in the pool's public snapshot that `GET /api/mesh/hubs` reads (find the accessor the route uses, e.g. `candidates()`/`snapshot()`); do **not** edit `mesh-routes.ts` yourself (another agent edits it) — instead report in your result the exact one-line change needed there, and add `rttMs?`/`rttAt?` to the `candidates` element type in `packages/api-client/src/auth/types.ts` (`MeshHubsResponse.candidates`) and to `authApi.listHubs()` normalization in `packages/api-client/src/auth/auth-api.ts` (pass through when present).
4. Tests: `uplink-pool*.test.ts` — node.list with writer flipping online triggers an immediate probe (fake timers/injected sleep, no 60 s wait); debounce; no trigger when nothing changed; RTT recorded on probe; periodic RTT probe schedule. Baselines: `apps/gateway` full `bun test` green (other agents add tests elsewhere; only your files matter), `bunx tsc --noEmit -p .` 0; `packages/api-client` `bun test` 140 pass, tsc **5 errors pre-existing** (do not add).
5. Update `docs/hub/2026090104-multi-hub-standby.md` §「故障切换与切回」 with the event-driven probe (2–4 lines).

## Scope — files you may edit
`apps/gateway/src/mesh/uplink-pool.ts`, `apps/gateway/src/mesh/uplink-pool*.test.ts`, `packages/api-client/src/auth/types.ts` (candidates element type only), `packages/api-client/src/auth/auth-api.ts` (+ its test), `docs/hub/2026090104-multi-hub-standby.md` (that section only; another agent G2 edits the 授权 sections of the same doc — re-read right before editing and keep your edit surgical). Do NOT touch `uplink-server.ts`, `hub-runtime.ts`, `mesh-runtime.ts`, `mesh-routes.ts`, `integration/**`, `apps/fe/**`.
