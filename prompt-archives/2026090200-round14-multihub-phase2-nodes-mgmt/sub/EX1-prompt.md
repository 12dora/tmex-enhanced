# EX1 — Explore: multi-hub phase 2 transport side (read-only, report as final message)

You are a read-only code explorer for the tmex monorepo (Bun runtime; `apps/gateway`, `packages/shared`, `packages/app`, `apps/fe`). Output your COMPLETE report as your final assistant message (you cannot write files). Use exact file paths and line numbers. Do not propose changes to files you have not read. Do NOT guess — read the code.

Context: phase 1 of multi-hub is shipped (see `docs/hub/2026090104-multi-hub-standby.md` and `docs/hub/2026082700-hub-node-architecture.md`). Key code: `apps/gateway/src/mesh/**` (MeshRuntime, HubRuntime, UplinkPool, MeshHubStore, relay, rtc signalling, hub peer status poll), `packages/shared/src/link/**` and `packages/shared/src/mesh/**` (wire protocol codecs: node.list, node.status, key.log.*, relay frames), `apps/gateway/src/tls/**` (TLS service, CA fingerprint advertising every 10 min).

We want to design phase 2. For EACH of the following, map the current implementation precisely (files, functions, message types, DB tables, timers) and then propose a concrete, minimal design that fits the existing architecture, with the list of files to touch and the wire/DB changes. Flag security implications (threat model: any single compromised point only affects itself; hub is NOT a trust root).

1. **Hub-to-hub relay**: today a node attached to hub B cannot reach a node attached to hub A (relay only works among nodes attached to the same hub). Map how relay frames are routed today (HubRuntime relay tables, `peer_cache`, session/carrier, `/n/<nodeId>/...` proxy, browser relay path, `rtc.signal` forwarding). Propose how hub A and hub B should forward relay + rtc.signal frames for nodes attached to the other hub (e.g. a hub↔hub link reusing the existing uplink transport with the standby's uplink to the writer, or a dedicated inter-hub link; how a hub learns which hub a node is attached to — is `attachedHubId` in node.list already?). Include loop prevention and what happens on failover.

2. **Choosing the nearest hub by RTT (node and browser)**: map how the node picks its uplink candidate order (`MeshHubStore.orderedEndpoints`, `pickWriterHub`, failback probe) and how the browser picks its entry (it just uses the current origin). Propose: node measures RTT to each authorized hub's `/healthz` (or `/api/hub/status`) and prefers the lowest RTT hub as its ATTACH target while writes still go to the writer (requires item 1 so nodes on different hubs can still reach each other). For the browser: expose the hub list + RTT to the FE (`/api/mesh/hubs` already exists — what does it return?) and let the FE show/choose; note any auth/session implications of switching origin.

3. **Automatic writer election**: map the current epoch/fencing/split-brain logic (`writerEpoch`, `setMode`, `fenced`, hub peer status poll `GET /api/hub/status`, `TMEX_HUB_PEERS` allowlist). Propose a safe, simple auto-promotion: e.g. a standby that cannot reach the writer for N consecutive probes AND is the lowest-priority-number authorized standby AND sees a majority of other authorized hubs agreeing the writer is down, promotes itself with epoch+1 (persisted), and the old writer is fenced when it returns via existing epoch fence. Discuss the no-quorum 2-hub case honestly (recommend: with only 2 hubs, auto-promote only if `TMEX_HUB_AUTO_PROMOTE=1` and after a longer timeout). Where does the persisted mode live (env `TMEX_HUB_MODE` vs `mesh_hubs` row)? How does `setMode` persist across restart today?

4. **Fail-back notification**: today a node attached to a standby checks the preferred hub every 60 s (±20%). Map the probe (`UplinkPool` failback timer). Propose an active signal: when the writer comes back it (a) announces via hub peer status poll immediately and (b) the standby hub pushes a `hub.available` / updated `node.list.hubs[]` frame to its attached nodes, which then run the failback probe immediately. Identify exactly where the standby learns the writer is back (peer status poll interval is also 60 s — so also propose that a restarting writer POSTs/GETs its peers' `/api/hub/status` right at startup and peers treat an inbound status fetch as a hint, or the writer pushes to standbys through its own uplink connections). Pick the simplest correct design.

5. **TLS CA rotation event**: map how CA fingerprints are advertised (`refreshTlsAndAdvertise`, 10-minute poll) and where TLS material changes (self-signed regen, ACME renew, external cert reload) in `apps/gateway/src/tls/**`. Propose an in-process change event (EventEmitter/callback) from the TLS service so mesh re-advertises immediately, keeping the 10-min poll as fallback.

Also report: existing integration test harness for multi-hub (`multi-hub.integration.test.ts`, in-memory transport `transport=memory`) and how to extend it for the above; and the current test counts baseline command (`cd apps/gateway && bun test`).

Deliver: a structured report with sections 1–5, each with "Current", "Proposed", "Files to touch", "Wire/DB changes", "Risks". Be concrete and terse. Do not pad.
