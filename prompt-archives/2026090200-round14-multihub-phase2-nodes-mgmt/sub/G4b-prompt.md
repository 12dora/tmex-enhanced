# Common rules for every coding agent on this worktree

- Worktree: `/Users/konata/code/tmex-enhanced-wt-r14` (branch `feat/round14-multihub-phase2-nodes-mgmt`). Runtime is **Bun** (`bun`, `bunx`); Node only for `packages/app` CLI. If `bun` is missing from PATH, source `~/.zshrc` PATH.
- **Other agents are editing this same worktree in parallel. Touch ONLY the files listed in your scope. Never run `git add/commit/stash/checkout/reset`.** The commander commits.
- Read `AGENTS.md` at the repo root first and follow it (Chinese comments only where logic is non-obvious; no unnecessary comments; no TODOs, no stubs, no "simple version first").
- **Never touch the production tmex**: do not read/write `~/Library/Application Support/tmex/`, do not curl port 9883, do not kill/restart launchd services, do not touch the tmux session named `tmex` or the default tmux socket. Tests must use temp dirs and free ports.
- Never lint/format generated files (`packages/shared/src/i18n/resources.ts`, `types.ts`, `resources/fe-dist/*`, `dist/*`).
- Look up library APIs in `node_modules` source before using them; do not guess.
- TDD: write/extend tests alongside the implementation. Before finishing run, in the package you changed: `bun test <dir>` (in `apps/fe` use `bun test src/...`, never bare `bun test`), `bunx tsc --noEmit -p .` (error count must not exceed the baseline given in your task), `bunx biome check <changed files>` (fix with `--write`). macOS has no `timeout` command; strip ANSI from bun test output with `sed 's/\x1b\[[0-9;]*m//g'`.
- When done, write a concise result report (what changed, file list, test/tsc numbers, anything left) to the absolute result path given in your task, **then exit**. Write the file only when finished.

# G4b — Backend fixes for review round 2 (writer forwarding, hub.tokens, retired-hub exclusion, force-keylog, server-side epoch)

Result file: `/Users/konata/code/tmex-enhanced-wt-r14/prompt-archives/2026090200-round14-multihub-phase2-nodes-mgmt/sub/G4b-result.md`

Read `sub/RV2-result.md` (findings 2,3,4,5,9 accepted; 1,6,10 rejected — see item 6 below) and the result reports `sub/G2-result.md`, `sub/G3-result.md`, `sub/G4-result.md`, `sub/G5-result.md`. Fix exactly:

1. **Writer forwarding must not forward caller credentials over HTTPS** (`apps/gateway/src/hub/writer-forward.ts`): the forwarded write payloads are self-authenticating (enrollment create carries a user-signed authorization; redeem carries an enroll-key-signed certificate; revoke/keylog are signed records; rename is a hub-API op on behalf of an authenticated user). Rework: forward these requests over the **authenticated hub↔hub uplink** (the same link G4 uses for `key.log.append` forwarding and G5 uses for `hub-relay` streams) as a new hub-only control frame `hub.write-forward { id, method, path, headers-subset (content-type, X-Tmex-Force-Keylog), body }` + response frame (or a `hub-relay`-style stream if frames are too small — pick what fits the codec; version-gate ≥1.1.13; append enum values at the END). Strip `cookie`/`authorization` entirely. On the writer, execute the request attributed to the forwarding hub: signed payloads verify as themselves; **rename** additionally requires that the forwarding standby asserts an authenticated user — include `uid` in the frame and accept it only because the standby is an authorized hub (document this trust explicitly in the doc). Remove the HTTPS+cookie path and its tests; keep 409 `HUB_NOT_WRITER` when no live writer uplink.
2. **Retired/unauthorized hubs must not re-enter** (`apps/gateway/src/hub/hub-replication.ts` `allowed()`, `apps/gateway/src/mesh/mesh-runtime.ts` seed handling ~L1002, `apps/gateway/src/mesh/mesh-routes.ts` writer selection ~L237, `MeshHubStore.orderedEndpoints`): after connection auth, re-check the merged authorization for the source; `signed-retired` excludes a hub even when it matches a seed URL or `TMEX_HUB_PEERS`; candidates, `pickWriterHub` inputs, replication acceptance and `/api/mesh/hubs` writer computation all filter to currently-authorized hubs (self included unless retired). Tests: retired hub reconnecting via seed cannot rejoin `mesh_hubs`/candidates/writer.
3. **`hub.tokens` hardening** (`apps/gateway/src/hub/hub-tokens.ts`, `uplink-server.ts` handler, `user-store.ts`): strip `entry_sid` and any callback/session metadata from `authorization_json` before replication (writer side) and on apply (defense in depth); accept non-ACK `hub.tokens` only from the **current writer** (verified: sender's hubNodeId equals `pickWriterHub()` and its `writerEpoch` ≥ local known max; otherwise drop+log); scope apply by `user_id` (rows must belong to the mesh user); paginate the connect-time snapshot into ≤48 KiB frames with a `more` flag and assert the encoded size before send. Tests for each.
4. **Force-keylog end-to-end** (`packages/shared/src/uplink/codec.ts` `key.log.append` payload + `apps/gateway/src/mesh/uplink-key-log-sync.ts` + `uplink-server.ts` append handler + `apps/gateway/src/mesh/auth-routes.ts`): add optional `force?: boolean` to the append frame (appended field, legacy-stripped); the entry's HTTP handler translates `X-Tmex-Force-Keylog: 1` into it; the writer's uplink gate honours it (same warning log as the HTTP force). Test: old-node-present + force → append succeeds via uplink.
5. **Server-side epoch allocation** (`apps/gateway/src/hub/hub-role-routes.ts`): `mode:'active'` with `writerEpoch` omitted → target allocates `max(env, own row, all mesh_hubs rows)+1` (log the allocation); explicit epoch keeps the strict `>` check. Contract comment already updated. Tests.
6. **Docs** (`docs/hub/2026090104-multi-hub-standby.md`): in 已知限制, add two explicitly-accepted trade-offs: (a) 节点会话即目标机完全控制权（升级/卸载/终端同模型），远程破坏性操作不再叠加独立签名；(b) 被 admit 的 hub 获得围栏权（epoch 自报即可 fence 其他 hub）——这正是 hub 授权需要用户签名的原因；失陷的已授权 hub 影响范围是 hub 控制面。And describe the new forwarding trust (rename attribution by authorized standby).

Baselines: `apps/gateway` full `bun test` green (report), tsc 0; `packages/shared` tests green, tsc 0. Scope: `apps/gateway/src/hub/**`, `apps/gateway/src/mesh/{mesh-runtime.ts,mesh-routes.ts,auth-routes.ts,uplink-key-log-sync.ts,uplink-client.ts}`, `apps/gateway/src/auth/{user-store.ts,mesh-hub-store.ts}` (mesh-hub-store lives under src/auth — locate it), `apps/gateway/src/mesh/integration/**`, `packages/shared/src/uplink/codec.ts` (+test), docs. Do NOT touch `uplink-pool.ts`, `apps/gateway/src/api/**`, `system/**`, `tunnel/**`, `apps/fe/**`.
