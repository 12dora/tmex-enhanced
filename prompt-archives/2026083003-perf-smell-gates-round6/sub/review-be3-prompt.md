You are reviewing a performance-optimisation batch in the tmex monorepo (Bun runtime, React 19, zustand). Worktree root: /Users/konata/code/tmex-enhanced-wt-r6 (read-only; you may run `bun test` in packages). The diff to review is in the file named below; read the surrounding code in the worktree as needed.
Review for: (1) behaviour regressions / correctness bugs introduced by the optimisation (ordering, stale caches, missed invalidation, race conditions, memory retention, lost events, rAF/timer cleanup on unmount, SSR/no-DOM paths); (2) places where the optimisation does not actually achieve its goal (memo defeated by unstable props, cache never hit, etc.); (3) tests that assert the wrong thing. Do NOT report style nits, naming, comment wording, or hypothetical hardening for inputs that cannot occur. Each finding: severity (blocker / should-fix / nit), file:line, what breaks (concrete scenario), suggested fix (1–3 sentences). Also list "verified OK" items in one line each. Your FINAL MESSAGE is the report (Markdown, English).

Diff file: /Users/konata/code/tmex-enhanced-wt-r6/prompt-archives/2026083003-perf-smell-gates-round6/sub/review-be3-diff.patch

This is a REFACTORING batch (code-smell round): the intent is behaviour-preserving extraction. Review with that lens: (1) StreamReplayState extraction + noteInbound returning {kind, deviceId} — malformed-frame parity, connectedForwarded dedup, resume cursor behaviour; forwarder pending TTL 15s→60s injectable. (2) user-key-persistence extraction — transaction boundaries identical (persistApplied/wipe/identity/bind run on the same tx stores in the same order), no store constructed outside the tx. (3) uplink-rate-limit extraction — API parity, no state shared across instances. (4) UplinkKeyLogSync extraction — reset/abort ordering, generation checks through host callbacks (any place where the old code read a field synchronously but now calls a callback that could observe a newer value?), fork teardown, snapshotTasks before reset. (5) reconnectControlChannel(policy, adapter) — ordering parity for local (incl. EAGAIN retry re-entry with 4× delay) and ssh; restartCount write-back on the host object; UTF-8 tail continuation-byte trim. (6) HTTP+RTC upload through one queue — bulk io chain ordering (done after pending writes, abort behaviour), offset snapshot semantics for RTC appends. (7) watch monotonic elapsed conversion — first-sample anchoring, service still injecting wall clock. (8) mesh-runtime wiring split — initialization ORDER identical (stores → bindings → uplink → peer holder → state-change → rtc → browser accept), holders not read before written, handleUplinkNodeList/pruneStaleListedPeers parity with the old onNodeList (incl. synthetic offline on hub loss); parseIpv6Words consolidation — zone-id, dotted-quad mixed, empty-compression edge parity for BOTH previous copies.
