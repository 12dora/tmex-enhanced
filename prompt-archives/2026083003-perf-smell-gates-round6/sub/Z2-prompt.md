# Exploration Z2
## Ground rules
- Repo: tmex monorepo (Bun-only runtime). Worktree root: /Users/konata/code/tmex-enhanced-wt-r6. You are READ-ONLY: do not modify files, do not run git write commands. You MAY run `bun test`, `bun run`, or small throwaway scripts written to /private/tmp/claude-501/-Users-konata-code-tmex-enhanced/6c2fc705-32ec-470c-8790-255ad37938cd/scratchpad/ (never inside the repo).
- Never touch the production tmex install (~/Library/Application Support/tmex, port 9883) or any tmux session named `tmex`; if you need tmux, use `tmux -L tmex-explore-<yourid>`.
- Report format: your FINAL MESSAGE is the report (Markdown, English). Rank findings by user-visible value. For each finding: file:line, what the hot path / cost is, concrete evidence (measured numbers where you can get them — write a micro benchmark in the scratchpad and run it with bun when feasible), the proposed fix in 2–5 sentences, expected gain, risk, and an estimate of net line-count change (we care about NOT growing the codebase). Mark each finding HIGH / MEDIUM / LOW value. Also list things you checked that are already fine (one line each) so nobody re-investigates them.
- Be concrete and skeptical: do not report a "possible" problem without reading the actual code path end-to-end. Prefer 5 real hot spots over 20 speculative ones.

## Task Z2 — Round 3 (verification): gateway before/after measurements, regression hunt, what is left
Two worktrees: BASE = /Users/konata/code/tmex-enhanced-wt-base (19dd4992) and HEAD = /Users/konata/code/tmex-enhanced-wt-r6 (current), both with node_modules. Read `git -C HEAD log --oneline 19dd4992..HEAD`, reports X3, Y2, review-be*, and all G*/R*-result.md in HEAD's prompt-archives/2026083003-perf-smell-gates-round6/sub/ (they name the scratchpad bench scripts that were used — rerun the ones still present in /private/tmp/claude-501/-Users-konata-code-tmex-enhanced/6c2fc705-32ec-470c-8790-255ad37938cd/scratchpad/ against BOTH worktrees by pointing their imports at each root; write new copies in the scratchpad rather than editing them).
1. Table: bench, BASE ms, HEAD ms, ratio, for: forwarder noteInbound 10 MiB, agent windowed history 10k, hub broadcast 100 links, rsync list 200k (ordered and reversed), cold pane ingest 5000×1 KiB, watch upsert 5000, devices list 100. Flag regressions.
2. Run `cd HEAD/apps/gateway && bun test` and `bunx tsc --noEmit -p .` (baseline 21 errors); also packages/app and packages/shared tests. Report.
3. Regression hunt on `git -C HEAD diff 19dd4992..HEAD -- apps/gateway packages/shared/src/ws-borsh packages/shared/src/uplink` with concrete reproductions only: peer-manager deadline timers under real timers (write a small test that runs the idle lifecycle with real 50 ms-scaled timeouts if the code allows injecting durations), hub node.list fingerprint vs key.log head changes (is a key.log-only change reflected in the projection so the list is re-broadcast?), forwarder TTL closing a stream whose `open` is merely slow, upload serialization vs the mesh/RTC upload path that still uses sync append, watch deadline scheduler when the process clock jumps, legacy history byte-cap tail on a UTF-8 boundary, refresh quiet period vs. tree updates after `tmux kill-window` from another client (not a tmex user command).
4. What is left: remaining HIGH-value gateway hot spots with evidence, or state explicitly that none remain above MEDIUM. Known-deferred: hasWsSecureCandidate/shouldTryDc listPeers().find, mesh/RTC sync appendUpload, canonical frame-size cache, tree endpoint 2 queries.
