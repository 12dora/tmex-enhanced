You are reviewing a performance-optimisation batch in the tmex monorepo (Bun runtime, React 19, zustand). Worktree root: /Users/konata/code/tmex-enhanced-wt-r6 (read-only; you may run `bun test` in packages). The diff to review is in the file named below; read the surrounding code in the worktree as needed.
Review for: (1) behaviour regressions / correctness bugs introduced by the optimisation (ordering, stale caches, missed invalidation, race conditions, memory retention, lost events, rAF/timer cleanup on unmount, SSR/no-DOM paths); (2) places where the optimisation does not actually achieve its goal (memo defeated by unstable props, cache never hit, etc.); (3) tests that assert the wrong thing. Do NOT report style nits, naming, comment wording, or hypothetical hardening for inputs that cannot occur. Each finding: severity (blocker / should-fix / nit), file:line, what breaks (concrete scenario), suggested fix (1–3 sentences). Also list "verified OK" items in one line each. Your FINAL MESSAGE is the report (Markdown, English).

Diff file: /Users/konata/code/tmex-enhanced-wt-r6/prompt-archives/2026083003-perf-smell-gates-round6/sub/review-be-diff.patch

Focus: gateway backend — forwarder pendingStreams cleanup + 15 s TTL (double close, token reuse, close racing open); PaneData header peek vs full decode parity (malformed frames, variant index coupling); frame-sizer byte-length cache key correctness; windowed agent history load (listAgentMessagesForWindow: page boundary off-by-one, stop condition 'remaining<=0 && seenUser' when the user message is newer than the budget cutoff, sessions with no user message, SQL length() of JSON vs JSON.stringify length; title generation using only the first user message — is that what maybeGenerateSessionTitle expects?); hub broadcastNodeList pre-encode + fingerprint skip (is there any consumer relying on receiving node.list after its own status update or a listVersion bump? new-link auth still gets the cached list?); getPeer/touchPeerLastSeenAt + skip-upsert (stale-metadata semantics, endpoint-upgrade notification suppressed when it should fire); peer-manager deadline timers replacing polling (re-arm coverage: stream open/close, quiesce ack, dispose; fake-clock tests; ImmediateScheduler recursion).
