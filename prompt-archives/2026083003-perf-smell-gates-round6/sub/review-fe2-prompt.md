You are reviewing a performance-optimisation batch in the tmex monorepo (Bun runtime, React 19, zustand). Worktree root: /Users/konata/code/tmex-enhanced-wt-r6 (read-only; you may run `bun test` in packages). The diff to review is in the file named below; read the surrounding code in the worktree as needed.
Review for: (1) behaviour regressions / correctness bugs introduced by the optimisation (ordering, stale caches, missed invalidation, race conditions, memory retention, lost events, rAF/timer cleanup on unmount, SSR/no-DOM paths); (2) places where the optimisation does not actually achieve its goal (memo defeated by unstable props, cache never hit, etc.); (3) tests that assert the wrong thing. Do NOT report style nits, naming, comment wording, or hypothetical hardening for inputs that cannot occur. Each finding: severity (blocker / should-fix / nit), file:line, what breaks (concrete scenario), suggested fix (1–3 sentences). Also list "verified OK" items in one line each. Your FINAL MESSAGE is the report (Markdown, English).

Diff file: /Users/konata/code/tmex-enhanced-wt-r6/prompt-archives/2026083003-perf-smell-gates-round6/sub/review-fe2-diff.patch

Focus: (1) agent history budget/LRU eviction in packages/stores (agent-history-budget.ts, agent-session-crud-actions.ts): pinned-set correctness (active per node, running/waiting_confirmation, inProgress streaming, queued, pendingConfirmations), eviction racing an in-flight history fetch or an incoming delta for the evicted session, reopening an evicted session (refetch exactly once, no duplicate subscribe), forgetSessions on remote-list refresh (a session that is temporarily missing from a partial/failed list response must not be wiped — check what loadSessionsRequest does on error/partial), node-scoped stores. (2) ToolCallCard bounded preview: previewEnd on multibyte/surrogate boundaries, copy-full action, image detection changes (data:image still works, base64 guess only for image tools), ToolDetailsBody memo keyed on call while streaming output updates. (3) fe startup split: session-key-store vs session-login dynamic import — secret zeroing paths, concurrent ensureNodeLogin calls, error paths when the chunk fails to load, LoginPage/auth barrel import changes, test guards using Bun.Transpiler.scanImports.
