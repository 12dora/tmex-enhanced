# Exploration Y1
## Ground rules
- Repo: tmex monorepo (Bun-only runtime). Worktree root: /Users/konata/code/tmex-enhanced-wt-r6. You are READ-ONLY: do not modify files, do not run git write commands. You MAY run `bun test`, `bun run`, or small throwaway scripts written to /private/tmp/claude-501/-Users-konata-code-tmex-enhanced/6c2fc705-32ec-470c-8790-255ad37938cd/scratchpad/ (never inside the repo).
- Never touch the production tmex install (~/Library/Application Support/tmex, port 9883) or any tmux session named `tmex`; if you need tmux, use `tmux -L tmex-explore-<yourid>`.
- Report format: your FINAL MESSAGE is the report (Markdown, English). Rank findings by user-visible value. For each finding: file:line, what the hot path / cost is, concrete evidence (measured numbers where you can get them — write a micro benchmark in the scratchpad and run it with bun when feasible), the proposed fix in 2–5 sentences, expected gain, risk, and an estimate of net line-count change (we care about NOT growing the codebase). Mark each finding HIGH / MEDIUM / LOW value. Also list things you checked that are already fine (one line each) so nobody re-investigates them.
- Be concrete and skeptical: do not report a "possible" problem without reading the actual code path end-to-end. Prefer 5 real hot spots over 20 speculative ones.

## Task Y1 — Frontend performance round 2: long-session memory growth, huge tool outputs, startup bundle composition
Round 1 already landed (read `prompt-archives/2026083003-perf-smell-gates-round6/sub/X1-report.md`, `X4-report.md` and `git log --oneline 19dd4992..HEAD` to see what was fixed — do not re-report those). Now look at what those did not cover:
1. Memory over a long session: does the agent store keep messages of every session ever opened (packages/stores/src/agent-state.ts, agent-session-map.ts, agent-history-sync.ts)? Are pane subscriptions / terminal controllers / ghostty instances disposed when panes or devices are closed (packages/stores/src/pane-subscriptions.ts, packages/terminal-ui, packages/ws-client pane-sink-registry)? Any listener/interval accumulation on reconnect (node-connection-manager.ts, ws-client state-machine/client)? Write a bun script that simulates open/close of 200 sessions or 200 reconnects through the real store code and checks retained object counts.
2. Tool-call cards with very large outputs (packages/panels/src/agent/messages/tool-call-card.tsx and whatever renders tool results / diffs / code): is a 500 KB tool output rendered as one giant DOM/pre or highlighted synchronously? What does `run-command` / file-read output look like in the store (is the full output kept in every delta)? Measure SSR of a card with 500 KB output.
3. Startup: the entry chunk is ~1.34 MB raw / 415 KB gz. Run `bun run --filter @tmex/fe build` in-memory or normally in the scratchpad (do not leave build output in the repo; if vite must write, write to a scratchpad outDir via `--outDir`), and determine the largest contributors to the ENTRY chunk (use `--sourcemap` and a small script that attributes bytes to source modules from the map, or vite's `build.reportCompressedSize` + manual chunk analysis). Report the top 15 modules by bytes in the entry and which of them are only needed on specific routes (candidates for lazy loading), plus any dependency that is imported twice under different paths.
4. First render path: what runs before the first paint (main.tsx providers, i18n init, font loading, runtime bootstrap, initial REST calls, WS connect)? Any serial awaits that could be parallel?
Report top hot spots with evidence; include LOW-value items only if they are trivial.
