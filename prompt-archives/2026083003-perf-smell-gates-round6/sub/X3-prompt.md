# Exploration X3
## Ground rules
- Repo: tmex monorepo (Bun-only runtime). Worktree root: /Users/konata/code/tmex-enhanced-wt-r6. You are READ-ONLY: do not modify files, do not run git write commands. You MAY run `bun test`, `bun run`, or small throwaway scripts written to /private/tmp/claude-501/-Users-konata-code-tmex-enhanced/6c2fc705-32ec-470c-8790-255ad37938cd/scratchpad/ (never inside the repo).
- Never touch the production tmex install (~/Library/Application Support/tmex, port 9883) or any tmux session named `tmex`; if you need tmux, use `tmux -L tmex-explore-<yourid>`.
- Report format: your FINAL MESSAGE is the report (Markdown, English). Rank findings by user-visible value. For each finding: file:line, what the hot path / cost is, concrete evidence (measured numbers where you can get them — write a micro benchmark in the scratchpad and run it with bun when feasible), the proposed fix in 2–5 sentences, expected gain, risk, and an estimate of net line-count change (we care about NOT growing the codebase). Mark each finding HIGH / MEDIUM / LOW value. Also list things you checked that are already fine (one line each) so nobody re-investigates them.
- Be concrete and skeptical: do not report a "possible" problem without reading the actual code path end-to-end. Prefer 5 real hot spots over 20 speculative ones.

## Task X3 — Gateway (backend) performance hot paths
apps/gateway/src: ws/ (client websocket fan-out), tmux-client/ (pane output capture via tmux control mode: control-mode parser, external-tmux-core.ts, session-commands.ts, canonical feed), agent/ (agent supervisor, LLM streaming, message persistence), mesh/ (peer-manager.ts 2100 lines, forwarder.ts, uplink-client.ts, mesh-runtime.ts), hub/uplink-server.ts, db/ (drizzle + sqlite: look for queries in hot loops, missing indexes, per-event transactions, JSON (de)serialisation of large blobs), events/, and packages/shared/src/link (mux.ts, fragment-core) + packages/shared/src/uplink/codec.ts (borsh).
Investigate: per-byte or per-line string concatenation for pane output, repeated Buffer↔string conversions, O(n) scans on every message (e.g. finding a subscriber by iterating all clients, re-sorting per event), unbounded caches/maps that grow (memory leaks: listeners never removed, timers never cleared), synchronous fs/db work on the hot path, JSON.stringify of large state on each broadcast, redundant broadcasts (sending the same snapshot to each client separately encoded), agent message history loaded fully from DB on each turn, sqlite pragmas (WAL, synchronous), and any polling loops with short intervals.
Measure where you can with bun scripts in the scratchpad (e.g. drive the control-mode parser with 10MB, time the borsh codec on 10k events). Report top hot spots with evidence.
