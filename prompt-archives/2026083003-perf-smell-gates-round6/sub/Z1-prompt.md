# Exploration Z1
## Ground rules
- Repo: tmex monorepo (Bun-only runtime). Worktree root: /Users/konata/code/tmex-enhanced-wt-r6. You are READ-ONLY: do not modify files, do not run git write commands. You MAY run `bun test`, `bun run`, or small throwaway scripts written to /private/tmp/claude-501/-Users-konata-code-tmex-enhanced/6c2fc705-32ec-470c-8790-255ad37938cd/scratchpad/ (never inside the repo).
- Never touch the production tmex install (~/Library/Application Support/tmex, port 9883) or any tmux session named `tmex`; if you need tmux, use `tmux -L tmex-explore-<yourid>`.
- Report format: your FINAL MESSAGE is the report (Markdown, English). Rank findings by user-visible value. For each finding: file:line, what the hot path / cost is, concrete evidence (measured numbers where you can get them — write a micro benchmark in the scratchpad and run it with bun when feasible), the proposed fix in 2–5 sentences, expected gain, risk, and an estimate of net line-count change (we care about NOT growing the codebase). Mark each finding HIGH / MEDIUM / LOW value. Also list things you checked that are already fine (one line each) so nobody re-investigates them.
- Be concrete and skeptical: do not report a "possible" problem without reading the actual code path end-to-end. Prefer 5 real hot spots over 20 speculative ones.

## Task Z1 — Round 3 (verification): measure before/after, hunt regressions, confirm nothing high-value is left (frontend + shared)
Two worktrees exist: BASE = /Users/konata/code/tmex-enhanced-wt-base (commit 19dd4992, before this round) and HEAD = /Users/konata/code/tmex-enhanced-wt-r6 (current). Both have node_modules installed. Read `git -C HEAD log --oneline 19dd4992..HEAD` and the reports X1, X2, X4, Y1 and all `*-result.md` in HEAD's prompt-archives/2026083003-perf-smell-gates-round6/sub/.
1. Re-run every benchmark that exists in HEAD under packages/*/bench (agent-thread, history-paging, normalization, render-bridge, write-vt, legacy-snapshot-diff …) on BOTH worktrees where the bench file exists in both (for new benches, port the harness to BASE in the scratchpad by importing BASE's sources). Produce one table: bench, BASE ms, HEAD ms, ratio. Flag any regression (HEAD slower by >10%).
2. Run the full unit suites in HEAD for packages/shared, ws-client, stores, panels, terminal-ui, ghostty-terminal, ui, api-client and apps/fe (`bun test src/`); report pass/fail per package. Also `bunx tsc --noEmit -p .` per package (baseline errors: stores 1, api-client 5, others 0).
3. Regression hunt on the round's changes (read the diff `git -C HEAD diff 19dd4992..HEAD -- packages apps/fe`): look specifically for behaviour that changed silently — e.g. the 4 ms output coalescer delay vs. keystroke echo latency; the 200-block chat window vs. search/find-in-page/anchors; history-page batching vs. live-output ordering; zero-copy views retained beyond the frame lifetime; row-dirty consume-and-clear when two render states share one terminal handle; lazy-loaded settings tabs/markdown on slow networks; per-device status store vs. device removal; history budget eviction vs. a session being viewed on two nodes. Each suspected regression must come with a concrete reproduction (a bun test you ran, or an exact code path) — no speculation.
4. What is left: after this round, list any remaining HIGH-value frontend hot spot with evidence, or state explicitly that none remain above MEDIUM. Known-deferred items (do not re-report unless you find they are actually HIGH): canvas text-run batching, DataChannel fragment double copy, scrollback memory budget, locale-gated first paint, directory virtualization.
