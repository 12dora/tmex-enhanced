You are reviewing a performance-optimisation batch in the tmex monorepo (Bun runtime, React 19, zustand). Worktree root: /Users/konata/code/tmex-enhanced-wt-r6 (read-only; you may run `bun test` in packages). The diff to review is in the file named below; read the surrounding code in the worktree as needed.
Review for: (1) behaviour regressions / correctness bugs introduced by the optimisation (ordering, stale caches, missed invalidation, race conditions, memory retention, lost events, rAF/timer cleanup on unmount, SSR/no-DOM paths); (2) places where the optimisation does not actually achieve its goal (memo defeated by unstable props, cache never hit, etc.); (3) tests that assert the wrong thing. Do NOT report style nits, naming, comment wording, or hypothetical hardening for inputs that cannot occur. Each finding: severity (blocker / should-fix / nit), file:line, what breaks (concrete scenario), suggested fix (1–3 sentences). Also list "verified OK" items in one line each. Your FINAL MESSAGE is the report (Markdown, English).

Diff file: /Users/konata/code/tmex-enhanced-wt-r6/prompt-archives/2026083003-perf-smell-gates-round6/sub/review-be2-diff.patch

Focus: gateway round 2 — (1) upload chunk: Content-Length checks vs chunked bodies without Content-Length, incremental read cap, async append ordering with concurrent chunks for the same session, partial-write/abort cleanup, 413 semantics; raw file temp streaming cleanup on cancel/error, Content-Length correctness; rsync list-only bounded collector: ordering equivalence with the old sortEntries for ≤ MAX_ENTRIES, truncated directories semantics, process exit/stderr handling when consuming stdout line by line. (2) tmux-client: bounded legacy history capture (alt-screen known → single capture; fallback removed when visible capture empty — is that a regression for panes with alt-screen apps?), in-flight coalescing keyed per pane (stale promise reuse after reconnect?), byte-limit kill path, 150 ms refresh quiet period (does any user-visible flow wait on requestSnapshot and now see +150 ms? tree updates after window create/kill), cold pane retention no-copy (latestSeq/dirtyWhileCold correctness when the pane becomes hot again — is the first hot segment correct?). (3) watch scheduler grouping: rule interval semantics with accrued ms, rule add/remove/update re-arming, pane-gone handling deleting rules, cooldown/LLM scheduling; writeWatchRuleState vs upsertWatchRuleState callers; device/tree LEFT JOIN + IN batch: SQLite IN parameter limits for many devices, default values parity, response shape parity.
