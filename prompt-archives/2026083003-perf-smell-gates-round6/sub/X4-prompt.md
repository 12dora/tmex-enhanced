# Exploration X4
## Ground rules
- Repo: tmex monorepo (Bun-only runtime). Worktree root: /Users/konata/code/tmex-enhanced-wt-r6. You are READ-ONLY: do not modify files, do not run git write commands. You MAY run `bun test`, `bun run`, or small throwaway scripts written to /private/tmp/claude-501/-Users-konata-code-tmex-enhanced/6c2fc705-32ec-470c-8790-255ad37938cd/scratchpad/ (never inside the repo).
- Never touch the production tmex install (~/Library/Application Support/tmex, port 9883) or any tmux session named `tmex`; if you need tmux, use `tmux -L tmex-explore-<yourid>`.
- Report format: your FINAL MESSAGE is the report (Markdown, English). Rank findings by user-visible value. For each finding: file:line, what the hot path / cost is, concrete evidence (measured numbers where you can get them — write a micro benchmark in the scratchpad and run it with bun when feasible), the proposed fix in 2–5 sentences, expected gain, risk, and an estimate of net line-count change (we care about NOT growing the codebase). Mark each finding HIGH / MEDIUM / LOW value. Also list things you checked that are already fine (one line each) so nobody re-investigates them.
- Be concrete and skeptical: do not report a "possible" problem without reading the actual code path end-to-end. Prefer 5 real hot spots over 20 speculative ones.

## Task X4 — Frontend performance: app-wide re-render structure, sidebar, stores, routing, bundle
apps/fe/src (pages, components, lib, page-wrapper, use-page-module), packages/stores (react.tsx bindings, runtime.ts, app-runtime.ts, node-connection-manager.ts, sidebar-device-visibility.ts, file-tree.ts), packages/panels (device-tree, device-management, files, settings, watch, code-viewer), packages/ui, packages/theme, packages/notifications.
Investigate: store subscription patterns that subscribe to the whole store or return fresh objects/arrays from selectors (causing every consumer to re-render on any change), context providers whose value object is recreated every render, large lists without keys/memo (device tree, file tree, session list), effects that run on every render, expensive derived data recomputed without useMemo, animation/DnD code doing layout reads in loops, and the code-viewer / file tree with large files or directories. Also check the Vite build: bundle size per chunk (run `bun run --filter @tmex/fe build` if it is fast enough, or inspect vite config), lazy loading of pages, duplicated heavy deps (two markdown/highlight libs?), and font/asset loading that blocks first paint.
Do NOT overlap with X1 (agent chat message list/store) and X2 (terminal rendering) — those are covered by other explorers; mention them only if you find a cross-cutting cause.
Report top hot spots with evidence.
