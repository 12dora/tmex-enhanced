# Exploration S2
## Ground rules
- Repo: tmex monorepo (Bun-only runtime). Worktree root: /Users/konata/code/tmex-enhanced-wt-r6. You are READ-ONLY: do not modify files, do not run git write commands. You MAY run `bun test`, `bun run`, or small throwaway scripts written to /private/tmp/claude-501/-Users-konata-code-tmex-enhanced/6c2fc705-32ec-470c-8790-255ad37938cd/scratchpad/ (never inside the repo).
- Never touch the production tmex install (~/Library/Application Support/tmex, port 9883) or any tmux session named `tmex`; if you need tmux, use `tmux -L tmex-explore-<yourid>`.
- Report format: your FINAL MESSAGE is the report (Markdown, English). Rank findings by user-visible value. For each finding: file:line, what the hot path / cost is, concrete evidence (measured numbers where you can get them — write a micro benchmark in the scratchpad and run it with bun when feasible), the proposed fix in 2–5 sentences, expected gain, risk, and an estimate of net line-count change (we care about NOT growing the codebase). Mark each finding HIGH / MEDIUM / LOW value. Also list things you checked that are already fine (one line each) so nobody re-investigates them.
- Be concrete and skeptical: do not report a "possible" problem without reading the actual code path end-to-end. Prefer 5 real hot spots over 20 speculative ones.

## Code-smell exploration — shared context
Worktree: /Users/konata/code/tmex-enhanced-wt-r6 (read-only). The gate `bun scripts/complexity/gate.ts` (McCabe CC ≤ 15, function ≤ 120 lines, file ≤ 900 lines) currently lists its violations in `prompt-archives/2026083003-perf-smell-gates-round6/sub/gate-violations-before-smell.txt`. Previous rounds deliberately RETAINED these (do not propose them again; they will go on the allowlist): protocol flat dispatch `emitOsc`, `encodeMouseEvent`, `classifySshError`, control-mode `parse`, `dispatchPaneStreamByte`, uplink codec `decodeHubInner`/`decodeMeshUplinkCtl`/`encodeMeshUplinkCtl`, `handlePeerCtl`, `projectMeshListNode`, `resolveNestedCommand`; dev scripts; grammar-as-branches parsers and per-field default chains (`sanitizeBunPath`, `runInit`, `buildInitConfig`, gesture-machine, `createWatchRuleDraft`, `parseIpv6ToBytes`, `decodeLegacyStateSnapshotDiff`); pure conditional JSX (`DeviceRow`, `DeviceCard`); and the cohesive big files `local-external-connection.ts`, `agent/supervisor.ts`, `canonical-feed-session.ts`, `external-tmux-core.ts`, `ws-client/state-machine.ts`, `api/agent.ts`, `messaging-routes.ts`, `tmux-command-handlers.ts`, `ws-client/client.ts`, `sidebar-agent-sessions.tsx`, `agent-session-actions.ts`, `tool-call-card.tsx`. The queued pump (`link-stream-carrier` ↔ `websocket-link`) extraction was tried and reverted (+80 lines).
Goal of this phase: real structural improvement with NET NEGATIVE or neutral line count — dead code, duplicated logic, functions doing two jobs, files that mix unrelated concerns. Not "move 300 lines to a new file to satisfy a number". For every proposal give: file:line, current CC/lines, the concrete split (which responsibilities become which functions/modules, names), expected CC/lines after, expected net line delta, risk, and which tests cover it. Also list violations you recommend ALLOWLISTING instead (with a one-line reason each) — be honest: a table-driven rewrite that only hides sequential semantics is not an improvement. Finally look for duplicated code across files (same 10+ line block in ≥2 places) and dead exports (no importer) with `grep`/`rg` evidence.
Report = your final message (Markdown, English).

## Scope S2 — everything outside S1: apps/fe, packages/app, packages/panels, packages/shared (except uplink codec), packages/ghostty-terminal, packages/ws-client, packages/stores, packages/terminal-ui, and apps/gateway outside mesh/hub/tunnel/ws/agent-tools (e.g. tmux-client, files, watch, db, api other than tunnel-routes)
Targets from the gate list: files >900 lines — ghostty-terminal/ghostty-wasm.ts (1620), render-state.ts (967), ws-client/direct/direct-carrier-controller.ts (1118); functions — fe `TunnelStatusCard` CC34/216 lines, `wizardStepState` CC27, panels `resolveDrop` CC19, `device-card <anon>` CC17, shared `verifyKeyLogRecord` 19 / `applyAdmitNode` 17 / `verifyKeyLogChain` 16, app `assembleTmex` 17 (150 lines) / `detectLibcFamily` 17 / `dispatchAuthCli` 17 / `pollAndAdmit` 16, `websocket-link pump` 16, tmux-client `reconnectControlClient` 16, stores `createTmuxStore` 363 lines, panels `SideBarDeviceList` 267 / `WeixinAccountLoginModal` 254 / `DeviceFolderTree` 226, terminal-ui `Terminal.tsx <anon>` 245 / `SplitTerminalArea` 211, fe `LoginForm` 218 / `AcmePanel` 216, plus every other >120-line function in this scope from the list. Also check the fe `pages/settings/remote-access/**` (new in r5) as a whole for duplication with the tunnel contract handling.
