你是资深 reviewer。请审查 `prompt-archives/2026082902-sidebar-devices-hierarchy/sub/review-3.diff`（相对仓库根，先 cat 读它；需要上下文可读仓库源码，尤其 `packages/shared/src/device-folders.ts`、`packages/panels/src/device-management/*`、`apps/fe/src/node/node-runtime-scope.tsx`、`apps/fe/src/components/global-device-provider.tsx`）。这是 tmex（Bun + React 19 + @dnd-kit + react-query + zustand）设备管理页的「文件夹层级」前端：`packages/panels/src/device-folders`（通用树 UI、dnd 落点解析）、`apps/fe/src/pages/devices`（数据层乐观更新、节点分组/单卡映射、远端节点 runtime scope 内取连接适配器）、`packages/stores` 的展开态持久化。
只报告**真实缺陷**：正确性（落点解析错误、乐观更新回滚遗漏、stale closure、hook 顺序、key 冲突、无限重渲染、事件泄漏）、数据破坏风险（错误的 layout 写回、把远端设备当成本机）、运行时崩溃、无障碍/reduced-motion 明显违背、性能陷阱（每次渲染新建 runtime/query、拖拽中 O(n²) 重算）。每条：文件:行、问题、原因、修法。按严重度排序，不要风格 nit 与防御性建议。最后一句结论：可以合并 / 需修后合并。简体中文。
