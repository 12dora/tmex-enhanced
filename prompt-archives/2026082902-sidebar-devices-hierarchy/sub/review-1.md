Code review (read-only). Diff is in `prompt-archives/2026082902-sidebar-devices-hierarchy/sub/review-1.diff` of this repo (Bun + React monorepo). Intent of the change: in the left sidebar, (a) a mesh node section whose devices are all hidden via `sidebarDeviceVisibility` should render nothing at all (self node keeps its "no devices" empty state when it truly has 0 devices), (b) remove the duplicated NodeBadge inside each device row, (c) remove the power connect/disconnect button, keeping the status dot. Connect/disconnect will move to the device management page cards in a separate change.

Review for real bugs only: regressions, hook-order/react-query cache-key mismatches in the new `use-sidebar-device-stats.ts`, stale-state cases (section hidden when it shouldn't be — e.g. the currently selected device), removed props still referenced, test coverage gaps. Do NOT flag style, defensive-null-check, or "consider adding" items. For each finding: file:line, severity (high/med/low), concrete failure scenario, suggested fix. Be concise.
