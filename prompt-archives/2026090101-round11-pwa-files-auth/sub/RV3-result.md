| severity (Blocker/Major/Minor/Nit) | file:line | issue | why it matters | suggested fix |
|---|---|---|---|---|
| Blocker | [apps/gateway/src/ws/tmux-command-handlers.ts:136](/Users/konata/code/tmex-enhanced-wt-r11/apps/gateway/src/ws/tmux-command-handlers.ts:136) | Sized `TMUX_SELECT` bypasses viewport arbitration. `applyTmuxSelection` directly calls `resizePane`/`selectPaneWithSize`; the following follower claim sees the owner geometry in `lastAppliedViewport` and performs no corrective resize at line 313. | An ordinary pane switch by an 80×24 follower can leave tmux at 80×24 while the policy still reports the 160×48 desktop as owner. This defeats the feature’s central guarantee. | Do not apply selection dimensions when another session owns the target window, or immediately recompute/reapply the winner after any sized selection. Add a two-client follower-selection regression test. |
| Major | [apps/gateway/src/ws/tmux-command-handlers.ts:313](/Users/konata/code/tmex-enhanced-wt-r11/apps/gateway/src/ws/tmux-command-handlers.ts:313) | `lastAppliedViewport` is treated as authoritative without checking current snapshot geometry. | With one client, after a stacked-layout, pane resize, or other tmux-side size change, repeating the same `terminal-sync-size` is suppressed. The old handler compared the live snapshot and would reapply it, so this is a single-client regression. | Invalidate/reconcile the cached geometry when non-policy commands or snapshots change a window, and cover repeated sync after an intervening layout change. |
| Major | [apps/gateway/src/ws/tmux-command-handlers.ts:271](/Users/konata/code/tmex-enhanced-wt-r11/apps/gateway/src/ws/tmux-command-handlers.ts:271)<br>[packages/stores/src/viewport-policy.ts:82](/Users/konata/code/tmex-enhanced-wt-r11/packages/stores/src/viewport-policy.ts:82) | Switching to another pane in the same window does not necessarily send that pane a policy. “First claim” is tracked per window key, so if the winner and geometry remain unchanged, the new `paneId` receives no event. | A follower switching `%0`→`%1` retains only `%0`’s `owner:false`; `%1` has no entry and therefore defaults to owner. It reports/resizes locally instead of using `follow` and pan. | Treat a claim’s `paneId` change as requiring an immediate policy notification, and clear/migrate the superseded pane policy. Add a same-window follower switch test. |
| Major | [apps/gateway/src/ws/tmux-command-handlers.ts:273](/Users/konata/code/tmex-enhanced-wt-r11/apps/gateway/src/ws/tmux-command-handlers.ts:273)<br>[apps/gateway/src/ws/tmux-command-handlers.ts:319](/Users/konata/code/tmex-enhanced-wt-r11/apps/gateway/src/ws/tmux-command-handlers.ts:319) | Claims are not migrated or removed when a pane moves between windows, while resize application re-resolves the stored pane against the current snapshot. | A claim can remain keyed to `@1` after its pane moves to `@2`. If it later wins `@1`, the code resizes `@2`; closed-window claims/maps also accumulate until session teardown. | Reconcile claims on snapshot pane/window changes, recompute both affected windows, remove closed-window state, and ensure policy application targets the policy’s `windowId`. |
| Minor | [packages/ghostty-terminal/src/terminal-dom.ts:256](/Users/konata/code/tmex-enhanced-wt-r11/packages/ghostty-terminal/src/terminal-dom.ts:256)<br>[packages/terminal-ui/src/components/touch/gesture-machine.ts:103](/Users/konata/code/tmex-enhanced-wt-r11/packages/terminal-ui/src/components/touch/gesture-machine.ts:103) | Pan mode sets `touch-action:none`, but the gesture machine still bypasses the right-edge scrollbar hotzone. | Touches starting in that hotzone enter `bypass`; custom pan stops, while `touch-action:none` prevents native viewport panning, leaving a dead strip on mobile followers. | When pan metrics are active, do not hotzone-bypass the pan viewport; retain bypass only for genuinely interactive nested scrollbar elements. |

What looks good: winner ordering exactly follows the contract and is deterministic; session/device cleanup ordering is sound, and reconnect-failure cleanup deletes the entry after dropping claims. Visibility/reconnect dedup, scrolled-surface hit-testing, IME positioning, and pan reset are well covered. The five targeted suites pass all 63 tests. Protocol kinds do not collide; older clients ignore the new S2C kind, while an older gateway’s unknown-C2S error remains non-fatal and legacy resize continues.