| severity (Blocker/Major/Minor/Nit) | file:line | issue | why it matters | suggested fix |
|---|---|---|---|---|
| Major | [apps/gateway/src/mesh/client-ip.ts:18](/Users/konata/code/tmex-enhanced-wt-r11/apps/gateway/src/mesh/client-ip.ts:18) | `CF-Connecting-IP` is only forced non-loopback when `trustProxy` is false. With `trustProxy=true`, loopback or malformed values fall back to a loopback decision. | Reproduced with `127.0.0.1`, `::1`, and `not-an-ip`: all return `true` when the socket is loopback. A tunnel/proxy that preserves an attacker-controlled or malformed header can therefore reopen unauthenticated local bootstrap, contrary to the stated invariant that CF header presence is always non-loopback. | Return `false` whenever `headers.has('cf-connecting-ip')`, before proxy parsing. Add trusted-proxy cases for loopback, mapped-loopback, and malformed CF values. |
| Major | [apps/fe/src/auth/session-key-persistence.ts:73](/Users/konata/code/tmex-enhanced-wt-r11/apps/fe/src/auth/session-key-persistence.ts:73), [apps/fe/src/auth/session-key-store.ts:95](/Users/konata/code/tmex-enhanced-wt-r11/apps/fe/src/auth/session-key-store.ts:95) | Persistence writes/deletes resolve on the individual request’s `success`, and `clearSessionKey()` discards the deletion promise. | An IndexedDB transaction may still abort after request success; the [IndexedDB specification](https://www.w3.org/TR/IndexedDB/#transaction-lifetime-concept) explicitly requires `complete` to establish successful commit. If logout is followed by navigation/termination, or the delete transaction later aborts, the record can survive and a new document can restore the still-valid delegation, defeating logout. | For `readwrite` operations, resolve only from `tx.oncomplete`; reject/return null from `abort` or `error`. Have `clearSessionKey()` return the deletion promise while clearing memory synchronously, and await it in logout/navigation paths. Test an abort fired after request success. |
| Minor | [apps/gateway/src/mesh/integration/mesh.integration.test.ts:969](/Users/konata/code/tmex-enhanced-wt-r11/apps/gateway/src/mesh/integration/mesh.integration.test.ts:969), [apps/gateway/src/mesh/integration/mesh.integration.test.ts:1007](/Users/konata/code/tmex-enhanced-wt-r11/apps/gateway/src/mesh/integration/mesh.integration.test.ts:1007) | Both new isolation tests pass through unrelated rejection paths. | The first submits A’s challenge to B, so B rejects it as `CHALLENGE_CONSUMED` before target binding at `auth-routes.ts:292`. The second uses a SID created in A’s database (`mesh.integration.test.ts:362`), so B rejects it as unknown before checking `viaNodeId`. These tests remain green if the bindings named in their titles regress. | Build the forged login from a B-issued challenge but retain A’s target/key and assert `TARGET_MISMATCH`. For session isolation, issue a SID in B bound to `self` or another entry, then present that same SID through A. |
| Minor | [apps/fe/src/auth/session-key-persistence.test.ts:388](/Users/konata/code/tmex-enhanced-wt-r11/apps/fe/src/auth/session-key-persistence.test.ts:388) | The expired-record test manually deletes the record before asserting deletion. | Removing the production cleanup at `session-key-store.ts:129` would not fail this test, despite its title claiming automatic deletion. | Remove the explicit `clearPersistedSession()` call, flush the persistence queue with a subsequent load or test helper, then assert the raw record is gone. |

What looks good: session-key generation uses a non-extractable WebCrypto private key without an export path, while the noble fallback and TOTP sessions remain memory-only. The generation guard prevents an in-flight restore from overwriting a newer login/logout, `ensureNodeLogin()` now awaits restoration, and no production caller still relies on synchronous `hasSessionKey()`. The device gate is single-flight per node and stops retrying after failure. The targeted WebCrypto, persistence, and client-IP suites passed 27/27 tests; the missing cases above explain why the CF regression remains undetected.