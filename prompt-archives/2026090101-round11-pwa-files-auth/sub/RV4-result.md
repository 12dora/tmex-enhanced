| severity (Blocker/Major/Minor/Nit) | file:line | issue | why it matters | suggested fix |
|---|---|---|---|---|
| Major | [apps/gateway/src/ws/tmux-command-handlers.ts:139](/Users/konata/code/tmex-enhanced-wt-r11/apps/gateway/src/ws/tmux-command-handlers.ts:139) | A sized cold select is now split into an unsized `selectPane` and an independently fired resize; the resize is omitted completely when the cached snapshot already matches. | `SessionCommands.fire()` does not serialize operations ([session-commands.ts:698](/Users/konata/code/tmex-enhanced-wt-r11/apps/gateway/src/tmux-client/external/session-commands.ts:698)). Consequently, the unsized select can reach history capture at [session-commands.ts:620](/Users/konata/code/tmex-enhanced-wt-r11/apps/gateway/src/tmux-client/external/session-commands.ts:620) before the separate resize completes. Previously, `selectPaneWithSize` resized at lines 612–614 before capture. This can return history captured at the wrong geometry on first selection and reconnect. If the snapshot is stale but happens to contain the requested dimensions, `applyWinnerGeometry` suppresses the resize altogether. The new arbitration test only exercises `wantHistory: false` ([viewport-claims.test.ts:247](/Users/konata/code/tmex-enhanced-wt-r11/apps/gateway/src/ws/viewport-claims.test.ts:247)), so it cannot catch either regression. | Resolve ownership before dispatching the select, then use the existing ordered `selectPaneWithSize` operation for an owning sized cold select and unsized `selectPane` for followers. Add a runtime-level test proving resize completes before history capture, including equal cached geometry and reconnect cases. |
| Major | [apps/gateway/src/ws/tmux-command-handlers.ts:395](/Users/konata/code/tmex-enhanced-wt-r11/apps/gateway/src/ws/tmux-command-handlers.ts:395) | Pane-move/window-close cleanup only reconciles the specific window key passed to `applyViewportPolicy`; snapshots never trigger reconciliation. | Snapshot delivery merely replaces `entry.lastSnapshot` and broadcasts it ([legacy-feed-broadcaster.ts:186](/Users/konata/code/tmex-enhanced-wt-r11/apps/gateway/src/ws/legacy-feed-broadcaster.ts:186)). A subsequent claim for the pane’s new window applies only the new key ([tmux-command-handlers.ts:359](/Users/konata/code/tmex-enhanced-wt-r11/apps/gateway/src/ws/tmux-command-handlers.ts:359)), leaving the old key present; the frontend claim effect also has no window ID dependency ([use-viewport-claims.ts:190](/Users/konata/code/tmex-enhanced-wt-r11/packages/panels/src/device-console/use-viewport-claims.ts:190)), so a move need not emit another claim at all. Stale claims can therefore accumulate after closed windows, fail to arbitrate the destination window, or become valid with obsolete geometry if a pane returns. The test masks this by manually calling `applyViewportPolicy` for the old key ([viewport-claims.test.ts:324](/Users/konata/code/tmex-enhanced-wt-r11/apps/gateway/src/ws/viewport-claims.test.ts:324)), which production snapshot handling does not do. | Reconcile/prune all affected device claim keys when installing a new snapshot and recompute both source and destination windows. Replace the manual-policy test with one that delivers the moved/closed-window snapshot through the real snapshot path. |

What looks good: sized follower selection now avoids the direct resize bypass, and same-window pane changes receive a fresh policy. Incoming snapshots with genuinely different width/layout fields still compare as changed, so later tmux-side resizes are not masked. Reconciliation is not on the terminal-output path, so there is no panes×claims work per output frame. Both rewritten mesh tests reached their exact intended errors (`TARGET_MISMATCH` and `via_mismatch`); all targeted tests passed, 103/103.