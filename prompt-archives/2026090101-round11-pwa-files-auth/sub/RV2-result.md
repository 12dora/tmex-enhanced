| severity | file:line | issue | why it matters | suggested fix |
|---|---|---|---|---|
| Major | `packages/panels/src/files/files-node-section.tsx:120`; `packages/panels/src/files/files-node-roots.tsx:99` | Collapsing a node no longer stops its roots query. `useVisibleFileRoots()` is mounted above `FilesNodeSectionShell`; the shell only unmounts its children, leaving the outer query active with `refetchOnWindowFocus: true`. | This regresses the documented cleanup behavior at `files-node-section.tsx:9`. In a hub with many nodes, collapsed sections still retain observers and issue one remote roots request per node whenever the window regains focus. | Make expansion state available before mounting `FilesNodeRootsSection`, and do not mount—or at minimum disable—the visibility query while collapsed. Add a test verifying that collapsing prevents subsequent focus/invalidation refetches. |
| Minor | `packages/app/src/runtime/serve-frontend.ts:24`; `apps/fe/vite.config.ts:71` | The hashed-asset regex accepts only alphanumeric hashes, but the configured Rollup default uses base64url hashes, which can contain `-` and `_`. A valid asset such as `index-a_b2c3d4.js` is therefore misclassified. | Those legitimate Vite assets receive `no-cache` instead of the intended one-year immutable policy, making cache behavior depend on the randomly generated hash characters. | Accept Rollup’s full base64url alphabet, e.g. `[A-Za-z0-9_-]{8,}`, and add coverage for underscore/hyphen hashes and dotted names such as `vendor.min-a_b2c3d4.js`. |

What looks good: stored file visibility remains authoritative and consistently keyed through `sidebarDeviceVisibilityKey()` on both the device card and sidebar, with the entry node mapped to `self`. Loading/error suppression, keyboard sorting, isolated device-grid/device-folder DnD contexts, Base UI inline-style precedence, conditional-request precedence, and bodyless 304 responses all check out. The e2e pointer sequence clearly crosses the 8px activation threshold before dropping. The targeted Bun run passed all 27 tests across the four affected unit-test files.