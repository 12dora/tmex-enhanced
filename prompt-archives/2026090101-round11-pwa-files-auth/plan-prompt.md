# 用户 prompt（2026-09-01）

继续开发, 请你分批commit并在最后push替换本机tmex, Think in English, Send English prompt.
任务：
1. iOS等pwa应用加载变慢了(首次进入, 后续使用), 检查是否传输了不必要的信息, 或者不在前端显示时依旧传递信息
    1. 使用pwa应用时, 电脑端终端画面也会跟着手机一起动, 例如画面缩成手机比例, 一起滚动. 你能否使二者分离(仅分离画面缩放, 滚动, 但是例如手机在输入框打字, 电脑还要同步到)
2. 管理设备-设备tab很多设备明明没有开启文件的侧栏显示功能, 却还显示在文件栏, 请你修复
3. 左侧文件栏拖动排序会使画布横向滚动,你应该固定画布(侧栏), 禁止横向滚动
4. 分析启用账号密码登录后暴露在公网的安全性, 但要注意绝对避免过度防御
5. 目前手机首次使用pwa时, 其他节点都需要每一个节点输入一次用户名和密码登录, 请你使其能自动利用本机凭证登录, 登录失败再让用户手工登录(但要注意有没有打破安全边界)

注:
1. grok 4.6, high担任后端编码(通过grok build调用)
2. opus5(high)担任前端编码
3. codex（gpt-5.6-luna,  xhigh)探索代码
4. codex（gpt-5.6-sol,  high)担任code reviewer, codex存在过度防御的问题, 你应该自行判断问题是否修复
5. 你担任指挥官和planner, 激进并行, 合理控制每个agent的工作量, 避免单一agent上下文过大
