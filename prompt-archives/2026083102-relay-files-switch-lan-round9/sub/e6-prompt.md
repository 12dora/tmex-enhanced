You are exploring a Bun + TypeScript monorepo (tmex). Read-only: do NOT modify files; you cannot write files — output the COMPLETE report as your final message (markdown, ≤220 lines, file:line citations, quoted code). Verify by reading code; do not speculate. Do not run servers or tests.

# Task E6: is the tmex self-upgrade crash-safe?

The install/upgrade CLI lives in `packages/app` (npm package `tmex-cli`, Node-compatible bootstrap; commands `init`, `upgrade`, `upgrade --apply-current-package`, plus the `install.sh` at repo root and the `~/.local/bin/tmex` shim). The installed layout is `<installDir>/{cli,resources,runtime,native,data/tmex.db,app.env,run.sh,install-meta.json,tmex.log}` under `~/Library/Application Support/tmex` (macOS, launchd) or `~/.local/share/tmex`-style dirs on Linux (systemd user unit). Release assets are downloaded from GitHub Releases (`packages/shared/src/release/source.ts`).

Answer, with evidence, the question: **if the machine loses power or the CLI process is killed at ANY point during `upgrade`, is the previously working version still runnable, and is the new version only made live after it is proven to work (health-checked/reachable), with automatic rollback otherwise?**

Trace the full upgrade sequence in order (cite each step's code): download → verify (checksum/signature?) → extract to where (temp/staging dir on the same filesystem?) → DB backup (`tmex.db` + `-wal` + `-shm`? consistent snapshot while the service is running?) → service stop → file replacement (rename/atomic swap? rm -rf then copy? per-directory? in what order: cli/, resources/, runtime/, native/) → env/run.sh rewrite (atomic write? temp+rename?) → migrations (run by the CLI or by the runtime at first start? are they transactional/idempotent; what happens if the new runtime applied a migration and then we roll back to the old runtime — can the old runtime open the newer schema?) → service start → health check (which endpoint, timeout, what counts as success: `/healthz` only, or also login page / mesh uplink?) → rollback (what triggers it, what it restores — files only or also DB; does it restart the old service; does it handle a *partially* replaced tree) → `install-meta.json` update (when is it written — before or after health check?) → cleanup of old/staging dirs.

For each step, state the crash window and the resulting state if interrupted there: (a) old version still intact and running/restartable? (b) service unit points to which files? (c) is there a marker that lets the next `upgrade`/`init` or the runtime itself detect and repair a half-finished upgrade? (d) is the DB at risk (backup taken before or after stop; WAL files included; the backup copied with the service still writing)?

Also check: the launchd/systemd unit and `run.sh` — do they reference version-specific paths (e.g. `<installDir>/runtime-<version>`) or a stable path that is swapped? Is there a "current" symlink pattern? How does the runtime find `TMEX_FE_DIST_DIR`/`TMEX_MIGRATIONS_DIR` (from run.sh) — could a mismatch between new runtime and old resources occur mid-upgrade? What does `install.sh` (fresh install path) do if interrupted? Any lock preventing two concurrent upgrades? Any existing tests covering rollback (`packages/app/src/**/*.test.ts`)?

Finish with: (1) a verdict table (step → interrupted → old version usable? auto-repair? data risk?), (2) the concrete gaps ranked by severity, (3) a minimal design for a crash-safe scheme that fits the existing layout (e.g. staged `versions/<v>/` + atomic `current` symlink flip after health check + journal file + rollback-on-boot), listing the files to change and the migration path for already-installed machines (the shim, run.sh, launchd plist).
