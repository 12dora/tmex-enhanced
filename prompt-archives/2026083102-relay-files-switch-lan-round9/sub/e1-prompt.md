You are exploring a Bun + TypeScript monorepo (tmex: multi-node tmux/terminal web app; gateway = apps/gateway, frontend = apps/fe, shared = packages/*). Read-only: do NOT modify files. You cannot write files; output the COMPLETE report as your final message (markdown). Be concrete: cite file paths with line numbers, quote the relevant code, name the exact symbols/props/i18n keys involved. Do not speculate — verify by reading code. Keep the report focused and under ~250 lines. Do not run the dev server or tests. Do not touch any tmux session named `tmex` and never run tmux commands on the default socket.

# Task E1: terminal header "connection details" — relay status shows "unknown"; download button

Context: In the terminal view there is a badge/popover at the top-right of a terminal card showing the link path (i18n zh_CN text "经Hub中转" = "relayed via Hub", plus "直连"/LAN/WAN variants). When the link is relayed, the "connection details" panel shows many fields as "未知" (unknown). Also there is a "download" button in the terminal page header whose purpose is unclear.

Find and report:
1. The component(s) rendering the link badge and the connection-details popover (apps/fe, probably under terminal/ or panels). List every field shown in the details popover, where each value comes from (which store / query / WS event / API), and under what condition each falls back to "unknown". Include the i18n keys (packages/shared/src/i18n/locales/zh_CN.json, en, ja) for "经Hub中转" and every "未知"/unknown fallback.
2. The data path: how does the frontend learn reach/transport/rtt/peer address for a node? Trace from gateway (mesh runtime NodeEvent / node status API / `/api/nodes` or similar, `transport`, `rttMs`, `reach` lan/wan/relay) to the frontend store. Identify exactly which fields the gateway does NOT populate in the relay case (e.g., peer address, rtt, transport, since, hub name) and whether the data exists somewhere on the gateway side (hub runtime, mesh transport, uplink state) so it could be populated. Be specific: which gateway file/function builds the status object, and what it emits in relay vs direct cases.
3. The "download" button in the terminal page header: find it, what handler it invokes, whether it does anything meaningful in the terminal context (vs the files page where a download button must be kept). Report the exact component/lines and any tests/e2e selectors referencing it.
4. Any existing tests covering the badge/details (unit or e2e) that a change would need to update.
