You are exploring a Bun + TypeScript monorepo (tmex: multi-node tmux/terminal web app; gateway = apps/gateway, frontend = apps/fe, shared = packages/*). Read-only: do NOT modify files. You cannot write files; output the COMPLETE report as your final message (markdown). Be concrete: cite file paths with line numbers, quote the relevant code, name the exact symbols/props/i18n keys involved. Do not speculate — verify by reading code. Keep the report focused and under ~250 lines. Do not run the dev server or tests. Do not touch any tmux session named `tmex` and never run tmux commands on the default socket.

# Task E2: sidebar "Files" tab only shows local files for remote-node devices

Context: In the devices management page, each device card has a toggle for "files" (sidebar files visibility, `sidebarFilesVisibility`, default = device has roots). Devices on remote nodes (non-self nodes in the hub mesh, e.g., a node named "jiefa-app") can have file roots configured, but when the sidebar "Files" tab is opened it only shows local (self-node) files. The user wants the sidebar Files UI refactored to support multiple nodes: clearly show which node each file root/device belongs to (node name like "jiefa-app"), and support drag-to-reorder.

Find and report:
1. The sidebar Files tab implementation (apps/fe): components, the store/query it reads (which runtime instance — self only? `resolveAgentStore`-style self resolution?), how roots/devices are listed, how the file tree is fetched (`/api/files/browse`? per-node runtime? via mesh forwarding?). Explain precisely why remote-node devices' files don't show (which runtime/API is called and with which node scoping).
2. How other sidebar tabs (terminals/sessions tab, agents) already aggregate multiple nodes — the pattern used (per-node runtime map, `RuntimeProvider`, node registry store, device→node mapping, node display names). Point to the exact hooks/selectors to reuse for node names and per-node API clients.
3. Existing drag-to-reorder implementation in the sidebar (devices/sessions ordering uses dnd — which library, which persistence: local storage? server-side order API? `sidebar order` preferences). Report the reusable pieces (hooks, order persistence keys, API endpoints) so a files-ordering feature can follow the same pattern.
4. Files-related API routes on the gateway: `/api/files/*` and how they are authenticated/forwarded across the mesh (`/api/mesh-internal/*`, `x-tmex-mesh-peer`, per-node HTTP forwarding), and whether calling a remote node's files API from the browser already works (per-node runtime base URL + node login gate `useNodeLoginGate`).
5. Relevant tests (unit + e2e) touching the sidebar files tab and sidebar ordering.
