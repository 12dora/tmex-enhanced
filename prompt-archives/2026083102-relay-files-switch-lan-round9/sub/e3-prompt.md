You are exploring a Bun + TypeScript monorepo (tmex: multi-node tmux/terminal web app; gateway = apps/gateway, frontend = apps/fe, shared = packages/*). Read-only: do NOT modify files. You cannot write files; output the COMPLETE report as your final message (markdown). Be concrete: cite file paths with line numbers, quote the relevant code, name the exact symbols/props/i18n keys involved. Do not speculate — verify by reading code. Keep the report focused and under ~250 lines. Do not run the dev server or tests. Do not touch any tmux session named `tmex` and never run tmux commands on the default socket.

# Task E3: latency when switching between terminals (even local ones)

Context: User reports noticeable delay when switching between terminal cards/panes, even between terminals on the local node. We need to determine whether the delay is real and where it comes from, before optimizing.

Find and report (frontend apps/fe + packages/ghostty-terminal / terminal-ui / ws-client, and gateway apps/gateway tmux/session side):
1. The full switch path: what happens when the user selects a different terminal (route change? `?pane=`/sid selection, "select transaction" with a 2.5s grace, `RuntimeProvider` remount by runtime key, terminal component mount/unmount, ghostty wasm instance creation/reuse, canvas allocation, font/atlas rebuild, WebSocket subscribe/unsubscribe, TERM_HISTORY / snapshot replay, resize handshake, `writeRestoredHistory`, attach lock on the gateway). List each step with file:line and whether it is synchronous, awaited, or debounced/delayed by timers (name every setTimeout/debounce/requestAnimationFrame/idle callback and its delay).
2. Identify likely sources of perceived latency: (a) is the previous terminal torn down and a new one created on each switch, or are instances kept alive/hidden? (b) is there a loading/placeholder state ("connecting to device") gated on a WS round trip or history replay before first paint? (c) any intentional delays (grace periods, debounced selection, transition animations with durations, Suspense/lazy chunk loading per switch)? (d) gateway side: what work happens on attach/subscribe (tmux capture-pane, history snapshot size, canonical subarray cache, attach lock serialization)? Quantify what you can from code (buffer sizes, timer values, animation durations).
3. Existing instrumentation: performance marks, debug logs, e2e helpers (`window.__tmexE2eXterm`) or perf docs from prior rounds (search docs/ and prompt-archives/*round6*, *round7* for terminal switch / perf notes) so we don't redo work; report what earlier rounds already optimized and what they left.
4. Propose a concrete measurement plan (which marks to add, how to measure switch-to-first-paint and switch-to-live-output in a temporary instance), and a ranked list of optimization candidates with expected impact and risk.
