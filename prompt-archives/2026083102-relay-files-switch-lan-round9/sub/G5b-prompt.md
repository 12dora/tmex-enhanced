## Ground rules (read fully)

- Repo: Bun + TypeScript monorepo `tmex` (gateway `apps/gateway`, frontend `apps/fe`, shared packages under `packages/*`). Work ONLY inside the worktree `/Users/konata/code/tmex-enhanced-wt-r9` (branch `feat/round9-relay-files-perf`). `bun` is at `~/.bun/bin/bun` (add to PATH if missing). Everything runs on Bun, never Node.
- Several other agents are editing this same worktree concurrently. Touch ONLY the files listed in your "Owned files" section (plus new test files next to them). If you believe you must edit a file outside your scope, do NOT edit it — describe the needed change in your result file instead.
- Do NOT run any git command that changes state (no add/commit/stash/checkout/reset). Read-only git (log/blame/diff) is fine. The commander commits.
- NEVER touch the production tmex service (launchd, port 9883, `~/Library/Application Support/tmex/`) and NEVER run tmux commands on the default socket or against a session named `tmex`. Any tmux you need for tests must use an isolated socket (`tmux -L tmex-r9-<yourid>`).
- Do not run the dev server (`bun run dev`) and do not run Playwright e2e. Unit tests only: inside the package dir run `bun test` (for `apps/fe` use `bun test src/`). Before editing, record the baseline pass/fail counts of the packages you touch and `bunx tsc --noEmit -p .` error count; after editing, counts must not regress. Bun test summary lines carry ANSI colors — strip with `sed 's/\x1b\[[0-9;]*m//g'`. macOS has no `timeout` command.
- Run `bunx biome check <changed files>` (no `--write` on files you don't own; never lint generated files such as `packages/shared/src/i18n/resources.ts`, `types.ts`, `dist/*`).
- i18n: locale files are `packages/shared/src/i18n/locales/{zh_CN,en_US,ja_JP}.json`. Edit only the sub-object assigned to you, keep all three languages in sync, then run `bun run build:i18n` at the repo root to regenerate `resources.ts`/`types.ts`. Copy rules for zh_CN (from `/Users/konata/code/tmex-copy-guidelines.md`, read it before writing copy): say 「本机」 not 「这台机器」, avoid 「你」, one short sentence per line, state before static explanation, qualifiers in parentheses, English buttons in Title Case.
- No unnecessary code comments. No TODOs, no "simplified version", no leaving work for later — finish the whole task. Do not widen scope.
- When finished, write a concise report (what changed, file list, test/tsc before→after numbers, anything out of scope the commander must do) to the absolute path given in "Result file", then exit. The commander polls for that file.

# Task G5b — release-gate fixes for the crash-safe upgrader (review I) + complexity gate

Same owned files as G5 (`packages/app/src/**`, `install.sh`, `apps/gateway/src/system/upgrade.ts`, `apps/gateway/src/api/system-routes.ts`, the docs file). Read `G5-result.md` and `review-I.md` in the result folder first — every item below refers to review-I by number. The commander already changed two things after G5: `runPreflight` now allocates a real ephemeral `TMEX_PEER_PORT` (the candidate crashed on `'0'`) and the candidate runner captures a stdout/stderr tail into the preflight error (`CandidateHandle.logTail`). Keep both.
Result file: `/Users/konata/code/tmex-enhanced/prompt-archives/2026083102-relay-files-switch-lan-round9/sub/G5b-result.md`

## Blockers (fix all)
B1 (review 1) Rollback health check: the OLD version (1.1.3) has no `version` in `/healthz`. When rolling back / repairing to `fromVersion`, accept a missing `version` (require `status==='ok'`, `current` → fromVersion, and `startedAt` newer than the moment we restarted it); keep the strict version check for the candidate/new version only. Regression test with a real 1.1.3-shaped health body `{status:'ok',startedAt,restarting,env,tmux}`.
B2 (review 2) DB restore: `rollbackToOld()` must abort (keep journal + backup, exit non-zero) if `service.stop()` fails or the process is still alive afterwards; `restoreDbTrio()` must first remove the target `tmex.db`, `-wal`, `-shm` and then restore exactly the backed-up set (never leave a newer WAL next to an older db); fsync files + dir. The direct-process (`--no-service`) SIGKILL path must wait for exit.
B3 (review 3) Service mode: persist `serviceMode: 'managed' | 'none'` in `InstallMeta`; `init --no-service` must start the runtime itself and write `tmex.pid`; `run.sh` must write `<installDir>/tmex.pid` (its own `$$` before `exec`) so manually started no-service gateways are stoppable; upgrade/repair read the mode from meta (flag only overrides for init); the Web-triggered upgrade must pass the persisted mode (never install launchd/systemd on a `none` install) and refuse when there is no pid ownership (`serviceMode==='none'` without a live pid file → error message telling the operator to stop the process first).
B4 (review 4) Candidate pid in journal: write `{candidatePid, candidateStartedAt}` to the journal when the preflight candidate starts; `--repair` from `preflight` must kill that pid (verify identity by command line containing the candidate `server.js` path) and wait for exit before deleting the candidate dir.
B5 (review 5) Native addon: legacy conversion copies `native/` into `versions/<from>/native`; when deploying the candidate, if the current version has a native addon installed (manifest present), install the pinned native addon into the candidate dir BEFORE preflight (reuse the real `direct` install code path with the candidate's version dir; verify manifest/checksum) — if that fails, abort the upgrade (do not switch to a version without native when the old had it) unless `--allow-missing-native`. Test with the real `reenableDirectIfNeeded`/install function against a fixture, not a no-op stub.
B6 (review 6) Checksum semantics shared by CLI, `install.sh` and the gateway web upgrade: only an explicit HTTP 404 for `SHA256SUMS` means "not published" (warn + continue); any network error / non-2xx must abort; verification happens before extraction/execution; the gateway `stageGithubRelease()` must verify too (share the helper via the existing package boundary or duplicate the minimal logic in the gateway with a test).
B7 (review 7) `~/.bun/bin/tmex`: never `rm` first — create a temp symlink and atomically rename over the managed target (same as `~/.local/bin`). Fault-injection test (die after rm no longer possible → assert no window without a target).
B8 (review 8) Lock: record `startedAt` + the lock owner's process start identity (e.g. `ps -o lstart=` / `/proc/<pid>/stat` starttime, or the command line containing `tmex`) and treat a lock as stale when the pid is dead OR its identity does not match; add the pid-alive-but-different-process test.
B9 (review 9) Repair from `switching`/`backup`: after `service.start()` verify health/running BEFORE cleaning transaction dirs or writing a terminal phase; on failure keep journal+backup and exit non-zero.

## Should-fix (do them unless it becomes disproportionate; say which you skipped)
S2 gateway `UpgradeController`: watch the child's early exit and reset to idle/error instead of staying `executing` forever.
S3 same-version upgrade (`toVersion === current`) must be a healthy no-op (or a reinstall txn that never removes `current`), not an aborted journal.
S4 record `keepBackup` in the journal so the next repair does not delete a backup the user asked to keep.
S5 `--repair` garbage sweep: remove orphan `staging/*` without journal, `*.tmp` leftovers (`upgrade-state.json.*.tmp`, `current.*.tmp`, `run.sh.*.tmp`, shim `tmex.*.tmp`), finish prune/legacy cleanup when journal is `committed`, and remove duplicate legacy top-level dirs once `current` exists and points to a version containing the same files; never touch `current`'s target or `data/`.
S1 preflight DB copy: use SQLite online backup (`bun:sqlite` via the *candidate* bun binary running a tiny script, since the CLI itself is Node) or at minimum copy after a `wal_checkpoint(TRUNCATE)` attempt; document the limitation if you keep file copy.

## Complexity gate
`cd /Users/konata/code/tmex-enhanced-wt-r9 && bun scripts/complexity/gate.ts` currently fails on your files: `upgrade-apply.ts applyUpgrade CC 24 > 15`, `init.ts buildInitConfig CC 18 > 17`, `init.ts runInit CC 23 > 19`, `upgrade.ts runUpgrade CC 17 > 15`. Bring them under by extracting phase functions/helpers (no allowlist edits).

## Verification
`packages/app` `bun test` (baseline 507 pass / 0 fail) + `bunx tsc --noEmit -p .` (≤1) + `bun run build:cli`; `apps/gateway` `bun test src/system src/api` + tsc ≤ 21; biome on changed files; gate green. Also run the scratch rehearsal the commander used (do NOT touch production; ALWAYS pass `--install-dir`; note `tmex upgrade` with an unknown flag such as `--help` silently runs a real upgrade against the default install dir — add strict unknown-flag rejection to the CLI arg parser as part of this task): `$SCRATCH=/private/tmp/claude-501/-Users-konata-code-tmex-enhanced/ca52e5db-7f6e-4446-8b64-e719939894f2/scratchpad/rehearsal` contains `pkg113/package` (1.1.3 tarball extracted) and the recipe in my rehearsal: build a legacy layout install dir with cli/runtime/resources + app.env (port 19893, `TMEX_TMUX_SOCKET=tmex-r9-rehearsal`, `TMEX_ROLES=standalone`, `TMEX_DIRECT_ENABLED=false`) + run.sh + install-meta.json (cliVersion 1.1.3), start it with `bash run.sh & echo $! > tmex.pid`, then `cd <fresh 1.1.4 package dir from npm pack> && node bin/tmex.js upgrade --apply-current-package --no-service --install-dir <dir> --lang zh-CN`; expect `committed`, healthz version 1.1.4, `versions/{1.1.3,1.1.4}`, empty staging/backups, no top-level cli/runtime/resources. Then simulate a failed switch (e.g. temporarily make the new version's `run.sh` target fail or set an impossible port) and assert `rolled_back` with the old version serving again. Kill everything you started (`pkill -f rehearsal/install` scoped to that path only) and `tmux -L tmex-r9-rehearsal kill-server`. Report the exact outputs.
