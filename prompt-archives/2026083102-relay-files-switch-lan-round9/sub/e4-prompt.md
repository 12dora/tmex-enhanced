You are exploring a Bun + TypeScript monorepo (tmex: multi-node tmux/terminal web app; gateway = apps/gateway, frontend = apps/fe, shared = packages/*). Read-only: do NOT modify files. You cannot write files; output the COMPLETE report as your final message (markdown). Be concrete: cite file paths with line numbers, quote the relevant code, name the exact symbols/props/i18n keys involved. Do not speculate — verify by reading code. Keep the report focused and under ~250 lines. Do not run the dev server or tests. Do not touch any tmux session named `tmex` and never run tmux commands on the default socket.

# Task E4: LAN direct connection instead of Hub relay

Context: This machine (macOS, node `konata-mac`, production instance on port 9883) and two Linux nodes `jiefa-app` (10.110.88.3) and `jiefa-dns-1` (10.110.88.5) are all on the same private network, but all joined a Hub at `https://ai.jiefakj.com:18443` (public server). Node↔node and browser↔node traffic currently appears to be relayed via the Hub. The mesh design claims reach levels lan/wan/relay with LAN upgrade and RTC (WebRTC data channel) direct connections. We want to know whether a direct LAN connection is possible in this topology and what prevents it.

Find and report (apps/gateway mesh/hub/rtc code, packages/shared link/auth, apps/fe rtc client, docs/hub/*):
1. How direct connections are attempted today: the exact mechanism(s) — (a) node→node direct TCP/WebSocket dial to advertised LAN addresses? (b) WebRTC data channels with STUN/TURN (which ICE servers are configured, defaults, env vars)? (c) browser→node direct (browser RTC / direct HTTP to node base URL)? For each: who initiates, what addresses are advertised (how a node learns its own LAN IPs — `/api/system/addresses`?), where candidates are exchanged (via hub signaling?), timeouts, and the conditions under which the code decides to stay on relay.
2. The reach classification: how `lan`/`wan`/`relay` is determined (private-address check on the peer address), where `transport`/`rttMs` are set, and what "LAN upgrade" actually does (from docs/hub/2026082700-hub-node-architecture.md, 2026082800-hub-node-operations.md, prompt-archives/2026082801-hub-docker-e2e-multi-theme/plan-00-result.md, 2026082802-hub-live-e2e/plan-00-result.md — read the relevant "遗留"/known-issues sections about RTC/direct connection).
3. What the *browser* uses when the user opens a terminal of a remote node: does the browser talk to the entry gateway (self) which forwards via mesh to the remote node (through hub relay or node↔node direct), or does the browser connect to the remote node directly? Identify the code path for terminal WS streams to remote panes (`/api/mesh-internal/tmux/*`, stream binding, failover to relay).
4. Likely blockers in this topology and configuration knobs: e.g., ICE needs a reachable STUN server (mainland China reachability), host candidates only (no STUN needed on same LAN — is mDNS/host candidate gathering enabled?), UDP blocked by local firewall/Surge on macOS, node not advertising LAN addresses, hub-only signaling never triggered for node↔node when both are "nodes" (only hub↔node?), direct dial only attempted for certain roles. Report the exact code conditions and env/config variables (list every env var / settings key touching RTC/ICE/direct/peer port, e.g. `TMEX_PEER_PORT`, ICE server config, `TMEX_RTC_*`).
5. Existing diagnostics: log prefixes (`[mesh][rtc]`), status APIs or UI that would tell us why direct failed on the live machines, and a step-by-step diagnostic plan the operator can run on the live nodes (read-only commands: which log lines to grep, which API endpoints to curl with auth) to confirm the blocker.
6. Ranked list of changes that would enable LAN direct here, with files to touch and risk.
