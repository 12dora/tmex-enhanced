You are exploring a Bun + TypeScript monorepo (tmex: multi-node tmux/terminal web app; gateway = apps/gateway). Read-only: do NOT modify files. You cannot write files; output the COMPLETE report as your final message (markdown, ≤200 lines) with file:line citations and quoted code. Do not speculate — verify by reading code. Do not run servers/tests/tmux.

# Task E5: why does a node's key-log replica / peer cache stop receiving newly admitted nodes?

## Observed on a live node (macOS, tmex 1.1.3, role node, joined Hub `https://ai.jiefakj.com:18443`; the Hub runs an older build reporting inventory version "1.0.2"):
- SQLite `node_certs` on this node contains only 3 rows with `admit_record_seq` 2 (hub node), 3 (self), 4 (docker-node). Two more nodes (`jiefa-app`, `jiefa-dns-1`) were admitted on the Hub today (2026-08-31) — they should be seq 5 and 6 — but never appeared locally.
- `peer_cache` has 3 rows (hub, hub-node, docker-node), `list_version = 30`, `last_seen_at` = 2026-08-30 13:22 local — i.e. no update for >30h although stderr shows the uplink repeatedly `[uplink] online hub=ai.jiefakj.com:18443` (with intermittent `offline reason=disconnected`) and the process restarted today 16:44.
- The node keeps RTC-dialing only the hub node id (`ec42f364…`); never the new nodes.

## Find and report
1. The complete path by which a node learns about other nodes: Hub `node.list` message (who sends it, when — on connect? on change? versioned by `list_version`?), how `uplink-client.ts` handles it (~`:297`, `:571`), how `peer_cache` rows are upserted/pruned and what condition skips the write (e.g. `list_version` not greater than stored, signature verification failure, unknown node cert, name/inventory decode failure). Quote the exact guard conditions.
2. The key-log replication path: how `node_certs` rows are added on a node (key-log records pulled from the hub: `key_log_head` fingerprint, catch-up from a seq, `admit` records, signature checks against the user's enrollment key), when it runs (connect-time only? periodic? on node.list version bump?), and every condition that would make the node silently stop at seq 4 (e.g. a record it cannot verify → stops the whole tail; a hub built before some protocol change sending a different record shape; head fingerprint mismatch handling; errors swallowed without logging). Identify log lines that would be printed in each failure case so the operator can grep for them (which log stream: stdout `tmex.log` or stderr `tmex.err.log`).
3. Version drift: the Hub is an older build (1.0.2 era, ~2026-08-29 `fa7f91e`) while the node is 1.1.3. List protocol changes between those (search `git log --since=2026-08-29 -- apps/gateway/src/hub apps/gateway/src/mesh/uplink-client.ts apps/gateway/src/mesh/key-log* packages/shared/src/auth` and read the relevant diffs) that could break node.list / key-log sync in either direction (new required fields, changed message types, changed signature domains, `list_version` semantics).
4. Whether the mesh relies on `node_certs` for: trusting a peer for ws-secure/dc handshakes (`isTrusted`), accepting relay streams, and the FE node list (`/api/mesh/nodes` — does it read the in-memory node.list or `peer_cache`?). I.e. what breaks for the user when the replica is stale (can they still see/use the new nodes via this entry?).
5. A ranked list of likely root causes for the observation with the exact code condition for each, the read-only diagnostic to confirm each (SQL against the node DB, log greps, API endpoints), and the fix (which files) for the top candidates, including any Hub-side upgrade requirement.
