## Ground rules (read fully)

- Repo: Bun + TypeScript monorepo `tmex` (gateway `apps/gateway`, frontend `apps/fe`, shared packages under `packages/*`). Work ONLY inside the worktree `/Users/konata/code/tmex-enhanced-wt-r9` (branch `feat/round9-relay-files-perf`). `bun` is at `~/.bun/bin/bun` (add to PATH if missing). Everything runs on Bun, never Node.
- Several other agents are editing this same worktree concurrently. Touch ONLY the files listed in your "Owned files" section (plus new test files next to them). If you believe you must edit a file outside your scope, do NOT edit it — describe the needed change in your result file instead.
- Do NOT run any git command that changes state (no add/commit/stash/checkout/reset). Read-only git (log/blame/diff) is fine. The commander commits.
- NEVER touch the production tmex service (launchd, port 9883, `~/Library/Application Support/tmex/`) and NEVER run tmux commands on the default socket or against a session named `tmex`. Any tmux you need for tests must use an isolated socket (`tmux -L tmex-r9-<yourid>`).
- Do not run the dev server (`bun run dev`) and do not run Playwright e2e. Unit tests only: inside the package dir run `bun test` (for `apps/fe` use `bun test src/`). Before editing, record the baseline pass/fail counts of the packages you touch and `bunx tsc --noEmit -p .` error count; after editing, counts must not regress. Bun test summary lines carry ANSI colors — strip with `sed 's/\x1b\[[0-9;]*m//g'`. macOS has no `timeout` command.
- Run `bunx biome check <changed files>` (no `--write` on files you don't own; never lint generated files such as `packages/shared/src/i18n/resources.ts`, `types.ts`, `dist/*`).
- i18n: locale files are `packages/shared/src/i18n/locales/{zh_CN,en_US,ja_JP}.json`. Edit only the sub-object assigned to you, keep all three languages in sync, then run `bun run build:i18n` at the repo root to regenerate `resources.ts`/`types.ts`. Copy rules for zh_CN (from `/Users/konata/code/tmex-copy-guidelines.md`, read it before writing copy): say 「本机」 not 「这台机器」, avoid 「你」, one short sentence per line, state before static explanation, qualifiers in parentheses, English buttons in Title Case.
- No unnecessary code comments. No TODOs, no "simplified version", no leaving work for later — finish the whole task. Do not widen scope.
- When finished, write a concise report (what changed, file list, test/tsc before→after numbers, anything out of scope the commander must do) to the absolute path given in "Result file", then exit. The commander polls for that file.

# Task G3c — fix the two remaining blockers in the concurrent ws-secure dial (review G)

Owned files: `apps/gateway/src/mesh/peer-manager.ts` (+ `peer-manager.test.ts`), `apps/gateway/src/mesh/peer-ws-race.ts` (+test), `apps/gateway/src/mesh/mesh-routes.ts`, `apps/gateway/src/mesh/node-list-projection.ts` (+tests). Read `G3b-result.md` and `review-G.md` in the result folder first.
Result file: `/Users/konata/code/tmex-enhanced/prompt-archives/2026083102-relay-files-switch-lan-round9/sub/G3c-result.md`

1. Blocker 1.1 — stop/generation change between the race returning a winner and `dialWsSecure()` resuming leaves an authenticated, untracked winner socket open (`peer-manager.ts:~1427`, `peer-ws-race.ts:~308`). Fix: in the stale branch close `winner.session` before throwing, AND make the race's parent-abort path close an already elected winner and return `winner: null`. Regression test: winner elected → stop() before track → assert the winner session is closed and nothing is live.

2. Blocker 1.2 — inbound side: `acceptDirect()` calls `rememberKeys()` after `track()` unconditionally (`peer-manager.ts:~1460/1522`). With the initiator now dialing several endpoints concurrently, the acceptor can authenticate two inbound sessions A and B from the same peer: `track(B)` parks B (A not yet quiesce-negotiated) and returns A, then `rememberKeys(B)` overwrites A's keys with B's; if B later wins and is promoted from parked, `ParkedInbound` has no keys so the new live has none. Fix: keys must travel with the concrete session through `track` → `parkInbound` → `installLive` (store keys on the parked record / pass them into installLive), and `rememberKeys` must only ever apply to the session that actually became live. Add a test that drives the ACCEPTOR PeerManager with two inbound authenticated sessions from the same node in the same turn, then closes the first so the parked one is promoted, asserting `sessionKeysOf()` matches the promoted session's keys at every step (extend the existing same-turn test harness for the acceptor side).

3. Should-fix 2.1 — `linkDetailOf()` still does a per-node `getPeer()` whose `endpoints` are then ignored by the projection. Make `linkDetailOf` purely in-memory (live + lastDirectAttempt; no DB) and keep endpoints coming from the already-built `peerById` in `collectNodes`/projection.

4. Should-fix 2.2 — add a test with an abort-aware scheduler holding pending sleeps: several staggered candidates queued, `stop()` called, assert every pending sleep rejected/cleared and no further `wsFactory` calls.

Run `apps/gateway` `bun test` before/after (baseline 3008 pass / 0 fail; a concurrent agent edits `ws/borsh/switch-barrier*` and `rtc/data-channel-carrier*` — failures there are not yours) and `bunx tsc --noEmit -p .` (21 baseline), biome on changed files, and the repo complexity gate (`bun scripts/complexity/gate.ts` at the repo root must stay green — extract helpers if you grow a function past its lock; do not edit the allowlist). Do not dial 127.0.0.1:39001 in tests (a real peer server listens there on this machine).
