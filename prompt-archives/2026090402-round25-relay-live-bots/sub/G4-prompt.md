## Common rules (read first)

- Repo worktree: `/Users/konata/code/tmex-r25` (branch `feat/round25-relay-live-bots`, base = main 1.1.24). Bun-only runtime (`bun`, `bunx`; if `bun` is missing, `export PATH="$HOME/.bun/bin:/opt/homebrew/bin:$PATH"`). Never use Node for app code; only `packages/app` CLI stays Node-compatible.
- OTHER AGENTS ARE EDITING THIS SAME WORKTREE IN PARALLEL. Touch ONLY the files in your scope (listed in your task). Do NOT run `git add/commit/stash/checkout/reset`, do NOT run `bun run build:i18n` / formatters on files outside your scope, do NOT run lint/format on generated files (`packages/shared/src/i18n/resources.ts`, `types.ts`, `resources/fe-dist/*`, `dist/*`). If you must add i18n keys, edit the source locale JSON files under `packages/shared/src/i18n/locales/` (or wherever the source JSON lives) for ALL THREE locales `zh_CN.json`, `en_US.json`, `ja_JP.json` (a locale-consistency test enforces identical key sets), editing only your own nested keys; the commander regenerates resources.
- Never touch the production tmex on this machine: service on port 9883, `~/Library/Application Support/tmex/`, launchd; never touch the tmux session named `tmex` or the default tmux socket. Any temporary instance must set `TMEX_TMUX_SOCKET=<unique>`, unique `GATEWAY_PORT`/`TMEX_PEER_PORT` (use 19xxx), `NODE_ENV=test`, its own `DATABASE_URL` under the scratch dir, and `TMEX_BIND_HOST=127.0.0.1`.
- Tests: run only the package you touch — `cd apps/gateway && bun test <path>` / `cd apps/fe && bun test src/<path>` (never bare `bun test` in apps/fe — it picks Playwright specs) / `cd packages/app && bun test <path>`. Type-check with `bunx tsc --noEmit -p .` in the package (baseline error counts: gateway 0, fe 0, app 0, shared 0, ws-client 0, stores 1, api-client 5, theme 9). Lint your files with `bunx biome check <files>` (no `--write` on files outside scope). macOS has no `timeout` command.
- Code style: TypeScript, no unnecessary comments (only for genuinely complex logic, written in Simplified Chinese), variable names in English, prefer small functions (complexity gate: cyclomatic ≤ 15 per function, file ≤ 600 lines, enforced by `bun run lint` at repo root — do not add allowlist entries).
- UI copy rules: Simplified Chinese (zh-CN) primary + English; terminology: 「中继」= relay only, 「Hub」stays "Hub"; read `/Users/konata/code/tmex-copy-guidelines.md` before writing any user-facing text.
- Do not leave TODOs, stubs or "simplified versions". Finish the whole scope. Do not widen scope.
- When done, write your report to the absolute result path given in the task (Markdown, Simplified Chinese OK): what changed (file list), how verified (commands + counts), anything left/uncertain. Write the result file LAST, then exit.

## Task G4 — Relay enrollment fan-out to all configured relays (backend)

Result file: `/Users/konata/code/tmex-r25/prompt-archives/2026090402-round25-relay-live-bots/sub/G4-result.md`.
Reference: `/Users/konata/code/tmex-r25/prompt-archives/2026090402-round25-relay-live-bots/sub/EX4-report.md` section L4, and `docs/relay/2026090304-relay-role.md` §5, §7.

Scope (only): `apps/gateway/src/relay/**` (relay-side: add the HTTP route + share the create logic with the uplink `relay.enroll.create` handler; tests), `apps/gateway/src/mesh/relay-routes.ts` (+tests) for the node-side fan-out, `apps/gateway/src/mesh/relay-uplink-client.ts` only if needed, `packages/app/src/lib/relay-pack-upload.ts` (+test) to request `join-material?scope=all`, `packages/app/src/commands/relay-join.ts` (+test) so redeem skips a relay that answers 404 `RELAY_ENROLLMENT_UNKNOWN` and continues with the next entry, `docs/relay/2026090304-relay-role.md` (§5 join-material paragraph + §7.1 route table + §13 "多中继" bullet). Do NOT touch `apps/fe` (task F3 adapts the browser to the new response), `hub-runtime.ts`, `hub-authorization.ts`, `auth-key-log-routes.ts`, `membership-reset.ts`, messaging/events (other agents).

Implement:
1. Relay side: `POST /api/relay/tenants/:tenantId/enrollments` (tenant-token auth, body `{ id, enroll_pk, authorization, authorization_sig, exp }`) sharing one function with the existing uplink `relay.enroll.create` handling (validate authorization against the tenant's current root pk, exp bounds, per-tenant enrollment quota 32 / rate 16 per 60 s — reuse the existing `ENROLLMENT_QUOTA` / `ENROLLMENT_RATE_LIMITED` logic), idempotent when the same `id` arrives with an identical payload (409 `RELAY_ENROLLMENT_CONFLICT` if the payload differs). Add it to the trusted-proxy / origin-guard exemptions exactly like the other `/api/relay/tenants/` routes (check `ACCESS_EXEMPT` / `isServicePath` handling for the prefix).
2. Node side: `POST /api/mesh/relay/enrollments` fans out to EVERY row in `mesh_relays` (attached one via the uplink as today OR via HTTP — pick one path for all: HTTP with the stored tenant token is simplest; keep the uplink path for the attached relay only if HTTP cannot reach it, e.g. loopback resolution via `resolveRelayDialUrl`), concurrently with a 5 s timeout per relay, `Promise.allSettled`; keep the local `enrollment_tokens` row if at least one relay accepted, otherwise invalidate it and return 502 `RELAY_ENROLL_FANOUT_FAILED`. Response (201): `{ ok: true, id, expiresAt, relays: [{ url, tenantId, token, accepted: true } | { url, tenantId, accepted: false, error }] }` — note `relays` changes from `string[]` to objects; also keep returning `token` per accepted relay so the browser can build the r3 join string without a second `join-material` call (that is what `join-material?scope=all` returns today — reuse its material builder; do not return tokens for relays that rejected).
3. `packages/app/src/lib/relay-pack-upload.ts:~164` must request `?scope=all` so every relay gets its own sealed pack. `relay-join.ts:~253` `redeemAgainstRelays()`: treat 404 `RELAY_ENROLLMENT_UNKNOWN` / `RELAY_NOT_FOUND` as "try next relay" (only CA-fingerprint mismatch stays fatal).
4. Tests: relay route (accept, quota, rate, idempotent, conflict, bad token), node fan-out (both accept; one timeout one accept; all reject → invalidated + 502), pack upload requests scope=all, CLI redeem continues past 404. Existing in-process relay integration tests (`apps/gateway/src/relay/integration/*.integration.test.ts` — these are bun tests despite the name? check how they are run: `bun test src/relay`) must stay green.
Verification: `cd apps/gateway && bun test src/relay src/mesh/relay && bunx tsc --noEmit -p .` (0); `cd packages/app && bun test src/lib/relay-pack-upload src/commands/relay-join && bunx tsc --noEmit -p .` (0); `bun run lint`.
