## Common rules (read first)

- Repo worktree: `/Users/konata/code/tmex-r25` (branch `feat/round25-relay-live-bots`, base = main 1.1.24). Bun-only runtime (`bun`, `bunx`; if `bun` is missing, `export PATH="$HOME/.bun/bin:/opt/homebrew/bin:$PATH"`). Never use Node for app code; only `packages/app` CLI stays Node-compatible.
- OTHER AGENTS ARE EDITING THIS SAME WORKTREE IN PARALLEL. Touch ONLY the files in your scope (listed in your task). Do NOT run `git add/commit/stash/checkout/reset`, do NOT run `bun run build:i18n` / formatters on files outside your scope, do NOT run lint/format on generated files (`packages/shared/src/i18n/resources.ts`, `types.ts`, `resources/fe-dist/*`, `dist/*`). If you must add i18n keys, edit the source locale JSON files under `packages/shared/src/i18n/locales/` (or wherever the source JSON lives) for ALL THREE locales `zh_CN.json`, `en_US.json`, `ja_JP.json` (a locale-consistency test enforces identical key sets), editing only your own nested keys; the commander regenerates resources.
- Never touch the production tmex on this machine: service on port 9883, `~/Library/Application Support/tmex/`, launchd; never touch the tmux session named `tmex` or the default tmux socket. Any temporary instance must set `TMEX_TMUX_SOCKET=<unique>`, unique `GATEWAY_PORT`/`TMEX_PEER_PORT` (use 19xxx), `NODE_ENV=test`, its own `DATABASE_URL` under the scratch dir, and `TMEX_BIND_HOST=127.0.0.1`.
- Tests: run only the package you touch — `cd apps/gateway && bun test <path>` / `cd apps/fe && bun test src/<path>` (never bare `bun test` in apps/fe — it picks Playwright specs) / `cd packages/app && bun test <path>`. Type-check with `bunx tsc --noEmit -p .` in the package (baseline error counts: gateway 0, fe 0, app 0, shared 0, ws-client 0, stores 1, api-client 5, theme 9). Lint your files with `bunx biome check <files>` (no `--write` on files outside scope). macOS has no `timeout` command.
- Code style: TypeScript, no unnecessary comments (only for genuinely complex logic, written in Simplified Chinese), variable names in English, prefer small functions (complexity gate: cyclomatic ≤ 15 per function, file ≤ 600 lines, enforced by `bun run lint` at repo root — do not add allowlist entries).
- UI copy rules: Simplified Chinese (zh-CN) primary + English; terminology: 「中继」= relay only, 「Hub」stays "Hub"; read `/Users/konata/code/tmex-copy-guidelines.md` before writing any user-facing text.
- Do not leave TODOs, stubs or "simplified versions". Finish the whole scope. Do not widen scope.
- When done, write your report to the absolute result path given in the task (Markdown, Simplified Chinese OK): what changed (file list), how verified (commands + counts), anything left/uncertain. Write the result file LAST, then exit.

## Task G6 — Fix backend review findings (R1)

Result file: `/Users/konata/code/tmex-r25/prompt-archives/2026090402-round25-relay-live-bots/sub/G6-result.md`. The full review is at `/Users/konata/code/tmex-r25/prompt-archives/2026090402-round25-relay-live-bots/sub/R1-report.md` — read it first. Fix exactly the items below (the commander decided the rest are not to be changed). All code is committed; you are the only agent editing the backend now, but still no git operations.

1. R1-#1 (blocker) Telegram group takeover via `/start`: `apps/gateway/src/telegram/service.ts` + `apps/gateway/src/db/telegram.ts` `createOrUpdatePendingTelegramChat()`. An authorized chat row must never have its `user_id` changed by a later `/start` (keep the "already authorized" reply, do not touch `user_id`; only pending rows may update it). For authorized GROUP/supergroup rows whose `user_id` is null (rows that predate migration 0044), commands must be refused (silently, like unauthorized) until an admin re-approves the chat — implement by making the approve route (`telegram-routes.ts` approve handler) accept the pending row's `user_id` as today, and add a tiny helper so that a legacy authorized group row can be re-bound: when `/start` arrives on an authorized group row with `user_id IS NULL`, DO NOT bind; reply with the existing "already authorized" text. Document in `docs/messaging/2026090402-messaging-command-template.md` that legacy group bindings need remove + re-bind to enable commands. Private chats (`chat.id === from.id`) keep working without the `user_id` check. Tests for both.
2. R1-#5: pass `localNodeId` to `inspectHubAuthRecordCompat` at `apps/gateway/src/hub/hub-runtime.ts:~651` and `apps/gateway/src/hub/uplink-server.ts:~1601` (`this.config.nodeId ?? this.config.hubNodeId` — verify the config field names). Test: the hub's own cert with a null/old `nodes.version` no longer blocks a record submitted via uplink/forward.
3. R1-#6: `apps/gateway/src/mesh/relay-enrollment-fanout.ts` — for the ATTACHED relay, treat HTTP 404/405 and relay error codes `RELAY_NOT_FOUND` / `RELAY_METHOD_NOT_ALLOWED` as "no HTTP route on this relay" and fall back to the uplink `relay.enroll.create` path (older 1.1.23/1.1.24 relays). Non-attached relays that 404 stay `accepted: false` with a clear error. Test both.
4. R1-#7: `packages/app/src/runtime/membership-reset.ts` `localRootPublicKey()` — resolve the user via `node_identity.userId` (not `listUsers()[0]`); if no identity or the user is missing, skip tenant deletion (log) rather than guessing. Test.
5. R1-#8: `apps/gateway/src/relay/relay-enroll-create.ts` — `exp` must be `Number.isSafeInteger` (and the same for any other numeric fields parsed there); invalid → 400 `RELAY_INVALID_BODY`. Test with `300000.5`.
6. R1-#9: `apps/gateway/src/messaging/adapter.ts` Telegram rendering — chunk BEFORE escaping (split the raw text on line boundaries within the limit accounting for the worst-case escape growth, then escape each chunk and wrap each chunk in its own `<pre>`/tags), so no entity or tag is ever split. Test with a `tail` output containing many `<`/`&` near the boundary.
7. R1-#10: `apps/gateway/src/events/channels/notification-format.ts` `nodeLabelLine(event)` — prefer `event.payload.nodeName` (then `nodeId`) over the local identity so remote-session credential warnings and agent events name the right node. Test.
Verification: `cd apps/gateway && bun test src/telegram src/messaging src/db src/api/telegram-routes src/hub/hub-runtime src/hub/uplink-server src/mesh/relay src/relay src/events && bunx tsc --noEmit -p .` (only the pre-existing `native-datachannel.ts` TS5097 may remain); `cd packages/app && bun test src/runtime/membership-reset && bunx tsc --noEmit -p .`; `bun run lint`.
