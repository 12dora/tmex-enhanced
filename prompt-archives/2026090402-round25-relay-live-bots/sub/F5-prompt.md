## Common rules (read first)

- Repo worktree: `/Users/konata/code/tmex-r25` (branch `feat/round25-relay-live-bots`, base = main 1.1.24). Bun-only runtime (`bun`, `bunx`; if `bun` is missing, `export PATH="$HOME/.bun/bin:/opt/homebrew/bin:$PATH"`). Never use Node for app code; only `packages/app` CLI stays Node-compatible.
- OTHER AGENTS ARE EDITING THIS SAME WORKTREE IN PARALLEL. Touch ONLY the files in your scope (listed in your task). Do NOT run `git add/commit/stash/checkout/reset`, do NOT run `bun run build:i18n` / formatters on files outside your scope, do NOT run lint/format on generated files (`packages/shared/src/i18n/resources.ts`, `types.ts`, `resources/fe-dist/*`, `dist/*`). If you must add i18n keys, edit the source locale JSON files under `packages/shared/src/i18n/locales/` (or wherever the source JSON lives) for ALL THREE locales `zh_CN.json`, `en_US.json`, `ja_JP.json` (a locale-consistency test enforces identical key sets), editing only your own nested keys; the commander regenerates resources.
- Never touch the production tmex on this machine: service on port 9883, `~/Library/Application Support/tmex/`, launchd; never touch the tmux session named `tmex` or the default tmux socket. Any temporary instance must set `TMEX_TMUX_SOCKET=<unique>`, unique `GATEWAY_PORT`/`TMEX_PEER_PORT` (use 19xxx), `NODE_ENV=test`, its own `DATABASE_URL` under the scratch dir, and `TMEX_BIND_HOST=127.0.0.1`.
- Tests: run only the package you touch — `cd apps/gateway && bun test <path>` / `cd apps/fe && bun test src/<path>` (never bare `bun test` in apps/fe — it picks Playwright specs) / `cd packages/app && bun test <path>`. Type-check with `bunx tsc --noEmit -p .` in the package (baseline error counts: gateway 0, fe 0, app 0, shared 0, ws-client 0, stores 1, api-client 5, theme 9). Lint your files with `bunx biome check <files>` (no `--write` on files outside scope). macOS has no `timeout` command.
- Code style: TypeScript, no unnecessary comments (only for genuinely complex logic, written in Simplified Chinese), variable names in English, prefer small functions (complexity gate: cyclomatic ≤ 15 per function, file ≤ 600 lines, enforced by `bun run lint` at repo root — do not add allowlist entries).
- UI copy rules: Simplified Chinese (zh-CN) primary + English; terminology: 「中继」= relay only, 「Hub」stays "Hub"; read `/Users/konata/code/tmex-copy-guidelines.md` before writing any user-facing text.
- Do not leave TODOs, stubs or "simplified versions". Finish the whole scope. Do not widen scope.
- When done, write your report to the absolute result path given in the task (Markdown, Simplified Chinese OK): what changed (file list), how verified (commands + counts), anything left/uncertain. Write the result file LAST, then exit.

## Task F5 — Fix frontend review findings (R2)

Result file: `/Users/konata/code/tmex-r25/prompt-archives/2026090402-round25-relay-live-bots/sub/F5-result.md`. Read the review at `/Users/konata/code/tmex-r25/prompt-archives/2026090402-round25-relay-live-bots/sub/R2-report.md` first; fix ALL seven items (1–6 should-fix, 7 nit). Everything is committed; a backend agent (G6) is editing `apps/gateway`/`packages/app` only — you own `apps/fe/**` and `packages/panels/**` (+ i18n locale JSON if a key is needed). No git operations.

Guidance per item:
1. Post-mutation refresh must not be satisfied by an in-flight request: add a trailing refresh to `HubLoadCoordinator` (`apps/fe/src/node/hub-load-coordinator.ts`, e.g. `refresh({ fresh: true })` that, if a request is in flight, schedules one more load after it settles and returns that promise); use it from the admit flow's `onChanged()`; for mesh nodes use the existing `ensureFreshMeshNodes()`. Test in `hub-load-coordinator.test.ts`.
2. In `admit()` (`use-node-row-actions.ts:~312`) / `admitPendingNode` (`admit-pending-node.ts`): keep a ref to the latest row material (id + enrollmentId + material) and mounted state; after the credential prompt resolves and again after acquiring the key-log lock, re-check that the row is still pending with the same enrollmentId; if not, cancel silently (`{ kind: 'cancelled' }`). Test.
3. `admit-pending-node.ts:~69`: when `appendKeyLog()` rejects (network/timeout) and an `unconfirmedRecord()` exists for that enrollment, return `{ kind: 'unconfirmed' }` (UI shows the existing `nodes.enrollment.hubNotConfirmed` warning), not `failed`. Test with a rejecting appendKeyLog.
4. `errors.ts` / `relay-join.ts`: map the server's real 502 `RELAY_ENROLL_FANOUT_FAILED` (see `apps/gateway/src/mesh/relay-routes.ts:~416`, body has `relays` with per-relay errors) to the same localized text as `RELAY_ENROLLMENT_NO_RELAY` (`nodes.enrollment.relayNoneAccepted`); fix the test to simulate the 502 path.
5. `pending-node-row.tsx:~81`: render the disabled reason as visible helper text under/next to the button (muted, `data-testid="pending-node-admit-hint"`) linked via `aria-describedby`; keep the button disabled.
6. `packages/panels/src/settings/integration-account-form-modal.tsx:~139-150`: give the Switch `id={field.inputId}`, turn the title into `<label htmlFor>`, give the help text a stable id and `aria-describedby`. Test that the label toggles the switch.
7. `merge-nodes.ts:~199`: dedupe pending rows by id (keep the first).
Verification: `cd apps/fe && bun test src/node src/pages/settings/nodes && bunx tsc --noEmit -p .` (0); `cd packages/panels && bun test src/settings && bunx tsc --noEmit -p .` (0); `bunx biome check` on your files; `bun scripts/complexity/gate.ts` at root ok without allowlist edits.
