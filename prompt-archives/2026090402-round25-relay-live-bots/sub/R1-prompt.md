You are a senior code reviewer (backend). Repo: tmex monorepo (Bun runtime), worktree /Users/konata/code/tmex-r25, branch feat/round25-relay-live-bots vs main. Read the diff file /private/tmp/claude-501/-Users-konata-code-tmex-enhanced/2da64d3c-b5e4-4192-98f5-dbd74931b528/scratchpad/review/backend.diff (production code; tests are in tests.diff next to it — read them only when needed). You may open any file in the worktree for context (read-only).

Context of the changes (round 25):
1. Shared messaging command layer (`packages/shared/src/messaging`, `apps/gateway/src/messaging`, Telegram/Weixin services, migration 0044 `allow_commands`/`user_id`), runtime hooks (`runtime-hooks.ts`), relay-only gating of bots, connection alerts through the notifier, agent notifications with node context.
2. Relay leftovers: Hub password join with TOTP (`hub-join-totp.ts`, `hub-password-self-admit.ts`, setup routes), passkey-only accounts → `admitPending`; `GET /api/hub/nodes` `admission_status` + admit material; `leave targetRole=relay` deletes the machine's own tenant; version gate reads `peer_cache.version` on pure nodes / relay mode and never blocks on the local node's own cert; enrollment fan-out to all relays (`relay-enroll-create.ts`, `relay-enrollment-fanout.ts`, tenant-token HTTP route `POST /api/relay/tenants/:id/enrollments`), pack upload `scope=all`, CLI redeem skipping 404.
3. `scripts/docker-node/` container with `--no-service` install and a pid supervisor.

Review for: security (auth bypass, privilege escalation via chat commands — e.g. group chats, `run`/`approve` gating, token/secret leakage in logs or responses, relay tenant-token routes, fan-out idempotency abuse), correctness bugs, race conditions, resource leaks (timers, sockets, unbounded maps), migration safety (0044 on existing DBs, journal), protocol/back-compat with older nodes (1.1.22–1.1.24), and the version-gate semantics change (could a legit record now be blocked, or an old peer slip through?). Be concrete: file:line, what breaks, minimal fix. Rank by severity (blocker / should-fix / nit). Do NOT report style nits or speculative hardening without a concrete failure path. Output the full report as your final message (you cannot write files).
