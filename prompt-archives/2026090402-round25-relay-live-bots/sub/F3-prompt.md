## Common rules (read first)

- Repo worktree: `/Users/konata/code/tmex-r25` (branch `feat/round25-relay-live-bots`, base = main 1.1.24). Bun-only runtime (`bun`, `bunx`; if `bun` is missing, `export PATH="$HOME/.bun/bin:/opt/homebrew/bin:$PATH"`). Never use Node for app code; only `packages/app` CLI stays Node-compatible.
- OTHER AGENTS ARE EDITING THIS SAME WORKTREE IN PARALLEL. Touch ONLY the files in your scope (listed in your task). Do NOT run `git add/commit/stash/checkout/reset`, do NOT run `bun run build:i18n` / formatters on files outside your scope, do NOT run lint/format on generated files (`packages/shared/src/i18n/resources.ts`, `types.ts`, `resources/fe-dist/*`, `dist/*`). If you must add i18n keys, edit the source locale JSON files under `packages/shared/src/i18n/locales/` (or wherever the source JSON lives) for ALL THREE locales `zh_CN.json`, `en_US.json`, `ja_JP.json` (a locale-consistency test enforces identical key sets), editing only your own nested keys; the commander regenerates resources.
- Never touch the production tmex on this machine: service on port 9883, `~/Library/Application Support/tmex/`, launchd; never touch the tmux session named `tmex` or the default tmux socket. Any temporary instance must set `TMEX_TMUX_SOCKET=<unique>`, unique `GATEWAY_PORT`/`TMEX_PEER_PORT` (use 19xxx), `NODE_ENV=test`, its own `DATABASE_URL` under the scratch dir, and `TMEX_BIND_HOST=127.0.0.1`.
- Tests: run only the package you touch — `cd apps/gateway && bun test <path>` / `cd apps/fe && bun test src/<path>` (never bare `bun test` in apps/fe — it picks Playwright specs) / `cd packages/app && bun test <path>`. Type-check with `bunx tsc --noEmit -p .` in the package (baseline error counts: gateway 0, fe 0, app 0, shared 0, ws-client 0, stores 1, api-client 5, theme 9). Lint your files with `bunx biome check <files>` (no `--write` on files outside scope). macOS has no `timeout` command.
- Code style: TypeScript, no unnecessary comments (only for genuinely complex logic, written in Simplified Chinese), variable names in English, prefer small functions (complexity gate: cyclomatic ≤ 15 per function, file ≤ 600 lines, enforced by `bun run lint` at repo root — do not add allowlist entries).
- UI copy rules: Simplified Chinese (zh-CN) primary + English; terminology: 「中继」= relay only, 「Hub」stays "Hub"; read `/Users/konata/code/tmex-copy-guidelines.md` before writing any user-facing text.
- Do not leave TODOs, stubs or "simplified versions". Finish the whole scope. Do not widen scope.
- When done, write your report to the absolute result path given in the task (Markdown, Simplified Chinese OK): what changed (file list), how verified (commands + counts), anything left/uncertain. Write the result file LAST, then exit.

## Task F3 — Node management: show "待批准" (pending admission) Hub nodes with a one-click Admit; relay enrollment uses only relays that accepted

Result file: `/Users/konata/code/tmex-r25/prompt-archives/2026090402-round25-relay-live-bots/sub/F3-result.md`.
Reference: `/Users/konata/code/tmex-r25/prompt-archives/2026090402-round25-relay-live-bots/sub/EX4-report.md` sections L3 and L4 (frontend parts). Two backend agents are implementing the server side IN PARALLEL right now; code against the contracts below and use fakes in tests — do not wait for them and do not edit backend files.

Scope (only): `apps/fe/src/node/hub-api.ts` (+test), `apps/fe/src/node/mesh-nodes.ts` ONLY `mergeNodes()` and the `HubNodeRow`/merge types (the file is at the complexity allowlist limit of 780 lines — if your change grows it, move `mergeNodes` and its helpers into a new `apps/fe/src/node/merge-nodes.ts` and re-export from `mesh-nodes.ts`; do NOT touch `useHubNode`/loading logic), `apps/fe/src/pages/settings/nodes/management/**` (table, actions, tests), `apps/fe/src/node/enrollment.ts` / `enrollment-engine.ts` only to expose an `admitPendingNode(row)` helper reusing `buildAdmitNodeRecord()` + `submitAdmitRecord()` + `op.prompt.request({ purpose: 'admit', reuse: true })`, `apps/fe/src/node/relay-join.ts` (+test), i18n keys under `nodes.*` in `zh_CN.json`/`en_US.json`/`ja_JP.json`. Do NOT touch `hub-uplink-panel.tsx`, `local-uplink-tabs.tsx`, `node-names.ts`, `node-runtimes.ts` (just changed by other agents).

Contracts being implemented on the backend:
- `GET /n/<hub>/api/hub/nodes` rows gain `admission_status: 'pending' | 'admitted' | 'revoked'`; `pending` rows also carry `enrollment_id`, `authorization`, `authorization_sig`, `certificate`, `cert_sig` (b64url strings, same encodings the enrollment wizard already consumes). Rows may lack `admission_status` when talking to an older Hub → treat as `admitted` (unchanged behaviour).
- Hub password join of a passkey-only account now finishes with `admitPending: true` (the joining node shows up as `pending` on the Hub until a browser signs `admit-node`).
- `POST /api/mesh/relay/enrollments` response `relays` becomes `[{ url, tenantId, token, accepted: true } | { url, tenantId, accepted: false, error }]` (previously `string[]`).

Implement:
1. `HubNodeRow` type + parsing: add the fields above.
2. `mergeNodes()`: today it iterates only mesh nodes (`/api/mesh/nodes`, the authoritative admitted set) and decorates with hub rows; now ALSO append Hub-only rows whose `admission_status === 'pending'` as rows with `online: false`, `pending: true`, no runtime, name from the hub row (or id prefix if none). Never duplicate a node already in the mesh list.
3. Nodes table: pending rows show a 「待批准」 badge (i18n `nodes.status.pending`), disable upgrade/remove/rename/open actions for them, and show an 「批准加入」 (`nodes.actions.admit`) button (only when the current hub is writable and the user can sign — same conditions the add-node wizard uses). Clicking runs `admitPendingNode(row)`: root/passkey prompt (reuse) → `buildAdmitNodeRecord` → `submitAdmitRecord` (`?hub=sync`) → refresh hub rows + mesh nodes; toast success/failure (reuse existing toast keys where they exist; add `nodes.admit.success` / `nodes.admit.failed` otherwise). In relay mode this row type never appears (relay has no hub list) — do nothing special.
4. `relay-join.ts:45-86` `createEnrollmentOnRelay()`: put ONLY `accepted: true` relays into the r3 token; if none accepted, surface the error; keep backward compatibility if the response still has `string[]` (treat all as accepted, tokens from `join-material?scope=all`).
5. Copy: follow `/Users/konata/code/tmex-copy-guidelines.md`. Three locales.
6. Tests: `hub-api` parsing; `mergeNodes` (pending appended, no duplicates, old hub without the field); table renders badge/button and disables actions; admit flow with fakes; `relay-join` accepted filtering + legacy shape. Run `cd apps/fe && bun test src/node src/pages/settings/nodes && bunx tsc --noEmit -p .` (0) and `bunx biome check` on your files; `bun scripts/complexity/gate.ts` at root must stay ok without allowlist edits.
