You are a read-only code explorer for the tmex monorepo (Bun runtime; apps/gateway = backend, apps/fe = React frontend, packages/* = shared libs). Output your COMPLETE report as your final message (you cannot write files). Be concrete: cite file paths with line numbers, quote the relevant code snippets, and name the exact functions/hooks/components involved.

Task EX2 — Node management page flashes "cannot connect to Hub" on first entry.
The user reports: every time they enter the multi-node ("nodes management" / 节点管理, in Settings → Nodes or the devices page) view, it first shows a "Hub connection failed / 无法连接到 Hub" state, then after the background connection/login completes it turns normal. Meanwhile the hub is actually connected (sidebar works fine). This is misleading.
Investigate:
1. Locate the hook/component that produces that error state (likely `useHubNode` in apps/fe, `ensureNodeLogin`, hub 401 → silent node login → retry logic introduced in v1.1.15; also the "writer not available → fall back to other online hub" path). Quote the state machine: initial state, when `error` is set, when loading is set, and what the UI renders for each.
2. Determine why the error appears before the first successful fetch: is the initial state `error` or is the first request failing (401 because the browser has no session on the hub node yet, or the hub fetch fires before the mesh node list is loaded, or before the hub id is known), and only the retry succeeds? Check the network sequencing: which requests fire on mount and in what order, and what the api-client interceptor does with 401 on `/n/:hubId/api/...`.
3. Find the UI components that render the hub error banner/empty state (i18n keys like `nodes.hubUnreachable`, `settings.nodes.hubError`, etc. in packages/shared/src/i18n source JSON) and how the sidebar derives its own "hub connected" state (so the two can be made consistent).
4. Propose the minimal fix: e.g. treat the first attempt as loading until login+retry finishes, do not surface error until N consecutive failures or until a grace period, or reuse the sidebar's connection state. Point to exact lines to change, and list existing unit tests (apps/fe/src/**/*.test.ts[x]) covering useHubNode so they can be extended.
