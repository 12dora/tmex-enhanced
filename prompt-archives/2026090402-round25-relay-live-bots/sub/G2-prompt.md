## Common rules (read first)

- Repo worktree: `/Users/konata/code/tmex-r25` (branch `feat/round25-relay-live-bots`, base = main 1.1.24). Bun-only runtime (`bun`, `bunx`; if `bun` is missing, `export PATH="$HOME/.bun/bin:/opt/homebrew/bin:$PATH"`). Never use Node for app code; only `packages/app` CLI stays Node-compatible.
- OTHER AGENTS ARE EDITING THIS SAME WORKTREE IN PARALLEL. Touch ONLY the files in your scope (listed in your task). Do NOT run `git add/commit/stash/checkout/reset`, do NOT run `bun run build:i18n` / formatters on files outside your scope, do NOT run lint/format on generated files (`packages/shared/src/i18n/resources.ts`, `types.ts`, `resources/fe-dist/*`, `dist/*`). If you must add i18n keys, edit the source locale JSON files under `packages/shared/src/i18n/locales/` (or wherever the source JSON lives) for ALL THREE locales `zh_CN.json`, `en_US.json`, `ja_JP.json` (a locale-consistency test enforces identical key sets), editing only your own nested keys; the commander regenerates resources.
- Never touch the production tmex on this machine: service on port 9883, `~/Library/Application Support/tmex/`, launchd; never touch the tmux session named `tmex` or the default tmux socket. Any temporary instance must set `TMEX_TMUX_SOCKET=<unique>`, unique `GATEWAY_PORT`/`TMEX_PEER_PORT` (use 19xxx), `NODE_ENV=test`, its own `DATABASE_URL` under the scratch dir, and `TMEX_BIND_HOST=127.0.0.1`.
- Tests: run only the package you touch — `cd apps/gateway && bun test <path>` / `cd apps/fe && bun test src/<path>` (never bare `bun test` in apps/fe — it picks Playwright specs) / `cd packages/app && bun test <path>`. Type-check with `bunx tsc --noEmit -p .` in the package (baseline error counts: gateway 0, fe 0, app 0, shared 0, ws-client 0, stores 1, api-client 5, theme 9). Lint your files with `bunx biome check <files>` (no `--write` on files outside scope). macOS has no `timeout` command.
- Code style: TypeScript, no unnecessary comments (only for genuinely complex logic, written in Simplified Chinese), variable names in English, prefer small functions (complexity gate: cyclomatic ≤ 15 per function, file ≤ 600 lines, enforced by `bun run lint` at repo root — do not add allowlist entries).
- UI copy rules: Simplified Chinese (zh-CN) primary + English; terminology: 「中继」= relay only, 「Hub」stays "Hub"; read `/Users/konata/code/tmex-copy-guidelines.md` before writing any user-facing text.
- Do not leave TODOs, stubs or "simplified versions". Finish the whole scope. Do not widen scope.
- When done, write your report to the absolute result path given in the task (Markdown, Simplified Chinese OK): what changed (file list), how verified (commands + counts), anything left/uncertain. Write the result file LAST, then exit.

## Task G2 — Hub password join: TOTP support, passkey-only fallback to "pending", and `admission_status` in the Hub node list

Result file: `/Users/konata/code/tmex-r25/prompt-archives/2026090402-round25-relay-live-bots/sub/G2-result.md`.
Reference: read `/Users/konata/code/tmex-r25/prompt-archives/2026090402-round25-relay-live-bots/sub/EX4-report.md` sections L1 and L3 first (an audit with file:line pointers; verify each claim).

Scope (only): `packages/app/src/lib/hub-password-self-admit.ts` (+test), `packages/app/src/lib/hub-password-join.ts` (+test), `packages/app/src/lib/hub-client.ts` (+test, error mapping only), `packages/app/src/runtime/setup-service.ts` (+test), `packages/app/src/runtime/setup-routes.ts` (+test), `packages/app/src/commands/hub.ts` (+test; it is on the complexity allowlist at 1287 lines — do not grow it, move new logic into a new file `packages/app/src/commands/hub-join-totp.ts` or similar), `packages/app/src/lib/args.ts` (+test), `packages/app/src/cli/help.ts`, `packages/app/src/i18n/**` (CLI strings), `apps/gateway/src/hub/hub-runtime.ts` (+ `hub-runtime.test.ts`), `apps/gateway/src/hub/node-persistence.ts` only if needed, `packages/shared/src/contracts/hub*.ts` if the Hub node DTO type lives there (find `HubNodeRow`-like type in shared or gateway). Do NOT touch `apps/fe`, `packages/panels`, `apps/gateway/src/messaging|telegram|weixin|events|push|agent|runtime.ts`, `apps/gateway/src/hub/hub-authorization.ts`, `apps/gateway/src/mesh/auth-key-log-routes.ts`, `packages/app/src/runtime/membership-reset.ts`, `apps/gateway/src/auth/mesh-membership-store.ts`, `apps/gateway/src/mesh/relay-routes.ts`, `apps/gateway/src/relay/**` (other agents).

L1a — TOTP: `assertPasswordJoinCanAdmit()` (`hub-password-self-admit.ts:~151`) currently throws `join_failed` when `mode.totpEnabled`. Implement: `PublishHubJoinSelfAdmitInput.totpCode?: string`; when TOTP is enabled derive `kTotp = deriveTotpKey(rootKey.seed, userId, rootEpoch)` (`packages/shared/src/auth/totp.ts`; `rootEpoch` from the local user row — check what the self-admit code already has) and pass `totp: { code, kTotp }` to `loginWithRootKey()` (`hub-client.ts:~170/220`); zero `kTotp` afterwards. Missing code with TOTP enabled → a stable error `totp_required` (JoinError code) so the FE/CLI can prompt. `JoinHubInput.totpCode?` flows through `setup-service.ts joinHub()` and `POST /api/setup/join` body (`setup-routes.ts:~65-80`, validate string 6-10 digits). CLI: `tmex hub join <url> --password [--totp <code>]` plus `TMEX_TOTP` env (add to COMMAND_FLAGS in `args.ts`), `HubIo.totpCode` already exists (`hub.ts:~66`) but is not passed (`hub.ts:~634`) — wire it; interactive TTY prompts for the code when the hub reports it is required. Map `TOTP_REQUIRED` / `TOTP_INVALID` in `hub-client.ts:~251`.
L1b — passkey-only accounts: instead of failing the whole join with `join_failed`, complete enrollment/redeem/local commit, skip self-admit, and finish with a result flag `admitPending: true` (setup route response + CLI message: 「已加入，等待已登录的浏览器批准」/ "Joined; waiting for approval from a signed-in browser"). The node then restarts as `node` and its uplink is rejected with `unknown-cert` until an admit-node record is appended from the browser (task F3 adds the UI using `admission_status` below). Keep TOTP+passkey combined accounts on the same pending path.
L3 backend — `GET /api/hub/nodes` (`hub-runtime.ts:~828`) rows gain `admission_status: 'pending' | 'admitted' | 'revoked'` derived as: `nodes.status === 'revoked'` or cert revoked → `revoked`; live `node_certs` row → `admitted`; enrollment redeemed with a stored certificate but no `node_certs` row → `pending`. For `pending` rows also include the material the browser needs to build `admit-node` (`enrollment_id`, `authorization`, `authorization_sig`, `certificate`, `cert_sig`) — the existing code already returns `certificate`/`cert_sig` for some rows; check what `buildAdmitNodeRecord()` in `apps/fe/src/node/enrollment.ts:~482` needs and provide exactly that. Do not change the `nodes.status` DB constraint. Tests in `hub-runtime.test.ts` for the three states.
Verification: `cd packages/app && bun test src/lib/hub-password src/runtime/setup src/commands/hub src/lib/args && bunx tsc --noEmit -p .` (0), `cd apps/gateway && bun test src/hub/hub-runtime && bunx tsc --noEmit -p .` (0), `bun run lint` at root for your files.
