## Common rules (read first)

- Repo worktree: `/Users/konata/code/tmex-r25` (branch `feat/round25-relay-live-bots`, base = main 1.1.24). Bun-only runtime (`bun`, `bunx`; if `bun` is missing, `export PATH="$HOME/.bun/bin:/opt/homebrew/bin:$PATH"`). Never use Node for app code; only `packages/app` CLI stays Node-compatible.
- OTHER AGENTS ARE EDITING THIS SAME WORKTREE IN PARALLEL. Touch ONLY the files in your scope (listed in your task). Do NOT run `git add/commit/stash/checkout/reset`, do NOT run `bun run build:i18n` / formatters on files outside your scope, do NOT run lint/format on generated files (`packages/shared/src/i18n/resources.ts`, `types.ts`, `resources/fe-dist/*`, `dist/*`). If you must add i18n keys, edit the source locale JSON files under `packages/shared/src/i18n/locales/` (or wherever the source JSON lives) for ALL THREE locales `zh_CN.json`, `en_US.json`, `ja_JP.json` (a locale-consistency test enforces identical key sets), editing only your own nested keys; the commander regenerates resources.
- Never touch the production tmex on this machine: service on port 9883, `~/Library/Application Support/tmex/`, launchd; never touch the tmux session named `tmex` or the default tmux socket. Any temporary instance must set `TMEX_TMUX_SOCKET=<unique>`, unique `GATEWAY_PORT`/`TMEX_PEER_PORT` (use 19xxx), `NODE_ENV=test`, its own `DATABASE_URL` under the scratch dir, and `TMEX_BIND_HOST=127.0.0.1`.
- Tests: run only the package you touch — `cd apps/gateway && bun test <path>` / `cd apps/fe && bun test src/<path>` (never bare `bun test` in apps/fe — it picks Playwright specs) / `cd packages/app && bun test <path>`. Type-check with `bunx tsc --noEmit -p .` in the package (baseline error counts: gateway 0, fe 0, app 0, shared 0, ws-client 0, stores 1, api-client 5, theme 9). Lint your files with `bunx biome check <files>` (no `--write` on files outside scope). macOS has no `timeout` command.
- Code style: TypeScript, no unnecessary comments (only for genuinely complex logic, written in Simplified Chinese), variable names in English, prefer small functions (complexity gate: cyclomatic ≤ 15 per function, file ≤ 600 lines, enforced by `bun run lint` at repo root — do not add allowlist entries).
- UI copy rules: Simplified Chinese (zh-CN) primary + English; terminology: 「中继」= relay only, 「Hub」stays "Hub"; read `/Users/konata/code/tmex-copy-guidelines.md` before writing any user-facing text.
- Do not leave TODOs, stubs or "simplified versions". Finish the whole scope. Do not widen scope.
- When done, write your report to the absolute result path given in the task (Markdown, Simplified Chinese OK): what changed (file list), how verified (commands + counts), anything left/uncertain. Write the result file LAST, then exit.

## Task F6 — Frontend: re-affirm members (`readmit-node`) during relay migration and from the relay status card

Result file: `/Users/konata/code/tmex-r25/prompt-archives/2026090402-round25-relay-live-bots/sub/F6-result.md`. You own `apps/fe/**` (+ locale JSON keys under `nodes.*`); a grok agent (G7) is implementing the backend in parallel — code against the contract below with fakes; no git operations.

Background: accounts that rotated their root (`rotate-root-keep`) have `admit-node` records signed by OLD root keys; a relay only knows the CURRENT root, so historical members are refused (`member-epoch_mismatch`). Backend adds a `readmit-node` key-log record (payload identical to `admit-node`: `authorization_bytes`, `authorization_sig`, `certificate_bytes`, `cert_sig`; root or passkey signer; `minVersion 1.1.26`; apply requires an existing unrevoked cert with identical certificate bytes and re-verifies the authorization signature against the current root).

Contract:
- `GET /api/mesh/relay/readmit/prepare` → `{ rootEpoch, entries: [{ nodeId, name, admitSeq, admitRootEpoch, authorization_bytes, certificate_bytes, cert_sig }] }` (b64url strings), empty `entries` when nothing is stale.
- `GET /api/mesh/relay/status` gains `readmitPending: number`.
- `POST /api/mesh/relay/enroll` response gains `readmitRequired: number`.
Signing per entry: new `authorization_sig` over `authorization_bytes` with the root key (or a passkey assertion over `sha256(authorization_bytes)` exactly like `buildAdmitNodeRecord()` in `apps/fe/src/node/enrollment.ts` does for passkey signers — reuse its signer plumbing), then `buildKeyLogRecord({ type: 'readmit-node', payload: encodeAdmitNodePayload({...}) })`, sign, `AuthApi.appendKeyLog(..., { hubSync: true })`, sequential heads under `withKeyLogLock`.

Implement:
1. `apps/fe/src/node/readmit-members.ts`: `readmitStaleMembers(deps)` — fetch prepare, prompt once for credentials (`purpose: 'admit'`, reuse), sign+append each entry in order, return `{ signed, failed }`; idempotent (re-running with empty entries is a no-op).
2. Relay migration / enroll flows in `apps/fe` (find where `POST /api/mesh/relay/enroll` is called and `set-relays` is signed: `apps/fe/src/node/relay-*.ts`, `pages/settings/nodes/uplink/*` — the "接入中继"/"hub → 中继迁移" forms): when the enroll response has `readmitRequired > 0`, run `readmitStaleMembers` BEFORE signing `set-relays`; if any entry fails, stop and show the error (do not submit `set-relays`).
3. Relay status card (uplink relay panel): when `readmitPending > 0` show a warning line 「有 N 台节点的成员记录是旧根签的，需要重新确认」 with a button 「重新确认成员」 that runs `readmitStaleMembers` and refreshes status. Copy per `/Users/konata/code/tmex-copy-guidelines.md`; three locales.
4. Tests for the helper (fakes), the enroll-flow ordering (readmit before set-relays; abort on failure), and the status-card warning/button. Run `cd apps/fe && bun test src/node src/pages/settings/nodes && bunx tsc --noEmit -p .` (0), biome on your files, complexity gate ok.
