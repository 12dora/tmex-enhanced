You are a senior security-minded code reviewer. Repo /Users/konata/code/tmex-r25 (Bun). Read the diff /private/tmp/claude-501/-Users-konata-code-tmex-enhanced/2da64d3c-b5e4-4192-98f5-dbd74931b528/scratchpad/review/readmit.diff and, for context, docs/relay/2026090304-relay-role.md §4 and packages/shared/src/auth/key-log.ts.
Change under review: a new key-log record type `readmit-node` (payload identical to `admit-node`) that re-affirms an already admitted, unrevoked node under the account's CURRENT root key after root rotations (the relay only knows the current root; old `admit-node` sidecars fail `epoch_mismatch`). Apply requires an existing unrevoked cert with byte-identical certificate and re-verifies the authorization signature against the current root (or passkey); it updates `node_certs.admit_record_seq` so the node presents the new record to the relay; version gate 1.1.26; CLI/FE sign one per stale member before `set-relays`.
Review strictly for: (1) any way this lets a revoked node come back, lets a different key claim an existing node id, or lets an old-epoch attacker replay; (2) whether `admit_record_seq` update could break hub-side flows (`unknown-cert`, node.list, enrollment lookups) or `selfAdmitMemberProof`; (3) relay-side: could a tenant-token holder forge a readmit sidecar (compare with admit) — is it any weaker than today; (4) migration 0045 safety on existing DBs (table rebuild pattern, indexes, foreign keys) and older nodes receiving the record (version gate correctness); (5) FE ordering: readmit runs before `set-relays` in hub mode — the records go through the hub writer; any failure path that leaves a half state. Report blocker/should-fix/nit with file:line and minimal fix. Output the full report as your final message.
