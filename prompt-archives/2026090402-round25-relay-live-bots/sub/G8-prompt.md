## Common rules (read first)

- Repo worktree: `/Users/konata/code/tmex-r25` (branch `feat/round25-relay-live-bots`, base = main 1.1.24). Bun-only runtime (`bun`, `bunx`; if `bun` is missing, `export PATH="$HOME/.bun/bin:/opt/homebrew/bin:$PATH"`). Never use Node for app code; only `packages/app` CLI stays Node-compatible.
- OTHER AGENTS ARE EDITING THIS SAME WORKTREE IN PARALLEL. Touch ONLY the files in your scope (listed in your task). Do NOT run `git add/commit/stash/checkout/reset`, do NOT run `bun run build:i18n` / formatters on files outside your scope, do NOT run lint/format on generated files (`packages/shared/src/i18n/resources.ts`, `types.ts`, `resources/fe-dist/*`, `dist/*`). If you must add i18n keys, edit the source locale JSON files under `packages/shared/src/i18n/locales/` (or wherever the source JSON lives) for ALL THREE locales `zh_CN.json`, `en_US.json`, `ja_JP.json` (a locale-consistency test enforces identical key sets), editing only your own nested keys; the commander regenerates resources.
- Never touch the production tmex on this machine: service on port 9883, `~/Library/Application Support/tmex/`, launchd; never touch the tmux session named `tmex` or the default tmux socket. Any temporary instance must set `TMEX_TMUX_SOCKET=<unique>`, unique `GATEWAY_PORT`/`TMEX_PEER_PORT` (use 19xxx), `NODE_ENV=test`, its own `DATABASE_URL` under the scratch dir, and `TMEX_BIND_HOST=127.0.0.1`.
- Tests: run only the package you touch — `cd apps/gateway && bun test <path>` / `cd apps/fe && bun test src/<path>` (never bare `bun test` in apps/fe — it picks Playwright specs) / `cd packages/app && bun test <path>`. Type-check with `bunx tsc --noEmit -p .` in the package (baseline error counts: gateway 0, fe 0, app 0, shared 0, ws-client 0, stores 1, api-client 5, theme 9). Lint your files with `bunx biome check <files>` (no `--write` on files outside scope). macOS has no `timeout` command.
- Code style: TypeScript, no unnecessary comments (only for genuinely complex logic, written in Simplified Chinese), variable names in English, prefer small functions (complexity gate: cyclomatic ≤ 15 per function, file ≤ 600 lines, enforced by `bun run lint` at repo root — do not add allowlist entries).
- UI copy rules: Simplified Chinese (zh-CN) primary + English; terminology: 「中继」= relay only, 「Hub」stays "Hub"; read `/Users/konata/code/tmex-copy-guidelines.md` before writing any user-facing text.
- Do not leave TODOs, stubs or "simplified versions". Finish the whole scope. Do not widen scope.
- When done, write your report to the absolute result path given in the task (Markdown, Simplified Chinese OK): what changed (file list), how verified (commands + counts), anything left/uncertain. Write the result file LAST, then exit.

## Task G8 — Fix readmit-node review findings (R3) — backend + shared + CLI

Result file: `/Users/konata/code/tmex-r25/prompt-archives/2026090402-round25-relay-live-bots/sub/G8-result.md`. Review: `/Users/konata/code/tmex-r25/prompt-archives/2026090402-round25-relay-live-bots/sub/R3-report.md` (read it). You own `packages/shared`, `apps/gateway`, `packages/app`; an Opus agent (F7) edits `apps/fe` in parallel. No git.

1. R3 Blocker 1 — `apps/gateway/src/hub/hub-authorization.ts`: `readmit-node` must NOT be in the empty-cache bootstrap exemption (it requires an existing cert, so there is no first-node scenario), and for `readmit-node` the relay-mode `skipUncached` must be disabled: every unrevoked, non-local cert with no cached version blocks (fail-closed). Tests: "empty peer cache but certs exist → blocked", "one cached + one uncached cert → blocked", plus the existing relay/hub cases unchanged.
2. R3 Should-fix — shared helper `buildRootReadmitAuthorization({ authorizationBytes, rootEpoch, rootKey })` in `packages/shared/src/auth/readmit-node-record.ts` (or next to it): decode the existing `Authorization`, re-encode a ROOT-signed authorization with the same `uid`/`enroll_pk`/other bindings, `signer: 'root'`, `credential_id: null`, current `root_epoch` (check the Authorization schema in `encoding.ts` for the exact fields), sign it with the root → `{ authorization_bytes, authorization_sig }`. `applyReadmitNode` must accept it (certificate bytes still identical; enroll_pk/uid checks pass). Use it in the CLI readmit path (`packages/app/src/lib/relay-session.ts` ~L353) instead of re-signing the original bytes, so members originally admitted by a passkey can be re-affirmed with the root. Tests: original passkey-admitted member re-affirmed by root; original root-admitted member; tampered enroll_pk rejected.
3. R3 Blocker 2 (CLI part) — `packages/app/src/commands/relay.ts` (~L186) `enroll`/`reauth`: run the local `GET /api/mesh/relay/readmit/prepare` + readmit loop BEFORE fetching proof material and calling the remote enroll (token reissue is irreversible); keep `readmitRequired` from the enroll response only as a post-check (if > 0 after enroll, abort before `set-relays` with a clear error). Update the CLI tests for the new order.
4. Doc: `docs/relay/2026090304-relay-role.md` §4/§12: order "readmit → enroll → set-relays" and the fail-closed gate note.
Verify: `cd apps/gateway && bun test src/hub/hub-authorization src/mesh/relay src/relay src/mesh/auth-key-log && bunx tsc --noEmit -p .` (only pre-existing TS5097); `cd packages/shared && bun test src/auth && bunx tsc --noEmit -p .`; `cd packages/app && bun test src/commands/relay src/lib/relay-session && bunx tsc --noEmit -p .`; `bun run lint`.
