## Common rules (read first)

- Repo worktree: `/Users/konata/code/tmex-r25` (branch `feat/round25-relay-live-bots`, base = main 1.1.24). Bun-only runtime (`bun`, `bunx`; if `bun` is missing, `export PATH="$HOME/.bun/bin:/opt/homebrew/bin:$PATH"`). Never use Node for app code; only `packages/app` CLI stays Node-compatible.
- OTHER AGENTS ARE EDITING THIS SAME WORKTREE IN PARALLEL. Touch ONLY the files in your scope (listed in your task). Do NOT run `git add/commit/stash/checkout/reset`, do NOT run `bun run build:i18n` / formatters on files outside your scope, do NOT run lint/format on generated files (`packages/shared/src/i18n/resources.ts`, `types.ts`, `resources/fe-dist/*`, `dist/*`). If you must add i18n keys, edit the source locale JSON files under `packages/shared/src/i18n/locales/` (or wherever the source JSON lives) for BOTH `zh-CN` and `en`, editing only your own nested keys; the commander regenerates resources.
- Never touch the production tmex on this machine: service on port 9883, `~/Library/Application Support/tmex/`, launchd; never touch the tmux session named `tmex` or the default tmux socket. Any temporary instance must set `TMEX_TMUX_SOCKET=<unique>`, unique `GATEWAY_PORT`/`TMEX_PEER_PORT` (use 19xxx), `NODE_ENV=test`, its own `DATABASE_URL` under the scratch dir, and `TMEX_BIND_HOST=127.0.0.1`.
- Tests: run only the package you touch — `cd apps/gateway && bun test <path>` / `cd apps/fe && bun test src/<path>` (never bare `bun test` in apps/fe — it picks Playwright specs) / `cd packages/app && bun test <path>`. Type-check with `bunx tsc --noEmit -p .` in the package (baseline error counts: gateway 0, fe 0, app 0, shared 0, ws-client 0, stores 1, api-client 5, theme 9). Lint your files with `bunx biome check <files>` (no `--write` on files outside scope). macOS has no `timeout` command.
- Code style: TypeScript, no unnecessary comments (only for genuinely complex logic, written in Simplified Chinese), variable names in English, prefer small functions (complexity gate: cyclomatic ≤ 15 per function, file ≤ 600 lines, enforced by `bun run lint` at repo root — do not add allowlist entries).
- UI copy rules: Simplified Chinese (zh-CN) primary + English; terminology: 「中继」= relay only, 「Hub」stays "Hub"; read `/Users/konata/code/tmex-copy-guidelines.md` before writing any user-facing text.
- Do not leave TODOs, stubs or "simplified versions". Finish the whole scope. Do not widen scope.
- When done, write your report to the absolute result path given in the task (Markdown, Simplified Chinese OK): what changed (file list), how verified (commands + counts), anything left/uncertain. Write the result file LAST, then exit.

## Task G1b — Messaging channel hygiene for multi-node / relay modes

Result file (absolute): `/Users/konata/code/tmex-r25/prompt-archives/2026090402-round25-relay-live-bots/sub/G1b-result.md`. Write it LAST, then exit.

Scope (only these paths): `apps/gateway/src/runtime.ts` (+ `runtime.test.ts` if exists), `apps/gateway/src/config.ts` (only if you need a roles accessor), `apps/gateway/src/push/connection-alerts.ts` (+ test), `apps/gateway/src/agent/supervisor.ts` (+ test), `apps/gateway/src/agent/run-notify.ts` (+ test), `apps/gateway/src/events/channels/telegram.ts`, `apps/gateway/src/events/channels/weixin.ts`, `apps/gateway/src/events/channels/notification-format.ts`, `apps/gateway/src/events/channels/pane-url.ts` (+ tests), `apps/gateway/src/events/index.ts` (+ test). Do NOT touch `apps/gateway/src/telegram/**`, `apps/gateway/src/weixin/**`, `apps/gateway/src/messaging/**`, `apps/gateway/src/db/**`, `packages/shared/src/contracts/**` — another agent (G1a) is rewriting those in parallel; the public methods you may call remain `telegramService.sendToAuthorizedChats({text})` and `weixinService.sendToAuthorizedUsers({text})` (verify the exact names in the current files, read-only).

Findings to fix (verify each with file:line first):
1. `startLiveGatewayServices()` (`apps/gateway/src/runtime.ts:~83`) starts Telegram/Weixin polling unconditionally, including on a pure `relay` role process which has no users/devices. Gate messaging start on roles: parse `TMEX_ROLES` (the parser lives in packages/shared or packages/app — find `parseTmexRoles`/`isRelayOnly`; if it is only in packages/app, add a minimal pure helper in gateway config reading `process.env.TMEX_ROLES`), skip Telegram/Weixin/watch startup and the "gateway online" messages when the process is relay-only. Test it.
2. `ConnectionAlertNotifier` (`apps/gateway/src/push/connection-alerts.ts:~81,152,232`) sends device connection errors to Telegram directly, bypassing Weixin and the per-bot `enableNotificationPush` / `disabledNotificationChannels` gating that every other notification respects. Route it through the same `EventNotifier` path (a `device_connection_error` event or whichever existing event kind fits — check `apps/gateway/src/events/index.ts` and the lifecycle bridge to avoid double-sending; the current code comments mention a lifecycle bridge event that Telegram/Weixin skip) so both channels receive it under the standard gating and dedupe. Keep the existing rate limiting per device/type. Update `connection-alerts.test.ts`.
3. `apps/gateway/src/agent/supervisor.ts:~386` sends the agent credential-leak warning to Telegram only; send it through the notifier so Weixin gets it too (respecting gating).
4. `notifyAgentEvent()` (`apps/gateway/src/agent/run-notify.ts:~18`) resolves the device with local `getDeviceById()` although agent sessions can have a remote `nodeId` (`apps/gateway/src/db/schema/agent.ts:~78`); for remote sessions fall back to the session's stored device/node names so the notification carries the node name and a correct link (see `pane-url.ts`; add a `nodeId`-aware URL builder: entry-node links for a remote node's pane use `/n/<nodeId>/devices/...` if that is how the FE routes remote nodes — check `apps/fe/src` routing for the remote device URL shape and mirror it; if the FE uses a different scheme, use that).
5. Notification bodies for terminal bell/notification/agent/watch events should name the node when the gateway is part of a mesh: add a `node` line (local node display name from `node_identity.name` → site name fallback) to the shared raw views in `notification-format.ts` only when the node has a mesh identity (hub or relay uplink), so standalone users see no change. Find the accessor for the local node name (`apps/gateway/src/mesh/*` or `db/node-identity`).
Tests: `cd apps/gateway && bun test src/push/connection-alerts src/agent src/events src/runtime` + `bunx tsc --noEmit -p .` (0 errors) + `bunx biome check` on touched files. Keep functions under CC 15.
