## Task LT — Multi-process rehearsal of the Hub → relay live migration (temporary instances only)

Result file: `/Users/konata/code/tmex-r25/prompt-archives/2026090402-round25-relay-live-bots/sub/LT-result.md` (write LAST). Driver + logs live in `/private/tmp/claude-501/-Users-konata-code-tmex-enhanced/2da64d3c-b5e4-4192-98f5-dbd74931b528/scratchpad/live/r25/` (`<live>`); a previous round's driver `<live>/live24.ts` (1389 lines, worked end-to-end), `<live>/clone-repo.sh` and `<live>/helper-bootstrap-user.ts` are there to COPY FROM — read them first, they encode every hard-won trick (APFS clone per instance so `test.env.local` never touches the repo, whitelist env, isolated tmux socket, root-key signing of `admit-node`/`set-relays`/`revoke-node`, sealed-pack upload, CLI invocation with `TMEX_PASSWORD`, polling helpers). Write a new `<live>/live25.ts` for the scenario below (reuse code freely; point imports at `/Users/konata/code/tmex-r25`).

HARD SAFETY RULES: never touch the production tmex (port 9883, `~/Library/Application Support/tmex/`), never touch the tmux session named `tmex` or the default tmux socket — every instance sets `TMEX_TMUX_SOCKET=tmex-live25`; listen only on 127.0.0.1 ports 19981–19989 (+19980 for helpers); `NODE_ENV=test`; own DB per instance under `<live>/inst/<NAME>/`; do not run `git` commands; do not edit repo files (other agents are editing the worktree; if the runtime fails to start because of an in-flight compile error in the worktree, wait a few minutes and retry up to 3 times, then report). Clean up on exit: kill instance processes, `tmux -L tmex-live25 kill-server`, free ports. You may need `apps/fe/dist` for the entry node's frontend — reuse an existing dist if present, otherwise `bun run --cwd apps/fe build` ONCE (it is fine to build; do not commit the output).

Scenario (mirrors the real migration we will run on production machines right after this; every step must be the same command/API the real machines will use):
- H1 = `hub,node` writer (this is "tmexhub-sh"); E = `node` joined to H1 with a join token, the driver holds the mesh account root key exactly as the browser would (this is the "mac" entry node); N1 = `node` joined to H1 by Hub password join (`POST /api/setup/join method=password` or CLI `tmex hub join <url> --password`) named `jiefa-app`; N2 = `node` joined with a token named `docker-node`. Optional (only if it can be set up from env without manual admit-hub signing — check `docs/hub/2026090104-multi-hub-standby.md`): A = `hub,node` standby.
1. Bring the hub mesh up; from E verify `/api/mesh/nodes` lists all nodes online. Record node ids.
2. Enable TOTP on the account (find the API: `/api/auth/totp/*`) and join a fresh node N3 via Hub password join WITH the TOTP code (`--totp` / `totpCode`, implemented this round by task G2 — if that code has not landed yet when you get there, record the exact failure and continue). Also verify a join without the code returns the stable `totp_required` error.
3. Migration order (this is the real runbook):
   a. H1: CLI `tmex hub leave` (non-interactive; check flags; it must not need a password) → H1 restarts as standalone; verify E's hub list now shows H1 unreachable but E stays up.
   b. H1: `POST /api/setup/relay { role: 'relay', relayPublicUrl: 'http://127.0.0.1:<H1 port>', relayPassword }` → pure relay; `GET /api/relay/health` 200; `GET /` 404.
   c. E (hub-mode node, still holding hub session): `POST /api/mesh/relay/enroll { url, password, proof }` (proof via `/enroll/proof-material` + `signRelayEnrollProof` with the root key) → sign `set-relays` → `POST /api/auth/keylog?hub=sync` → E switches to relay mode (`GET /api/mesh/relay/status` mode=relay, attached) → seal + `POST /api/mesh/relay/pack`. Then inspect H1's relay DB (`relay_nodes`) and assert the historical nodes (H1-old-node, N1, N2, N3) were admitted from the uploaded sidecars.
   d. N1: CLI `tmex hub leave` → `TMEX_PASSWORD=<mesh pw> tmex relay join http://127.0.0.1:<H1> --tenant <id> --name jiefa-app --no-restart?` (find which flags the real machines need; the real machines run systemd-user services so restarts are normal) → attached to relay; E sees N1 online via relay with a NEW node id and the SAME name; open a canonical terminal stream from E to N1 (reuse the r24 driver's WS check if it has one; otherwise assert `/n/<N1>/api/...` proxying works via the relay stream).
   e. H1: `tmex relay join <own url> --tenant <id> --name tmexhub-sh` → role becomes `relay,node`; E sees it.
   f. N2, N3 the same as N1.
   g. E: sign `revoke-node` for every OLD node id (they are ghosts now) + `meta-key {op:'rotate'}`; assert H1's registry marks them revoked and all live nodes stay attached.
   h. `POST /api/mesh/relay/enrollments` from E → assert the new fan-out response shape (`relays: [{url, tenantId, token, accepted}]`, task G4 — record if not landed) and that a token join (`tmex hub join --token r3.…`) of a fresh node N4 still works.
4. Negative/leftover checks: `POST /api/local/leave { targetRole: 'relay' }` on H1 (relay,node) → its own tenant is removed from `relay_tenants` (task G3) and E is disconnected; then H1 `relay join` again to recover. Old peer version gate: skip.
5. Write the result as a PASS/FAIL table with evidence lines, plus a "runbook" section listing the EXACT commands/API calls that worked, in order, with the flags — I will replay them on the real machines.
