## Common rules (read first)

- Repo worktree: `/Users/konata/code/tmex-r25` (branch `feat/round25-relay-live-bots`, base = main 1.1.24). Bun-only runtime (`bun`, `bunx`; if `bun` is missing, `export PATH="$HOME/.bun/bin:/opt/homebrew/bin:$PATH"`). Never use Node for app code; only `packages/app` CLI stays Node-compatible.
- OTHER AGENTS ARE EDITING THIS SAME WORKTREE IN PARALLEL. Touch ONLY the files in your scope (listed in your task). Do NOT run `git add/commit/stash/checkout/reset`, do NOT run `bun run build:i18n` / formatters on files outside your scope, do NOT run lint/format on generated files (`packages/shared/src/i18n/resources.ts`, `types.ts`, `resources/fe-dist/*`, `dist/*`). If you must add i18n keys, edit the source locale JSON files under `packages/shared/src/i18n/locales/` (or wherever the source JSON lives) for BOTH `zh-CN` and `en`, editing only your own nested keys; the commander regenerates resources.
- Never touch the production tmex on this machine: service on port 9883, `~/Library/Application Support/tmex/`, launchd; never touch the tmux session named `tmex` or the default tmux socket. Any temporary instance must set `TMEX_TMUX_SOCKET=<unique>`, unique `GATEWAY_PORT`/`TMEX_PEER_PORT` (use 19xxx), `NODE_ENV=test`, its own `DATABASE_URL` under the scratch dir, and `TMEX_BIND_HOST=127.0.0.1`.
- Tests: run only the package you touch — `cd apps/gateway && bun test <path>` / `cd apps/fe && bun test src/<path>` (never bare `bun test` in apps/fe — it picks Playwright specs) / `cd packages/app && bun test <path>`. Type-check with `bunx tsc --noEmit -p .` in the package (baseline error counts: gateway 0, fe 0, app 0, shared 0, ws-client 0, stores 1, api-client 5, theme 9). Lint your files with `bunx biome check <files>` (no `--write` on files outside scope). macOS has no `timeout` command.
- Code style: TypeScript, no unnecessary comments (only for genuinely complex logic, written in Simplified Chinese), variable names in English, prefer small functions (complexity gate: cyclomatic ≤ 15 per function, file ≤ 600 lines, enforced by `bun run lint` at repo root — do not add allowlist entries).
- UI copy rules: Simplified Chinese (zh-CN) primary + English; terminology: 「中继」= relay only, 「Hub」stays "Hub"; read `/Users/konata/code/tmex-copy-guidelines.md` before writing any user-facing text.
- Do not leave TODOs, stubs or "simplified versions". Finish the whole scope. Do not widen scope.
- When done, write your report to the absolute result path given in the task (Markdown, Simplified Chinese OK): what changed (file list), how verified (commands + counts), anything left/uncertain. Write the result file LAST, then exit.

## Task D1 — Upgradable docker node image (`scripts/docker-node/`)

Scope (only these paths): `scripts/docker-node/**` (new), `docs/hub/2026090402-docker-node.md` (new). Result file: `/Users/konata/code/tmex-r25/prompt-archives/2026090402-round25-relay-live-bots/sub/D1-result.md`.

Background: a hand-deployed container `tmex-node-docker` (image `tmex-node:test`, `/opt/tmex` untarred by hand, `/entry.sh` loop running `bun dist/runtime/server.js`) cannot be upgraded from the web UI: the gateway reports `UPGRADE_NOT_ALLOWED` because there is no `install-meta.json` (see `apps/gateway/src/system/install-info.ts` `getInstallInfo`, `info-public.ts` `canSelfUpdate`). The CLI already supports a service-less install: `tmex init ... --no-service` writes `install-meta.json` with `serviceMode: 'none'` (`packages/app/src/commands/init.ts` ~L174/337/383/450), `run.sh` writes `<installDir>/tmex.pid` then `exec bun .../server.js` (`packages/app/src/lib/install.ts` `buildRunScriptContent`), and the upgrader uses `createDirectProcessControl` (kill pid, wait, spawn `bash run.sh` detached; `packages/app/src/lib/upgrade-process.ts` L223+, `upgrade-apply.ts` `createServiceControl`) — the gateway-triggered upgrade passes `--no-service` when `install-meta.serviceMode === 'none'` (`apps/gateway/src/system/upgrade.ts` ~L690-775). Read all of these before writing anything.

Deliverables:
1. `scripts/docker-node/Dockerfile`: ubuntu:24.04 + Node 22 official tarball + Bun 1.3.14 + tmux/openssl/curl/procps/ca-certificates (mirror the download approach in `scripts/hub-e2e/Dockerfile`, including the optional prefetched `build/bun-linux-<arch>.zip`; multi-arch amd64/arm64; on this Mac build arm64 natively). Copy a `tmex-cli-<version>.tgz` (build arg `TMEX_TARBALL`) into the image under `/opt/tmex-pkg/`.
2. `scripts/docker-node/entrypoint.sh` (PID 1): on first boot (no `/opt/tmex/install-meta.json`) run the CLI from the tarball to install non-interactively as `standalone` with `--no-service --install-dir /opt/tmex --host 0.0.0.0 --port 9883 --db-path /var/lib/tmex/tmex.db --autostart false` (+ whatever flags are needed to skip prompts and dependency installs; read `init.ts`/`args.ts`/`cli/help.ts` for the exact flag set; bun path is `/usr/local/bin/bun`). Ensure `app.env` gets `TMEX_SITE_NAME=${TMEX_SITE_NAME:-docker-node}`, `TMEX_PEER_PORT=39001`, `TMEX_PEER_BIND_HOST=0.0.0.0`, `TMEX_TMUX_SOCKET` unset (container default socket is fine, it's isolated) — check which keys `init` writes and append only missing ones. Then supervise: loop forever; if `/opt/tmex/tmex.pid` is absent/dead **for more than ~20 s continuously** AND no upgrade transaction is in progress (inspect `packages/app/src/lib/upgrade-lock.ts` / `upgrade-state.ts` / journal paths for the file(s) to test), start `bash /opt/tmex/run.sh` in the background (stdout to the container log). The delay/lock check exists so the in-place upgrader's stop→start gap is not raced (a second `run.sh` would overwrite `tmex.pid` with a process that dies on port bind and break the upgrader's health check). Reap zombies (`trap`/`wait -n` or `exec` via `tini`-less approach with a wait loop). Also create a tmux session `demo` (default socket inside the container) so the node has something to show.
3. `scripts/docker-node/build.sh`: builds the tarball from the worktree (`bun run build` at repo root then `cd packages/app && npm pack --pack-destination <dir>`; note `bun run build` currently fails at `build:runtime` on main? — check `packages/app/scripts/build-runtime.ts` for `--external cpu-features`; if the root build works, use it) and `docker build --build-arg TMEX_TARBALL=... -t tmex-node:<version> scripts/docker-node`. Accept `TMEX_TARBALL=<path>` to skip the build.
4. `scripts/docker-node/run.sh`: `up` (create container `${TMEX_DOCKER_NAME:-tmex-node-docker}` with volumes `tmex-node-opt:/opt/tmex` and `tmex-node-data:/var/lib/tmex`, ports `29883:9883`, `39001:39001`, restart unless-stopped), `down`, `logs`, `shell`. Do NOT touch the existing container named `tmex-node-docker` during your testing — use `TMEX_DOCKER_NAME=tmex-node-d1` and volume names suffixed with the container name, and clean them up when done (`down -v`).
5. Verify end-to-end on this Mac (Docker Desktop is running, arm64): build image from the current worktree; `up`; `curl 127.0.0.1:<port>/healthz` (map to 29884 for your test container) returns the version; `docker exec` shows `install-meta.json` with `serviceMode":"none"`, `tmex.pid` alive; then prove the in-container upgrader works with the supervisor: copy the same tarball into the container and run `bun /opt/tmex/current/cli/bin/tmex.js upgrade --apply-current-package --yes --no-service --install-dir /opt/tmex` from within the container (check `packages/app/src/commands/upgrade.ts` for the exact flags for applying a local package; `--apply-current-package` must be run from the unpacked package's own `bin/tmex.js`, e.g. `mkdir /tmp/pkg && tar -xzf ... -C /tmp/pkg && node /tmp/pkg/package/bin/tmex.js upgrade --apply-current-package --yes --no-service --install-dir /opt/tmex`), then confirm: healthz comes back, `current` symlink points to the version dir, only ONE server process is running, and the supervisor did not spawn a duplicate. Also verify that after `docker restart` the node comes back. Record all commands and outputs in the result file.
6. `docs/hub/2026090402-docker-node.md` (Simplified Chinese, concise): purpose, layout, how upgrades work in the container, commands, caveats (e.g. the web UI's "upgrade" hits GitHub Releases from inside the container; app.env location; how to join a relay/hub from inside: `docker exec -it <c> bun /opt/tmex/current/cli/bin/tmex.js relay join <url> --tenant <id> --password`).

Do not edit anything under `packages/app/src` — if you find the CLI genuinely cannot do a `--no-service` init non-interactively, stop, describe the exact blocker with file:line in the result file, and exit.
