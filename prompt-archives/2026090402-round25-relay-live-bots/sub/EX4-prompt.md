You are a read-only code explorer for the tmex monorepo (Bun runtime; apps/gateway = backend, apps/fe = React frontend, packages/app = CLI + runtime assembly, packages/shared = shared libs). Output your COMPLETE report as your final message (you cannot write files). Be concrete: cite file paths with line numbers, quote code, name exact functions.

Task EX4 — Scope the relay-mode leftovers from round 24 so an implementer can start immediately. Read docs/relay/2026090304-relay-role.md first (sections 5b, 7, 9, 13) and prompt-archives/2026090401-round24-relay-local-role/plan-00-result.md section 五 (遗留). For EACH item below: locate the exact code, explain the current behavior with line refs, and propose the minimal concrete change (files, functions, new routes/records/migrations if needed, tests to add). Flag anything that is infeasible or already done.

L1. "TOTP / passkey two-factor accounts cannot use password join": for Hub password join (`POST /api/hub/enrollments/by-password`, `/api/setup/join method=password`, CLI `tmex hub join --password`, G7 self-admit via root key) the joiner must log in to the Hub with the root key; find where 2FA blocks that (auth mode/second-factor checks in apps/gateway/src/auth/* and mesh login), and design how to accept a TOTP code (and whether passkey-only accounts can be supported at all — e.g. allow TOTP code param, or an "admit later from an already-logged-in browser" fallback). ALSO answer separately: does RELAY password join (`performRelayPasswordJoin`, `/api/setup/relay-join`, `tmex relay join --tenant`) require any login that 2FA would block? (It derives the root seed and self-signs admit-node against the relay — does anything there check second factor?) Be precise.
L2. "`leave targetRole=relay` leaves a ghost tenant in the machine's own relay registry": find `clearMeshMembership` / `clearRelayOperatorState` in the leave handler (apps/gateway or packages/app setup-service) and the relay tenant table; propose deleting (or kicking) the tenant whose root_public_key equals the leaving local user's root pk when the local relay runtime is present — and what about `leave` to standalone from `relay,node` (does it currently drop relay operator state entirely? doc says yes).
L3. "Hub node list has only enrolled/revoked, no 'not yet admitted' state": find the hub node list projection (apps/gateway/src/hub/* node.list, `nodes` table status column, `admit-node` application, `unknown-cert` uplink rejection) and the FE nodes management table; propose how to surface `pending`/unadmitted nodes (node has a cert in `nodes`/enrollment redeemed but no admit-node record in key log) including the DTO field and the i18n label, and a one-click "admit" action if a signing path already exists in the FE (`meta-key/prepare` op admit exists for relay; for hub there is admit flow in the add-node wizard).
L4. "enrollment fan-out to all relays" (round23 B2): today `relay.enroll.create` only lands on the attached relay and `GET /api/mesh/relay/join-material` returns only the attached relay; r3 join at another relay 404s. Locate `POST /api/mesh/relay/enrollments` and the uplink pool; propose fan-out (send create to every configured relay via short-lived HTTP or uplink, tolerate partial failure, return which relays accepted) and what the r3 token should encode.
L5. Doc §13 last bullet: `inspectHubAuthRecordCompat` / `KEYLOG_RECORD_COMPAT` gate reads the `nodes` table (hub-side only), so in relay mode it cannot block too-old peers; `peer_cache.version` now exists (migration 0041). Locate both gates (`hub-authorization.ts`, `auth-key-log-routes.ts`) and propose reading `peer_cache.version` in relay mode (with the empty-registry exemption kept only where still needed).
L6. Migration procedure audit (IMPORTANT for a live migration this round): we want to convert a live mesh (hub B `hub,node` writer + hub A `hub,node` standby + 4 plain nodes, entry node = local mac holding the root key in the browser) into: B = `relay,node`, everything else = `node` attached to B's relay, ideally KEEPING the existing user root key (so passkeys/TOTP survive). Trace precisely what each step does and what breaks: (a) B `POST /api/local/leave` from `hub,node` (what happens to its hub role, mesh_hubs on other nodes, does the standby A auto-promote or must `POST /api/hub/role` be called first?), (b) B `tmex init --role relay` / `POST /api/setup/relay` with role `relay` on a fresh standalone, (c) local mac "hub → 中继迁移" (`POST /api/mesh/relay/enroll` in hub mode + `set-relays` via `?hub=sync`) — confirm the record is NOT pushed to the old hub and what happens to the mac's mesh_hubs/hub session, and whether the key-log catch-up upload to the relay carries `member` sidecars for historical admit-node records (so the relay registry learns all existing nodes) or whether each node must self-present its admit-node in `relay.auth`, (d) for each other node: `POST /api/local/leave` then `tmex relay join <url> --tenant <id> --password` (or `/api/setup/relay-join`) — confirm they get a NEW node id, confirm `rename`/`--name` keeps the display name, confirm nothing requires a hub, (e) B itself: from pure `relay` role, `tmex relay join` → becomes `relay,node` (doc says so) — confirm code path (`relayJoinRoleName`), (f) whether the CLI has `tmex hub leave` (or equivalent) usable non-interactively with `TMEX_PASSWORD`, and what `leave` needs (local session? password?). List every CLI command signature involved with its flags from packages/app/src/commands/*.ts.
