## Common rules (read first)

- Repo worktree: `/Users/konata/code/tmex-r25` (branch `feat/round25-relay-live-bots`, base = main 1.1.24). Bun-only runtime (`bun`, `bunx`; if `bun` is missing, `export PATH="$HOME/.bun/bin:/opt/homebrew/bin:$PATH"`). Never use Node for app code; only `packages/app` CLI stays Node-compatible.
- OTHER AGENTS ARE EDITING THIS SAME WORKTREE IN PARALLEL. Touch ONLY the files in your scope (listed in your task). Do NOT run `git add/commit/stash/checkout/reset`, do NOT run `bun run build:i18n` / formatters on files outside your scope, do NOT run lint/format on generated files (`packages/shared/src/i18n/resources.ts`, `types.ts`, `resources/fe-dist/*`, `dist/*`). If you must add i18n keys, edit the source locale JSON files under `packages/shared/src/i18n/locales/` (or wherever the source JSON lives) for ALL THREE locales `zh_CN.json`, `en_US.json`, `ja_JP.json` (a locale-consistency test enforces identical key sets), editing only your own nested keys; the commander regenerates resources.
- Never touch the production tmex on this machine: service on port 9883, `~/Library/Application Support/tmex/`, launchd; never touch the tmux session named `tmex` or the default tmux socket. Any temporary instance must set `TMEX_TMUX_SOCKET=<unique>`, unique `GATEWAY_PORT`/`TMEX_PEER_PORT` (use 19xxx), `NODE_ENV=test`, its own `DATABASE_URL` under the scratch dir, and `TMEX_BIND_HOST=127.0.0.1`.
- Tests: run only the package you touch — `cd apps/gateway && bun test <path>` / `cd apps/fe && bun test src/<path>` (never bare `bun test` in apps/fe — it picks Playwright specs) / `cd packages/app && bun test <path>`. Type-check with `bunx tsc --noEmit -p .` in the package (baseline error counts: gateway 0, fe 0, app 0, shared 0, ws-client 0, stores 1, api-client 5, theme 9). Lint your files with `bunx biome check <files>` (no `--write` on files outside scope). macOS has no `timeout` command.
- Code style: TypeScript, no unnecessary comments (only for genuinely complex logic, written in Simplified Chinese), variable names in English, prefer small functions (complexity gate: cyclomatic ≤ 15 per function, file ≤ 600 lines, enforced by `bun run lint` at repo root — do not add allowlist entries).
- UI copy rules: Simplified Chinese (zh-CN) primary + English; terminology: 「中继」= relay only, 「Hub」stays "Hub"; read `/Users/konata/code/tmex-copy-guidelines.md` before writing any user-facing text.
- Do not leave TODOs, stubs or "simplified versions". Finish the whole scope. Do not widen scope.
- When done, write your report to the absolute result path given in the task (Markdown, Simplified Chinese OK): what changed (file list), how verified (commands + counts), anything left/uncertain. Write the result file LAST, then exit.

## Task G3 — Relay leftovers: ghost tenant on `leave → relay`, and relay-mode version gate must read `peer_cache.version`

Result file: `/Users/konata/code/tmex-r25/prompt-archives/2026090402-round25-relay-live-bots/sub/G3-result.md`.
Reference: `/Users/konata/code/tmex-r25/prompt-archives/2026090402-round25-relay-live-bots/sub/EX4-report.md` sections L2 and L5 (verify every file:line yourself).

Scope (only): `packages/app/src/runtime/membership-reset.ts` (+ `.test.ts`), `apps/gateway/src/auth/mesh-membership-store.ts` (+test), `apps/gateway/src/relay/relay-tenant-store.ts` only if you need a helper, `apps/gateway/src/hub/hub-authorization.ts` (+test), `apps/gateway/src/mesh/auth-key-log-routes.ts` (+ its tests incl. `auth-key-log-relay.test.ts`), `apps/gateway/src/auth/user-store.ts` only for a `listActivePeers`/`getPeer` accessor if missing, `docs/relay/2026090304-relay-role.md` (update §7.3 and §13 last bullet to describe the new behaviour). Do NOT touch anything else (other agents are editing hub-runtime.ts, setup-service.ts, relay-routes.ts, messaging, events).

L2: `clearMembershipForTarget()` (`membership-reset.ts:~137`) with `targetRole === 'relay'` calls `clearMeshMembership()` which leaves the machine's own tenant (root_public_key = the leaving local user's root pk) in `relay_tenants` (a ghost). Capture the primary local user's `rootPublicKey` before clearing, then in the SAME transaction delete the `relay_tenants` row whose `root_public_key` matches (cascade removes relay_nodes/enrollments/key_log); other tenants stay. `relay,node → standalone` keeps calling `clearAll()`. If the tenant has live uplinks, disconnect them via the relay runtime if a hook is reachable from membership-reset (check how `leave` already stops things; if no hook is reachable, document it — the runtime restarts right after leave anyway). Update `membership-reset.test.ts:~356` expectation and add: matching tenant removed, foreign tenant kept, cascade verified, standalone path unchanged.
L5: `inspectHubAuthRecordCompat()` (`hub-authorization.ts:~183`) reads `nodes.version` (hub-only table) so in relay mode too-old peers are never blocked, and the empty-registry exemption (`~216`, `listNodes().length === 0`) plus the `rotate-root-keep` bypass in `auth-key-log-routes.ts:~464/475` are wide open. Implement a version source abstraction: hub mode → `nodes.version`; relay mode → `peer_cache.version` for active certs (peer present but version missing/unparseable → fail-closed as too old). Shrink the exemption to relay mode + node-side record types (`set-relays`, `meta-key`, `rename-node`) + zero active peers; hub-auth records and `rotate-root-keep` stay fail-closed (remove/narrow the `rotate-root-keep` bypass — but make sure the legitimate relay-mode `rotate-root-keep` flow with all peers ≥ its minVersion still passes; check `MIN_*_RECORD_VERSION` constants in `packages/shared/src/auth/key-log.ts`). Tests for: relay peer too old → blocked; peers ok → allowed; empty peer cache bootstrap exemption only for the three record types; version missing → blocked; hub-mode tests unchanged.
Verification: `cd apps/gateway && bun test src/hub/hub-authorization src/mesh/auth-key-log src/auth/mesh-membership && bunx tsc --noEmit -p .` (0); `cd packages/app && bun test src/runtime/membership-reset && bunx tsc --noEmit -p .` (0); `bun run lint`.
