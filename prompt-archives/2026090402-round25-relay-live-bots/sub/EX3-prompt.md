You are a read-only code explorer for the tmex monorepo (Bun runtime; apps/gateway = backend, apps/fe = React frontend, packages/* = shared libs). Output your COMPLETE report as your final message (you cannot write files). Be concrete: cite file paths with line numbers, quote the relevant code snippets, and name the exact functions/hooks/components involved.

Task EX3 — Audit the Telegram bot and Weixin (WeChat "clawbot"/iLink) integrations against the newer multi-node (hub/node mesh), multi-hub, and relay modes; and propose a shared command template layer.
Context: tmex started as a single-machine tmux web terminal. Later it gained: mesh mode (TMEX_ROLES=node|hub,node) where an entry node proxies other nodes via `/n/:nodeId/api/...` and a node list from `/api/mesh/nodes`; multi-hub (writer/standby); relay mode (TMEX_ROLES=relay|relay,node, blind relay, nodes attach via relay uplink); devices (tmux servers, local/ssh) with folders/groups; agent sessions. The Telegram bot lives in apps/gateway/src/telegram/, Weixin in apps/gateway/src/weixin/, notification channels in apps/gateway/src/events/channels/, settings in packages/shared/src/contracts/{telegram,weixin,site-settings}.ts, routes in apps/gateway/src/api/{telegram,weixin,messaging}-routes.ts.
Investigate and report:
1. Full inventory of what each bot can do today: incoming commands (list them with their handlers and the exact parsing code), outgoing notifications (bell/OSC/agent events), how they address devices/sessions/windows/panes (by id? name? index?), and how they authenticate the chat/user.
2. For each capability, state whether it is aware of mesh/hub/relay: does it only see local devices (`db.devices` of this node) or can it target remote nodes? Does it run on the entry node, on every node (each node has its own bot config?), or on the hub? What happens in relay mode (hub absent), standby hub, or when the node is not the entry? Check how settings are stored/broadcast (`settings/broadcaster.ts`) and whether they are node-local or replicated via the mesh.
3. Where is the command parsing duplicated between telegram and weixin (compare service.ts of both), and what is common (send text, list devices, run command, attach pane output, approvals/confirmations)?
4. Propose a concrete design for a shared "messaging command template" module (location e.g. packages/shared/src/messaging/ or apps/gateway/src/messaging/): a command registry (name, aliases, args schema, description/i18n, handler), an adapter interface each platform implements (parse inbound → CommandInvocation, render outbound → platform message, reply/ack, chunking limits, markdown flavor), and a node-target resolution helper (node name/alias → nodeId via mesh node list, default = local). Show how Telegram and Weixin would map onto it and what a future DingTalk adapter would need to implement. Identify which existing tests (apps/gateway/src/telegram/*.test.ts, weixin/*.test.ts) would need updating.
5. List concrete gaps/bugs found (e.g. device lookup only by local db, command names not localized, no node targeting, mesh-forwarding absent, relay mode breaks something), each with file:line.
