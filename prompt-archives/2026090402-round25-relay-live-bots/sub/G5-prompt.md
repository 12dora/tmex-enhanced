## Common rules (read first)

- Repo worktree: `/Users/konata/code/tmex-r25` (branch `feat/round25-relay-live-bots`, base = main 1.1.24). Bun-only runtime (`bun`, `bunx`; if `bun` is missing, `export PATH="$HOME/.bun/bin:/opt/homebrew/bin:$PATH"`). Never use Node for app code; only `packages/app` CLI stays Node-compatible.
- OTHER AGENTS ARE EDITING THIS SAME WORKTREE IN PARALLEL. Touch ONLY the files in your scope (listed in your task). Do NOT run `git add/commit/stash/checkout/reset`, do NOT run `bun run build:i18n` / formatters on files outside your scope, do NOT run lint/format on generated files (`packages/shared/src/i18n/resources.ts`, `types.ts`, `resources/fe-dist/*`, `dist/*`). If you must add i18n keys, edit the source locale JSON files under `packages/shared/src/i18n/locales/` (or wherever the source JSON lives) for ALL THREE locales `zh_CN.json`, `en_US.json`, `ja_JP.json` (a locale-consistency test enforces identical key sets), editing only your own nested keys; the commander regenerates resources.
- Never touch the production tmex on this machine: service on port 9883, `~/Library/Application Support/tmex/`, launchd; never touch the tmux session named `tmex` or the default tmux socket. Any temporary instance must set `TMEX_TMUX_SOCKET=<unique>`, unique `GATEWAY_PORT`/`TMEX_PEER_PORT` (use 19xxx), `NODE_ENV=test`, its own `DATABASE_URL` under the scratch dir, and `TMEX_BIND_HOST=127.0.0.1`.
- Tests: run only the package you touch — `cd apps/gateway && bun test <path>` / `cd apps/fe && bun test src/<path>` (never bare `bun test` in apps/fe — it picks Playwright specs) / `cd packages/app && bun test <path>`. Type-check with `bunx tsc --noEmit -p .` in the package (baseline error counts: gateway 0, fe 0, app 0, shared 0, ws-client 0, stores 1, api-client 5, theme 9). Lint your files with `bunx biome check <files>` (no `--write` on files outside scope). macOS has no `timeout` command.
- Code style: TypeScript, no unnecessary comments (only for genuinely complex logic, written in Simplified Chinese), variable names in English, prefer small functions (complexity gate: cyclomatic ≤ 15 per function, file ≤ 600 lines, enforced by `bun run lint` at repo root — do not add allowlist entries).
- UI copy rules: Simplified Chinese (zh-CN) primary + English; terminology: 「中继」= relay only, 「Hub」stays "Hub"; read `/Users/konata/code/tmex-copy-guidelines.md` before writing any user-facing text.
- Do not leave TODOs, stubs or "simplified versions". Finish the whole scope. Do not widen scope.
- When done, write your report to the absolute result path given in the task (Markdown, Simplified Chinese OK): what changed (file list), how verified (commands + counts), anything left/uncertain. Write the result file LAST, then exit.

## Task G5 — Wire the messaging runtime hooks so chat commands actually work

Result file: `/Users/konata/code/tmex-r25/prompt-archives/2026090402-round25-relay-live-bots/sub/G5-result.md`.

Background: task G1a (already committed, see `docs/messaging/2026090402-messaging-command-template.md` and `apps/gateway/src/messaging/context.ts`) added a command layer with `registerMessagingRuntime(hooks)` (`MessagingRuntimeHooks`: `getUplinkStatus`, `listMeshNodes`, `getDeviceTree`, `capturePane`, `sendKeys`, `decideConfirmation`). Nobody registers the hooks yet, so `devices`/`windows`/`panes`/`tail`/`run` report devices as disconnected or throw `capture-unavailable`, `status` reports `attached: unknown`, and `approve`/`deny` decide in the DB without notifying the AgentSupervisor. Your job is to register real implementations.

Scope (only): `apps/gateway/src/runtime.ts` (+ `runtime.test.ts`), `apps/gateway/src/messaging/context.ts` and `apps/gateway/src/messaging/handlers/**` ONLY if a hook signature must change (keep G1a's tests passing, adjust them if you change signatures), NEW `apps/gateway/src/messaging/runtime-hooks.ts` (+test) holding the gateway-side implementations, `packages/app/src/runtime/assemble.ts` (+test) for the mesh-side hooks (uplink status, mesh node list with online flags from the same source `/api/mesh/nodes` uses — find the function `mesh-routes.ts` calls, e.g. a `collectNodes`/projection in `apps/gateway/src/mesh/`; if it is reachable from gateway directly, register it in `runtime-hooks.ts` instead and leave assemble.ts alone), `apps/gateway/src/mesh/mesh-runtime.ts` only to expose an accessor if none exists. Do NOT touch `apps/gateway/src/hub/**`, `apps/gateway/src/relay/**`, `apps/gateway/src/mesh/relay-routes.ts`, `auth-key-log-routes.ts`, `packages/app/src/runtime/setup-*`, `membership-reset.ts`, `packages/app/src/commands/**`, `apps/fe`, `packages/panels` (other agents).

Implement:
1. `getDeviceTree(deviceId)`: from the gateway's live tmux state (the same snapshot the WS `STATE_SNAPSHOT` / `GET /api/tmux/tree` serve — find it: `apps/gateway/src/tmux-client/*`, `push/supervisor.ts` or the ws state store), returning windows/panes with ids, indexes, names, active flags; `null` if the device is not connected.
2. `capturePane(deviceId, paneId, lines)`: run `capture-pane -p -t <paneId> -S -<lines>` through the device's existing tmux client/control connection (never spawn `tmux` against the default socket yourself; go through the per-device client so remote/ssh devices work), strip trailing blank lines.
3. `sendKeys(deviceId, paneId, text)`: use the same input path the canonical `TerminalInput` WS handler uses (find `tmux-command-handlers.ts` / the input service), sending `text + '\r'` literally (no key-name interpretation).
4. `decideConfirmation`: route through `AgentSupervisor` (`apps/gateway/src/agent/supervisor.ts` has a resolve/decide method that the HTTP route `agent-confirmation-routes.ts` uses — call the same thing so WS clients get the update).
5. `getUplinkStatus` / `listMeshNodes`: hub/relay attached state and online peers. Standalone → `kind: 'none'`.
6. Register all hooks in `startLiveGatewayServices()` (or wherever runtime deps exist) BEFORE Telegram/Weixin refresh; ensure relay-only still skips messaging (task G1b's gating, already in runtime.ts).
7. Tests: unit tests for each hook with fakes (device tree from a fake snapshot, capture output trimming, sendKeys payload, confirmation routed to supervisor, uplink status per role) + a runtime test that hooks are registered on start.
Verification: `cd apps/gateway && bun test src/messaging src/runtime src/agent && bunx tsc --noEmit -p .` (only the pre-existing `native-datachannel.ts` TS5097 may remain), `bun run lint`.
