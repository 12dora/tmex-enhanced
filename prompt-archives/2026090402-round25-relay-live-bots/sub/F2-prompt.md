## Common rules (read first)

- Repo worktree: `/Users/konata/code/tmex-r25` (branch `feat/round25-relay-live-bots`, base = main 1.1.24). Bun-only runtime (`bun`, `bunx`; if `bun` is missing, `export PATH="$HOME/.bun/bin:/opt/homebrew/bin:$PATH"`). Never use Node for app code; only `packages/app` CLI stays Node-compatible.
- OTHER AGENTS ARE EDITING THIS SAME WORKTREE IN PARALLEL. Touch ONLY the files in your scope (listed in your task). Do NOT run `git add/commit/stash/checkout/reset`, do NOT run `bun run build:i18n` / formatters on files outside your scope, do NOT run lint/format on generated files (`packages/shared/src/i18n/resources.ts`, `types.ts`, `resources/fe-dist/*`, `dist/*`). If you must add i18n keys, edit the source locale JSON files under `packages/shared/src/i18n/locales/` (or wherever the source JSON lives) for ALL THREE locales `zh_CN.json`, `en_US.json`, `ja_JP.json` (a locale-consistency test enforces identical key sets), editing only your own nested keys; the commander regenerates resources.
- Never touch the production tmex on this machine: service on port 9883, `~/Library/Application Support/tmex/`, launchd; never touch the tmux session named `tmex` or the default tmux socket. Any temporary instance must set `TMEX_TMUX_SOCKET=<unique>`, unique `GATEWAY_PORT`/`TMEX_PEER_PORT` (use 19xxx), `NODE_ENV=test`, its own `DATABASE_URL` under the scratch dir, and `TMEX_BIND_HOST=127.0.0.1`.
- Tests: run only the package you touch — `cd apps/gateway && bun test <path>` / `cd apps/fe && bun test src/<path>` (never bare `bun test` in apps/fe — it picks Playwright specs) / `cd packages/app && bun test <path>`. Type-check with `bunx tsc --noEmit -p .` in the package (baseline error counts: gateway 0, fe 0, app 0, shared 0, ws-client 0, stores 1, api-client 5, theme 9). Lint your files with `bunx biome check <files>` (no `--write` on files outside scope). macOS has no `timeout` command.
- Code style: TypeScript, no unnecessary comments (only for genuinely complex logic, written in Simplified Chinese), variable names in English, prefer small functions (complexity gate: cyclomatic ≤ 15 per function, file ≤ 600 lines, enforced by `bun run lint` at repo root — do not add allowlist entries).
- UI copy rules: Simplified Chinese (zh-CN) primary + English; terminology: 「中继」= relay only, 「Hub」stays "Hub"; read `/Users/konata/code/tmex-copy-guidelines.md` before writing any user-facing text.
- Do not leave TODOs, stubs or "simplified versions". Finish the whole scope. Do not widen scope.
- When done, write your report to the absolute result path given in the task (Markdown, Simplified Chinese OK): what changed (file list), how verified (commands + counts), anything left/uncertain. Write the result file LAST, then exit.

## Task F2 — "Terminal connection failed" notices must show the node's display name (e.g. `jiefa-app`), not an id prefix

Result file: `/Users/konata/code/tmex-r25/prompt-archives/2026090402-round25-relay-live-bots/sub/F2-result.md`.

Scope (only these paths): `packages/stores/src/tmux-event-router.ts` (+ `.test.ts`), `packages/stores/src/runtime.ts` (+ test) for the resolver option, `packages/stores/src/node-connection-manager.ts` only if the option must pass through it, `apps/fe/src/node/mesh-nodes.ts` (add a pure `resolveMeshNodeName(nodeId)` helper next to `getMeshNodesState`; do NOT touch `useHubNode` or hub-related code — another agent just edited hub UI files, and `mesh-nodes.ts` hub-loading logic must stay untouched) + its test, `apps/fe/src/node/node-runtimes.ts` (+ test) to inject the resolver. Do not touch `packages/ws-client`, `packages/shared/src/ws-borsh`, or the gateway: the wire protocol keeps carrying `nodeId` only.

Findings (verify): `packages/stores/src/tmux-event-router.ts:45-55` `tooOldMessage()` builds the `websocket.nodeTooOld` toast with `name: nodeId.slice(0, 8)`; the locale strings (`zh_CN.json`/`en_US.json`/`ja_JP.json` under `websocket.nodeTooOld`) already take `{{name}}`. The canonical-state-v1.1 error contract (`canonical-state-v1.1 required: node <nodeId> version <ver> < <min>`) is parsed in `packages/ws-client` into `{type:'server-too-old', side, nodeId, version, minVersion}`. The frontend already has the authoritative node directory in `apps/fe/src/node/mesh-nodes.ts` (`getMeshNodesState().nodes[].{id,name}`, `entryNodeId`; `SELF_NODE_ID` sentinel maps to the entry node). `apps/fe/src/node/node-runtimes.ts:318-333` creates per-node runtimes with `runtimeOptions`, and `packages/stores/src/runtime.ts:96-157` defines `RuntimeCore`/`AppRuntimeOptions`.

Implement:
1. `AppRuntimeOptions.resolveNodeName?: (nodeId: string) => string | null` stored on `RuntimeCore`; `tooOldMessage()` uses it: name found → `websocket.nodeTooOld` with the display name; not found → keep the existing behaviour (id prefix). `event.nodeId` (the remote node that is too old) takes precedence over `ctx.core.nodeId`.
2. `apps/fe`: `resolveMeshNodeName(nodeId)` (handles `SELF_NODE_ID` → entry node) and inject it into every runtime created in `node-runtimes.ts` (it must read the live store on each call, not a snapshot).
3. Audit the other user-facing notices in `packages/stores`/`packages/panels` that interpolate a node id (search `nodeId.slice`, `slice(0, 8)`, `node ${`): fix any that render an id where a name exists, using the same resolver. Report what you found even if nothing else needed changing.
4. Tests: update `tmux-event-router.test.ts` (resolver returns `jiefa-app` → params name `jiefa-app`; resolver null → prefix fallback; `event.nodeId` precedence), add a test for `resolveMeshNodeName`, and one for the injection in `node-runtimes.test.ts` if such a test file exists. Run `cd packages/stores && bun test && bunx tsc --noEmit -p .` (baseline: 1 pre-existing tsc error — do not add more) and `cd apps/fe && bun test src/node && bunx tsc --noEmit -p .` (0). `bunx biome check` on your files.
