# T4b — relay performance monitoring UI: Sparkline/StatTile primitives, metrics store, compact tiles on the 本机 card, full dashboard in the Relay tab

Result file: /Users/konata/code/tmex-r26/prompt-archives/2026090403-round26-pwa-login-localtab/sub/T4b-result.md

## Scope (files you may edit)
- packages/ui/src/components/sparkline.tsx, stat-tile.tsx (+ tests) and their export in the packages/ui barrel/`package.json` exports (check how other components are exported — e.g. `@tmex/ui/sidebar` uses wildcard exports).
- apps/fe/src/pages/settings/relay/** (the top-level Relay settings tab: relay-tab.tsx, relay-cards.tsx, relay-status-store.ts, new relay-metrics-store.ts, tests).
- apps/fe/src/pages/settings/nodes/relay/relay-service-metrics.tsx (+ test) — a stub exists exporting `RelayServiceMetrics({ publicUrl, hasPassword, onOpenConsole })`; keep that signature (you may add optional props). Everything else under `apps/fe/src/pages/settings/nodes/**` belongs to another agent (T4a) — do not edit.
- apps/fe/src/lib/format-*.ts if a bytes/rate formatter is missing (check `apps/fe/src/pages/settings/relay/relay-format.ts` first and extend it instead).
- i18n: keys under a NEW `relay.metrics.*` object in `packages/shared/src/i18n/locales/{zh_CN,en_US,ja_JP}.json`; another agent edits `nodes.*`/`relay.tenant.*` concurrently — edit only your sub-object, then `bun run build:i18n`.
- Do NOT edit apps/gateway/**, packages/api-client/** (the backend agent adds `RelayAdminApi.metrics()` there), packages/stores/**.

## Data contract
`GET /api/relay/metrics` (relay admin auth = the local logged-in user; same auth as the existing `/api/relay/status` used by `relay-status-store.ts`). Response type: `RelayMetricsResponse` in `packages/api-client/src/relay/metrics-types.ts` (already exists — read it; import the type from `@tmex/api-client` if exported there, otherwise via the relay module path the existing code uses for `RelayStatusResponse`). The backend agent is implementing it now; `RelayAdminApi.metrics()` will exist when you're done — until then call `client.get('/api/relay/metrics')` through the same client the status store uses, and switch to `metrics()` if it has landed (check `admin-api.ts` at the end). `history.samples` holds ≤60 samples at `intervalMs` (5 s) — enough for sparklines; totals carry `bytesInPerSec/bytesOutPerSec/framesInPerSec/framesOutPerSec`, `process` carries memory/cpu/eventLoop/openSockets/loadAvg, `members[]` carries per-member `rttMs/reconnects/activeStreams/bytes*PerSec/connectedAt/online`.

## Design (read /Users/konata/code/tmex-copy-guidelines.md for copy; load the `frontend-design` skill guidance mentally: this must look intentional and modern, not a generic admin template)
1. `packages/ui` primitives:
   - `Sparkline`: inline SVG, props `values: number[]`, `width/height` (default 96×28), `stroke` token class (use `--chart-1..5` tokens via `text-[color:var(--chart-1)]`-style classes or a `tone` prop mapped to chart tokens), optional filled area with low opacity, `ariaLabel`. Handles empty/NaN/constant series without division by zero. Respect `motion-reduce` (no animation needed).
   - `StatTile`: `Card size="sm"` layout — muted uppercase label, large tabular-nums value + unit, optional `sub` line (e.g. ↑ 12.3 KB/s · ↓ 4.1 KB/s), optional `sparkline` slot on the right/bottom, optional `tone` (default/success/warning/destructive) that tints the value. Tests via react-dom/server static markup (repo has no DOM test env).
2. `apps/fe/src/pages/settings/relay/relay-metrics-store.ts`: zustand-style store like `relay-status-store.ts` — poll every 5 s while the page is visible (`document.visibilityState`), stop when hidden or unmounted, keep `lastError`, expose `data`, `loading`, `refresh()`. Test with fake timers/fetch like the sibling store tests.
3. `RelayServiceMetrics` (compact, for the 本机 card): a 2×2 (mobile) / 4-across (desktop) row of StatTiles: 在线节点 (membersOnline/members), 活跃流, 吞吐 (in+out per sec with sparkline of bytes), 延迟 (median member RTT; sub: event-loop lag) — plus a second thin row: 内存 RSS, CPU %, 运行时长; a muted "打开中继控制台 →" link calling `onOpenConsole`. Loading skeletons on first load; when the request fails, a single muted line with retry (do not blank the tiles if stale data exists — show stale with a subtle "已暂停/更新失败" hint).
4. Relay tab dashboard (`relay-tab.tsx`): replace `RelayHealthCard` + `RelayTotalsCard` with: a header strip (status dot, version, uptime, tenants), a tile row (同 3 但完整: membersOnline, activeStreams, bytesIn/s, bytesOut/s, frames/s, RTT median/max, event-loop lag, RSS, heap, CPU, open sockets), a trends card with 3 larger sparklines (throughput in/out overlaid, active streams, event-loop lag) over the 5-minute history with min/max annotations, and a members table (name or short node id, online, RTT, streams, ↑/↓ rate, reconnects, connected since) sorted online-first. Keep the password / default quota / tenants cards as they are (only restyle spacing if needed to fit). Numbers via a shared formatter (`formatBytesPerSec`, `formatBytes`, `formatDuration`, `formatMs`) with tests.
5. i18n keys under `relay.metrics.*` (all three locales), copy per the guidelines (concise noun labels, no exclamation marks, no internal jargon in labels — tooltips may explain).

Verify: `cd packages/ui && bun test` (370/0 baseline), `cd apps/fe && bun test src/` (2179/0 baseline) + `bunx tsc --noEmit -p .` = 0, biome clean, complexity gate `bun scripts/complexity/gate.ts` no new violations.
