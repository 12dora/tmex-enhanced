# EX4 — root causes of current test failures (CI + local baseline)

Goal: make CI green on `main` and clear the known local failures. Diagnose (do not fix) the following, each with root cause, evidence and a concrete fix proposal.

1. CI workflow `CI` (`.github/workflows/*.yml`, job `unit-tests`, running `bun run test:unit` = `scripts/ci/unit-tests.ts`) fails on every push to main. The failing group is `apps/gateway: bun test src/relay`: all tests pass, but bun reports `2 errors` — "Unhandled error between tests": a `LinkError: relay-rst` thrown from `abort()` in `packages/shared/src/link/mux.ts:247` via `handleRst ← handleFrame ← drainIncoming ← in-memory-link send ← sendFrame ← resetStream ← reset ← abortBoth (apps/gateway/src/relay/relay-stream-router.ts:90)`. It even fails "retrying once in isolation". Find which test triggers it (likely relay-stream-router tests using `InMemoryLink` where reset on one side synchronously delivers RST to the other side, whose stream promise rejects with nobody awaiting → unhandled rejection), why it is only reported as an *unhandled error between tests* (promise rejection after the test finished / a `.catch` missing / an `onError` callback not attached), and whether it reproduces locally (`cd apps/gateway && bun test src/relay` — you may run tests, they are read-only operations). Propose the fix in production code if the rejection is genuinely unobserved there (e.g. the router should attach a catch to the stream's `closed` promise), or in the test if it's test-only.
2. `packages/panels`: 15 tests fail locally ("environmental" per previous notes). Run `cd packages/panels && bun test 2>&1 | tail -80` (read-only) and identify the failing files and the actual reason (missing DOM API in happy-dom? ResizeObserver? canvas? timing? a wasm asset path?). Decide whether they are fixable in test setup (preload / polyfills in `bunfig.toml`) or in code.
3. `apps/fe` e2e known failures (Playwright, do NOT run them): read `apps/fe/e2e/**` for `viewport-policy` (2 known failing) and `terminal-render-regressions` (2 flaky under load) and the memory note that says fe e2e had a baseline of failures; summarize what those specs assert and, from the code, why they fail / flake. Only static analysis.
4. Run (read-only) `bun run lint` at repo root and `bunx tsc --noEmit -p .` in `apps/gateway`, `packages/stores`, `packages/api-client` and report the current error counts (baseline is: gateway 1 known TS5097, stores 1, api-client 5). If any of them are trivially fixable, describe the fix.
5. Check `scripts/ci/unit-tests.ts` for the retry/isolation logic and whether there is a better way to keep "unhandled error between tests" from failing the whole matrix without hiding real bugs.

Final message = the full report.
