# R3 — backend review of commit 18a25a8a (relay metrics)
Diff file: /private/tmp/claude-501/-Users-konata-code-tmex-enhanced/f162c75c-ae5d-41f6-8245-2e3de8d399e8/scratchpad/sub/R3-diff.patch
The commit adds `GET /api/relay/metrics` (relay admin auth), a `RelayMetricsCollector` (5 s sampling, 60-sample ring, cumulative-delta rates, `unref`'d timer), server-side member RTT via ping/pong timestamps, `connectedAt`/`reconnects`, per-member active streams and live byte counters in the stream router, `LinkMux.stats()` counters, `relay_tenants.sealed_pack_updated_at` (migration 0046), and `RelayAdminApi.metrics()`.
Pay special attention to: leaks of secrets in the response; per-member counters not released on stream reset/abort paths (leaks growing forever); reconnect/RTT bookkeeping when a link is replaced while the old one is still closing; correctness of rate computation on the first sample and after counter resets; the migration (journal entry, idempotency, `rotateRoot` clearing the timestamp); whether the collector timer keeps the process alive or double-starts on runtime restart; frames/bytes counters when the production link is a `WebSocketLink` wrapping the mux (do the metrics actually see traffic in production, or only in tests using `InMemoryLink`?); admin-route auth parity with `/api/relay/status`.
