# Common rules (read fully)

You are a backend engineer working in the tmex monorepo at /Users/konata/code/tmex-enhanced-wt-tabs (Bun runtime, TypeScript, biome). Read AGENTS.md first.
- OTHER AGENTS ARE EDITING OTHER FILES IN THIS SAME WORKTREE IN PARALLEL. Only modify the files listed in "Scope" below, plus new files you create in the same directories and their tests. Never touch files outside scope; if you believe a change outside scope is required, describe it in your result file instead.
- Do NOT run any git command that changes state (no add/commit/stash/checkout/reset). The commander commits.
- Never touch the production tmex service (~/Library/Application Support/tmex, port 9883) or the tmux session named `tmex`. Tests run with NODE_ENV=test only (bun test does this via test.env).
- Verification before finishing: `cd apps/gateway && bun test <relevant test files>` (and the whole package `bun test` at the end — other agents' in-flight edits may cause unrelated transient failures — mention them, don't fix them), `bunx tsc --noEmit -p .` must not add errors (gateway baseline: 25 errors, all pre-existing), `bunx biome check --write <your files>`.
- Code style: no unnecessary comments; standard English identifiers; keep functions small (target CC ≤ 12, ≤ 60 lines); no `as never`/`any` escapes; no TODOs, no partial versions. Preserve public behavior unless the task says it is a bug.
- Every bug fix needs a regression test that fails before and passes after.
- Do not create prompt-archives/ folders or docs; write your final report to the path given in "Report" (markdown: what changed, files, bugs fixed, test/tsc results, anything left out and why).


# Task: review2-backend-fixes

Gateway baseline: 1826+ pass / 0 fail, tsc 25 errors. Fix four confirmed code-review findings (regression test first, red → green). Other agents are editing tmux-client/retention/subscription-coordinator.ts, tmux-client/snapshot-format.ts, tmux-client/external-tmux-core.ts, external/snapshot-projector.ts, api/file-http.ts, pane-emulator-create.ts — do not touch those.
1. apps/gateway/src/ws/borsh/session-state.ts (~L443 `emitResourceExhaustedGap`): the select output gate serves LEGACY clients (legacy-feed-broadcaster skips canonicalClients), but the gap is emitted as a canonical `SourceGap` event which legacy clients never decode (packages/ws-client/src/transport-message-decoder.ts has no KIND_CANONICAL_EVENT branch) — so after overflow the client never rebases and permanently misses output. Find the LEGACY rebase/gap signalling path the client already handles (grep packages/ws-client/src/transport-message-decoder.ts and packages/stores/src/tmux-event-router.ts for the event that ends in `dispatchPaneRebase(..., 'resource_exhausted')`, and the gateway side that emits it, e.g. a pane rebase / gap kind in packages/shared/src/ws-borsh/kind.ts) and emit THAT frame for legacy sessions (emit canonical SourceGap only if the session is canonical). Regression test must go through the real client decoder: encode the frame the gateway sends and assert `decodeGatewayTransportMessage` in packages/ws-client yields the rebase/gap event (you may import ws-client in a gateway test if other tests do; otherwise assert against the shared kind + schema and add a decoder test in packages/ws-client/src/transport-message-decoder.test.ts — that file is free to edit).
2. apps/gateway/src/ws/device-connection-registry.ts `handleDeviceConnect` (~L373 area): after `finalizeReconnectFailure` released observers but kept `subscribedPanes`, a manual reconnect never re-syncs, and tracked devices with zero count no longer fall back to scanning → output silently dropped. Call `host.syncLegacyPaneObservers(ws, deviceId)` after the client is added to the entry in handleDeviceConnect. Test: subscribe → finalizeReconnectFailure → connect → output still delivered (add to ws/legacy-observer-wiring.test.ts).
3. session-state.ts (~L520 `shouldAllowNotification` / `pruneNotificationThrottles`): pruning uses the entry's stored throttleSeconds, so raising the throttle from 10s to 60s lets a notification through at 31s (old code kept suppressing until 60s). Fix: don't prune the key being checked before checking it, or update the entry's throttleSeconds to the current value first. Test: 10s → 60s, at 31s still rejected.
4. apps/gateway/src/api/llm.ts: provider create/update calls `broadcastSettingsUpdate('llm')` BEFORE `await refreshModelsCache(...)`, so other clients refetch and cache an empty model list with no later broadcast. Move the broadcast after the models cache refresh (create), and after the optional refresh flow in update — one broadcast per request. Test: with a delayed models fetch, the broadcast occurs only after the list endpoint would return the final models.
Scope: apps/gateway/src/ws/borsh/session-state.ts (+session-state.test.ts), ws/device-connection-registry.ts, ws/legacy-observer-wiring.test.ts, ws/index.test.ts (append only), api/llm.ts (+llm tests), packages/ws-client/src/transport-message-decoder.test.ts (append only). Verify: `cd apps/gateway && bun test src/ws src/api/llm*` and `cd packages/ws-client && bun test`.

Report: write to prompt-archives/2026082702-code-smell-round3/sub/review2-backend-fixes/result.md
