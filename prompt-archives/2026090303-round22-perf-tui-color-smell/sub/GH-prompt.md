# TASK GH

Backend performance items T6 (mesh part) + T9 from the exploration report `/Users/konata/code/tmex-r22/prompt-archives/2026090303-round22-perf-tui-color-smell/sub/EX1-fluency-hotspots.md` (read sections T6, T9, 1.11 first).

### T6 — mesh relay decodes every frame just to read `kind`/`seq`
`apps/gateway/src/mesh/stream-replay-state.ts` ~:75 (`tryDecodeEnvelope`, used by `noteOutbound` ~:81 / `noteInbound` ~:149, called from `apps/gateway/src/mesh/forwarder.ts` ~:169 and ~:387 on every relayed frame in both directions) and `apps/gateway/src/mesh/stream-targets.ts` ~:542 do a full byte-by-byte `decodeEnvelope` where `decodeEnvelopeView` (`packages/shared/src/ws-borsh/codec.ts` ~:88) gives kind/seq/payload view in constant time (~300× on 32 KiB). Switch them, auditing every branch for payload retention: where a branch keeps `env.payload` beyond the call, copy explicitly (the report notes `noteOutbound` slices `bytes` not `env.payload`, which is safe — verify). Add a micro-bench or timing test showing the before/after on a 32 KiB frame and unit tests proving identical replay-state decisions for a recorded sequence of frames. Do NOT touch `apps/gateway/src/ws/index.ts` (another agent owns it).

### T9 — legacy backpressure terminates the socket instead of resyncing
`apps/gateway/src/ws/websocket-send-guard.ts` (~:97-98, :121-125 silently drop terminal frames once `ws.send` returned −1; ~:208-231 → :349-369 terminate the whole socket with `backpressure_gap` on drain if any frame was skipped) plus `apps/gateway/src/runtime.ts` ~:218-219 (`backpressureLimit: 1 MiB`, `closeOnBackpressureLimit: true`). The canonical path already does the right thing (`apps/gateway/src/ws/canonical/pane-stream.ts` ~:234-292 accounts dropped bytes and emits an explicit `pane_gap` so the client rebases). Make the legacy path do the equivalent: on drain after skipped frames, instead of terminating, trigger the existing legacy resync mechanism (find what the client already handles — e.g. a LIVE_RESUME / snapshot refresh / history replay request kind in `packages/shared/src/ws-borsh/*` and `packages/ws-client/src/*` — and reuse it; do not invent a new wire kind unless nothing exists, and if so explain in the result). Keep `closeOnBackpressureLimit` semantics only as the last-resort hard limit; consider raising the limit modestly if the guard's own accounting makes it redundant, but justify with numbers. Tests: skipped-frame → drain → resync message sent, socket stays open; repeated skips do not spam resyncs (one per gap); the hard limit still closes.

Files you own: `apps/gateway/src/mesh/stream-replay-state.ts`, `apps/gateway/src/mesh/stream-targets.ts`, `apps/gateway/src/mesh/forwarder.ts` (only if the call site must change), `apps/gateway/src/ws/websocket-send-guard.ts`, `apps/gateway/src/runtime.ts` (backpressure lines only), and their tests. NOT `mesh-runtime.ts`, `peer-manager*.ts`, `ws/index.ts`, `legacy-feed-broadcaster.ts`, `terminal-output-batcher.ts`.
Acceptance: `cd apps/gateway && bun test src/mesh src/ws` no fewer passes than baseline (mesh has 4–5 known flakes under full-parallel runs; rerun a failing file alone before concluding); tsc not above baseline; gate ok.
