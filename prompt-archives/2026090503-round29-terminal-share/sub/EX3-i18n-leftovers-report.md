# EX3 — 链路信息窗 i18n 缺失 + 遗留项定位（Opus explore，2026-09-05，节选）

## A. 信息窗（apps/fe/src/node/device-node-badges.tsx）
- 所有 label 已 i18n；未翻译的全是服务端/浏览器原始值：`failure.ws`、`failure.dc`（`directFailureRows` :170-177）、`ice.connectionState`/`iceConnectionState`、candidate 类型、`selectedPair`（`packages/ws-client/src/direct/ice-stats.ts:96-100`）、`ms` 单位（:72、:123）。`peerAddress` 保留原文。
- `failure.ws` 产生点：`apps/gateway/src/mesh/peer-manager.ts:983`（`all endpoints backing off (next eligible in ${secs}s)`）与 `:1021`（`raced.lastReason` = `formatWsDialFailure`，`peer-ws-race.ts:214-225`，kind 由 `classifyWsDialKind` :136-149 算出后被丢弃：timeout/open-timeout/refused/unreachable/reset/protocol/aborted/other）。`dialWsSecure` :975 无端点时什么都不记（静默洞）。
- `failure.dc` 产生点：`peer-manager.ts:1891` → `dcFailureReason` → `peer-dc-upgrade.ts:514-524`（`direct_capable=false` / `datachannel unavailable` / `Error.message` 自由文本）。已有分类器 `classifyRtcDialFailure`（`rtc/rtc-dial-breaker.ts:101-110`）只喂熔断器不进 attempt.dc。熔断冷却时 `tryDc` 返回 null 不设 dcError（:842-849）→ 无 DC 行；`MeshNode.dcBreaker`（`packages/api-client/src/auth/types.ts:227-233`，`peer-manager.ts:397`）前端从未读取。
- 方案：DTO（`packages/api-client/src/auth/types.ts:209-217`、`apps/gateway/src/mesh/peer-manager-types.ts:83`、`node-list-projection.ts:25,47`）加 `wsCode/dcCode: DirectFailureCode`、`wsParams {url?,seconds?}`、`dcParams {until?}`；`raceWsSecureEndpoints` 返回 `lastKind`；`dcFailureReason` 复用 `classifyRtcDialFailure`；前端 `directFailureRows` 按 `nodes.badge.failure.<code>` 翻译、原文兜底；可选 `nodes.badge.ice.<state>`、`nodes.badge.candidate.<type>`。
- 码表：timeout | refused | unreachable | reset | tls | handshake | revoked | untrusted | backoff | no_endpoints | ice_failed | no_candidates | dc_open_timeout | dc_closed | liveness_timeout | signal_dropped | signaling_state | rtc_unavailable | not_direct_capable | breaker_cooling | aborted | other。
- 侧栏/设备树无 via/relay 字面量。locale 差异：`nodes.hub` zh_CN 为 "Hub"（ja 已译）；`relay.admin.tenants.columns.id` ja_JP 为 "ID"。

## B. 遗留
1. KI-2 下载进度：`apps/gateway/src/system/release-download.ts:149-196 downloadVerifiedRelease`，`InflightDownload`(:31-36)/`InflightWaiter`(:25-29) 无进度字段；`downloadTarballToFile` :301-345 已在 Transform 里计 bytes(:319-325)。调用方 `system/upgrade.ts:1021`、`system/remote-upgrade-job.ts:663`（`runDownloadPhase` :287-313）。推包进度模式：`job.pushedBytes/totalBytes`（remote-upgrade-job.ts:35,61,153,356-364,470）→ `snapshotOf` :636 → `upgrade-service.ts:382-389` → `packages/shared/src/contracts/system.ts:64-73 RemoteUpgradeProgress` → 前端 `use-node-upgrade.ts:795-812 upgradePhaseText`、stall watchdog `upgrade-budget.ts:79,39`。改法：inflight 加 downloadedBytes/totalBytes/onProgress 集合，Transform 里节流上报（512 KiB 或 500 ms），content-length 取 total，`RemoteUpgradeProgress` 加 `downloadedBytes`，前端 phase==='download' 时显示字节。
2. KI-7：两个 ctl switch = `packages/shared/src/uplink/codec-hub.ts:302-413 decodeHubInner`（CC 42）与 `codec-mesh.ts:165-260 decodeMeshUplinkCtl`（CC 32），另 `encodeMeshUplinkCtl` :356-459 CC 49；合并为参数化 `decodeUplinkCtl(input,{readers,allowKeyLogRes,pendingKeyLogId})`。上帝类：`hub/uplink-server.ts` 2224 行 93 成员 → 拆 `uplink-auth-session.ts` / `hub-federation.ts` / `uplink-node-list.ts`；`mesh/peer-manager.ts` 1907 行 81 成员 → 拆 `peer-dialer.ts` / `peer-live-registry.ts`（方法清单见原报告）。
3. openAddDevice 预选 SSH：已落地（`packages/panels/src/device-management/events.ts`、`ssh-steps.tsx:19`），无需改。
4. `mesh/uplink-pool.ts:1554-1566 anyAbort` 真泄漏（三处 :724/:770/:1292 长寿 stop signal 配短寿 signal）；改用 `packages/shared/src/async/abort.ts:7-35 combineAbortSignals`（返回 `AbortSignal|undefined`），并把 `peer-ws-race.ts:199` 本地 `combineAbortSignals` 折进共享实现。
5. 盲中继节点名：`relay-metrics.ts:572` name=null，UI 已用 nodeId 前 8 位兜底（`relay-metrics-model.ts:80-85`），按设计关闭。
