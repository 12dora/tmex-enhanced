# RV1 — Code review: frontend changes (keep-alive cold subscription, mesh polling, hidden heartbeat)

You are a senior reviewer. Read-only sandbox; output the complete review as your final message (Markdown, English, < 1200 words). Cite `path:line` from the diff/code. Rank findings by severity: **blocker** (correctness/data loss/regression), **should-fix**, **nit**. For each finding give a concrete failure scenario; skip theoretical hardening, style, and anything already covered by tests unless the test is wrong. Do not propose adding defensive code for cases the architecture already rules out.

The diff to review is in `DIFF_PATH` (git diff main..HEAD limited to `packages/` and `apps/fe/src`). Context for each change:

1. **Keep-alive cold subscription** (`packages/panels/src/device-console/terminal-keep-alive.ts`, `terminal-stage.tsx`, `packages/terminal-ui/src/components/{Terminal.tsx,types.ts,hooks/usePaneSinkRegistration.ts}`): hidden keep-alive terminal instances (up to 2) used to stay in the wire pane-subscription set forever. Now after `KEEP_ALIVE_COLD_DELAY_MS` (60 s) hidden they are marked cold, `Terminal subscribe={false}` withdraws only the `mountPane` contribution (sink + Ghostty instance stay), and re-show of a cold pane is treated as non-warm so the existing cold-select path (`wantHistory:true`, `selectPaneWithSize` ordering, switch barrier) replays history. Check especially: (a) the render-time `retainKeepAlivePane` + `useReducer` bump from a timer callback — any setState-after-unmount, StrictMode double-invoke, or stale-pool issue; (b) the dep-less `useEffect` `sync` — timer churn or a case where a cooling pane never gets a timer / never gets cancelled; (c) re-show ordering: does the child's `mountPane` effect (re-subscribe) run before the parent's select dispatch, and does the gateway's `handleSubscribePanes` flush + select transaction still deliver history before live output; (d) interaction with `applyKeepAliveStreamState` (reconnect) and `retainLiveKeepAlivePanes` (pane deleted while cold); (e) whether the sink registry could start buffering (`pane-sink-registry.ts` pending path) in any window.
2. **Mesh polling** (`apps/fe/src/node/mesh-nodes.ts`, `mesh-nodes-resident.tsx`, `apps/fe/src/pages/settings/nodes/management/nodes-management.tsx`): fallback poll 30 s → 5 min; immediate throttled refresh on mesh WS connect/reconnect and on NODE_EVENT for an unknown node id (once per fallback tick), visibility refresh with 30 s stale threshold. Check: (a) `loggedIn`/`isHub`/membership staleness paths that used to be caught by the 30 s poll and are now not covered by any trigger (e.g. remote session expiry, node revoked while WS down, hub switch); (b) ref-counted `acquireMeshNodesPolling` lifecycle with the new listeners (leak on last release, double-subscribe on re-acquire); (c) the `events` option threading through `useMeshNodes` — a new object identity per render would re-acquire polling every render.
3. **Hidden heartbeat** (`packages/ws-client/src/{heartbeat-controller.ts,client.ts,index.ts}`): 5 s/10 s visible → 30 s/60 s hidden via `setCadence`; hidden→visible applies fast cadence then the existing immediate check. Check: (a) a PONG timeout armed with 60 s right before becoming visible — does the visible handler's `clearPongTimeout` + reconnect path handle it; (b) `setCadence` re-arming the interval resets the phase — could a rapid hidden/visible flapping starve PINGs; (c) any consumer that relied on `heartbeatIntervalMs` being the effective interval (e.g. RTT sampling, direct-carrier stats).

Also flag anything in the diff that changes behaviour beyond these three items.
