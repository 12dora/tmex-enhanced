# v1.0.0 提交、合并与发版计划

## 背景

当前 `vibex/main` worktree 已包含 Settings 文件根目录页面的 query cache 冲突修复和回归测试。用户要求将改动提交并合并到 `main`，随后完成 `v1.0.0` 正式发版。

## 目标

1. 确认修复和回归测试在当前 worktree 通过。
2. 对改动执行有明确范围的 commit，并在合并前完成代码审查。
3. 将提交快进/合并到 `main`，在合并结果上重新验证。
4. 按仓库现有发布脚本和版本约定完成 `v1.0.0` 发版，记录所有外部发布动作和结果。

## 注意事项

- 代码只在 `/Users/krhougs/LocalCodes/tmex-wt/vibex-main` worktree 修改；主仓只用于管理 worktree、合并和发版。
- 发版前必须检查工作区、远端状态、版本文件、CHANGELOG、构建产物和测试结果。
- 不得读取、复制、覆盖或重启 `~/Library/Application Support/tmex/` 下的生产文件或服务；不执行生产升级。
- 测试如需 tmux，使用项目测试专用 socket/session，不操作名为 `tmex` 的 session。
- 发布脚本的具体行为、是否会推送 tag 或发布 npm 包，必须先阅读仓库脚本和文档后再执行。

## 任务清单

- [ ] 检查当前 worktree、主分支、远端和未提交改动。
- [ ] 运行针对性回归测试及必要的完整测试/构建。
- [ ] 请求并完成合并前代码审查。
- [ ] 提交修复和测试改动。
- [ ] 在主仓库更新并合并 `vibex/main`。
- [ ] 在合并结果上重新运行验证。
- [ ] 阅读并执行仓库现有的 v1.0.0 版本更新和发布流程。
- [ ] 验证发布结果，并记录失败、跳过或需要用户执行的外部步骤。

## 验收标准

- `main` 包含 Settings 文件根目录页面修复和回归测试。
- 针对性测试、必要的完整测试和构建均有新鲜成功输出。
- 版本元数据统一为 `1.0.0`，CHANGELOG 与发布产物符合仓库现有约定。
- 发布动作没有触碰生产常驻服务或生产数据库。
