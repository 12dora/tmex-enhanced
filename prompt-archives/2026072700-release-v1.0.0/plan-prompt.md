# Prompt Archive

## 2026-07-27

### User

commit and merge to main, then do full release v1.0.0

### Execution notes

- 当前任务基于 `vibex/main` worktree 的设置页 query key 修复。
- 需要在提交、合并和正式发版前分别完成验证。
- 禁止触碰本机生产 tmex 服务、生产安装目录及名为 `tmex` 的 tmux session。
