# 原始需求归档

日期：2026-07-10

## 用户需求

先 commit，然后查终端包分屏相关功能退化：

1. 调整分屏大小的 split handle 不见了。
2. pane 的标题栏背景颜色都消失了。
3. 分屏拖动标题栏移动位置时，预览位置的颜色区域也不见了。

## 执行边界

- 先保留已验收的侧边栏／设备管理改动提交，再调查分屏退化。
- 本阶段先完成可复现、历史比对和根因定位；在用户明确要求修复前，不修改终端功能实现。
- 不触碰生产 tmex、默认 tmux socket 或安装版数据目录；验证仅使用当前 task 的开发服务与 `tmex-e2e` 独立 socket。

## 后续用户指令

我们好好修一下

帮我顺手做一下小修改，gutter按下拖动的颜色太抢眼，稍微改淡一点
