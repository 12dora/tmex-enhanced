# Task B2-10 — address the B2-7 review (direct-path session lifecycle, per-connection binding, control-send semantics, link upgrade/retire, dc signaling bounds, realistic tests)

Context: `sub/b2-7-result.md`, `sub/b2-9-result.md` (rtcSession now flows through attachDirect/switch/ACK — review Major 2 is done; keep it), review `sub/b2-7-review.md` — coordinator: ALL remaining items valid. Design §2 (node-session 18 h sliding / 7 d hard, revocation effects), §3 (载体切换屏障, 直连授权), §1 transport order.

1. **Direct inbound lifecycle (blocker)**: every direct-carrier inbound Borsh frame and every bulk operation re-verifies the bound `{sid, via}` through `NodeSessionStore.verify` with the same 5-min throttle as `/ws`; expired/revoked → close the whole `GatewaySession` (primary + direct + PeerConnection) and abort bulk channels; key-log effects (`revokeAllSessions`, `revokeSessionsByCredential`, `revokeSessionsVia`) and logout close direct/PC too. Tests: session revoked while direct active → next direct frame rejected, PC closed, bulk aborted.
2. **Per-connection identity**: each gateway WS/stream session gets a `connectionId` (random); `/api/rtc/authorize` takes `connectionId` from the request (the browser sends the one its `GatewayConnection` learned from HELLO response — add `connectionId` to the HELLO S2C payload? No protocol change: instead the target returns the `connectionId` in `attachStreamSession`/upgrade data and the mesh exposes `GET /api/mesh/connection` returning `{connectionId}` for the current sid+session; simpler: authorize picks the **most recent live** session for `{sid, via}` and refuses if more than one exists → 409 `MULTIPLE_CONNECTIONS` with a hint) — choose the connectionId route: registry keyed by `connectionId`, authorize body includes it, browser side obtains it via a tiny `GET /api/mesh/connection` (session + `x-tmex-connection` header set by the gateway WS HELLO response? Keep it simple and document exactly what the browser must send; report the frontend delta). Tests: two tabs same sid → each attaches to its own session.
3. `sendControl()` semantics: distinguish `queued-backpressure` (frame accepted, wait for drain, then switch — no resend) from `blocked` (not sent; wait for drain, send once); never mark a skipped frame in the guard from the probe; tests with the real `WebSocketSendGuard` + `LinkStreamCarrier`.
4. Old-carrier close cancels the pending switch: race `onDrain` vs a session/primary close notification; on close resolve, clean state, close the unused direct carrier; test triggers only close.
5. Link upgrade path: relay → try DC then ws-secure; ws-secure → try DC; a failed higher candidate must not short-circuit to the old link before trying the remaining higher candidates; `retiring` set tracked so `onRevoked`/`stop`/cert invalidation force-close active **and** retiring links; test opens a stream on the old link, upgrades, asserts the stream still works, then revoke closes both.
6. `dc:A:B` signaling: hub accepts only `from:'node'` for deterministic sessions; target caches local signals only when source == authorized `via` and target == self and an authorization exists; hard cap on inbox/map sizes; `acceptingBrowser`, owner, listener and inbox cleaned in `finally` on success/failure/timeout/close; tests including a hostile-node flood.
7. Integration tests realism: `direct-path.integration.test.ts` creates sessions through the real `/ws` upgrade path (assemble) or `acceptWsStream`, two sessions sharing a sid, and asserts wrong connectionId / uid / via / expired / revoked cannot attach; bulk uses a real transfer owned by the uid (`init` via `dispatchHttp`) and asserts wrong uid → forbidden. `mesh.integration.test.ts`: replace the 2-byte negative assertion with a longer plaintext-marker check + decrypt-verify.

File scope: `apps/gateway/src/mesh/**` (incl. rtc), `apps/gateway/src/hub/uplink-server.ts` (+test), `apps/gateway/src/ws/index.ts`, `packages/app/src/runtime/assemble.ts` (+test). Do not touch frontend. Acceptance: `cd apps/gateway && bun test` green (baseline 1802), `cd packages/app && bun test src/runtime` green, tsc ≤ 23, biome clean. Result: `prompt-archives/2026082701-hub-multinode-design/sub/b2-10-result.md` with the frontend delta for `connectionId`.
