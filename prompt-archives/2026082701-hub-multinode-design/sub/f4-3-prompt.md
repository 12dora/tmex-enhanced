# Task F4-3 — Nodes page, sidebar aggregation, node badges, enrollment / admit / revoke flows

You are a senior frontend engineer working in the git worktree `/Users/konata/code/tmex-enhanced-wt-hub` (branch `feat/hub-node`). Backend agents are concurrently editing `apps/gateway/**`, `packages/app/**`, `packages/shared/src/{auth,link}` — never touch those. **Only touch files in your scope. Never run git commands that change state. Do not run `bun install`.** Bun runtime `/Users/konata/.bun/bin/bun`; tests `cd <pkg> && bun test` (`apps/fe`: `bun test src/`), `bunx tsc --noEmit -p <pkg>`, `bunx biome check <files>` from repo root. No e2e. i18n: add keys to `packages/shared/src/i18n/locales/*.json` only (all three locales); the coordinator regenerates `resources.ts` — but you may run `bun run build:i18n` yourself so `t()` types resolve; do not hand-edit generated files. Baselines: `apps/fe` 49 tests / 0 tsc; `packages/panels` 196 / 0; `packages/stores` 119 / 1; `packages/api-client` 69 / 5.

## Context

Design: `docs/hub/2026082700-hub-node-architecture.md` §4 ("Nodes 管理页", "侧边栏聚合视图", "可见性"), §2 "node 注册（enrollment）与节点证书" steps 1 and 3 (the browser generates the enrollment key pair, signs the authorization, shows the join token, keeps a pending entry in memory + `sessionStorage`, and signs `admit-node` only for a certificate matching its pending `enroll_pk`), "撤销" (revoke = signed `revoke-node` record). Previous frontend work you build on — read their reports first: `sub/f4-2-result.md` (`NodeConnectionManager`, `NodeRuntimeBoundary`, `resolveNodeUrl`, `/n/:nodeId` routes, `host.appPath`, per-node `QueryClient`), `sub/f4-1-result.md` (`AuthApi` in `@tmex/api-client/auth/index`, `session-key-store` — `sk_sess` cannot sign records; `key-log-actions.ts` builds and signs records with root key / passkey and `appendKeyLog`; `NodeLoginButton` from `@/auth`; `useAuthMode`). Backend contracts: `sub/b2-2b-result.md` (`GET /api/mesh/nodes`, `GET /api/mesh/rtc-config`, `/mesh/ws` Borsh `NODE_EVENT`/`RTC_SIGNAL`, `POST /api/auth/keylog`, `NODE_LOGIN_REQUIRED`), `sub/b2-1-result.md` (hub management API via the hub machine's node: `GET /n/<hubNodeId>/api/hub/nodes`, `POST .../nodes/:id/rename {name}`, `POST .../nodes/:id/revoke {bytes, sig}` — a signed `revoke-node` record, `POST .../enrollments {enroll_pk, authorization, authorization_sig, exp}`; `enroll.redeemed` is pushed to the entry that created the enrollment — surfaced to the browser how? The mesh `/mesh/ws` `NODE_EVENT` stream does not yet carry it: implement the browser side to poll `GET /api/mesh/nodes` + hub `GET /api/hub/nodes` every 5 s while a pending enrollment exists and look for a node whose certificate `enroll_pk` matches; the backend will add a push later — isolate the detection in one function). Shared crypto: `@tmex/shared/auth` (`createEnrollment`, `encodeJoinToken`, `decodeCertificate`, `verifyNodeCertificate`, `encodeAdmitNodePayload`, `encodeRevokeNodePayload`, `buildKeyLogRecord`… — see `sub/b1-3a-result.md` + `sub/b1-3a-fix-result.md`). Borsh kinds: `wsBorsh.KIND_NODE_EVENT` etc. from `@tmex/shared`.

## Deliverables

1. **Nodes page** `apps/fe/src/pages/NodesPage.tsx` at route `/nodes` (add the route to `apps/fe/src/main.tsx` top-level, like `/login`): table of nodes from `GET /api/mesh/nodes` merged with hub `GET /n/<hubNodeId>/api/hub/nodes` when a hub node is reachable (the hub node = the node whose inventory/flags mark it as hub — if the contract lacks a flag, treat the node whose id equals `mode.hubNodeId` if present, else the first node with `reach !== null` that answers `/api/hub/nodes` — encapsulate in `useHubNode()`): columns online/offline, reach path badge (`lan / relay / —`), version, last heartbeat, direct-capable, login state (renders `NodeLoginButton` when not logged in), public-key fingerprint (first 16 hex of sha256 of pk). Actions: **Add node** (enrollment): re-prompt password (root) or passkey via the F4-1 primitives → `createEnrollment` → POST to hub → store pending `{enrollPk, enrollSk, authorizationBytes, authorizationSig, exp}` in memory + `sessionStorage` (never the hub) → show the join token and a copyable `npx tmex-cli hub join <hubPublicUrl> --token <token> [--name]` command; while pending, poll for the redeemed certificate; on match verify `cert_sig` with `enroll_pk`, confirm `exp` not passed, then sign `admit-node` (root/passkey, reusing the credential prompt within 5 min if F4-1 exposes such a cache; otherwise prompt again) → `POST /api/auth/keylog` → clear pending; a certificate that doesn't match pending → toast "收到未知节点证书" and ignore. Pending items whose page was reloaded appear as "待确认" rows with a Confirm button. **Rename** (hub API). **Revoke**: confirm dialog → sign `revoke-node {node_id, reason}` → `POST /api/auth/keylog` **and** hub revoke endpoint with the same `{bytes, sig}`. Actions disabled with a hint when the hub is offline. Account-security link to `/account/security`.
2. **Sidebar aggregation** (`apps/fe/src/components/page-layouts/components/sidebar-device-list.tsx` and friends, `packages/panels/src/device-tree/**` as needed): flatten devices of all nodes (`self` first) using each node's runtime from `NodeConnectionManager` (acquire lazily only for nodes that are online and logged in); each device row shows a node badge (name from `/api/mesh/nodes`), offline nodes greyed with their last known inventory (from `inventory` in the nodes list), not-logged-in nodes show `NodeLoginButton` instead of devices; links go to `/n/<id>/devices/<deviceId>` (self keeps legacy paths). Subscribe to `/mesh/ws` `NODE_EVENT` via a small `apps/fe/src/node/mesh-events.ts` (Borsh decode with `@tmex/shared` ws-borsh helpers; reconnect with backoff; `RTC_SIGNAL` frames are passed to a registered handler for Phase 3 — keep the hook) to update online/reach live. In standalone (`mode:'none'`) render exactly today's single-node sidebar (no badges, no fetches to `/api/mesh/*`).
3. **Device page header badges** (`apps/fe/src/pages/DevicePage.tsx` or the header component it uses): two badges — browser↔node path + RTT, entry↔node path (`reach`); hidden for `self`; clicking opens a popover with ICE diagnostics placeholder fed by a `useDirectDiagnostics(nodeId)` hook that reads from `packages/ws-client`'s connection (`activeCarrier` / diagnostics if exposed; else a typed stub returning `{ path: 'primary', rtt: null }` that F3-1 will fill — keep the interface in `packages/ws-client/src/direct/types.ts` and say so).
4. Tests (`bun test src/` in `apps/fe`, `bun test` in `packages/panels`): nodes merge/`useHubNode`; enrollment: pending persisted to `sessionStorage`, matching cert → `admit-node` record built and verifies with `@tmex/shared/auth`; non-matching cert ignored; expired pending refused; revoke record shape; sidebar flattening with two fake node runtimes (self + one remote, one offline) and standalone rendering unchanged; `mesh-events` decode + reconnect.

## Your file scope

`apps/fe/src/pages/Nodes*`, `apps/fe/src/node/**` (new files only; don't rewrite F4-2's existing files there beyond small additive exports), `apps/fe/src/components/page-layouts/components/sidebar-*.tsx`, `apps/fe/src/components/page-layouts/components/app-sidebar.tsx`, `apps/fe/src/pages/DevicePage.tsx` (+ header component it owns), `packages/panels/src/device-tree/**`, `packages/ws-client/src/direct/types.ts` (new), `apps/fe/src/main.tsx` (only adding the `/nodes` route line), i18n locale JSON. Nothing else.

## Result file

Chinese markdown at `prompt-archives/2026082701-hub-multinode-design/sub/f4-3-result.md`: files, public API/hooks, tests summary per package, tsc before/after, what the backend must add (e.g. `enroll.redeemed` push to the browser, hub node discovery flag), open points.
