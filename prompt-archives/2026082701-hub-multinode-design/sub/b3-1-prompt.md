# Task B3-1 — RtcPeerManager, DataChannelCarrier, DataChannelLink, browser↔node direct authorization, CARRIER_SWITCH barrier (apps/gateway/src/mesh/rtc/)

Context: design `docs/hub/2026082700-hub-node-architecture.md` §1 (peer link transport order, NAT strategy), §2 "链路身份与握手" (DataChannel path: transcript with `path:'dc'`, both DTLS fingerprints in the hellos, `remoteFingerprint()` check, no extra encryption over DTLS), §3 "直连授权", "载体抽象", "载体切换屏障", "DataChannel 消息尺寸与背压 / bulk 协议" (bulk itself is B3-2 — leave `bulk:*` channels to it but expose the PeerConnection so it can `createDataChannel`), "node 侧 RtcPeerManager". Native loader facts: `sub/c5-2-result.md` (`loadNodeDatachannel({nativeDir})` from `packages/app/src/lib/native-datachannel.ts` — the gateway cannot import from `packages/app`; copy the *interface* `NodeDatachannelModule` into `rtc/native.ts` and take the loader as an injected function `loadNative: () => Promise<NodeDatachannelModule | null>`; the assembler passes the real loader. PoC gotchas: `sendMessageBinary(Buffer.from(u8))`, `remoteFingerprint()` values have colons — normalize with `normalizeFingerprint` from `@tmex/shared/auth` before comparing; `maxMessageSize()` 262144; ICE with an empty server list times out — tests must pass `iceServers: []` with `enableIceUdpMux`? no: use `stun:stun.l.google.com:19302` only in `*.integration.ts` and host candidates for loopback unit tests as the PoC did (check `packages/app/scripts/poc/node-datachannel-loader.ts` for the exact working config). Existing mesh APIs: `sub/b2-2a-result.md` (+ `sub/b2-2a-fix-result.md` when present — a fix pass is rewriting `peer-manager.ts`/`peer-protocol.ts` right now: DO NOT edit those files; implement against their exported types and describe the one-line integration points in your report), `sub/b1-1-result.md` + `sub/b1-1-fix-result.md` (`Carrier`, `GatewaySession.attachCarrier/switchActiveCarrier`, `WebSocketServer.closeSession/handleCarrierClose`), `sub/b2-2b-result.md` (`/api/rtc/authorize` uses injected `rtc.fingerprint` / `rtc.signals`; `/mesh/ws` carries `RTC_SIGNAL` Borsh frames), Borsh kinds in `packages/shared/src/ws-borsh` (`KIND_CARRIER_SWITCH`, `KIND_CARRIER_SWITCH_ACK`, `KIND_RTC_SIGNAL`, schemas).

## Deliverables (`apps/gateway/src/mesh/rtc/`)

1. `native.ts` — `NodeDatachannelModule` interface + `PeerConnectionLike`/`DataChannelLike` minimal typings used by everything else (so unit tests can use fakes).
2. `fragmenter.ts` — 64 KiB fragmentation `[frameId u32][idx u16][total u16]` + reassembly with per-frame timeout and max in-flight frames; tests.
3. `data-channel-carrier.ts` — `DataChannelCarrier implements Carrier` over a `DataChannelLike`: `send()` → fragments → `sendMessageBinary(Buffer)`; `'backpressure'` when `bufferedAmount() > 4 MiB`; `setBufferedAmountLowThreshold(1 MiB)` + `onBufferedAmountLow` → `onDrain`; `close/terminate`; inbound reassembled frames → `onMessage(cb)`.
4. `data-channel-link.ts` — `DataChannelLink`: `ByteTransport` (from `@tmex/shared/link`) over a DataChannel so `LinkMux` can run node↔node on it (`transport: 'dc'` slot in B2-2a's `PeerTransportKind`).
5. `rtc-peer-manager.ts` — `RtcPeerManager({loadNative, iceConfigProvider: () => {stun, turn}, identity, userStore, now?})`: `available` (native loaded); `createPeerConnection(role: 'offerer'|'answerer')` with local fingerprint parsed from `localDescription().sdp`; **node↔node**: `connectToPeer(peerNodeId, signaling: {send(msg), onMessage(cb)})` — offerer = lexicographically smaller nodeId, exchange `rtc.signal` over the given signaling (peer-port WS ctl or hub ctl), hello/transcript per §2 with `path:'dc'` and both fingerprints, verify remote fingerprint == `remoteFingerprint()`, return a `DataChannelLink` ready for `LinkMux`; **browser↔node**: `authorizeBrowser({rtcSession, uid, fpBrowser}) → {nonce, fpNode}` (2-min registry; this is what `/api/rtc/authorize` calls via the `rtc.fingerprint` hook shape from B2-2b — implement that hook interface), `acceptBrowser(rtcSession, signaling)` (node is answerer; browser offers), on `sess` channel open: first frame must be `{nonce}` matching the registration and `remoteFingerprint()` must equal `fpBrowser`, else close; then `attachDirect(session: GatewaySession, carrier)`.
6. `carrier-switch.ts` — the barrier per §3: after `attachDirect`, send `CARRIER_SWITCH{epoch, to:'direct'}` on the current active carrier via the session's send path (take a `sendControl(session, kind, payload)` function injected from `WebSocketServer` — describe the required tiny addition to `ws/index.ts` in the report rather than editing it: `sendEnvelope(session, kind, payload)` is already internal; expose it), buffer inbound frames from the direct carrier until `CARRIER_SWITCH_ACK{epoch}` arrives on the old carrier, then `session.switchActiveCarrier(direct)` and flush; on direct close: switch back to primary, send `CARRIER_SWITCH{epoch+1, to:'primary'}`. Tests with fake carriers for ordering: no frame delivered out of order across the switch, ACK for a stale epoch ignored.
7. `signaling.ts` — `RtcSignalRouter` implementation for B2-2b's `rtc.signals` hook: routes `RTC_SIGNAL` from `/mesh/ws` (browser) to the target node (via a `PeerLinkProvider`-like `sendCtl(nodeId, msg)` injected) and back; validates `rtcSession` ownership `(browser session, target nodeId)`.
8. Tests: unit (fakes) for all of the above; `rtc-loopback.integration.ts` (real node-datachannel via the packages/app loader path — it is not importable from the gateway, so the integration test loads `TMEX_NATIVE_DIR` addon through a small local `require` helper mirroring C5-2's loader, skipped with a clear message when `TMEX_NATIVE_DIR` is unset) covering node↔node DataChannelLink + LinkMux round-trip, fingerprint mismatch rejection, browser-style `sess` nonce path with two PeerConnections in one process, carrier switch with a real GatewaySession + fake primary.

File scope: `apps/gateway/src/mesh/rtc/**` (new) only. Do NOT edit `peer-manager.ts`, `peer-protocol.ts`, `mesh-runtime.ts`, `mesh-routes.ts`, `ws/**`, `packages/**`. Acceptance: `cd apps/gateway && bun test src/mesh/rtc` green (integration file not auto-discovered), tsc 0 in your files, biome clean. Result: `prompt-archives/2026082701-hub-multinode-design/sub/b3-1-result.md` — with the exact integration points (assembler wiring, `ws/index.ts` sendControl exposure, `peer-manager.ts` `'dc'` slot hookup) so a follow-up can wire them.
