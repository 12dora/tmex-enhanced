# Task B2-2b-fix — address security review of the mesh HTTP surface + assembly wiring gaps

Context: files from `sub/b2-2b-result.md` (`mesh-deps/auth-routes/session-middleware/mesh-routes/forwarder/mesh-http`), assembly from `sub/b2-3-result.md` (`apps/gateway/src/mesh/mesh-runtime.ts`, `packages/app/src/runtime/assemble.ts`). Review: `sub/b2-2b-review.md` — the coordinator judged ALL items valid with these decisions. Concurrent agents: B2-2a-fix is rewriting `stream-targets.ts`/`peer-manager.ts`/`uplink-client.ts` and changing `GatewayRuntime.dispatchHttp(request, {uid, viaNodeId, renewedExpiresAt})` + `requestDispatchContext` WeakMap in `mesh/types.ts` (see `sub/b2-2a-fix-prompt.md` item 4; its result `sub/b2-2a-fix-result.md` may appear while you work — align to it); B2-1-fix owns `hub/**`; B3-1 owns `mesh/rtc/**` and will provide `RtcPeerManager.authorizeBrowser({rtcSession, uid, fpBrowser}) → {nonce, fpNode}` + an `RtcSignalRouter` that validates `rtcSession` ownership. Do not edit their files.

1. **Local `/ws` must require `tmex_s_self` in mesh mode**: in `mesh-http.ts` add `guardGatewayWebSocket(req) → Response | null` and in `assemble.ts` run it before the gateway upgrade for `/ws` (also `/n/self/ws`); if the cookie is missing/invalid: complete the upgrade and close with `4401` (design §4) — implement by upgrading with `data.kind='mesh-reject-4401'` and closing in `open`. Bind `{sid, uid, via:'self'}` to the socket data so the gateway session can be closed on logout/revoke: expose `MeshHttpRuntime.closeSocketsForUser(uid)` / `closeSocketsForSid(sid)` (keep a registry of accepted sockets with their sid) and call it from logout and from key-log effects (`revokeAllSessions`, `revokeSessionsByCredential`, `revokeSessionsVia`) — `UserKeyService.apply` returns `effects`; `mesh-runtime.ts` wires that. WS per-frame throttled renewal for local `/ws`: verify the sid every 5 min on inbound message (cheap timer check) and close 4401 on failure.
2. **Target-side dispatch chain**: `mesh-runtime.ts` passes to `PeerManager`/`acceptHttpStream` a `dispatchHttp` that: `setMeshRequestContext(req, {via: peerNodeId, auth, clientIp})` → `MeshHttpRuntime.handleRequest` (auth/mesh/hub? — hub management for cross-node: also run `hub.handleRequest` when `roles.hub`) → falls back to `gateway.dispatchHttp(req, ctx)`; if B2-2a-fix's `requestDispatchContext` exists, read `via` from it and treat it as the trusted source (drop the duplicate WeakMap in `mesh-deps.ts` and re-export the one from `types.ts`).
3. **Passkey `delegation_sig` = Borsh `PasskeyAssertion` bytes** (b64url); decode with `decodePasskeyAssertionSig` from `auth/passkey.ts`; the passkey verifier must check `storedKey.userId === user.id === delegation.uid`; add a real passkey login test using the synthetic ES256 authenticator from `auth/passkey.test.ts`.
4. **RTC authorize**: `/api/rtc/authorize` delegates entirely to the injected `rtc.fingerprint` hook with the new shape `authorizeBrowser({rtcSession, uid, via, fpBrowser}) → {nonce, fpNode} | null` (B3-1 implements; keep a fake in tests); remove the `ChallengeStore` usage there. `/mesh/ws` `RTC_SIGNAL` from browsers: force `from:'browser'`, and pass `{uid, sid}` to `rtc.signals.send` so the router can validate ownership; ignore `from:'node'` from browsers.
5. `/n/self/*` rewrite: mesh returns the rewritten `Request` to the assembler explicitly (`handleRequest` result variant `{ rewritten: Request }` or a `rewriteSelf(req) → Request | null` API); assembler dispatches that Request through guard → mesh → gateway. Guards apply to the rewritten path. `/api/mesh/nodes` and `/api/mesh/rtc-config` require a session (login page gets the node list from a new public `GET /api/auth/nodes` returning only `{id, name, online}` — minimal, no keys/inventory; `publicKey` is only needed after login, so the login page fetches `/api/mesh/nodes` with the fresh self session before fan-out; document for the frontend).
6. Login response: JSON body `{expires_at}` only (no sid); `x-tmex-set-session` is consumed by the entry forwarder (turned into `Set-Cookie` and **deleted** from the response) and by the self path (`Set-Cookie` directly, header deleted). Logout emits `x-tmex-set-session: ;0` so the forwarder clears `tmex_s_<T>`.
7. Renewal propagation: `authenticateRequest` result (incl. `renewedExpiresAt`) is attached to the request context; the assembler applies `x-tmex-session-renewed` to the gateway's response for local requests; the forwarder already refreshes Max-Age — add a test for the local path.
8. 401 augmentation: read at most 64 KiB of the upstream body then cancel; drop `content-length/content-range/etag/content-disposition` when rewriting.
9. `/mesh/ws` without session → upgrade then close `4401`; socket data carries `{sid, uid}`; closed on logout/revoke via the registry from item 1.
10. Trusted public origin: add config `TMEX_TRUST_PROXY` (boolean, default false) in `apps/gateway/src/config.ts`; when true, `x-forwarded-proto` / `x-forwarded-host` from the local Bun socket are used for `origin`, `Secure` cookie and `passkeyAvailable`; never for forwarded (`via ≠ self`) requests. Document in the report; the CLI writes it later.
11. Standalone `GET /api/auth/mode` → `{mode:'none'}`: in `assemble.ts` mount a tiny handler for that single path when standalone (no other mesh code constructed) — test that standalone otherwise behaves exactly as before.
12. Minor: inject `clientIp` from the Bun socket in `assemble.ts` (`server.requestIP(req)`) and from the peer link for forwarded logins; `/healthz` in mesh mode returns only `{status:'ok'}` without a session (full body requires `tmex_s_self`).

Each item needs a regression test. File scope: `apps/gateway/src/mesh/{mesh-deps,auth-routes,session-middleware,mesh-routes,forwarder,mesh-http,mesh-runtime}.ts` (+tests), `apps/gateway/src/config.ts` (+test) for item 10, `packages/app/src/runtime/assemble.ts` (+test). Acceptance: `cd apps/gateway && bun test src/mesh` green except files owned by B2-2a-fix/B3-1 (list any), `cd packages/app && bun test src/runtime` green, tsc ≤ 23 (gateway), biome clean. Result: `prompt-archives/2026082701-hub-multinode-design/sub/b2-2b-fix-result.md` — include the updated HTTP contract deltas for the frontend (login body, `/api/auth/nodes`, passkey sig encoding) explicitly.
