# Task F4-fix — address frontend security reviews (login/session/runtime + Nodes page)

You are a senior frontend engineer in the git worktree `/Users/konata/code/tmex-enhanced-wt-hub` (branch `feat/hub-node`). Backend agents edit `apps/gateway/**`, `packages/app/**`; an Opus agent (F3-1) edits `packages/ws-client/src/direct/**`, `packages/ws-client/src/carrier-switch.ts`, small hooks in `packages/ws-client/src/client.ts`, `apps/fe/src/node/direct-diagnostics.ts`, `apps/fe/src/node/node-runtimes.ts` — do not touch those. **Only touch files in your scope. Never run git commands that change state. Do not run `bun install`.** Bun at `/Users/konata/.bun/bin/bun`; tests `cd apps/fe && bun test src/`, `cd packages/api-client && bun test`, `cd packages/stores && bun test`; `bunx tsc --noEmit -p <pkg>`; `bunx biome check <files>` from repo root; i18n keys in `packages/shared/src/i18n/locales/*.json` then `bun run build:i18n`. Baselines: fe 101 / tsc 0; api-client 69 / 5; stores 119 / 1; panels 199 / 0.

Reviews to address — the coordinator judged ALL items valid: `sub/f4-1-review.md` (login/session/runtime) and `sub/f4-3-review.md` (Nodes page). Final backend contract: `sub/b2-2b-fix-result.md` ("前端 HTTP 契约增量") and `sub/b2-5-prompt.md` (being implemented right now; its `sub/b2-5-result.md` wins if present when you finish — re-check it): `GET /api/auth/keylog/head`, `GET /api/auth/passkeys`, `/api/auth/mode` adds `rootEpoch`/`rootPublicKey`/`hubNodeId`/`hubPublicUrl`, `/api/mesh/nodes` adds `isHub`, hub enrollment create returns `public_url`+`id`, `GET /api/hub/enrollments/:id` returns the redeemed certificate, `/mesh/ws` pushes `KIND_ENROLL_REDEEMED {enrollPk, certificate, certSig, nodeId}`, `POST /api/auth/keylog?hub=sync` returns `{ok, seq, hash, hubAck}`, login body `{expires_at}` only, `GET /api/auth/nodes` public minimal list, passkey `delegation_sig` = b64url Borsh `PasskeyAssertion`, 4401 close codes on `/ws`, `/n/:id/ws`, `/mesh/ws`.

Do all of the following, each with a regression test:

1. Contract alignment: login uses `/api/auth/nodes` before login and `/api/mesh/nodes` after `self` login for fan-out; no `sid` expected in the login body; passkey `delegation_sig` as Borsh `PasskeyAssertion` via `encodePasskeyAssertion` from `@tmex/shared/auth`; `rootEpoch` **required** in mesh mode (abort with a "protocol mismatch" error if missing — never default to 0); `keylogHead()` / `listPasskeys()` against the real endpoints; `hubPublicUrl` from mode/nodes used for the join command (never `location.origin`); `isHub` for hub-node discovery (drop the heuristics).
2. `enroll_sk` never persisted: `PendingEnrollment` stored in `sessionStorage` = `{enrollPk, authorizationBytes, authorizationSig, exp, hubEnrollmentId}` only; the join token is shown once from memory and the `enrollSk` is zeroed after the token string is built; page reload keeps the pending as "待确认" (admit only needs `enroll_pk` + the certificate). Expiry: timer-based cleanup while mounted, `created` state cleared on admit/expiry, `signAdmit` re-checks `exp` after the async head fetch.
3. Redeemed certificate delivery: subscribe to `KIND_ENROLL_REDEEMED` in `mesh-events.ts` and poll `GET /api/hub/enrollments/:id` as fallback; feed both into `offerCertificate()`.
4. Admit/revoke reliability: use `POST /api/auth/keylog?hub=sync`; clear pending only when `hubAck === true`; otherwise keep pending and show "hub 未确认，稍后重试" with a retry button; revoke uses the same single call (no separate hub revoke POST from the entry; hub-machine UI can still call the hub endpoint when `isHub` for self — keep one path).
5. Node id validation: one helper `assertNodeId(id)` in `packages/api-client/src/node-url.ts` — `self` or exactly 32 lowercase hex — used by `resolveNodeUrl`, `nodeWsUrl`, `nodeAuthPath`; test `..`, `%2e%2e`, uppercase.
6. 4401 handling: `mesh-events.ts` and the per-node gateway connection (`packages/stores/src/node-connection-manager.ts` with whatever close-code hook `createGatewayConnection` exposes — if `ws-client` lacks a close-code callback, add a minimal `onClose(code)` option in `packages/ws-client/src/connection.ts` coordinating with F3-1's edits by keeping the change additive and tiny) stop reconnecting on 4401 and dispatch `handleGlobalUnauthorized()` (self) / `handleNodeLoginRequired(nodeId)`; backoff 1–60 s with jitter, attempt reset only after a stable open (≥ 10 s or first valid frame).
7. Passkey credential choice: pick from `allowCredentials` filtered by the current origin/RP (the backend filters; if it still returns all, filter client-side by the stored `origin` from `/api/auth/passkeys`); no blind "first".
8. TOTP setup two-phase: show secret + QR → user enters a 6-digit code verified locally with `verifyTotpCode` → only then append `set-totp`.
9. Seed/root-key hygiene: `withRootSigner(password, fn)` zeroes the seed in `finally`; the remembered signer for the 5-min window is wiped on expiry/replace (timer) and after use when the window ends.
10. Interceptor classification: response hook receives the resolved URL pathname; a 401 for `/n/<id>/...` maps to that node even without the JSON code; self only for un-prefixed paths.
11. Login page: distinguish "no targets" vs "list failed"; only navigate to `next` after ≥ 1 successful login; wipe the fresh session key on total failure.
12. Minor: dispose the per-node `QueryClient` from the manager's dispose callback (touch `node-runtimes.ts` only if F3-1's file is unchanged at that moment — otherwise describe the one-liner in the report).

File scope: `apps/fe/src/auth/**`, `apps/fe/src/pages/{LoginPage,AccountSecurityPage,NodesPage}*`, `apps/fe/src/node/{enrollment,enrollment-watch,mesh-events,mesh-nodes,hub-api}*`, `packages/api-client/src/{auth/**,node-url.ts,client.ts}`, `packages/stores/src/node-connection-manager.ts`, `packages/ws-client/src/connection.ts` (additive only), i18n locale JSON. Result: `prompt-archives/2026082701-hub-multinode-design/sub/f4-fix-result.md` (item → change → test; tests/tsc per package).
