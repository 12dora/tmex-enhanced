# Task B2-3 — role assembly: MeshRuntime composition, `TMEX_ROLES` startup matrix, request order, shutdown (apps/gateway/src/mesh/mesh-runtime.ts + packages/app/src/runtime/server.ts + apps/gateway/src/runtime.ts options)

Context: design `docs/hub/2026082700-hub-node-architecture.md` §5 "角色与启动矩阵" and §3 "entry 侧路由"; Phase-0 facts in `sub/e0-3-result.md` §1 (startup chain), §7 (role matrix landing points). Building blocks and their APIs (read the reports):
- `sub/b1-3b-result.md`, `sub/b1-3c-result.md` (+ `sub/b1-3c-fix-result.md` when present): stores, `UserKeyService`, `KeyLogStore`, `NodeIdentityStore` + `ensureNodeIdentity`, `ChallengeStore`, cookies; `config.roles/hubUrl/peerPort/...`.
- `sub/b2-1-result.md`: `HubRuntime` (`handleRequest`, `attachLocalNode(link)`, `isUplinkSocket`, `handleUplink*`, `HubKeyLogSource`, `authenticate(req) → {userId, entryNodeId}`).
- `sub/b2-2a-result.md`: `UplinkClient`, `PeerManager`, `PeerServer`, `openHttpStream/openWsStream`, `KeyLogApplier`, `GatewayRuntime.dispatchHttp`, `WebSocketServer.attachStreamSession`.
- B2-2b (concurrent, `sub/b2-2b-result.md` when it appears; until then `sub/b2-2b-prompt.md` is the contract): `MeshHttpRuntime({roles, nodeId, userStore, keyLogService, challengeStore, nodeSessionStore, peers, streams, publisher, rtc?})` with `handleRequest(req, server) → Response | null`, `localUiGuard(req)`, WebSocket callbacks for `/mesh/ws` and `/n/:id/ws`; `mesh-deps.ts` declares `PeerLinkProvider`, `StreamOpener`, `KeyLogPublisher`, `RequestVia` WeakMap.
- `@tmex/shared/link`: `createInMemoryLinkPair()` for the in-process hub↔node uplink.

## Deliverables

1. **`apps/gateway/src/mesh/mesh-runtime.ts`** — `createMeshRuntime({ db, gateway: GatewayRuntime, config, hub?: HubRuntime })` → `MeshRuntime { nodeId, handleRequest(req, server), localUiGuard(req), websocket: { open, message, drain, close } dispatch for mesh sockets, start(), stop() }`. Wires: `ensureNodeIdentity`, `UserKeyService` (+ `makeVerifyPasskeyAssertion`), `KeyLogApplier` adapter (head/applyMany/list over `KeyLogStore`/`UserKeyService`), `UplinkClient` (real WS to `config.hubUrl`, or `createInMemoryLinkPair()` + `hub.attachLocalNode` when `roles.hub`), `PeerManager` (+ `PeerServer` on `config.peerPort`, with `sessionStore`, `dispatchHttp: gateway.dispatchHttp`, `wsServer: gateway.wsServer` — check the runtime's actual property name), `MeshHttpRuntime` with `peers = PeerManager`, `streams = {openHttpStream, openWsStream}`, `publisher` → `uplink.sendCtl({t:'key.log.append', ...})`. `hub` role also gets `HubKeyLogSource` implemented over `UserKeyService`/`KeyLogStore` and `authenticate(req)` over `session-middleware` (`via` = `self` for local Bun requests; for forwarded requests use the `RequestVia` WeakMap).
2. **`apps/gateway/src/runtime.ts`** — constructor options `{ roles?: TmexRoles }` only if something in gateway construction must differ by role (design table says migrations/tmux/supervisors are identical for all roles — so probably nothing; verify and report). Expose what mesh needs (`wsServer`, `db`) if not already public.
3. **`packages/app/src/runtime/server.ts`** — role matrix: parse `config.roles`; standalone → exactly today's behaviour (no mesh objects created, no auth); `node` → gateway + mesh; `hub,node` → hub + gateway + mesh (in-memory uplink); single `GatewayRuntime` (migrations once). `fetch` order: hub → mesh (`localUiGuard` first for `/api/*` when roles ≠ standalone, then `handleRequest`) → gateway → static/SPA (the SPA must also serve `/login`, `/nodes`, `/n/:id/...` deep links). Bun `websocket` handlers dispatch by `ws.data.kind` (`hub-uplink` → hub; mesh kinds → mesh; otherwise gateway). Shutdown order on SIGINT/SIGTERM: peer links → uplink → hub → gateway (add signal handlers — today there are none; `process.exit` after stop or 5 s timeout). Bind: `TMEX_BIND_HOST` unchanged; peer port separate.
4. **Tests**: `packages/app` — `server.ts` assembly with a fake `Bun.serve` capturing `fetch`/`websocket`: request order per role, standalone untouched (no `/api/auth/mode` route → falls to gateway 404 as today; if B2-2b returns `mode:'none'` for standalone, then the mesh handler must simply not be mounted in standalone — assert the mesh module is not even constructed), `hub,node` uses in-memory link and `hub.attachLocalNode` is called, shutdown order. `apps/gateway` — `createMeshRuntime` smoke test with `test-db.ts`: node role starts `PeerServer` on an ephemeral port and `UplinkClient` against a fake WS factory; `hub,node` role: in-memory uplink completes `auth.*` handshake (bootstrap a user, self-sign the node cert via `selfSignedNodeCertificate` + `signAndApply(admit-node)` so `node_certs` has the local node) and `node.list` arrives.

## File scope

`apps/gateway/src/mesh/mesh-runtime.ts` (+test), `apps/gateway/src/runtime.ts` (options/exposure only), `packages/app/src/runtime/server.ts` (+tests under `packages/app/src/runtime/`), `packages/app/src/runtime/*.ts` new helpers if needed. Do not touch other mesh files (B2-2b is writing `mesh-http.ts` etc. right now; B1-1-fix is editing `ws/**` and `managed-entry.ts`), `hub/**`, `auth/**`. If B2-2b's real exports differ from its prompt when you finish, adapt to the real ones and note it.

Acceptance: `cd packages/app && bun test` green (baseline 90 pass / 1 tsc error), `cd apps/gateway && bun test src/mesh/mesh-runtime.test.ts` green, gateway tsc ≤ 23, biome clean. Result: `prompt-archives/2026082701-hub-multinode-design/sub/b2-3-result.md` with the exact startup matrix implemented and the env variables read.
