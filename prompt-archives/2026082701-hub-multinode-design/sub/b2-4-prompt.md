# Task B2-4 — wiring leftovers + Phase-2 integration tests (hub + two nodes in one process)

Context: everything below is committed; read the reports: `sub/b2-3-result.md` + `sub/b2-3-review.md` (assembly review — items not yet fixed: LAN endpoint advertisement, standalone must not gain signal handlers, shutdown budget, idempotent stop, public in-memory uplink entry), `sub/b2-2a-fix-result.md` ("协调者必须做" 3–5), `sub/b2-2b-fix-result.md` ("协调者必须做" 3–6), `sub/b3-1-result.md` ("协调者必须接的钩子" 1–4: RtcPeerManager construction, `ws/index.ts` CARRIER_SWITCH_ACK hook, `peer-manager.ts` `'dc'` slot, `mesh/index.ts` barrel), `sub/b2-1-fix-result.md` (registerRtcSession shape), `sub/c5-1-result.md` item 4 (redeem cert persistence is done by B2-5 — not you). Design: `docs/hub/2026082700-hub-node-architecture.md` "测试策略" 集成 + 验收标准 4.

## Deliverables

1. **Wiring** (`apps/gateway/src/mesh/mesh-runtime.ts`, `apps/gateway/src/mesh/index.ts`, `apps/gateway/src/mesh/uplink-client.ts`, `apps/gateway/src/mesh/peer-manager.ts`, `apps/gateway/src/ws/index.ts`, `packages/app/src/runtime/{assemble,server}.ts`):
   - `UplinkClient.connectWithLink(link)` public API for the in-memory uplink (remove the private-member casts in mesh-runtime).
   - Advertise real endpoints: enumerate `os.networkInterfaces()` non-internal IPv4/IPv6 addresses + `TMEX_PEER_PORT` → `ws://[addr]:port/peer`; `peer_cache.endpoints_json` consumers unchanged; make PeerServer bind host explicit (`TMEX_PEER_BIND_HOST`, default dual-stack).
   - `assemble.ts`/`server.ts`: standalone path installs **no** signal handlers and constructs nothing new except the tiny `/api/auth/mode` handler; mesh roles get the shutdown orchestrator with a single cached `stopPromise` shared by restart/SIGINT/SIGTERM and a total budget of 20 s.
   - RTC: construct `RtcPeerManager` + `MeshRtcSignalRouter` in mesh-runtime per B3-1's report (native loader injected from `packages/app` via an option `loadNative`, passed from `assemble.ts` using `loadNodeDatachannel({nativeDir: installLayout.nativeDir})`; when absent → `direct_capable=false`), wire `rtc.fingerprint/signals/config` into `MeshHttpRuntime`, add the `CARRIER_SWITCH_ACK` hook in `ws/index.ts` (`onCarrierSwitchAck` callback registered by mesh-runtime), fill the `'dc'` slot in `peer-manager.ts` (node↔node: try DataChannel via `RtcPeerManager.connectToPeer` using the peer-port WS as signaling before falling back to `ws-secure`; then relay), `node.status.direct_capable` from `rtc.available`.
   - `mesh/index.ts` barrel exports for `createMeshRuntime`, `MeshHttpRuntime`, rtc.
2. **Integration tests** `apps/gateway/src/mesh/integration/mesh.integration-test.ts` (auto-discovered by `bun test`; no real network — hub + node A + node B in one process using `createInMemoryLinkPair` for both uplinks and an in-memory peer signaling/relay; use `test-db.ts` per runtime with separate DBs; bootstrap user on the hub node via `UserKeyService.bootstrapUserWithSelfAdmit` if present else `bootstrapUser` + self-admit; enroll B through the real `HubRuntime` redeem + `admit-node` record; `MeshHttpRuntime` instances with real `PeerManager` over in-memory links (add a `linkFactory` injection to `PeerManager` if needed, minimal):
   - browser-style login fan-out: root delegation → challenge/login on A (`self`) and on B via `/n/B/api/auth/login` through A's forwarder → cookies set on A's origin;
   - `GET /n/B/api/devices` through A returns B's gateway data;
   - `/n/B/ws` HELLO → DEVICE_CONNECT passthrough reaches B's `WebSocketServer` (fake device ok);
   - relay path: force A→B peer link to relay through the hub and assert bytes on the hub side are SecureChannel ciphertext (no Borsh magic visible);
   - upload cancel: start an `http` stream with a body and abort → target sees `Request.signal` aborted and temp cleanup hook called;
   - revoke B via signed `revoke-node` → B's uplink disconnected, A's peer link closed, `via=B` sessions revoked, `/n/B/*` → 503/401;
   - **compromise cases**: (a) a client holding only node A's private key + A's DB cannot obtain anything from B (`http`/`ws`/`relay` all rejected; no session forgeable); (b) a client holding the hub's entire DB cannot mint a credential B accepts and cannot make A accept a non-admitted node (forged `node.list` entry ignored; forged `key.log` record rejected); (c) hub swaps B's public key in `node.list` / tampers signaling → login to B fails (`target_pk` mismatch) and the DataChannel handshake fails (fingerprint mismatch, using fakes for the PC).
3. Tests for the wiring items (standalone no-signal-handlers, stop idempotency, endpoint enumeration with a fake `networkInterfaces`).

File scope: the files listed in item 1, `apps/gateway/src/mesh/integration/**` (new), `apps/gateway/src/mesh/peer-manager.test.ts`, `packages/app/src/runtime/*.test.ts`. Do NOT touch `auth-routes.ts`, `mesh-routes.ts`, `hub/**`, `packages/shared/src/ws-borsh/**` (B2-5 is editing those concurrently — if you need a hub-side change, describe it), `auth/**` (C5-4). Acceptance: `cd apps/gateway && bun test` green (baseline now 1704ish — report the number), `cd packages/app && bun test src/runtime` green, tsc gateway ≤ 24, biome clean. Result: `prompt-archives/2026082701-hub-multinode-design/sub/b2-4-result.md` including which of the design's 验收标准 4 sub-cases are proven by which test.
