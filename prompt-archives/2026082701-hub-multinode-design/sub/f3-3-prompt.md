# Task F3-3 — browser direct/bulk review fixes

You are a senior frontend engineer in the git worktree `/Users/konata/code/tmex-enhanced-wt-hub` (branch `feat/hub-node`). Concurrent: Opus F4-5 edits `apps/fe/src/{auth,node/enrollment*,node/node-runtimes*,pages/NodesPage*}` and `packages/stores/**`; grok agents edit `apps/gateway/**` and `packages/shared/src/ws-borsh/**` (a node-side task will append `rtcSession: string` to `CarrierSwitchSchema`/`CarrierSwitchAckSchema` — see item 1). **Only touch files in your scope. Never run git commands that change state. Do not run `bun install`.** Bun `/Users/konata/.bun/bin/bun`; `cd packages/ws-client && bun test`, `cd packages/panels && bun test`; tsc per package; biome. Baselines: ws-client 209 / tsc 0; panels 212 / 0.

Review `sub/f3-review.md` — coordinator: blocker is backend (B2-7); fix the four majors + minor:
1. Bind switch frames to the attempt: `packages/shared/src/ws-borsh/schema.ts` gets `rtcSession: string` appended to `CarrierSwitchSchema` and `CarrierSwitchAckSchema` (you MAY make this one additive schema change + its test in `packages/shared/src/ws-borsh/index.test.ts`, since the node side will be patched to fill it; keep decoding tolerant: if the node sends an empty string, treat as legacy and accept only when exactly one pending attempt exists). `attachDirect(carrier, {rtcSession})` registers the expected value; `to:'direct'` frames with a different `rtcSession` are ignored (logged); ACK echoes it. Tests: delayed stale frame from attempt A ignored after B attached.
2. `bulk-client.ts`: operation-level idle timeout (default 30 s, renewed on any valid frame/control) for put/get; on timeout send `abort`, close channel, reject with `BulkTransferError('timeout')` so the panel falls back to REST. Tests.
3. `data-channel-carrier.ts`: inbound reassembly always uses the protocol cap (64 KiB message / 65528 payload, 1 MiB frame); `sctp.maxMessageSize` only shapes outbound. Test: oversized inbound message → `chunk-too-large` → carrier closed.
4. `panels/files/bulk-transfer.ts`: download tracks received bytes against `prepare.size`; exceeding → cancel + fallback; EOF with mismatch → treat as bulk failure, clean up, re-prepare via REST. Upload already checks `done`; add the analogous size guard client-side. Tests.
5. Minor: when `signal.aborted` during cleanup, throw a standard `AbortError` (unless the original already is one).

File scope: `packages/ws-client/src/{carrier-switch.ts,client.ts,direct/**}` (+tests), `packages/panels/src/files/bulk-transfer*.ts`, `packages/shared/src/ws-borsh/schema.ts` + `index.test.ts` (item 1 only). Result: `prompt-archives/2026082701-hub-multinode-design/sub/f3-3-result.md` — state the exact schema change so the node side can mirror it.
