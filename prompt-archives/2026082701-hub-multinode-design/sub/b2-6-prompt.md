# Task B2-6 — address B2-5 review + finish production wiring of the frontend contract + hub redeem idempotency

Context: `sub/b2-5-result.md`, review `sub/b2-5-review.md` (coordinator: ALL items valid), wiring state `sub/b2-4-result.md` (`createMeshRuntime` now wires RTC, bulk, connectWithLink; read `apps/gateway/src/mesh/mesh-runtime.ts` first), CLI expectation `sub/c5-4-result.md` "协调者必须做的 hub 侧补丁" B (idempotent redeem retry). Concurrent: grok B3-1-fix edits `mesh/rtc/**` only; Opus agents edit frontend packages. Do not touch `mesh/rtc/**`, `ws/**`, `packages/app`, frontend.

1. `hub=sync` (blocker): pre-validate locally (verify + dry-run apply without persisting), send to hub via `UplinkClient.appendAndAck` with a request id, and only on a positive ack persist locally; explicit hub rejection → 409 `{code: hubError}` with nothing persisted; timeout → retry the identical record once, then query hub head: if hub head hash == this record's hash treat as acked, else 504 `{code:'HUB_TIMEOUT'}`. Local-first mode remains only when `hub=sync` is absent AND roles.hub (hub machine itself) — otherwise `hub=sync` is the default for node role. Tests for fork-avoidance (hub already has a different seq N+1).
2. Production wiring: `MeshHttpRuntime` publisher gets `publishAndAck` from `UplinkClient.appendAndAck` (required for node role); `HubRuntime` config gets the hub's own `nodeId` from `node_identity` (mesh-runtime creates identity before hub — reorder if needed) and `hubPublicUrl` reaches `MeshHttpRuntime`; `UplinkClient.onEnrollRedeemed` → `forwardEnrollRedeemed`. Integration test through `createMeshRuntime` (hub,node) proving: `node.list.hub` present, `/api/auth/mode.hubNodeId` set, a redeem push reaches a `/mesh/ws` socket.
3. Targeted delivery: enrollment creation stores the creator's session sid (inside `authorizationJson` alongside `entry_node_id`); hub's `enroll.redeemed` carries `entry_sid`; the entry forwards `ENROLL_REDEEMED` only to `/mesh/ws` sockets whose `ws.data.sid` matches (fallback: none — polling via `GET /api/hub/enrollments/:id` covers the rest). Test: two browser sockets, only the creator receives it.
4. Hub ack ordering: persist → ack immediately → then effects/broadcast; broadcast/effects made idempotent and re-run on identical-record retry; a failing `send()` to a dead uplink never blocks or throws out of the append path (test).
5. Bounds on node-side uplink codec for the new messages (mirror hub limits: total bytes, string lengths; `enroll_pk` 32 B, `cert_sig` 64 B, `node_id` 32-hex); `EnrollRedeemedSchema` uses `b.bytes(32)` / `b.bytes(64)` and caps certificate length; tests.
6. Hub redeem idempotency (C5-4 patch B): if the token is already used and the presented certificate+sig equal the stored `certificate_b64`/`cert_sig_b64`, return the same success payload instead of `reused`; different cert → `reused`. Test.

File scope: `apps/gateway/src/mesh/{auth-routes,mesh-routes,mesh-http,mesh-runtime,uplink-protocol,uplink-client,mesh-deps}.ts` (+tests), `apps/gateway/src/mesh/integration/**` (add a file), `apps/gateway/src/hub/**`, `packages/shared/src/ws-borsh/**` (+tests). Acceptance: `cd apps/gateway && bun test src/mesh src/hub` green except `mesh/rtc/**` (B3-1-fix in flight — report), shared green, tsc gateway ≤ 24 outside rtc, biome clean. Result: `prompt-archives/2026082701-hub-multinode-design/sub/b2-6-result.md` with the final contract deltas (keylog sync semantics for the frontend).
