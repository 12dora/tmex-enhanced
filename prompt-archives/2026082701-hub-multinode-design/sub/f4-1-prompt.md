# Task F4-1 — login, session handling and account security in the frontend

You are a senior frontend engineer working in the git worktree `/Users/konata/code/tmex-enhanced-wt-hub` (branch `feat/hub-node`). Other agents are editing OTHER files concurrently: backend `apps/gateway/**`, `packages/shared/src/{auth,link,ws-borsh}`; frontend agent F4-2 owns routing / `NodeConnectionManager` / `ApiClient` URL resolution (`packages/api-client/src/node-url.ts`, `packages/stores/**`, `apps/fe/src/main.tsx`, `packages/ws-client/**`). **Only touch files in your scope. Never run git commands that change state. Do not run `bun install`.** Bun runtime at `/Users/konata/.bun/bin/bun`; tests `cd <pkg> && bun test` (`apps/fe`: `bun test src/`), `bunx tsc --noEmit -p <pkg>`, `bunx biome check <files>` from repo root. No e2e. i18n: add keys to `packages/shared/src/i18n/locales/*.json` only (all locales present there; the coordinator regenerates `resources.ts`). Baselines: `packages/api-client` 34 tests / 5 tsc errors; `apps/fe` 9 / 0; new files tsc-clean.

## Context

Design: `docs/hub/2026082700-hub-node-architecture.md` §2 ("用户密钥" — root key, passkey, browser session key `sk_sess` + `delegation`, TOTP `k_totp`; "登录" — the 4-step protocol and "登录页体验"; "node 注册" only to know that `sk_sess` may never sign persistent records) and §4 "身份与入口". The shared crypto/encoding module is `packages/shared/src/auth/` — import from `@tmex/shared/auth` (browser-safe: hash-wasm argon2id + noble; no `crypto.subtle` needed for password login). Its API is documented in `prompt-archives/2026082701-hub-multinode-design/sub/b1-3a-result.md` (plus `b1-3a-fix-result.md` if it exists — a security fix pass is in flight: `verifyDelegationTimes`, `PasskeyAssertion` Borsh encoding for passkey-signed records, `Authorization` gained `signer`/`credential_id`; re-check `packages/shared/src/auth/index.ts` before finishing). `@simplewebauthn/browser` is NOT installed yet — implement passkey UI against a thin adapter `packages/api-client/src/auth/webauthn.ts` that wraps `navigator.credentials.create/get` with the JSON ↔ ArrayBuffer conversions yourself (base64url), so no dependency is needed.

Backend HTTP contract (being built now by B2-2b; if `sub/b2-2b-result.md` exists when you start, it wins):
- `GET /api/auth/mode → {mode:'none'|'mesh', nodeId, username, kdfParams:{salt(b64url), memory_kib, iterations, parallelism}, passkeysForThisOrigin, passkeyAvailable}`
- `POST /n/:T/api/auth/challenge {uid} → {challenge_id, nonce(b64url), nodePk(b64url)}`
- `POST /n/:T/api/auth/login {login(b64url borsh), sig(b64url), delegation(b64url borsh), delegation_sig(b64url), totp?:{code, k_totp(b64url)}} → 200 {sid, expires_at}` (cookie `tmex_s_<T>` is set by the entry) | 401 `{code}` | 429
- `POST /n/:T/api/auth/logout`
- `POST /api/auth/passkey/register/options` → `PublicKeyCredentialCreationOptionsJSON`; `POST /api/auth/passkey/register/verify {response}` → add-passkey payload fields; `POST /api/auth/passkey/login/options {uid, delegation(b64url)}` → `PublicKeyCredentialRequestOptionsJSON`
- `POST /api/auth/keylog {bytes(b64url), sig(b64url)}` → 200 | 409 `{code:'KEY_LOG_FORK'}`
- `GET /api/mesh/nodes → {nodes:[{id, name, publicKey(b64url), online, reach, version, direct_capable, inventory, loggedIn}]}`
- Any request → 401 means "not logged in on this node"; a forwarded `/n/:id/*` 401 carries `{code:'NODE_LOGIN_REQUIRED', nodeId}`; WS close code 4401 likewise.

## Deliverables

1. `packages/api-client/src/auth/` — typed client for the endpoints above (`AuthApi`), plus `session-interceptor.ts`: an `ApiClient` response hook that on 401 dispatches an `auth:required {nodeId}` event (global 401 on `self` → navigate to `/login?next=`; `NODE_LOGIN_REQUIRED` → event only, no navigation). Check how `ApiClient.fetch` is structured and add the hook minimally (`packages/api-client/src/client.ts` is shared with F4-2's `node-url.ts` work — only add the interceptor; do not touch URL logic).
2. `apps/fe/src/auth/session-key-store.ts` — in-memory holder for `sk_sess`/`delegation` (never persisted), `loginToNode(nodeId)` performing steps 1–3 with `@tmex/shared/auth` (`buildLogin/signLogin`, pk check against `/api/mesh/nodes`), fan-out `loginToAllReachable()`, `k_totp` derivation from the seed only when `delegation.method === 'root'` and the user has TOTP (mode response tells you), and immediate wiping of the seed after deriving root key + `k_totp`.
3. `apps/fe/src/pages/LoginPage.tsx` (+ route `/login` registered in whatever route table F4-2 leaves — coordinate by exporting the page and a `loginRoute` object; if `main.tsx` is untouched when you finish, tell the coordinator the one-line change needed instead of editing it): username + password (+ TOTP field shown when required), "Use passkey" button when `passkeyAvailable`, "Register a passkey for this entry" link to the security page, per-node progress list during fan-out, error display per node. Hidden entirely when `mode === 'none'`.
4. `apps/fe/src/pages/AccountSecurityPage.tsx` (or a section in Settings if that fits the existing layout better — pick one and say why): change password (warns that all passkeys and TOTP will be reset; generates `rotate-root` record signed by the *old* root key → `POST /api/auth/keylog`), set / clear TOTP (generate secret, show otpauth URI + QR — use an existing QR lib only if already in deps, otherwise render the URI text and a `<canvas>` QR with a small inline implementation), register / remove passkey (registration ceremony → build `add-passkey` record signed by root key or by another passkey via `PasskeyAssertion`), each action re-prompting for the password (root key) or a passkey since `sk_sess` cannot sign records.
5. "Login to this node" affordance: a small `NodeLoginButton` component (`apps/fe/src/auth/NodeLoginButton.tsx`) that F4-3 will place in the sidebar; it calls `loginToNode(nodeId)` automatically when `sk_sess` is present and otherwise opens the login page with `?node=`.
6. Tests (`bun test src/` in `apps/fe`, `bun test` in `packages/api-client`): interceptor 401 vs `NODE_LOGIN_REQUIRED` behaviour; `loginToNode` against a mocked fetch performing the real crypto (delegation by a test root key, login signature verifies with `verifyLogin` from `@tmex/shared/auth`); TOTP inclusion only for `method=root`; pk mismatch aborts; `rotate-root` record built by the security page verifies with the shared verifier; LoginPage renders nothing in `mode:'none'`.

## Your file scope

`packages/api-client/src/auth/**` (new), one minimal hook in `packages/api-client/src/client.ts`, `apps/fe/src/auth/**` (new), `apps/fe/src/pages/LoginPage*`, `apps/fe/src/pages/AccountSecurity*`, i18n locale JSON. Nothing else.

## Result file

Chinese markdown at `prompt-archives/2026082701-hub-multinode-design/sub/f4-1-result.md`: files, public API, tests summary lines, tsc counts before/after, open points, anything the coordinator must wire (routes, i18n build).
