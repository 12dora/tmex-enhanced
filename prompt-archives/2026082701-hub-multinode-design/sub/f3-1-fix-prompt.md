# Task F3-1-fix — address review of the browser DirectCarrierController

You are a senior frontend engineer in the git worktree `/Users/konata/code/tmex-enhanced-wt-hub` (branch `feat/hub-node`). Concurrent agents: Opus F3-2 edits `packages/ws-client/src/direct/bulk-client*.ts`, `packages/ws-client/src/direct/index.ts` (additive) and `packages/panels/src/files/**`; Opus F4-fix edits `apps/fe/src/{auth,pages}/**`, `apps/fe/src/node/{enrollment*,mesh-events,mesh-nodes,hub-api}.ts`, `packages/api-client/**`, `packages/stores/src/node-connection-manager.ts` and may add a tiny `onClose(code)` option in `packages/ws-client/src/connection.ts`; grok B3-1-fix edits `apps/gateway/src/mesh/rtc/**`. **Only touch files in your scope. Never run git commands that change state. Do not run `bun install`.** Bun `/Users/konata/.bun/bin/bun`; `cd packages/ws-client && bun test`; `bunx tsc --noEmit -p packages/ws-client`; `bunx biome check <files>`. Baseline ws-client 140 / tsc 0.

Your previous work: `sub/f3-1-result.md`. Review: `sub/f3-1-review.md` — the coordinator judged ALL items valid. Node-side counterpart changes in flight (`sub/b3-1-fix-prompt.md`, result `sub/b3-1-fix-result.md` when present): message cap = 64 KiB **including** the 8-byte header (payload 65528), effective payload `min(65528, maxMessageSize - 8)`; reassembled frame ≤ 1 MiB; node now switches its outbound to direct immediately after sending `CARRIER_SWITCH{to:'direct'}` (so frames after the switch frame arrive on direct — your buffering already handles that).

Fix each with a regression test:

1. Fingerprint (blocker): parse SDP per RFC 8122 — media-level `a=fingerprint` under the `m=application` section overrides session-level; require exactly one effective sha-256 fingerprint for that section (conflicting/extra → reject the attempt); apply the same parser to the local SDP; compare normalized against `fp_node`.
2. Attempt lifecycle: create the `Attempt` (generation id, `AbortController`, its own fresh `rtcSession`) before any `await`; every callback/catch/teardown checks generation; replaced attempts always close their PeerConnection; fetch/authorize use the attempt's signal.
3. Resume hook wired for real: `GatewayConnection` exposes `setResumeSubscribedPanes(fn)`; `apps/fe/src/node/node-runtimes.ts` (only this file in apps/fe) wires it to the pane subscription manager (find the existing subscription/resume logic in `packages/ws-client` — re-send subscriptions + request history/screen) and shows the design's "直连已断开，最近输入可能未送达" toast via the runtime's notification sink.
4. Barrier phases `primary | pending-direct | direct | pending-primary`: buffer only in `pending-direct`; flush only on the matching `to:'direct'` epoch; after `to:'primary'` or direct close discard direct buffer and trigger resume; never flush on close.
5. Buffer cap: ≤ 1 MiB total bytes / 64 frames in `pending-direct`; overflow → abort the direct attempt, keep primary, resume.
6. Fragmenter bounds both directions: fragment payload ≤ 65528, frame ≤ 1 MiB, `total ≤ 16` (1 MiB / 65528 → 17 max? compute exactly: ceil(1048576/65528) = 17 — use that), per-frame and global cumulative byte tracking; violation closes the direct carrier.
7. Carrier backpressure: check `bufferedAmount` before starting a frame; queue whole frames; resume on `bufferedamountlow`; mid-frame send exception → close the carrier (never a recoverable half-frame); `CarrierSwitchBarrier.send()` treats `backpressure` as "queued, pause" and the barrier's caller (`client.ts`) must respect it the way it does for the primary WS.
8. ICE signaling ordering: queue local candidates until authorize + offer sent; queue remote candidates until `setRemoteDescription` resolves; process signaling serially per attempt.
9. Signaling transport: `send()` returns `boolean | Promise<boolean>`; expose `onReady(cb)`; controller queues signals for the current attempt while not ready and starts attempts only when ready; mesh WS recovery resets the direct backoff and triggers a retry (use whatever `MeshEventSource` exposes; if it lacks a ready/state subscription, add a minimal one — `apps/fe/src/node/mesh-events.ts` is F4-fix's file: describe the needed addition in the report instead of editing it, and code against an optional `onReady` on the transport interface).
10. Active only after the switch: keep `connecting` + timeout after channel open; set `active` (and reset attempts, start stats) only on `onCarrierChange('direct')` after the ACK was sent.
11. Network changes: handle `iceConnectionState === 'disconnected'` with a 5 s grace then fall back to primary and start a fresh attempt; listen to `navigator.connection?.addEventListener('change')` (debounced) in addition to `online`.
12. Minor: ice-stats selection order `selectedCandidatePairId → nominated&&succeeded → selected → any succeeded`; publish `route` (lan/v6/v4-p2p/turn/relay) in `DirectDiagnostics` separately from `path`.

File scope: `packages/ws-client/src/direct/**` except `bulk-client*.ts`, `packages/ws-client/src/carrier-switch.ts`, `packages/ws-client/src/client.ts` (+tests), `apps/fe/src/node/node-runtimes.ts` (+ its test). Result: `prompt-archives/2026082701-hub-multinode-design/sub/f3-1-fix-result.md` (item → change → test; anything needed from F4-fix's files).
