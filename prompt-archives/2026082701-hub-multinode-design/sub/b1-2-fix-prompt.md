# Task B1-2-fix — address code review of packages/shared/src/link/

Context: you are fixing the link layer you can read in `packages/shared/src/link/**` (spec: `docs/hub/2026082700-hub-node-architecture.md` §3 "帧格式" and §2 "链路身份与握手" step 3; original task: `prompt-archives/2026082701-hub-multinode-design/sub/b1-2-prompt.md`, its report `b1-2-result.md`). A reviewer found the issues in `prompt-archives/2026082701-hub-multinode-design/sub/b1-2-review.md`. The coordinator has judged ALL of them valid; fix every one, each with a regression test that fails before and passes after:

1. SecureChannelLink: serialize the whole "assign counter → encrypt → inner.send" pipeline through one link-level send queue so wire order == counter order; any encrypt/send failure closes the channel and rejects the rest of the queue.
2. SecureChannelLink AAD = the **wire** header actually sent (`len` = ciphertext+tag length). Decrypt uses the received wire header as AAD, then rebuilds the plaintext header. Update the test vectors accordingly and document the wire format in the module header comment (this is the interop contract).
3. LinkStream.end(): enqueue END behind all previously issued writes on the same send chain; `end()` returns `Promise<void>`; new writes after end() reject immediately.
4. WINDOW validation: track per-stream outstanding (unacked) bytes; accept only `0 < delta <= outstanding`; anything else (including WINDOW on ctl or on a stream with zero outstanding) is a protocol error → close link. Global `unacked` decremented by the same delta only.
5. RST (local or remote) and link close release the stream's remaining outstanding credit from the global counter; late WINDOW for a terminated stream is ignored (not double-counted).
6. WebSocketLink: link-level send queue. Bun server socket: `send()` returning `-1` → pause until the adapter's `drain` hook; `0` → close the LinkSession with an error. Client WebSocket: throttle on `bufferedAmount` (pause above 4 MiB, resume when below 1 MiB via polling with setTimeout or the `drain` hook when available). Pre-open backlog goes through the same queue.
7. Remote OPEN must use the opposite parity of the local role and a strictly increasing id; local allocation must never overwrite an existing id (protocol error instead).
8. Split `WebSocketLike` into two explicit adapters: `createClientWebSocketTransport(ws: WebSocket)` (standard DOM/Bun client `WebSocket`, `send(): void`, event listeners) and `ServerSocketAdapter` interface `{ send(bytes): number; close(code?, reason?); onMessage(cb); onClose(cb); onDrain(cb) }` for wrapping a Bun `ServerWebSocket` outside this package. Both must type-check against real `WebSocket` / a structural fake; add a compile-time test using the `WebSocket` global type.
9. FrameDecoder: keep a chunk list + cursor; only concatenate once when a full frame is available. Test with a 1 MiB frame delivered in 1-byte chunks (must finish well under a second) and "3 frames + half a frame" coalesced input.

Keep the public API otherwise unchanged (other agents already consume `LinkMux`, `LinkSession`, `LinkStream`, `SecureChannelLink`, `secureChannelDirections`, `byteTransportFromStream`, `deriveSecureChannelKeys`, `x25519SharedSecret`, `createInMemoryLinkPair`). If you must rename `WebSocketLink`'s constructor input, keep a `WebSocketLink` class taking either adapter and say so in the report.

File scope: `packages/shared/src/link/**` only. Acceptance: `cd packages/shared && bun test` green, tsc 0, biome clean. Result file: `prompt-archives/2026082701-hub-multinode-design/sub/b1-2-fix-result.md` (list each review item → what changed → test name).
