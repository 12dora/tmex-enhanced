# Task F3-1 — browser DirectCarrierController (packages/ws-client/src/direct/)

You are a senior frontend engineer working in the git worktree `/Users/konata/code/tmex-enhanced-wt-hub` (branch `feat/hub-node`). Backend agents are concurrently editing `apps/gateway/**` and `packages/app/**` — never touch those. **Only touch files in your scope. Never run git commands that change state. Do not run `bun install`.** Bun runtime `/Users/konata/.bun/bin/bun`; tests `cd packages/ws-client && bun test`, `bunx tsc --noEmit -p packages/ws-client`, `bunx biome check <files>` from repo root. Baselines: `packages/ws-client` 75 tests / 0 tsc; `apps/fe` 101 / 0.

## Context

Design: `docs/hub/2026082700-hub-node-architecture.md` §3 "直连授权" (browser creates `RTCPeerConnection`, `createOffer` + `setLocalDescription`, parses its DTLS fingerprint from `localDescription.sdp`, `POST /n/:T/api/rtc/authorize {rtcSession, fp_browser}` → `{nonce, fp_node}`; browser verifies the remote SDP fingerprint equals `fp_node` else abandons; `sess` channel first frame `{nonce}`), "载体切换屏障" (the 4-step `CARRIER_SWITCH` / `CARRIER_SWITCH_ACK` protocol — the browser buffers frames arriving on the direct channel until it has received `CARRIER_SWITCH{epoch,to:'direct'}` on the current active carrier, then flushes, makes direct the active receive source, sends `CARRIER_SWITCH_ACK{epoch}` on the OLD carrier, and only then sends outbound on direct; on `CARRIER_SWITCH{epoch+1,to:'primary'}` it switches back and triggers a resume for subscribed panes; primary close ends everything), "DataChannel 消息尺寸与背压" (64 KiB fragments `[frameId u32][idx u16][total u16]`, high-water 4 MiB, `bufferedAmountLowThreshold` 1 MiB), §4 "连接层" and "可见性". Signaling: `/mesh/ws` carries Borsh `RTC_SIGNAL {rtcSession, from:'browser'|'node', to: nodeId, sdp?, candidate?}` — see `packages/shared/src/ws-borsh/schema.ts` (`RtcSignalSchema`, `CarrierSwitchSchema`, `CarrierSwitchAckSchema`, kinds `KIND_RTC_SIGNAL`, `KIND_CARRIER_SWITCH`, `KIND_CARRIER_SWITCH_ACK`, enums `RTC_SIGNAL_FROM_BROWSER`, `CARRIER_SWITCH_TO_DIRECT/PRIMARY`). The browser subscribes to `/mesh/ws` already in `apps/fe/src/node/mesh-events.ts` (F4-3) which exposes an `RTC_SIGNAL` hook — read it and design the controller to plug into such a signaling transport via an injected interface `{ send(signal), onSignal(cb) }` (no direct dependency on apps/fe). ICE config: `GET /api/mesh/rtc-config → {stun, turn}`. Node id for `self` is never direct. The existing connection layer is `packages/ws-client/src/*` (`createGatewayConnection`, state machine, envelope codec — read `client.ts`, `state-machine.ts`, and how frames are sent/received); F4-3 created `packages/ws-client/src/direct/types.ts` with the diagnostics contract — implement it.

## Deliverables (`packages/ws-client/src/direct/`)

1. `fragmenter.ts` — fragment/reassemble (same header as the node side), tests.
2. `data-channel-carrier.ts` — browser `RTCDataChannel` wrapper: `send(bytes) → 'sent'|'backpressure'|'closed'` (bufferedAmount > 4 MiB → backpressure; `bufferedamountlow` → drain), `onMessage`, `close`.
3. `direct-carrier-controller.ts` — `DirectCarrierController({ nodeId, apiClient (for `/api/rtc/authorize` and `/api/mesh/rtc-config` via the node-prefixed client), signaling, connection: GatewayConnectionLike, rtcFactory? })`: lifecycle `start()` → create PC (offerer), `sess` channel (ordered, reliable), gather local fingerprint, authorize, send offer + candidates as `RTC_SIGNAL`, apply answer/candidates, verify remote fingerprint, on channel open send `{nonce}` first frame (JSON UTF-8), then hand the carrier to the connection's switch barrier; `stop()`; retry with backoff (1 s → 30 s, max 5 attempts, then give up until `retry()`), network change (`online` event) → restart; expose `state: 'idle'|'connecting'|'active'|'failed'`, `path: 'lan'|'v6'|'v4-p2p'|'turn'|'relay'|null` derived from `getStats()` (selected candidate pair types/protocol), `rtt`, and `diagnostics()` per `direct/types.ts`.
4. Switch barrier in the connection (`packages/ws-client/src/carrier-switch.ts` + minimal hooks in `client.ts`): the `GatewayConnection` gains `attachDirectCarrier(carrier)`, `activeCarrier: 'primary'|'direct'`, `onCarrierChange(cb)`; inbound demux handles `CARRIER_SWITCH`/`CARRIER_SWITCH_ACK` exactly per design, buffering direct-side frames until the switch frame is seen on the active carrier; outbound routes through the active carrier; direct close → back to primary + a `resumeSubscribedPanes()` callback hook; primary close → direct closed.
5. Tests (`bun test` in `packages/ws-client`): fragmenter; carrier backpressure; controller happy path with a fake `RTCPeerConnection`/`RTCDataChannel` (fake SDP with `a=fingerprint`, fake stats), fingerprint mismatch abandons, nonce first frame, retry/backoff, path derivation from stats; switch barrier ordering with two fake carriers (no reordering across the switch; stale-epoch ACK ignored; switch back triggers resume; primary close closes direct).
6. `apps/fe` wiring: `apps/fe/src/node/direct-diagnostics.ts` (F4-3's stub) reads from the controller; create the controller per non-self node runtime in `apps/fe/src/node/node-runtimes.ts` (or the boundary) using `mesh-events` as signaling; add a test in `apps/fe` that a non-self node runtime starts a controller and `self` does not.

## Your file scope

`packages/ws-client/src/direct/**`, `packages/ws-client/src/carrier-switch.ts` (new), minimal additive hooks in `packages/ws-client/src/client.ts` (+ its tests), `apps/fe/src/node/direct-diagnostics.ts`, `apps/fe/src/node/node-runtimes.ts` (additive), `apps/fe/src/node/*.test.ts` new. i18n keys if any (locale JSON only). Nothing else.

## Result file

Chinese markdown at `prompt-archives/2026082701-hub-multinode-design/sub/f3-1-result.md`: files, public API, tests per package, tsc before/after, what the node side (B3-1) must match (first-frame format, fragment header, epoch handling), open points.
