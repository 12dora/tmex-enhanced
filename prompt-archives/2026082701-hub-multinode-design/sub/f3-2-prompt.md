# Task F3-2 — browser bulk file transfer over the direct PeerConnection (ws-client direct + files panel)

You are a senior frontend engineer in the git worktree `/Users/konata/code/tmex-enhanced-wt-hub` (branch `feat/hub-node`). Concurrent agents: Opus F4-fix edits `apps/fe/src/auth/**`, `apps/fe/src/pages/*`, `apps/fe/src/node/{enrollment*,mesh-events,mesh-nodes,hub-api}.ts`, `packages/api-client/src/{auth,node-url,client}.ts`, `packages/stores/src/node-connection-manager.ts`, `packages/ws-client/src/connection.ts`; backend agents edit `apps/gateway/**`. **Only touch files in your scope. Never run git commands that change state. Do not run `bun install`.** Bun `/Users/konata/.bun/bin/bun`; tests `cd packages/ws-client && bun test`, `cd packages/panels && bun test`, `cd apps/fe && bun test src/`; `bunx tsc --noEmit -p <pkg>`; `bunx biome check <files>`. Baselines: ws-client 140 / tsc 0; panels 199 / 0; fe tsc 0 (fe tests are temporarily red from F4-fix's in-progress work — only assert your own new tests).

Context: design `docs/hub/2026082700-hub-node-architecture.md` §3 "DataChannel 消息尺寸与背压 / bulk 协议" and §4 "连接层" (bulk when available else REST). Node side is done: `sub/b3-2-result.md` (exact protocol: channel label `bulk:<transferId>`, first message JSON `{op:'put', transferId, size}` → whole 64 KiB binary messages (no fragment header) → `{op:'done'}` → node `{ok:true}|{ok:false, code}`; download `{op:'get'}` → 64 KiB frames → `{op:'eof'}`; `{op:'abort'}`; 30 s idle timeout; control vs data heuristic). Browser direct layer: `sub/f3-1-result.md` (`DirectCarrierController` owns the `RTCPeerConnection`; expose a way to `createDataChannel(label)` on the active authorized PC and to know `state === 'active'`). Existing REST upload/download client: find it in `packages/panels/src/files/**` and `packages/api-client/src/**` (upload = `init` → PUT chunks → `commit`; download = `prepare` → HTTP stream) and read it fully before changing anything.

## Deliverables

1. `packages/ws-client/src/direct/bulk-client.ts`: `BulkClient(controller)` with `upload({transferId, size, source: Blob|ReadableStream, signal, onProgress}) → Promise<{ok:true}|{ok:false, code}>` and `download({transferId, signal, onProgress}) → ReadableStream<Uint8Array>`; honors `bufferedAmount` backpressure (high 4 MiB / low 1 MiB), sends exactly 64 KiB messages (last one shorter), `abort` on signal/close, resolves the node's `{ok}`; `isAvailable()` = controller active. Unit tests with fake data channels (progress, backpressure pause/resume, abort mid-way, node `{ok:false}` → rejects, download eof, oversize guard).
2. Files panel integration (`packages/panels/src/files/**`, minimal): after REST `init`, if the node runtime's connection has an active direct carrier and the node is not `self`, run the bulk upload then REST `commit`; on any bulk failure, fall back to the existing REST chunk PUT path for the whole transfer (v1 retry semantics) — the fallback must not double-commit. Download: after `prepare`, try `bulk.download` and stream it into the existing save/preview path; on failure fall back to the HTTP stream. Progress UI unchanged. Add a small path badge/tooltip "direct" vs "relay" on the transfer progress if the panel has one (skip if none).
3. Tests in `packages/panels`: upload chooses bulk when available and falls back to REST on failure without double commit; download fallback; self never uses bulk.

File scope: `packages/ws-client/src/direct/bulk-client.ts` (+test), additive export in `packages/ws-client/src/direct/index.ts` or `src/index.ts` (create `direct/index.ts` if missing), `packages/panels/src/files/**`, i18n locale JSON if needed. Nothing else. Result: `prompt-archives/2026082701-hub-multinode-design/sub/f3-2-result.md`.
