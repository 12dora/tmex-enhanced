# 第七轮 prompt 存档

## 2026-08-31 初始 prompt（用户）

继续开发, 请你分批commit并在最后push替换本机tmex, Think in English, Send English prompt.
任务：
1. 对本项目进行性能热点分析并深度调优, 循环进行分析, 直到你认为所有高价值点已被修复, 或总轮数超过3轮
2. 扫描并修复本项目的代码坏味道, 例如高圈复杂度, 巨大函数, 巨大文件等), 循环进行, 直到你认为所有高价值点已被修复, 或总轮数超过3轮(你需要等1的完毕后再进行这一项)
3. 顺便修复其中遇到的bug

注:
1. cursor（grok 4.6, high)担任后端编码
2. opus5(high)担任前端编码
3. codex（gpt-5.6-luna,  xhigh)探索代码
4. codex（gpt-5.6-sol,  high)担任code reviewer, codex存在过度防御的问题, 你应该自行判断问题是否修复
5. 你担任指挥官和planner, 激进并行, 合理控制每个agent的工作量, 避免单一agent上下文过大

## 2026-08-31 追加 prompt（用户，与性能探索并行）

并行任务：
1. 目前ios pwa应用
    1. 使用双指滑动scroll, 单指选择并复制, 请你改为单指scroll, 长按选择并复制, 就像ios上文字类app的经典逻辑一样
    2. Pwa应用目前选择文字后自动复制会提示复制失败
    3. 终端窗口右上角点击终端设置icon, 提示加载失败
2. 终端窗口的*号会变成emoji, 请你修复

## 2026-08-31 更正（用户）

我的意思是终端窗口标题的*会变成emoji

## 2026-08-31 追加 prompt（用户）

顺便修掉现存的所有错误

## 2026-08-31 追加 prompt（用户，遗留项处置）

1:修复 2:评估一下性能影响,特别是对于低性能设备 3:修复,但toast文案要专业易懂,连接恢复前输入未送出看不懂

## 2026-08-31 追加 prompt（用户，新 bug）

有一个小问题, ios pwa长按拖选复制时,黏贴按钮, 复制/黏贴文字被切割成2行, 请你修复 2.有时候文字会莫名变成绿色,通过类似选择绿色文字导致其重新渲染后恢复正常

## 2026-08-31 追加 prompt（用户，远程访问页）

并行任务：
1. 设置-远程访问
    1. 目前tunnel明明已指向本机tmex的主机名, 我访问tmex.konata.tv即可链接本机,但提示没有指向所以无法接管
    2. 点击登录cf并在网站授权后提示cf进程启动失败
    3. Access明明已配置, 但是侦测不到还是显示未配置
    4. 该页面似乎未能良好侦测本机tunnel, access状态, 请检查
    5. 重写本页文案, 使其专业简洁易懂, 就像大型软件里的专业文案一样

## 2026-08-31 更正（用户，绿色文字）

我认为奇怪绿色文字可能不是因为实际渲染变了,例如我在输入框打字,前面都正常,突然文字就变成绿色,再打几个字又变成正常颜色了.这里应该不涉及绿色文字

## 2026-08-31 补充（用户，绿色文字二）

而且变成绿色的只是新打的几个字,并不影响前面的字,恢复正常后那几个字还是绿色的, 除非选中使其刷新一下

## 2026-08-31 补充（用户，绿色文字三）

你的推测应该不对,因为我打的是中文自然语言,没有任何命令,也不会被zsh理解为任何命令

## 2026-08-31 追加 prompt（用户，侧栏折叠）

一个小问题:左侧菜单栏设备点击折叠/展开按钮, 点击折叠后无事发生(没有断开),点击展开后会触发重连接,这是为什么,似乎没什么逻辑
