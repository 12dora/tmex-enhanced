# Task O13 — Fix review findings: remote access tab, sidebar offline rule, split-close capture (frontend)

Read `common-rules.md`, then `review-fe-3-report.md` (authoritative; fix items 1–5) and `O11-result.md`, `O12-result.md`, `O10-result.md`. Contract updates (commit 58c52442, read `packages/shared/src/contracts/tunnel.ts`): `configure_access { rules, hostname? }`, `sync_access { hostname? }`, `remove_access { acknowledgeExposure? }`, `set_access_enforce { enforceJwt, acknowledgeExposure? }`, `TunnelAccessStatus.effective` (= configured && enforceJwt && hostname matches the current tunnel) and `bypassAppId`. Backend agent G9 implements these in parallel.

## Scope (files you own)
apps/fe/src/pages/settings/remote-access/** (+tests), apps/fe/src/components/page-layouts/components/sidebar-node-section.tsx, sidebar-device-list.tsx (+tests), apps/fe/src/pages/devices/device-snapshot-store.ts only if you need cached device names, packages/terminal-ui/src/components/split/SplitPaneView.tsx (+test), packages/panels/src/device-console/use-pane-selection-dispatch.ts (+test), i18n `settings.remoteAccess` keys (targeted edits).

## Fix list
1. Dropping the last protection needs the same exposure acknowledgement: `set_access_enforce(false)` and `remove_access` go through `withExposureAck` when `!loginEnforced` and the tunnel is running; the enforce switch shows the ExposureWarning + checkbox before turning off; the remove dialog includes the acknowledgement checkbox; map 409 `exposure_ack_required` the same way as for start.
2. Split view: clicking the close control of a NON-focused pane must not first navigate to that pane through the root `onPointerDownCapture` selection handler — make the capture handler ignore events originating in the close control (e.g. `event.target.closest('[data-pane-close]')`) and mark the close button with that attribute; `handleClosePane` must use the current route pane at call time. Add a unit test for the capture-ignore predicate and extend `apps/fe/tests/split-close-pane.spec.ts` with a "close a non-focused pane keeps the URL on the focused pane" case (do NOT run e2e).
3. Access step/hostname: the Access step passes the wizard's confirmed hostname draft (`hostname` field) to `configure_access` when `config.mode === 'off'`; `accessTargetHostname` = `config.hostname ?? draft.hostname` for configure and `config.hostname ?? external.hostnames[0]` for sync; `access.hostname` is only displayed as "currently covered". Keep the step before create (backend now supports configuring Access for the confirmed hostname first). Enable the "Apply" button whenever a target hostname exists.
4. Sidebar offline rule: an offline remote node whose devices are switched on for the sidebar must keep its section (the offline branch currently derives devices from `inventory` which has no device list): derive the visible device ids from the visibility keys with that node's prefix and render rows with cached names when available (device-snapshot-store / last known device list) else the device id; test "enabled device, node goes offline → section stays".
5. Access pill + exposure warning: derive the protection state with the backend predicate (`access.effective`; fall back to `configured && enforceJwt && access.hostname === config.hostname` when `effective` is undefined in older payloads); add a distinct pill state 「已配置（主机名不匹配）」/「已配置但未强制」; the warning says 「当前没有生效的访问保护」 and must NOT suggest a LAN/trusted network makes it safer — a tunnel URL is public regardless (rewrite the three locales accordingly, concise).

Verify fe / panels / terminal-ui tests + tsc + biome. Write your result to: /Users/konata/code/tmex-enhanced-wt-r5/prompt-archives/2026083002-remote-agent-files-tunnel-round5/sub/O13-result.md
