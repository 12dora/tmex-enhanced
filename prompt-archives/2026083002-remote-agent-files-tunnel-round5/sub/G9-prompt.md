# Task G9 — Fix review findings: Cloudflare Access / tunnel discovery (backend)

## Ground rules (read carefully)
- Repo: tmex monorepo, Bun-only runtime (`bun`, `bunx`; never node/npm/npx for running). Worktree root: /Users/konata/code/tmex-enhanced-wt-r5. Work ONLY inside this worktree.
- Several other agents are editing this same worktree in parallel. Touch ONLY the files/directories listed in your scope (plus new files you create inside those directories). Do NOT run `git add/commit/stash/checkout/reset`. Do NOT run formatters over the whole repo; `bunx biome check --write <your files>` only.
- Never edit generated files: `packages/shared/src/i18n/resources.ts`, `packages/shared/src/i18n/types.ts`, anything under `resources/`, `dist/`. For i18n, edit the source locale JSONs `packages/shared/src/i18n/locales/{zh_CN,en_US,ja_JP}.json` (all three, same keys, only inside the sub-object named in your scope) — the commander runs `bun run build:i18n`. If you need the generated types updated to typecheck, you may run `bun run build:i18n` from the repo root yourself (it only regenerates from the JSONs).
- Copy (UI strings) must be concise, professional and plain — the tone of mature large-scale software (think VS Code / GitHub settings). No exclamation marks, no chatty phrasing. zh_CN uses Chinese punctuation.
- Comments in code: only when the logic is genuinely non-obvious; existing comments in this repo are in Simplified Chinese — follow that.
- Do not touch anything about the production tmex install (`~/Library/Application Support/tmex`, port 9883) or any tmux session named `tmex`. Tests must use isolated tmux sockets / test env only.
- Never leave TODOs, stubs, or "simplified versions". Finish the whole scope. If something in the scope is impossible, explain exactly why in your result file.
- Verify before finishing: run the relevant package tests (`cd <pkg> && bun test` — for apps/fe use `bun test src/`), `bunx tsc --noEmit -p .` in each package you touched (error count must not exceed the baseline given below), and `bunx biome check <files you changed>`. Baseline (before this round): apps/gateway 2500 pass / tsc 21 errors (pre-existing); apps/fe 671 / 0; packages/panels 507 / 0; packages/stores 282 / 1; packages/shared 365 / 0; packages/api-client 132 / 5 (pre-existing); packages/ui 47 / 0. Note: the commander already changed shared contracts (agent `nodeId`, MeshNode `reach`/`rttMs`, files browse, tunnel) so some test fixtures now fail tsc until the owning agent updates them — that is expected and yours to fix if in your scope.
- Shared contracts are already written and are FIXED (do not change their shape; you may add doc comments): `packages/shared/src/contracts/agent.ts` (`AgentSessionDto.nodeId`, `CreateAgentSessionRequest.nodeId`), `packages/shared/src/contracts/files.ts` (`BrowseDirectory*`), `packages/shared/src/contracts/tunnel.ts`, `packages/api-client/src/auth/types.ts` (`MeshNode.reach: 'lan'|'wan'|'relay'|null`, `rttMs`), api-client functions `browseDirectory` (file-resources.ts), `fetchAgentSessions(client, {nodeId})` (agent.ts), `fetchTunnelStatus/runTunnelAction` (local/tunnel-api.ts).
- When done, write a concise result report (what changed, file list, test/tsc/biome numbers, anything left or risky) to the absolute path given in your task, then exit.


Read `G8-result.md` and `review-be-3-report.md` (authoritative — fix all 10 items) plus `review-fe-3-report.md` items 1 and 3 (backend halves). The commander extended the contract (`packages/shared/src/contracts/tunnel.ts`, commit 58c52442): `configure_access { rules, hostname? }`, `sync_access { hostname? }`, `remove_access { acknowledgeExposure? }`, `set_access_enforce { enforceJwt, acknowledgeExposure? }`, `TunnelAccessStatus.effective` and `bypassAppId`. Frontend agent O13 works in parallel against it.

## Scope (files you own)
apps/gateway/src/tunnel/**, apps/gateway/src/api/tunnel-routes.ts, apps/gateway/src/index.ts, apps/gateway/src/managed-entry.ts, apps/gateway/src/mesh/mesh-http.ts (guard hook only), apps/gateway/src/mesh/forwarder.ts (header filtering only), packages/app/src/runtime/assemble.ts (guard placement), drizzle migration if the schema needs a column (bypass app id), tests.

## Fix list
1. (blocker) Guard placement: run `guardTunnelAccess` at the OUTERMOST Bun `fetch` (and the WS upgrade path) of every entry — assemble runtime (before TLS/local/setup/hub handlers), `apps/gateway/src/index.ts`, `managed-entry.ts` — with explicit exemptions: peer-inbound requests (peer marker / `isPeerInboundRequest`), machine endpoints used by nodes and the CLI (`/hub/uplink`, hub enrollment/redeem/admit endpoints, `/healthz`, anything the hub `uplink-server` / enrollment routes serve to non-browser clients — enumerate them by reading apps/gateway/src/hub/** and apps/gateway/src/mesh/mesh-http.ts and list them in the report), and requests without `cf-connecting-ip`. Integration tests for each entry (direct gateway, managed, assembled): header+no JWT → 403, valid JWT → pass, `/hub/uplink` without JWT → not blocked by the guard.
2. (should-fix) Do not re-run the guard on peer-forwarded requests at the remote node, and strip Cloudflare identity headers (`cf-connecting-ip`, `cf-access-jwt-assertion`, `cf-access-authenticated-user-email`, `cf-ray`, cookie `CF_Authorization`) in `forwarder.ts` header filtering before forwarding to a node. Test.
3. (blocker) Site-wide Access app would block mesh machine traffic: when the instance has the hub role (or any node role), `configure_access` must ALSO create (and `remove_access` delete) a second self-hosted app scoped to the machine paths — Cloudflare supports `domain: "<hostname>/hub/"`-style path apps and more-specific paths take precedence (docs: https://developers.cloudflare.com/cloudflare-one/access-controls/policies/app-paths/) — with a single policy `decision: 'bypass'` include `everyone`. Store its id in `bypassAppId`; `sync_access` recognises it. Cover every machine path prefix your item-1 enumeration found (if they do not share one prefix, create one bypass app per prefix and store a JSON array in the same column; contract's `bypassAppId` may then hold the first id — document it). Tests with fake fetch.
4. (blocker) `replaceAllowPolicy`: manage ONLY the policy named by tmex (e.g. `tmex-allow`, remember its id); if other allow/bypass/service-auth policies exist that were not created by tmex, do not silently delete them — fail the job with `access_api_failed` and a message listing their names (the user must clean up in the dashboard), unless they are the tmex bypass app policies. Any delete/update failure fails the job; re-read policies before persisting `rules`/`enforceJwt=true` and verify the state.
5. (should-fix) JWT: require header and payload to be plain objects, `alg === 'RS256'`, `exp` a finite number and in the future, `nbf` finite when present; every parse/verify exception → `false` (never 500). Tests: missing exp, `null` header, alg none/HS256.
6. (should-fix) Guard state: enforcement applies only when `access.effective` (= configured && enforceJwt && access.hostname === current tunnel hostname, where the current hostname is `config.hostname` for named/adopted mode; quick/off mode → not effective). Expose `effective` in status. Tests: quick tunnel after named removal → guard off.
7. (should-fix) External detection: build independent candidates per process / launchd unit / systemd unit / config file, keyed by tunnel id, token file or config path; read the `--config` path the candidate actually declares; `running` true only when a process of THAT candidate is alive; accept a hostname only with ingress evidence pointing to our origin port (config.yml ingress, API configuration, or the log's ingress JSON) — a sibling `hostname` file alone is not enough unless the log/API confirms the origin. Prefer the running candidate. Tests.
8. (should-fix) `check` behind Access: treat a response that is an Access login redirect (302/303 to `*.cloudflareaccess.com`) or Cloudflare 403 with `cf-access-*` markers as job `done` with `step: 'access_protected'` (edge reachable, origin protected) rather than an error; a real origin `/healthz` 200 → `ok`; else error.
9. (should-fix) `remove_access`: only a confirmed 404 counts as already-deleted; other errors fail the job and keep local state. Also require `acknowledgeExposure` (409 `exposure_ack_required`) for `remove_access` and `set_access_enforce(false)` when the tunnel is running and this is the last protection (`!loginEnforced`).
10. (should-fix) Credentials permission copy: the `set_access_credentials` error/message and the contract comment must say the token needs `Access: Apps and Policies — Edit` AND `Access: Organizations, Identity Providers, and Groups — Read`; keep the organizations call.
11. `configure_access`/`sync_access` accept `hostname` (validated RFC 1123); default `config.hostname`; when `config.mode === 'off'` the explicit hostname is required and is stored on the access record so that a later `create` with the same hostname is `exposureProtected`; `sync_access` default `config.hostname ?? external.hostnames[0]`.

Verify: `cd apps/gateway && bun test` (0 fail), tsc ≤ 21, biome; `packages/app` assemble tests. Write your result to: /Users/konata/code/tmex-enhanced-wt-r5/prompt-archives/2026083002-remote-agent-files-tunnel-round5/sub/G9-result.md
