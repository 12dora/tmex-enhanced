# prompt 存档

## 2026-08-30 第五轮需求

> 继续开发, 请你分批commit并在最后push替换本机tmex, Think in English, Send English prompt.
> 任务：
> 1. 设置-设备与文件
>     1. 目前添加目录, 只支持人工输入路径, 请你在路径input右侧新增按钮指向优雅的路径选择器, 让用户可以图形化的选择路径
> 2. 智能体功能目前只支持本机会话, 请你支持多节点模式下的远程会话, 实现方式都是调用本机智能体
>     1. 默认提示语后面"选择一个Pane来开启会话"改为"选择一个会话"
> 3. 非局域网远程机例如s0522, 右上角液已经显示为局域网, 中转延迟未知, 请修复
>     1. 本地docker机例如c9dc…属于局域网, 但是还有中转, 延迟未知标签
> 4. 管理设备-设备卡片
>     1. 原显示在侧栏改为2个开关"侧栏显示: 终端 文件", 2个开关左侧共享"侧栏显示", 你需要展现合理的层级关系. 这2个开关用于控制是否在侧栏的 终端 / 文件页面显示(如没有配置该设备的文件, 则该开关为关闭灰色状态", 配置好文件后默认打开该开关
>     2. 设备卡片3点菜单新增文件选项, 点击后跳出弹窗配置该设备文件, 就像"设置-设备与文件"一样, 每个设备卡片均只允许配置本设备下的文件
> 5. 智能体, 终端针对远端设备, 你都需要考虑到断线/上线后各处的显示状态变化, 例如文件当远端设备未连接或断开时, 应自动消失
> 6. 设置-在终端tab右侧新增远程控制tab
>     1. 该tab用来展示和配置目前tmex的远程控制状态, 例如cloudflare tunnel等, 并可以图形化的从0到配置完成
> 注:
> 1. cursor（grok 4.6, high)担任后端编码
> 2. opus5(high)担任前端编码
> 3. codex（gpt-5.6-luna,  xhigh)探索代码
> 4. codex（gpt-5.6-sol,  high)担任code reviewer, codex存在过度防御的问题, 你应该自行判断问题是否修复
> 5. 你担任指挥官和planner, 激进并行, 合理控制每个agent的工作量, 避免单一agent上下文过大

> 涉及到文案的, 文案必须简洁专业易懂, 就像所有大型软件的文案一样

> 顺便清掉除legacy以外的worktree

## 2026-08-30 追加任务（第一批 agent 完成后）

> 追加任务：
> 1. 点击右上角分栏按钮后会创建新的窗口并分栏, 但在分栏界面关闭这些终端窗口时, 有时会卡在连接设备, 过一段时间才能恢复, 请你修复该问题
> 2. 目前还有多处i18n没有翻译,例如pane -> 终端, agent ->智能体, file -> 文件,请你全面扫描后翻译.
>     1. 把所有pane全部替换成terminal, 包括panes的复数形式
> 3. 点击右上角图标展开命令输入框, 该输入框进入, 消失没有动画, 请你补充优雅动画
> 4. 左右上角所有图标没有tooltip, 用户难以猜测用途, 请你全部加入hover显示tooltip, 为图标增加简短标题
> 5. 智能体tag中"向agent描述…中协作这句话, 删掉"开始"两个字

> 追加任务: 设备卡片-修改设备弹框内容溢出弹框外了,请你修复

> stand alone模式cf tunnel是需要配合cf access一起用的,否则会无鉴权暴露在公网,因此你的设置面板里应该同时包括tunnel和access

> access可选,但是要警告用户暴露在公网的风险.考虑到例如纯局域网其实并不需要access. 除了搭配access, 你还可搭配node模式下那套鉴权系统(用户名, 密码, 2fa)

> 目前该机已经配置好了Access和tunnel,所以理论上你的设置面板里应该能够看到这2者

> 追加任务:初始登录时, 只登录本机, 未登录其他节点时. 左侧菜单显示其余节点和登录按钮, 此时点击其他节点的登录按钮后, 左侧菜单节点会消失(但实际是已连接, 在设备管理中可以看到). 你应该使登录后默认不在左侧菜单显示其他节点, 仅在管理设备中显示其他节点
