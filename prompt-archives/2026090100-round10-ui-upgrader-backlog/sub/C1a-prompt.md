# C1a: Crash-safe upgrader — CLI/runtime blockers (packages/app + gateway runtime)

You are the backend coder working in /Users/konata/code/tmex-enhanced-wt-upg (branch feat/crash-safe-upgrade merged with main; tmex monorepo; Bun runtime — use `bun`/`bunx`). Another agent (C1b) works IN PARALLEL in the same worktree on `apps/gateway/src/system/upgrade.ts`, `apps/gateway/src/system/upgrade.test.ts`, `install.sh`, `packages/app/src/lib/install-script.test.ts` — you MUST NOT touch those four files. NO git commands. NEVER touch the production tmex install (`~/Library/Application Support/tmex/`, port 9883, launchd service) or the tmux session named `tmex`; never dial 127.0.0.1:39001; all filesystem tests use temp dirs.

## Mission

Fix the remaining crash-safe upgrader blockers and should-fix items from review-J. A fresh read-only verification with exact current anchors and per-item fix plans is in `/Users/konata/code/tmex-enhanced-wt-r10/prompt-archives/2026090100-round10-ui-upgrader-backlog/sub/EX3-result.md` — READ IT FIRST and follow its fix plans (they are well-grounded; deviate only with a stated reason). Design doc to keep in sync: `docs/release/2026083101-upgrade-crash-safety.md`.

Your items (EX3 sections):

1. **Blocker 1** — repair must not delete the currently-executing txn's staging: thread `activeTxnId` (from `--txn`) through `repairUpgrade`/`repairMissingJournal`/`repairAbortCandidate`/`repairRestartOld`/`repairTerminalCleanup`/`cleanupTxn`/`sweepUpgradeGarbage` (`keepTxnId`). Add the real download→extract→no-journal-repair→apply test.
2. **Blocker 2** — `repairMissingJournal()` must NOT call `removeLegacyTopLevelDirs()`; legacy top-level dirs are removed only by `finishCommittedCleanup()` after committed. Combined test with blocker 1 (legacy layout + old service running + preflight failure ⇒ top-level dirs intact).
3. **Blocker 3** — explicit preflight runtime mode (`RuntimeMode = 'normal' | 'preflight'`): `apps/gateway/src/runtime.ts` (skip seed/refresh/push/agent/watch/tunnel/notifications/remote-session-restore; still run migrations), `packages/app/src/runtime/assemble.ts` (no auth/mesh/TLS/frontend in preflight), `packages/app/src/runtime/server.ts` (no `tls.startup()` in preflight; minimal `/healthz` `{status,version,startedAt}`, 404 elsewhere). `runPreflight` in `upgrade-apply.ts` passes the mode (env var, e.g. `TMEX_RUNTIME_MODE=preflight`, consumed by server.ts/assemble). Add side-effect-probe tests asserting zero external calls in preflight. NOTE: old candidate versions won't know the env var — that's fine (preflight of OLD versions never happens; candidates are always ≥ this release).
4. **Blocker 4** — 1.0.2 healthz has neither `version` nor `startedAt`: implement the shared old-service verification per EX3 (managed + service-manager-running + `status==='ok'` compat path; keep strict checks for candidates/new versions; don't force a restart when the old service is already running). Apply to both `verifyOldServiceRunning()` and `rollbackToOld()`; thread serviceMode into `repairRestartOld()`/`repairVerifyOrRollback()`. Tests with the real 1.0.2 body `{"status":"ok"}` (also `{status:'ok',startedAt:<epoch-number>}` for 1.1.3 — number timestamps must keep working).
5. **Blocker 5 (CLI side)** — PID ownership for `serviceMode=none`: add `assertOwnedInstallProcess()` in `upgrade-process.ts` (cmdline/identity must match `<installDir>/current/runtime/server.js` or legacy `<installDir>/runtime/server.js`, realpath-tolerant); extend pid record (backward compatible with plain-number `tmex.pid`); `createDirectProcessControl` verifies ownership before any signal; foreign live PID ⇒ ownership error (NOT `isRunning()===false`), no signal, no pid-file deletion, and upgrade must not proceed to DB copy/restore. Tests per EX3 (foreign PID, owned positive case; update tests that used bash/process.pid as "valid").
6. **Blocker 6 (CLI side)** — SHA256SUMS policy: target ≥ 1.1.4 requires SHA256SUMS 200 + exact entry + digest match (404 aborts); older targets only with explicit `--allow-unverified` (add to flag tables; used only by the outer download step, not passed to the apply subprocess). Implement `assertReleaseIntegrity()` per EX3; update `delegateUpgrade()`; update the design doc paragraph (line ~39). Do NOT touch install.sh or gateway upgrade.ts (C1b owns those; the policy wording must match this spec exactly).
7. **Blocker 7** — add `stopping` phase before `service.stop()` (`backup` written only after `assertStopped()`); `recoveryAction('stopping') = 'restart_old'`; recovery from legacy `backup` journals first checks `service.isRunning()` and must NOT double-start. Tests per EX3.
8. **Should-fix A** — `upgrade-db.ts` `bun -e` argv off-by-one (`process.argv[1]/[2]`), non-empty validation, real `VACUUM INTO` test.
9. **Should-fix B** — native addon offline reuse: verify old addon (manifest SHA256/platform/version/NAPI vs pin) and copy instead of npm download when matching; keep `--allow-missing-native` semantics. Tests per EX3.
10. **Should-fix D** — legacy conversion writes shim only when `current/cli/bin/tmex.js` exists; keep existing shim otherwise. Tests per EX3.
11. **Should-fix I remainder** — pass `shimDirs` to `sweepUpgradeGarbage()` from repair entry points (local bin + bun bin dirs); test shim `*.tmp` cleanup and that foreign shims are untouched.
12. **Flag-parsing unification (blocking bug from the merge)** — `UPGRADE_FLAGS` in `commands/upgrade.ts` is missing `no-service`, `txn` (and needs `allow-unverified`), so the extracted-CLI delegated apply is rejected by `assertKnownUpgradeFlags`. Unify to a single flag definition (prefer one source of truth; keep unknown-flag rejection and `--help` working — note `main()` handles `--help` before dispatch, see index.ts:97). Update `UPGRADE_USAGE`. Add tests that the FULL argv passed by `passthroughUpgradeFlags()` passes validation end-to-end.
13. **Should-fix E (TLS readiness, lowest priority)** — healthz gains `tls: { mode, listenerRunning }` (assembled runtime), `HealthCheckOpts.requireTlsListener`, commit readiness requires listener for selfsigned/acme, not for none/external, preflight exempt. You own `apps/gateway/src/api/system-routes.ts` for the healthz field. If after everything else this proves too invasive, you may descope it with a written justification in your report — do not half-implement.

## File ownership

Everything under `packages/app/` EXCEPT `packages/app/src/lib/install-script.test.ts`; plus `apps/gateway/src/runtime.ts` (+ a new preflight test file), `apps/gateway/src/api/system-routes.ts` (+ its healthz test), `docs/release/2026083101-upgrade-crash-safety.md`. FORBIDDEN: `apps/gateway/src/system/upgrade.ts`, `apps/gateway/src/system/upgrade.test.ts`, `install.sh`, `packages/app/src/lib/install-script.test.ts`, anything else.

## Verification (mandatory; record all numbers in the report)

- Baselines BEFORE coding: `cd packages/app && bun test 2>&1 | tail -3` (strip ANSI: `sed 's/\x1b\[[0-9;]*m//g'`) and `bunx tsc --noEmit -p . 2>&1 | wc -l` (baseline: 1 pre-existing `@types/node` error). Gateway: `cd apps/gateway && bun test src/system src/api 2>&1 | tail -3` and tsc (baseline ~21 errors).
- After: all tests pass (0 fail), tsc not increased, `bunx biome check <changed files>` clean, `bun run build:cli` succeeds in packages/app, complexity gate passes: `bun scripts/complexity/gate.ts` from repo root (refactor into helpers rather than editing the allowlist).
- macOS: no `timeout` command; don't put `grep -c` in `&&` chains.
- No laziness: no TODOs, no simplified stubs. If something is genuinely blocked, explain precisely in the report.

## Report

Write a per-item report (fix summary, tests added, numbers, deviations) to the ABSOLUTE path `/Users/konata/code/tmex-enhanced-wt-r10/prompt-archives/2026090100-round10-ui-upgrader-backlog/sub/C1a-result.md` as your LAST action before exiting.
