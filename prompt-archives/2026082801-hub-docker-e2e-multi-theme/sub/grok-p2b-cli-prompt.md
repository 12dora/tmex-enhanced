# Backend task (packages/app only) — review fixes for commit c8fa053 (auth-spawn streaming + env-file symlink)

Worktree `/Users/konata/code/tmex-enhanced-wt-merge` (branch chore/merge-hub-tabs). Read `AGENTS.md`, `prompt-archives/2026082801-hub-docker-e2e-multi-theme/sub/grok-p2-cli-result.md` and the review `sub/review-cli2.md`. Touch ONLY `packages/app/**`; another agent edits `apps/gateway/**`. No git operations, no TODOs. Baseline `packages/app`: 234 pass / 0 fail, tsc 1 (pre-existing), biome clean.

1. `lib/auth-spawn.ts` signal handling (review P1, high): while the Bun child runs, the Node parent must (a) install SIGINT/SIGTERM handlers that forward the signal to the child once and keep the parent alive until the child closes and the piped output is drained, then restore default handlers; (b) on `close` with `code === null && signal`, propagate as exit code `128 + signal number` (use `os.constants.signals`) instead of a flat 1; (c) never throw EPIPE on the parent's stdout/stderr if the terminal went away (attach an `error` listener that ignores EPIPE). Keep TTY stdin inheritance for password prompts. Tests: fake child that traps SIGINT, prints a line, exits 130 → parent exits 130 and the line is forwarded; fake child killed by SIGTERM → parent exit code 143.
2. `lib/env-file.ts` dangling symlink (review P2): on `ENOENT` from `realpath`, `lstat` the path; if it is a symlink, resolve its target with `readlink` (relative to the link's directory) and write via tmp+rename in the TARGET's directory (creating the target file, leaving the link intact); if the link chain cannot be resolved, throw a clear error rather than replacing the link. Tests: absolute and relative dangling symlink.

Report to `prompt-archives/2026082801-hub-docker-e2e-multi-theme/sub/grok-p2b-cli-result.md` with numbers before/after.
