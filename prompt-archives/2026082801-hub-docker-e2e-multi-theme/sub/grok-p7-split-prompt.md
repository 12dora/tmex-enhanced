# Devops/backend task: "split" hub e2e — remote public hub (real domain + real cert) ↔ local Docker nodes across the internet

Worktree `/Users/konata/code/tmex-enhanced-wt-merge` (branch chore/merge-hub-tabs). Read `AGENTS.md`, `docs/hub/2026082800-hub-node-operations.md`, `docs/hub/2026082801-hub-docker-e2e.md` and the existing single-host harness `scripts/hub-e2e/` (Dockerfile, entrypoint.sh, run.sh, driver/*.ts, build-driver.sh). Reuse the image, entrypoint and driver scripts. Create files ONLY under `scripts/hub-e2e/split/` plus a new section in `docs/hub/2026082801-hub-docker-e2e.md` ("分体拓扑：远端 hub × 本地 NAT node"). Another agent edits `apps/gateway/**` concurrently — do not touch application source; product bugs → record evidence, continue. No git operations, no TODOs.

## Agreed design (do not deviate)
- Remote hub: compose project `tmex-split` on the server; services `caddy` (publishes **0.0.0.0:18443:443**, TLS with the REAL Let's Encrypt cert already on the server at `/root/tmex-e2e/certs/fullchain.pem` + `privkey.pem` for `ai.jiefakj.com`; `reverse_proxy hub:9883`) and `hub` (`TMEX_ROLES=hub,node`, `TMEX_HUB_PUBLIC_URL=https://ai.jiefakj.com:18443`, `TMEX_BASE_URL` same, `TMEX_TRUST_PROXY=true`, publishes **39001:39001**, `TMEX_PEER_BIND_HOST=0.0.0.0`, named volume, own tmux socket). Image tag on the server: `tmex-e2e:split` (build it there from the Dockerfile with the tarball below; do NOT retag `tmex-e2e:latest`, another agent uses it).
- NO port probing logic. `split/run.sh` starts by printing a clear notice: "需在远端放行入站 TCP 18443（必需，hub HTTPS）与 TCP 39001（可选，hub peer 口）——包括云安全组/面板防火墙/ufw", then continues. (ufw on the server already allows both; nginx owns 80/443 — never touch nginx or those ports.)
- Local nodes: compose project `tmex-split-local`; `node-a`, `node-b` on separate bridges `nat-a`/`nat-b` (outbound only, no published ports), optional `lan` network joining both (connect/disconnect at runtime), `driver` container. Local image: build `tmex-e2e:split` locally (`--platform linux/amd64`) from the same Dockerfile. All local containers get `extra_hosts: ["ai.jiefakj.com:43.248.129.233"]` because this Mac's default resolver returns proxy fake-IPs (198.18.x.x) — never rely on host DNS for that name. No custom CA needed (real cert).
- Package tarball: use `/private/tmp/claude-501/-Users-konata-code-tmex-enhanced/741cc3a1-5392-48be-8081-06f3803bdeb4/scratchpad/pkg-p6/tmex-cli-1.0.2.tgz` (already contains `hub join --no-restart`, migration 0020, all mesh fixes). Copy it to `scripts/hub-e2e/build/tmex-cli.tgz` for the local build and it is already on the server at `/root/tmex-e2e/tmex-cli-1.0.2.tgz`. Do not rebuild the package.
- Remote access: `/private/tmp/claude-501/-Users-konata-code-tmex-enhanced/741cc3a1-5392-48be-8081-06f3803bdeb4/scratchpad/rssh '<cmd>'` (root). Sync your split files: `sshpass -p "$SSH_PASSWORD" rsync -az -e "ssh -o StrictHostKeyChecking=no -p 10022" --exclude out --exclude ca --exclude build /Users/konata/code/tmex-enhanced-wt-merge/scripts/hub-e2e/ root@43.248.129.233:/root/tmex-e2e/repo/scripts/hub-e2e/`. Remote repo: `/root/tmex-e2e/repo` (no node_modules; use `driver-dist/` bundles from `build-driver.sh`, synced by the rsync above after you run it). Another compose project `tmex-e2e` (single-host harness, port 127.0.0.1:18543) may run there concurrently — leave it alone.
- Enrollment: `enroll` on the remote hub container (`bun /opt/tmex/runtime/cli-auth.js enroll --ttl 10m --install-dir /opt/tmex` under nohup, parse the token like run.sh), `hub join https://ai.jiefakj.com:18443 --token … --name node-a --install-dir /opt/tmex --no-restart` inside the local node (via `bin/tmex.js` with node, or `cli-auth.js` with bun), then restart the node container.

## Scenarios (PASS/FAIL/SKIP with evidence → `split/out/report.md`)
A. node-a, node-b join over the internet; `/api/hub/nodes` online; login at hub entry (`https://ai.jiefakj.com:18443`, driver from inside the local driver container); terminal marker round-trip on node-a THROUGH the hub (relay); files list/read on node-b through the hub.
B. node-a as entry (driver → `http://node-a:9883`): login to the remote hub node (`isHub:true` row), create a local device + tmux session ON THE HUB CONTAINER (via ssh docker exec), tmux tree, terminal marker round-trip node-a → remote hub node; record the observed `reach` (relay vs lan via 39001).
C. connect `lan`; node-a sees node-b `reach:'lan'` within 90 s; marker via LAN; `docker stop` the remote hub (ssh) → node-a session still reaches node-b (terminal + files), `/api/mesh/nodes` still lists node-b; `docker start` hub → both nodes online on `/api/hub/nodes` within 120 s; existing cookies still valid.
D. `direct enable` in node-a and in the remote hub container (`bun /opt/tmex-pkg/package/bin/tmex.js direct enable --install-dir /opt/tmex`), restart both, wait `direct_capable:true` for both rows; open a stream from node-a to the hub node and report the path (`reach`, logs for RTC/DataChannel vs ws-secure via 39001). Document what is provable.
E. `docker restart node-a` → re-uplinks, key log/certs intact, reaches hub node; remote hub restart → nodes reconnect; no ghost rows.
F. Browser from the Mac with Playwright (repo `node_modules/.bun/playwright@1.58.2`, run with `bun`; Chromium launched with `--host-resolver-rules="MAP ai.jiefakj.com 43.248.129.233"`; real cert so NO ignoreHTTPSErrors): open `https://ai.jiefakj.com:18443/login`, password login, sidebar shows node-a/node-b, open node-a's device terminal, type a marker and assert it renders; then Account Security page: register a passkey with a virtual authenticator (CDP `WebAuthn.enable` + `addVirtualAuthenticator`, protocol ctap2, resident key, user verified), log out, log in with the passkey. Screenshots to `split/out/`. Reuse selectors from `apps/fe/tests/` and `apps/fe/src/pages/LoginPage.tsx` / account security page.
G. Revocation: revoke node-b from the hub (Nodes page via Playwright is acceptable, or the API `POST /api/auth/keylog?hub=sync` with a signed `revoke-node` record using the root key helpers from `packages/shared/src/auth` as `driver/login.ts` does); afterwards node-b's uplink must be rejected and node-b unreachable from entries. If not feasible non-interactively, SKIP with reason.

Deliverables: `split/setup-remote.sh`, `split/setup-local.sh`, `split/run.sh` (A→G, `down` for both sides), compose files + Caddyfile, `split/.gitignore` (out/), doc section. Iterate until A–C and E PASS; D/F/G may end FAIL/SKIP with evidence. Never touch the Mac's production tmex or the tmux session named `tmex`. Report to `prompt-archives/2026082801-hub-docker-e2e-multi-theme/sub/grok-p7-split-result.md`: final table, product bugs (file:line hypotheses), ports actually needed, and how to clean up both sides.
