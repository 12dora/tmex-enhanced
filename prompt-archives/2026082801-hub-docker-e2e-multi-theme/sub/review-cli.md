- **major** — [packages/app/src/commands/hub.ts:127](/Users/konata/code/tmex-enhanced-wt-merge/packages/app/src/commands/hub.ts:127)：Linux 下 `detectServiceManager()` 仅通过 [platform.ts:10](/Users/konata/code/tmex-enhanced-wt-merge/packages/app/src/lib/platform.ts:10) 执行 `systemctl --version`。该命令只输出版本，并不连接 user manager；`--user` 才会连接调用者的 manager（[systemctl 官方文档](https://cgit.freedesktop.org/systemd/systemd/tree/man/systemctl.xml?id=99504dd4c13af7516a976fffc0f68e6f26d3faac)）。因此，在安装了 systemctl、但没有可用 user manager/user bus 的容器或 SSH 环境中，代码会误判为 `systemd-user`，随后 `systemctl --user restart` 失败；此时 join/leave 状态已经写入，命令仍以非零退出，未实现预期的提示并退出 0。修复时应探测实际 user manager 连接，例如带超时执行 `systemctl --user show-environment`，不可连接时返回 `none`；并增加“`--version` 成功、user manager 探测失败”的测试。

**Verdict：不通过；需修复上述 major 后合并。**