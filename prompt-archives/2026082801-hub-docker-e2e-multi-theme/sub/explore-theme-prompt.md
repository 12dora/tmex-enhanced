You are a read-only code explorer for the tmex repo (Bun monorepo, React frontend in `apps/fe`, shared packages under `packages/*`). Do NOT modify any file. Print your final report as Markdown.

## Goal
Product change: today the UI only toggles dark/light (plus an apparently unused/partially used `data-theme-preset` mechanism in `packages/theme/src/presets.ts` / `themes.css` with presets like `underground`, `ocean-breeze`, ...). We want proper multi-theme support: named color schemes such as Dracula, Tokyo Night (night/storm/light), Catppuccin (mocha/latte), Nord, One Dark, Solarized dark/light, Gruvbox, GitHub light/dark, plus the existing default light & dark. Each scheme must drive BOTH the app UI tokens (CSS variables) AND the terminal palette (xterm/ghostty ANSI 16 colors + fg/bg/cursor/selection). UI changes: the sidebar's theme toggle icon becomes a button that opens a theme picker menu (popover/dropdown with previews); the "dark mode" switch in Settings → General is removed.

## What I need (precise file paths + line numbers, short code excerpts where useful)
1. **Theme token architecture.** `packages/theme/src/{tokens.css,tokens.generated.css,themes.css,presets.ts,index.ts,terminal-shortcut-tokens.ts}`: how light/dark is expressed (`data-theme`? `.dark` class? `prefers-color-scheme`?), what `tokens.generated.css` is generated from (which script — check `package.json` scripts and `scripts/`), what the `[data-theme-preset]` presets actually override, and whether any UI currently lets the user pick a preset (grep `themePreset` in `packages/stores/src/ui.ts`, `apps/fe/src/main.tsx`). List the full set of semantic CSS variables a new theme must define (name them all).
2. **Where dark/light state lives.** `packages/stores/src/ui.ts` (fields, persistence key, localStorage schema/version, migrations), `apps/fe/src/main.tsx` (how it's applied to `document`), `apps/fe/src/components/page-layouts/components/sidebar-title.tsx` (the toggle icon), `apps/fe/src/pages/settings/general-settings-tab.tsx` (the dark mode switch), and any other consumer of `isDark`/`theme` (grep `useUiStore`, `theme ===`, `isDark`, `colorScheme`, `prefers-color-scheme`, `dark:` Tailwind variants count per package — is Tailwind's `dark:` variant used widely? how is it configured (`darkMode: 'class'|'selector'|'media'`) in `apps/fe/tailwind.config.*` / CSS `@custom-variant`?). This determines whether a "dark-ish" theme like Dracula must also set the dark class.
3. **Terminal palette.** How the terminal (`packages/terminal-ui`, ghostty wasm / xterm) receives colors today: grep `foreground`, `background`, `ansi`, `palette`, `theme` in `packages/terminal-ui/src` and `packages/panels/src`. Is there a theme object built from CSS variables or hardcoded? Where would a per-scheme ANSI palette plug in, and is there an existing mechanism to re-theme a live terminal on change?
4. **Markdown/mermaid & other theme-aware components**: `packages/panels/src/markdown/mermaid-block.tsx` and anything else that branches on dark/light (code highlighting theme, charts, monaco/codemirror if any).
5. **Sidebar structure** (3-tab sidebar, `apps/fe/src/components/page-layouts/**`): where the toggle icon sits, what popover/dropdown primitives exist in `packages/ui` (Radix? shadcn-style `dropdown-menu.tsx`, `popover.tsx`?) that a theme picker should reuse, and the icon set in use (lucide?).
6. **Settings page**: the exact block to remove for the dark mode switch and any i18n keys (`packages/shared/src/i18n/**` source files — which files are hand-written sources vs generated `resources.ts`/`types.ts`, and the build command `bun run build:i18n`) to add/remove for the new picker (keys for each theme name, "Theme", "System", etc.). Both locales present (en / zh-CN?) — list locale files.
7. **Tests** touching theme: `packages/theme/src/*.test.ts`, stores tests for ui store, fe unit tests, e2e specs referencing the dark toggle (`data-testid` names) so we know what will break.
8. **Persistence to server?** Is the UI theme persisted only in localStorage, or also in a server-side user settings API (`/api/settings`)? Check `packages/api-client` and gateway settings routes for theme fields.

Finish with: (a) a proposed data model (`ThemeId` union, `{ id, label, appearance: 'light'|'dark', ui: {...vars}, terminal: {...ansi} }`, and a `'system'` option that follows OS and maps to default light/dark), (b) a file-by-file change list split into two disjoint work packages that can be edited in parallel without touching the same files: **WP-A "theme package + terminal palette"** (packages/theme, packages/terminal-ui, stores) and **WP-B "fe UI: picker menu + settings + i18n"** (apps/fe, packages/ui, i18n sources), noting any shared interface both depend on (define it concretely so both sides can code against it). Keep under ~350 lines.
