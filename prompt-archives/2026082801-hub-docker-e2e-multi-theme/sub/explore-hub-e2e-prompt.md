You are a read-only code explorer for the tmex repo (Bun monorepo). Do NOT modify any file. Write your final report as Markdown to the output path given on the command line (the -o flag handles it; just print the report as your final answer).

## Goal
We must run a REAL end-to-end verification of the hub/node mesh feature using multiple Docker containers (Linux x64, Ubuntu-based hosts with Docker 29). Layout intended:
- One remote Linux server with a public IPv4 (no domain; we can use `<ip-dashed>.sslip.io` if a real certificate is needed) running the hub (`TMEX_ROLES=hub,node`) in a container, plus 1–2 node containers on the same host but on a separate docker network (to simulate "NAT-ed devices"), and optionally 1 node container on a developer macOS laptop (Docker Desktop, behind NAT).
- Verify: (1) two-machine flow: `hub user add` → `enroll` → `hub join` from another container → node appears online in `/api/hub/nodes` and `/api/mesh/nodes`, login from entry, open terminal/tmux session of a remote node through the entry (relay), file listing across nodes; (2) hub down: existing peer links / cached LAN signalling still let entry reach nodes, and hub recovery needs no re-login; (3) direct (RTC) connection interruption doesn't lose output (may be limited by `direct enable` needing native addon download from npm — check what the CLI does and whether it is feasible inside a container).

## What I need from you (be precise, quote file paths and line numbers)
1. **Building a runnable artifact for Linux containers.** Read `package.json` scripts, `packages/app/scripts/build-runtime.ts`, `packages/app/package.json`, and the CLI (`packages/app/src/**`). Explain exactly how to produce a tarball (`npm pack`) on macOS that runs on Linux x64 (any platform-specific pieces? `cpu-features`/ssh2 externals, native addon for direct connection, Bun binary download — does `init` download Bun? from where? does it need network?). Identify the minimal Dockerfile: base image, whether Node.js is needed for `npx tmex-cli` bootstrap, how `init --no-interactive` works without systemd (`--autostart false`?), and how to start the server directly (`run.sh`? `runtime/server.js` with which env vars?) as PID 1 so the container keeps running.
2. **`hub join` / `hub leave` / `mesh reset-root` restart semantics.** These commands "restart the service". Find the code: what do they do when there is no launchd/systemd (container)? Do they fail, or just write app.env and print? Is there a flag to skip restart? Since the server will run as the container's PID 1, describe the safest sequence (e.g. run `hub join` while server is running, then restart the container; or stop, join, start).
3. **HTTPS requirement.** `hub join` requires `https:` unless `--insecure-local` with `http://127.0.0.1|localhost` and `NODE_ENV !== production`. Confirm exact rules in code (file/line). Is a non-loopback http URL ever accepted (e.g. with NODE_ENV=development)? If not, we need TLS: does the gateway/hub serve TLS itself or must we put a reverse proxy (Caddy) in front? What env is needed behind a proxy (`TMEX_TRUST_PROXY`, `TMEX_HUB_PUBLIC_URL`)? How do node→hub uplink (`/hub/uplink` WSS) and CA trust work — does the runtime honour `NODE_EXTRA_CA_CERTS` (Bun) so a Caddy internal CA could be used, or is a publicly-trusted cert (Let's Encrypt via sslip.io) the only safe path? Check for any `rejectUnauthorized`/`tls` options in uplink client code.
4. **Non-interactive credentials.** For `hub user add` (`TMEX_PASSWORD`), `enroll` (password via env? TOTP?), `hub join --token`, login via HTTP API (`/api/auth/*` — list the exact request sequence a script would need to log in with a password, i.e. what does the browser do: fetch mode, derive key, sign challenge… is there any existing test helper / CLI (`cli-auth.js`) we can reuse from a shell script to obtain the session cookie?). Point to existing integration tests (`apps/gateway/src/mesh/**/*.test.ts`, `*.integration.ts`) that already exercise the multi-node flow in-process, so we know which assertions to replicate.
5. **Observable endpoints for assertions.** List HTTP endpoints/CLI commands and their JSON shape that a bash/bun script can poll: node online/offline, reach path (`relay`/`peer`/`lan`), version, `direct_capable`, `GET /api/mesh/connection`, terminal/tmux endpoints reachable via `/n/:id/...` (what's the minimal API to create a tmux session on a remote node and read output — e.g. REST endpoints under `/api/...` for sessions/panes, or must it go through WS?). Also the peer port and LAN address reporting, so we can verify hub-down behaviour.
6. **Existing e2e infra.** `apps/fe` Playwright config, how standalone e2e boots the gateway (`scripts/`, `test.env`), whether a mesh e2e harness already exists (search for `virtual authenticator`, `mesh` in `apps/fe/e2e`), and what could be reused to drive a browser against the remote hub.
7. **Risks / unknowns** you find (e.g. things that assume macOS paths, `~/.local/share/tmex`, Bun version pin, port defaults 9883/39001, IPv6 dual-stack bind failing in docker without IPv6).

Output a structured report with sections matching 1–7, and finish with a concrete proposed `docker compose` topology (services, networks, env per service, ports) and an ordered verification script outline. Keep it under ~400 lines.
