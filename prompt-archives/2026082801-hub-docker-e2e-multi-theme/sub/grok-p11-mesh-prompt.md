# Backend task — round 8: fix the 6 findings of review-p10

Worktree `/Users/konata/code/tmex-enhanced-wt-merge` (branch chore/merge-hub-tabs, HEAD 2357a7c). Read `AGENTS.md`, `prompt-archives/2026082801-hub-docker-e2e-multi-theme/sub/review-p10.md` (6 findings, all accepted) and `sub/grok-p10-mesh-result.md`. Scope: `apps/gateway/src/mesh/**`, `apps/gateway/src/hub/**`, `apps/gateway/src/auth/user-key-service.ts`, `packages/shared/src/link/mux.ts`, `apps/fe/src/node/mesh-events.ts` + `mesh-nodes.ts` (+ tests). Nobody else edits these. No git operations, no TODOs. Failing-then-passing test per finding. IMPORTANT: a split harness run may be using the remote server; do NOT run `remote-cycle.sh` until your unit tests are green, then run it once (`bash /private/tmp/claude-501/-Users-konata-code-tmex-enhanced/741cc3a1-5392-48be-8081-06f3803bdeb4/scratchpad/remote-cycle.sh p11`, project tmex-e2e/port 18543 — leave tmex-split alone).

1. Revocation vs parked links: `onRevoked` must drop parked links for that node before/instead of `activateParked`; `activateParked()` and `track()` re-validate trust (cert present, not revoked, same user) before promoting any link.
2. Parked links: register a ctl drain immediately when parking; RST every inbound OPEN while parked; add hard caps on per-link pending streams / ctl inbox in `LinkMux` (drop + close link on overflow); replacing a parked link must not reset the fence timeout (keep the original deadline).
3. Key-log paging: `key.log.req` gains `limit` (default e.g. 256 records / ~1 MiB) and `key.log.res` returns `has_more`; hub enforces caps; client loops pages, each page applied atomically with head-CAS; client rejects oversized responses. `applyMany` must not keep per-step full state snapshots — validate incrementally with O(N) memory. Test with 2000 records.
4. NODE_EVENT decoder: keep `null`/`undefined` for absent optional fields; the fe patcher updates `direct_capable`/`version`/`name` only when the wire value is present. Test legacy 4-field payload keeps existing `direct_capable:true`.
5. Overflow fairness: nodes beyond the per-user fair capacity are explicitly rate-limited (`rate_limited` error with a retry hint) instead of sharing a remainder bucket; keep a bounded per-node map with LRU + TTL so a reconnecting node regains a slot fairly. Test: 9th node gets explicit rate_limited but a 1st node's burst is unaffected; after TTL the 9th node gets a slot.
6. `mux.ts`: on sync or async send failure call the unified `close(reason)` path which closes the transport (idempotent) then completes mux close, then rejects the original call; post-close transport callbacks are ignored (no `pendingChunks` growth). Test: transport whose send rejects without onClose → `transport.close` called exactly once, chunks after close dropped.

Verify: `bun test` in apps/gateway (baseline 2296), packages/shared, packages/ws-client, apps/fe (`bun test src/`) 0 fail; tsc ≤ baselines (gateway 21, others 0); biome clean; then the single remote cycle all-PASS. Report to `prompt-archives/2026082801-hub-docker-e2e-multi-theme/sub/grok-p11-mesh-result.md`.
