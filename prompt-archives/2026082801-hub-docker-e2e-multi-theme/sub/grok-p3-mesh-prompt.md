# Backend task (gateway mesh) — round 3: LAN path upgrade (harness 4c) and self direct_capable (harness 8)

Worktree `/Users/konata/code/tmex-enhanced-wt-merge` (branch chore/merge-hub-tabs). Read `AGENTS.md`, `docs/hub/2026082700-hub-node-architecture.md`, your previous reports `prompt-archives/2026082801-hub-docker-e2e-multi-theme/sub/grok-p1-mesh-result.md` and `sub/grok-p1b-mesh-result.md` (commits cddeba7, 0a83e41). You own `apps/gateway/**` (+ `packages/shared/src/**` only if strictly needed). No git operations, no TODOs. Nobody else is editing the gateway now.

## Fast verification loop (USE THIS, not the local qemu harness)
`bash /private/tmp/claude-501/-Users-konata-code-tmex-enhanced/741cc3a1-5392-48be-8081-06f3803bdeb4/scratchpad/remote-cycle.sh <tag>` — packs `packages/app` runtime+cli from the current tree (~30 s), uploads to a native x86 test server, builds the image there (layer-cached, seconds), runs the full `scripts/hub-e2e/run.sh` natively (~3.5 min) and prints the PASS/FAIL table; container logs/JSON land in `/private/tmp/claude-501/-Users-konata-code-tmex-enhanced/741cc3a1-5392-48be-8081-06f3803bdeb4/scratchpad/remote-out-<tag>/` (node-a.log, node-b.log, hub.log, mesh-nodes-*.json, run.log …). You may also inspect the remote stack directly after a run: `/private/tmp/claude-501/-Users-konata-code-tmex-enhanced/741cc3a1-5392-48be-8081-06f3803bdeb4/scratchpad/rssh 'docker exec tmex-e2e-node-a …'` / `docker logs -t tmex-e2e-node-a` / the driver: `/private/tmp/claude-501/-Users-konata-code-tmex-enhanced/741cc3a1-5392-48be-8081-06f3803bdeb4/scratchpad/rssh 'docker exec -w /workspace -e NODE_EXTRA_CA_CERTS=/ca/ca.crt tmex-e2e-driver bun /workspace/scripts/hub-e2e/driver-dist/nodes.js mesh-list --base-url https://entry.tmex.test --cookie-file /out/cookies-entry.json'`. If you change `scripts/hub-e2e/**` (driver or run.sh) the cycle script syncs it automatically. Do not run the harness locally. Never touch the local production tmex or the tmux session named `tmex`.

Current state (run tag `p1b`, commit 0a83e41): all scenarios PASS except:

## 4c — `reach` for node-b as seen from node-a does not become `lan` within 60 s after `docker network connect lan`
Sequence in run.sh: 4b logs in to node-b via entry node-a BEFORE the `lan` network exists (so a relay link via hub is established and sticky), then `docker network connect tmex-e2e_lan node-a` + `node-b`, then `wait-reach lan` for 60 s → still `relay`. Later (scenario 6, after `docker stop hub`) reach DOES become `lan` and terminal/files keep working, so LAN dialing itself works — the problem is upgrade timing while a relay link is alive. Investigate: (1) does a node re-advertise its endpoints (`os.networkInterfaces()` + `TMEX_PEER_PORT`) when interfaces change while running, or only at uplink connect? add periodic (e.g. every 15–30 s) or change-detected re-advertisement via `node.status`; (2) when the hub broadcasts a `node.list` with new endpoints for a peer that currently has a relay link, does `PeerManager` attempt an upgrade dial to `ws-secure`/LAN and switch the active link (design says LAN preferred over relay)? implement the upgrade (dial higher-rank candidates when endpoints change, swap on success, keep the old link until the new one is authenticated, no session loss); (3) add rate limits so this cannot storm. Add unit tests for both pieces (endpoint change detection; upgrade on new endpoints) modelled on existing `peer-manager*.test.ts` / `mesh-runtime.test.ts`. Acceptance: 4c PASS in the remote cycle.

## 8 — after `direct enable` + container restart, `/api/mesh/nodes` `self.direct_capable` stays false
Evidence: after restart the node no longer logs `native addon not found` (so the addon loaded), `/opt/tmex/native/node_datachannel.node` + `manifest.json` exist, but the `self` row (and the hub registry `nodes.direct_capable`) stays `false`. Also `self.version` / `self.inventory` are `null` in that row while peers have values. Find where the self row is built in `mesh-routes.ts` and where `direct_capable` is computed/reported (`mesh-runtime.ts` `node.status`, native loader `direct_capable` flag) and make self report its real capability, version and inventory; make sure the hub receives `direct_capable:true` in `node.status` after restart. Acceptance: scenario 8 PASS (`wait-direct-capable --name self`). If the harness step itself is wrong (e.g. should check the hub's `/api/hub/nodes` instead), fix the harness step and explain.

Verify: `bun test` in `apps/gateway` 0 fail (baseline 2244 pass), tsc ≤ 23 (currently 21), biome clean; then the remote cycle table with 4c and 8 PASS and nothing regressed. Report to `prompt-archives/2026082801-hub-docker-e2e-multi-theme/sub/grok-p3-mesh-result.md` with root causes (file:line), changes, tests, final table and the run tag.
