You are a code reviewer for the tmex repo (Bun monorepo). Review commit c8fa053 on branch chore/merge-hub-tabs (diff at `/private/tmp/claude-501/-Users-konata-code-tmex-enhanced/741cc3a1-5392-48be-8081-06f3803bdeb4/scratchpad/review-cli2-diff.patch`; read-only working tree). Two fixes in `packages/app`:
1. `lib/auth-spawn.ts`: the Node CLI spawns `bun runtime/cli-auth.js` for auth subcommands (`hub user add`, `enroll`, `hub join`…). Previously `stdio: 'inherit'`; in non-TTY containers the child's stdout never reached `docker exec` output (non-blocking inherited fd / buffering). Now stdout/stderr are always piped and forwarded chunk-by-chunk with backpressure; stdin is inherited only when the parent is a TTY (password prompts), otherwise ignored; exit code propagated after streams end.
2. `lib/env-file.ts`: `writeEnvFile` now resolves symlinks with realpath and does the tmp+rename in the target's directory so a symlinked `app.env` keeps pointing at the updated real file.

Check for real defects: interactive password prompts on a TTY (does `cli-auth.js` use raw-mode/readline on stdin — is stdout being a pipe instead of a TTY going to break hidden input or prompt rendering, e.g. `process.stdout.isTTY` checks in `packages/app/src/runtime`/`commands` used to decide between prompt and `TMEX_PASSWORD`?), Ctrl-C/SIGINT propagation to the child while `enroll` waits (does the child still get SIGINT when the parent is in the same process group? was it relying on inherited TTY?), ordering between stdout and stderr, exit-code propagation when the child is killed by a signal, and any regression in `packages/app/src/index.ts` callers. For env-file: EXDEV, dangling symlink, permissions of the new file. Report only real defects (severity, file:line, scenario, fix) and end with a verdict.
