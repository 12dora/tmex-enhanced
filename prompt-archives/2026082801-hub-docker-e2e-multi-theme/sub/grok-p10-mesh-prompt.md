# Backend task (gateway mesh + shared link/ws-borsh) — round 7: fix the 5 findings of review-p9

Worktree `/Users/konata/code/tmex-enhanced-wt-merge` (branch chore/merge-hub-tabs, HEAD 530cb3b). Read `AGENTS.md`, `prompt-archives/2026082801-hub-docker-e2e-multi-theme/sub/review-p9.md` (5 findings, all accepted) and `sub/grok-p9-mesh-result.md`. Scope: `apps/gateway/src/mesh/**`, `apps/gateway/src/hub/**`, `apps/gateway/src/auth/user-key-service.ts` (applyMany abort), `packages/shared/src/link/mux.ts`, `packages/shared/src/ws-borsh/**` + `packages/ws-client/src/**` (NODE_EVENT schema) and the minimal frontend patcher `apps/fe/src/node/mesh-nodes.ts` (+ its test). Nobody else edits these. No git operations, no TODOs. Failing-then-passing test per finding.

1. Inbound replacement: when `track()` receives a higher-priority link from the peer while a live link exists, do NOT `retirePeer(prev)` unless `prev` is quiesce-capable (ACKed); otherwise park the new link (keep it authenticated but inactive, or close it) until the fence completes; legacy peers that dial upgrades must not cause in-flight loss. Test: legacy peer initiates upgrade with an OPEN in flight.
2. Applier cancellation: `applyMany`/`list`/`head` accept an `AbortSignal`; `applyMany` checks it between records (and ideally validates the whole batch before committing, committing with a head-CAS); the uplink tracks the RAW applier promises per generation and awaits them on reconnect (bounded) so no two generations mutate the key log concurrently. Test: abort mid-batch → no further records committed; reconnect waits for the in-flight commit.
3. NODE_EVENT: extend the Borsh NODE_EVENT payload (shared schema + encoder/decoder + ws-client types + gateway encoder + fe `mesh-nodes.ts` patcher) with `version`, `direct_capable`, `name` (optional fields, backward compatible if the schema supports optionals; otherwise bump the frame version the way existing frames do — read `packages/shared/src/ws-borsh/schema.ts` conventions). Test encode/decode round trip and the fe patcher.
4. Overflow limiter: per-user overflow state bounded with TTL (sweep) and counted in `size`; per-node fairness inside overflow (e.g. small per-node sub-buckets with a hard cap on distinct nodes, or round-robin quota); rate-limited requests get an explicit `key.log.res` error (`rate_limited`) instead of silence; expose counters for tests.
5. `mux.ts`: async `sendFrame` rejection must transition the mux to closed (`finishClose`) before rejecting; fire-and-forget callers may then ignore the rejection safely. Test with a transport whose send returns a rejected promise without firing onClose → mux ends closed.

Verify: `bun test` in `apps/gateway` (baseline 2292, 0 fail), `packages/shared`, `packages/ws-client`, `apps/fe` (`bun test src/`), tsc ≤ baselines (gateway 21, others 0), biome clean; then ONE remote single-host run `bash /private/tmp/claude-501/-Users-konata-code-tmex-enhanced/741cc3a1-5392-48be-8081-06f3803bdeb4/scratchpad/remote-cycle.sh p10` all-PASS (project `tmex-e2e`, port 18543; leave `tmex-split` alone). Report to `prompt-archives/2026082801-hub-docker-e2e-multi-theme/sub/grok-p10-mesh-result.md`.
