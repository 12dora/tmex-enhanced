# Backend task: persist the node's userId at `hub join` and pass it to the mesh runtime

Worktree `/Users/konata/code/tmex-enhanced-wt-merge` (branch chore/merge-hub-tabs). Read `AGENTS.md` and `prompt-archives/2026082801-hub-docker-e2e-multi-theme/sub/grok-p4-mesh-result.md` §"Finding 6" — it specifies exactly what to do. Scope: `apps/gateway/src/db/schema.ts`, a NEW drizzle migration `apps/gateway/drizzle/0020_node_identity_user.sql` (+ `meta/_journal.json` entry and `meta/0020_snapshot.json`, generated with `bunx drizzle-kit generate` from `apps/gateway` — read `apps/gateway/drizzle.config.*` and the previous migration `0019_hub_auth.sql` / `meta/0019_snapshot.json` for the conventions; the migration must be `ALTER TABLE node_identity ADD COLUMN user_id text` (nullable) so existing rows keep working), the node-identity store in `apps/gateway/src/mesh/**` (`NodeIdentityRecord` / `SaveNodeIdentityInput` / load|save), `packages/app/src/commands/hub.ts` (`commitVerifiedJoin`: save `userId: genesisUid`), `packages/app/src/runtime/assemble.ts` (`assembleTmex`: pass `userId` from the loaded identity to `createMeshRuntime`), and their tests. Another agent is concurrently editing `apps/gateway/src/mesh/peer-manager.ts` / `link-mux*.ts` — do not touch those. No git operations, no TODOs.

Also: `hub user add` on the hub machine self-admits — make sure the hub's own identity row gets `user_id` too (look at where `hub user add` creates the local identity/cert in `packages/app` or `apps/gateway/src/auth`), and `mesh reset-root` keeps/updates it. `resolveUserId` fallback (self cert → unique user) stays as a fallback for pre-existing installs.

Verify: `bun test` in `apps/gateway` and `packages/app` (0 fail; gateway baseline 2263, app 239), tsc ≤ baseline (gateway 21, app 1), biome clean, `bunx drizzle-kit generate` reports no further schema drift after your migration is in place, and a migration test if the repo has a pattern for it (grep `0019` in `apps/gateway/src/**/*.test.ts`). Then ONE remote harness run: `bash /private/tmp/claude-501/-Users-konata-code-tmex-enhanced/741cc3a1-5392-48be-8081-06f3803bdeb4/scratchpad/remote-cycle.sh p6` must be all-PASS. Report to `prompt-archives/2026082801-hub-docker-e2e-multi-theme/sub/grok-p6-userid-result.md`.
