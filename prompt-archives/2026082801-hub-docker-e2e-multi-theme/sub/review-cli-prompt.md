You are a code reviewer for the tmex repo (Bun monorepo). Review commit 764dbba on branch chore/merge-hub-tabs (diff at `/private/tmp/claude-501/-Users-konata-code-tmex-enhanced/741cc3a1-5392-48be-8081-06f3803bdeb4/scratchpad/review-cli-diff.patch`; read-only access to the working tree). It fixes three CLI defects in `packages/app`:
1. `direct enable|disable` ran in the Node.js CLI (`dist/cli-node.js`, built with `--target node`) but used `Bun.write`/`Bun.file` → replaced with node:fs; failures other than "platform unsupported" now exit non-zero; `init` still tolerates failure but prints the real error.
2. `loginWithRootKey` in `lib/hub-client.ts` expected `sid` in the login JSON; the gateway (`apps/gateway/src/mesh/auth-routes.ts`, `session-middleware.ts`) returns `{expires_at}` and conveys the session via `x-tmex-set-session` header (rewritten to a `Set-Cookie: tmex_s_self=…` for via=self). Now parsed from header or Set-Cookie; follow-up requests send `Cookie: tmex_s_self=<sid>` (+ `tmex_s_<nodeId>`).
3. `hub join`/`hub leave` gained `--no-restart`; when no service manager exists they print a hint and exit 0 instead of throwing after state was written.

Verify against the gateway source that (2) is actually correct: cookie names, whether `via=self` requests from the CLI (loopback vs. remote hub over HTTPS) get the header or the cookie, and whether `requireSession`/`authenticateRequest` accepts what the CLI now sends for `POST /api/hub/enrollments` and `GET /api/hub/nodes` on a *remote* hub (the CLI on node-a talks to hub.example over HTTPS — is that via=self from the hub's perspective? does the hub set the cookie with `Secure`, and does the CLI's Cookie header need anything else?). Also check remaining `Bun.` references reachable from the Node entry (`packages/app/src/index.ts` import graph excluding `auth-spawn`), and error/exit-code behaviour. Report only real defects with severity (blocker/major/minor), file:line, failure scenario, fix; skip style nits and purely defensive suggestions. End with a verdict.
