You are a security-minded code reviewer for the tmex repo (Bun monorepo). Review commit cddeba7 on branch chore/merge-hub-tabs (diff at `/private/tmp/claude-501/-Users-konata-code-tmex-enhanced/741cc3a1-5392-48be-8081-06f3803bdeb4/scratchpad/review-p1-diff.patch`; read-only working tree). Context: design doc `docs/hub/2026082700-hub-node-architecture.md` (v3.2) — hub is NOT a trust root; nodes only trust peer certificates backed by an `admit-node` record verified against the user key log; compromise of any single node/hub must not grant access elsewhere. The root cause report is `prompt-archives/2026082801-hub-docker-e2e-multi-theme/sub/grok-p1-mesh-result.md`.

The commit fixes: (1) `node.list` decoding rejected `version: null` and the whole frame was silently dropped (so `hub_meta` never persisted and key-log catch-up never ran); (2) peer_cache is persisted again after key-log catch-up; (3) `onNodeList` no longer deletes the `hub` sentinel row in peer_cache; (4) a `listReach` overlay marks uplink-online, locally-admitted nodes as `reach:'relay'` even without a live link; (5) `key.log.req` gets a 10 s timeout.

Verify: no weakening of certificate/key-log verification (can a malicious hub inject a peer into peer_cache or the node list without a verified admit-node? can `hub_meta` from an unauthenticated frame be persisted? is the 10 s timeout leaving partial state?); correctness of the catch-up ordering and idempotency under repeated node.list; whether the reach overlay misleads the UI or the peer manager (e.g. reporting relay for a node that is actually unreachable, or racing with `getLink` lazy creation); and silent-drop behaviour in `handleCtl` — should decode failures at least be logged? Report only real defects (severity, file:line, scenario, fix) and end with a verdict.
