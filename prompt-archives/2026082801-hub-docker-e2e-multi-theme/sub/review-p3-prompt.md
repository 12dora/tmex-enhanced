You are a reviewer for the tmex repo (Bun monorepo). Review the gateway part of commit 44138c7 on branch chore/merge-hub-tabs (diff at `/private/tmp/claude-501/-Users-konata-code-tmex-enhanced/741cc3a1-5392-48be-8081-06f3803bdeb4/scratchpad/review-p3-diff.patch`; read-only tree; report `prompt-archives/2026082801-hub-docker-e2e-multi-theme/sub/grok-p3-mesh-result.md`; design `docs/hub/2026082700-hub-node-architecture.md`). It adds (1) background upgrade of a live relay peer link to a higher-rank LAN/ws-secure link when endpoints change (`PeerManager.notifyPeerEndpointsChanged`, 10 s cooldown, periodic 15 s scan, old link retired after the new one authenticates) and (2) the self row in `/api/mesh/nodes` reporting live direct_capable/version/inventory from `statusProvider`. Focus: link swap correctness (in-flight streams on the old link, ordering, double-dial races with `getLink`'s own upgrade path, retire vs teardown, reconnect storms when the LAN dial keeps failing — is the cooldown enough? unbounded pending queue?), trust checks on the upgrade dial (same cert/fingerprint verification as the primary dial), timers cleaned up on stop(), and any test gaps. Real defects only (severity, file:line, scenario, fix) + verdict.
