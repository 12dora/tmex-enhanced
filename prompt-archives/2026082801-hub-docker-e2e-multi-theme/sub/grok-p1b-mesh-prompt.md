# Backend task (gateway mesh) — round 2: key-log catch-up still never runs in real containers + review findings

Worktree `/Users/konata/code/tmex-enhanced-wt-merge` (branch chore/merge-hub-tabs). Read `AGENTS.md`, your previous report `prompt-archives/2026082801-hub-docker-e2e-multi-theme/sub/grok-p1-mesh-result.md` (commit cddeba7), and the review `sub/review-p1.md`. You own `apps/gateway/**` (+ `packages/shared/src/**` only if strictly needed). Another agent edits `packages/app/**`. No git operations, no TODOs.

## New evidence from the Docker harness run WITH commit cddeba7 (containers are still up: `tmex-e2e-hub`, `tmex-e2e-node-a`, `tmex-e2e-node-b`, `tmex-e2e-driver`, `tmex-e2e-caddy`; use `docker exec`/`docker logs` freely — read `scripts/hub-e2e/run.sh` and `docs/hub/2026082801-hub-docker-e2e.md` for how the stack works)
Progress: node-a now persists `hub_meta` (its `/api/mesh/nodes` shows the hub row `isHub:true, online:true, reach:'relay'`). node-b is STILL absent. SQLite dump (readonly, `/var/lib/tmex/tmex.db` inside each container):
- hub: `user_key_log` seq 1 reset-root, 2 admit-node(hub self), 3 admit-node(node-a), 4 admit-node(node-b); `node_certs` for all three; `peer_cache` list_version 6 with node-a, node-b, hub sentinel.
- node-a: `user_key_log` seq 1–2 only (its OWN admit record seq 3 is missing too!); `node_certs` only the hub's; `peer_cache` only the hub sentinel (list_version 6).
- node-b: `user_key_log` seq 1–3 (missing its own seq 4); `node_certs` hub + node-a; `peer_cache` hub sentinel + a stale hub row (list_version 0).
So each node's key log is frozen at what the hub had at `hub join` time (`redeem` copies the log up to that point) and the post-join `key.log.req` catch-up NEVER brings new records over a real WSS uplink (through Caddy), although `mesh.integration.test.ts` (in-process `Bun.serve`) passes. The `node.list` frame IS processed (list_version 6 persisted). Node logs show nothing because `handleCtl` swallows errors. Possible angles: is `key.log.req` even sent (does `ingestNodeList` compare `target.seq`/`keyLogHeadHash` correctly against the local head — check what the hub puts in `node.list` for the key-log head vs what the node compares); does the hub's `key.log.req` handler reply on this link (`apps/gateway/src/hub/uplink-server.ts`); does the 10 s timeout hit and get treated as success (review #1); does `applyMany` reject the records (signature/prev-hash check against the node's local head — note the node's log lacks its own admit record, so the hash chain must be contiguous from seq 3); are frames larger than some limit dropped by Caddy/WS?

## Task
1. First add real diagnostics: in `UplinkClient.handleCtl` distinguish decode errors from handler errors and `console.warn` (rate-limited) with message type, length and error message (never the full payload); log key-log catch-up start/target/result and `applyMany` rejections with reason. Rebuild + rerun the harness to get the evidence (see below), then root-cause and fix.
2. Fix the review findings from `sub/review-p1.md`:
   - #1 (high): a `key.log.req` timeout must NOT call `finishNodeList()` as if synced; treat as error, retry with backoff (bounded) or drop the uplink to force a clean reconnect; add a request correlation id or discard late responses.
   - #2: a newer `node.list` received while an older one is still in catch-up must win; persist only the latest snapshot per connection generation.
   - #3: REMOVE the speculative `listReach` overlay (`reach:'relay'` without a live link) — `reach` must only reflect an established link; keep `online` from the hub list. (Scenario 3f in the harness passed without the overlay in the first run.)
   - #4: gate `node.list` / key-log / RTC / enrollment frames on an explicit per-connection `authenticated` flag set when `auth.ok` is processed; cleared on disconnect.
   - #5: covered by step 1.
3. Verify with the real harness — you MAY use local Docker (nobody else is using it now): `bun run build && (cd packages/app && npm pack --pack-destination /private/tmp/claude-501/-Users-konata-code-tmex-enhanced/741cc3a1-5392-48be-8081-06f3803bdeb4/scratchpad/pkg4)` then `TMEX_TARBALL=/private/tmp/claude-501/-Users-konata-code-tmex-enhanced/741cc3a1-5392-48be-8081-06f3803bdeb4/scratchpad/pkg4/tmex-cli-1.0.2.tgz bash scripts/hub-e2e/run.sh` (≈15–25 min on qemu; output in `scripts/hub-e2e/out/`, report `out/report.md`). Never touch the machine's production tmex or the tmux session named `tmex`. Goal: scenarios 4b/4c/4d and 6a/6b/6c PASS (4c requires `reach:'lan'` once the `lan` network is connected — if that specific part fails for a reason unrelated to your fix, document it precisely). Iterate until green or until you have a precise, evidenced explanation.
4. `bun test` in `apps/gateway` 0 fail, tsc ≤ 23, biome clean.

Report to `prompt-archives/2026082801-hub-docker-e2e-multi-theme/sub/grok-p1b-mesh-result.md`: root cause with file:line, evidence lines from container logs, changes, tests, final harness table.
