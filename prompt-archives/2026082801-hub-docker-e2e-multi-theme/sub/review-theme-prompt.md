You are a code reviewer for the tmex repo (Bun monorepo, React 19 + Tailwind v4 + Zustand; Ghostty-wasm terminal). Review commit dd6c134 on branch chore/merge-hub-tabs, which adds multi-theme support. The diff (excluding generated CSS/i18n files) is at `/private/tmp/claude-501/-Users-konata-code-tmex-enhanced/741cc3a1-5392-48be-8081-06f3803bdeb4/scratchpad/review-theme-diff.patch`; you can also read the full working tree (read-only).

Design decisions already made (do NOT dispute them, review the implementation against them):
- Server-side site `theme` stays `'dark'|'light'` (appearance) and keeps driving `.dark`, tmux DECSET 997, OSC 11. Named color schemes are a per-browser preset stored in localStorage (`useUIStore().themePreset`), applied via `data-theme-preset` on <html>. Each preset has a fixed appearance; choosing a preset also updates the site appearance; if the server changes appearance to something that mismatches the preset, the preset resets to null.
- Terminal palette: `resolveTerminalTheme(appearance, preset)` gives full colors; gateway-side tmux window style / OSC 11 remain appearance-only (known, accepted inconsistency).

Focus on real defects, ranked by severity: runtime errors, wrong state transitions (race between updateTheme and setThemePreset, rehydrate ordering with main.tsx pre-mount DOM application, S2C reset rule), stale-closure/effect dependency bugs in `useTerminalBootSurface`/`TerminalPreview`, memory leaks, a11y/keyboard problems in the Base UI dropdown usage, CSS specificity problems (preset blocks vs `.dark` vs `data-theme-radius`), broken e2e selectors, and test gaps. Point out anything in the palettes that is clearly wrong (e.g. ANSI slot mismatched). Skip style nits and skip suggestions that only add defensive checks without a concrete failure scenario. For each finding: severity (blocker/major/minor), file:line, concrete failure scenario, suggested fix. Finish with a short overall verdict.
