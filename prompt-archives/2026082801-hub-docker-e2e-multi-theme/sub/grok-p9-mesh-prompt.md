# Backend task (gateway mesh) — round 6: fix the 4 findings of review-p8

Worktree `/Users/konata/code/tmex-enhanced-wt-merge` (branch chore/merge-hub-tabs, HEAD 97f5cda). Read `AGENTS.md`, `prompt-archives/2026082801-hub-docker-e2e-multi-theme/sub/review-p8.md` (4 findings, all accepted) and `sub/grok-p8-mesh-result.md`. Scope: `apps/gateway/src/mesh/**`, `apps/gateway/src/hub/**` (+ tests); `packages/shared/src/auth/**` only if the transcript change requires it. Nobody else edits these now. No git operations, no TODOs. Failing-then-passing unit test per finding.

1. Quiesce capability: remove the unauthenticated raw `caps` from the handshake JSON (or bind it into the signed transcript/HKDF with a versioned schema and downgrade protection). Capability is only trusted after the encrypted `link.hello` ACK on the authenticated link. Until an ACK is received, NO path (including `getLink` user path with `streams === 0`) may replace an existing link; peers that never ACK keep their existing link (no auto-upgrade) — user-path may still dial a NEW link only when no link exists.
2. Catch-up generation: pass `(generation, epoch, userId, AbortSignal)` into every helper (`pushMissingToHub`, apply/list/head wrappers); check before every `appendAndAck`/send and after every await; abort/timeout applier calls on teardown; track outstanding catch-up promises per generation and await/cancel them on reconnect instead of just resetting `catchUpChain`.
3. NODE_EVENT de-dup: de-duplicate only the synthesized offline events per connection generation; normal `node.list`-driven events must be emitted whenever the projected event payload (online/reach/version/inventory/direct_capable/name) changes; give the last-emitted map a lifecycle (remove on revoke, clear on stop, bounded).
4. Hub key-log rate limiting: when the LRU is full, do not create a fresh full bucket — fall back to a shared non-evictable overflow bucket (per user or global) so that > capacity fleets/attackers cannot reset burst by cycling; make capacity configurable with a sane default; test with 1025 nodes.

Verify: `bun test` in `apps/gateway` 0 fail (baseline 2283), tsc ≤ 21, biome clean; then ONE remote single-host harness run `bash /private/tmp/claude-501/-Users-konata-code-tmex-enhanced/741cc3a1-5392-48be-8081-06f3803bdeb4/scratchpad/remote-cycle.sh p9` all-PASS (project `tmex-e2e` on the server; the `tmex-split` project may be in use concurrently — leave it alone). Report to `prompt-archives/2026082801-hub-docker-e2e-multi-theme/sub/grok-p9-mesh-result.md`.
