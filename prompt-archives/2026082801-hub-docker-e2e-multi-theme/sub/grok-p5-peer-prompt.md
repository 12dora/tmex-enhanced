# Backend task (gateway peer-manager only) — review fixes for the relay→LAN upgrade (commit 44138c7)

Worktree `/Users/konata/code/tmex-enhanced-wt-merge` (branch chore/merge-hub-tabs). Read `AGENTS.md`, `prompt-archives/2026082801-hub-docker-e2e-multi-theme/sub/review-p3.md` (3 findings, all accepted) and `sub/grok-p3-mesh-result.md`. **Scope: ONLY `apps/gateway/src/mesh/peer-manager.ts`, `peer-manager*.test.ts`, and `link-mux*.ts` (+ tests) if a wire-level fence is needed.** Another agent is concurrently editing `uplink-client.ts`, `mesh-runtime.ts`, `mesh-routes.ts`, `uplink-server.ts` — do NOT touch those; if `mesh-runtime.ts` needs a change, describe it in the report. No git operations, no TODOs. Do NOT run the Docker harness (local or remote) — the commander runs it after both agents land; unit tests only.

1. (P1) Link replacement loses in-flight streams: `retirePeer()` closes the old link as soon as the local `streams` count is 0, but a stream `OPEN` may still be in flight to the peer (which then answers `replaced`/`stale-link`). Implement a quiesce: after the new link is authenticated, mark the old link retiring (no new streams), keep it open for a grace window (e.g. until `streams === 0` for ≥ 2 s AND at least 5 s since retire, bounded by a 30 s hard cap), and on the receiving side keep accepting `OPEN` on a retiring/replaced link until it is actually closed (never `stale-link` a link that is still open). If a proper fence in `LinkMux` is cheap (a control frame the peer acks after processing earlier OPENs), prefer it. Tests with a delayed-delivery in-memory transport: single-sided upgrade with an OPEN in flight; both sides upgrading simultaneously.
2. (P1) Rate limiting: apply a minimum interval between upgrade dials per peer regardless of endpoint changes (coalesce changes that happen while pending/cooling into one later attempt), exponential backoff with jitter on failure (cap ~5 min), a global semaphore for concurrent upgrade dials (e.g. 4), and cap accepted endpoints per peer (count ≤ 16, length ≤ 256 each). Tests: alternating endpoints 20× within cooldown → ≤ 2 dials; failures back off; periodic scan with 10 relay peers respects the semaphore.
3. (P2) `lastAdvertisedStatusJson` must be per `LivePeer` (hash of the last status sent on that link), so a peer that did not receive the update still gets it on the next refresh. Three-node test: A connected to B and C, A's endpoints change, only B's link is rebuilt → C still receives the new status on the next refresh.

Verify: `bun test` in `apps/gateway` (0 fail; the other agent's concurrent edits may temporarily break unrelated files — re-run and report per-file results if so), tsc ≤ 21, biome clean. Report to `prompt-archives/2026082801-hub-docker-e2e-multi-theme/sub/grok-p5-peer-result.md`.
