# 第十九轮 prompt 存档

## 2026-09-02（用户）

继续开发, 请你分批commit并在最后push替换本机tmex, Think in English, Send English prompt.
任务：
1. 设置-通用： 站点名称，访问url没有和节点互联联动。 例如tmexhub-sh的站点名称应该和节点名称联动， 在此处修改等于修改节点名称；站点访问url页应联动（在 hub 模式下此处应不能修改）
2. 点击接入设备有概率跳出Application Error，且该错误界面提示开发者可以改善错误页面，请你顺便改善
3. 设置-多节点互联
    1. 本机-目前有加入地址，Hub更改地址，当前 Hub（也有地址）3 个地址让人困惑，请你对应精简修改合并
    2. 本机-直连插件目前 pill 太多，请你将本平台支持/已安装合并（如未安装显示未安装，不支持显示不支持，已安装显示已安装+版本）；删掉已启用 pill 并把下方直连开关移到此处（“启用”+开关）
    3. HTTPS 里文案同样让人困惑例如“对外 HTTPS…已通过当前请求确认”，内置监听器:”未监听“等请你把标题改为 HTTPS 设置， 并统一优化文案
        1. 当前 hub 实际上是装在 lxd 容器外，但是通过 lxd 容器内的宝塔面板续期证书等。产生了割裂，使系统以为是通过反向代理（这种检测也不一定正确），请你改为由 tmex 里设置并续期 LE 证书，并拆除原宝塔面板的网站，证书，续期（仅拆除该域名相关！）
    4. 有时配置了域名的节点可能并不想对外提供 http 服务，而只是想提供 hub 或者 node 的服务，请你增加允许域名访问的开关，关闭后不通过域名对外提供服务（但是内网 ip 地址依旧可访问）
        1. 在本机 box 最下方新增”通用设置“行， 该行新增允许域名访问开关（关闭要警告用户，防止用户正在通过域名访问，关闭后造成失联）
    5. 节点管理-Hub 集群，主 Hub pill 删除后面的写入等文字， 仅保留像备hub 那样显示 ”hub名 + 主/备Hub”
    6. 节点管理表格，把重命名替换成更多按钮，点击弹出节点详细设置弹窗，包括名称编辑框，第 4 点的允许域名访问开关（注意如果用户没有编辑，即使点击保存也不触发保存逻辑，避免例如每次点击保存，虽然根本没有改名，但还是走了一遍重命名逻辑， 其他选项同理）
        1. 3 个按钮排列为升级，更多，移除
    7. 接入设备页面-移动设备 tab，新增二维码，用户扫码即可快速跳转到域名。如果有多个地址，你需要在上上一步让用户先选择 1 个地址。请你据此重新设计 iOS/Android步骤
    8. 我现在正在另一台电脑通过网页版访问tmexhub-sh左上角延迟一般是1Xms，但有时候会突然暴增到 200 多 ms，这个延迟具体测量的是什么（请你增加tooltip hover显示进行简短解释），请你排查优化延迟提升的问题
注:
1. cursor （grok 4.6, high）担任后端编码
2. opus5(high)担任前端编码
3. codex（gpt-5.6-luna,  xhigh)探索代码
4. codex（gpt-5.6-sol,  high)担任code reviewer, codex存在过度防御的问题, 你应该自行判断问题是否修复
5. 你担任指挥官和planner, 激进并行
6. 为避免压缩轮数太多影响会话质量, 请你在适当时候开新会话, 而不是无限继续用老会话

## 2026-09-03（用户，追加）

继续，并追加任务：1.之前还有known-issue，在项目文件里有记录，一并修复 2. 局域网直连探测打向不可达地址。 tmex 反复尝试连接 13 个对端内网地址，包括 Docker 网段和运营商级 NAT 地址，端口 39001，每次等 20 秒超时，全天 1693 次。应该按可达性做负向退避缓存。
3.  WebRTC 熔断阈值形同虚设。 3.5 小时内拨号 1875 次，数据通道失败 2096 次，而熔断器只触发了 4 次。
