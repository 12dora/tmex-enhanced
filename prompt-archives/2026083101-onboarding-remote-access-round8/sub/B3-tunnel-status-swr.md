## Common rules (read first)

- Worktree: /Users/konata/code/tmex-enhanced-wt-r8 (branch feat/round8-onboarding). Other agents are editing OTHER files in this same worktree in parallel. Touch ONLY the files listed in your scope. NEVER run git commands that change state (no add/commit/stash/checkout/reset). Do not touch files outside your scope; if you believe a change outside scope is required, describe it in your result file instead.
- Runtime is Bun only. Tests: `bun test <path>` inside the package dir (for apps/fe use `bun test src/...`, never bare `bun test` there). Typecheck: `bunx tsc --noEmit -p .` in the package dir; count `error TS` lines before and after your change — must not increase (baselines: apps/fe 0, apps/gateway 21, packages/app 1). Lint only the files you changed: `bunx biome check <files>` from the repo root (fix with `--write`). macOS has no `timeout` command.
- i18n: locale JSON lives in packages/shared/src/i18n/locales/{en_US,zh_CN,ja_JP}.json. Keys must stay in sync across all three. After editing JSON run `bun run --filter @tmex/shared build:i18n` from the repo root to regenerate resources.ts/types.ts (never hand-edit or lint those generated files). Edit JSON with targeted edits; do not reformat the whole file.
- Copy style (all languages): short, professional, no filler. Chinese: use 「本机」 not 「这台机器」, avoid 「你/你的」, full-width Chinese punctuation. English: plain, direct. Japanese: polite です/ます form, concise.
- Code comments: only where logic is non-obvious; write them in Simplified Chinese (project convention). Variable names in English.
- No laziness: no TODOs, no "simplified version", no skipped tests. Finish the whole scope.
- When done, write a concise Markdown result report (English) to the path given in your task: what changed (file list), test/tsc/lint results with numbers, anything out of scope you noticed. Write the result file as your LAST action.

## Task B3 — make GET /api/tunnel/status fast: stale-while-revalidate external tunnel detection + Cloudflare probe timeouts

Problem (from profiling report sub/E5-result.md, read the "Remote access" section and the P0 items): the settings "Remote access" tab renders only a spinner until `GET /api/tunnel/status` resolves. The route awaits `manager.refreshExternal()` (apps/gateway/src/api/tunnel-routes.ts ~162-172 → apps/gateway/src/tunnel/manager.ts ~578-584). The detector (apps/gateway/src/tunnel/external-detect.ts) caches for 30s, but on first request / after expiry it synchronously runs `ps`, scans launchd/systemd dirs, reads cloudflared config/token files, and makes several sequential Cloudflare API calls (tunnel-name lookup, ingress lookup, Access probe via listApps with up to 50 pages + getOrganization) with NO timeout (apps/gateway/src/tunnel/access-client.ts ~431-480). Multi-second tab loads result.

Scope (files you may edit): `apps/gateway/src/tunnel/external-detect.ts` (+ its test), `apps/gateway/src/tunnel/manager.ts` (+ test), `apps/gateway/src/api/tunnel-routes.ts` (+ test), `apps/gateway/src/tunnel/access-client.ts` (+ test), and `packages/shared/src/contracts/tunnel.ts` ONLY to add an optional additive field (see 3). Nothing else. Another agent is concurrently editing packages/app/src/runtime/* and apps/gateway/src/mesh/auth-routes.ts — do not touch those.

Required changes:
1. Stale-while-revalidate in the external detector: keep the cached result and serve it immediately whenever any cached value exists (even expired); when the TTL has expired, kick off ONE background refresh (single in-flight promise shared by concurrent callers; errors logged, last good result retained). Only the very first call with no cache at all may await detection — and even then cap the awaited time: if detection takes longer than ~1.5s, return a "not yet detected" result (`detected:false` + a freshness flag) and let the background refresh complete; subsequent status polls (the UI polls every 10s / 2s) pick up the result. Keep an explicit `force` refresh path for user actions that need fresh data (e.g. adopt/check actions) — read manager.ts to see which callers need a guaranteed fresh detection and keep them awaiting.
2. Warm the cache at manager startup (fire-and-forget after the gateway is listening; must not delay startup and must be safe when the platform is unsupported).
3. Expose freshness: add an optional additive field to the status contract's `external` object, e.g. `probing?: boolean` (true while a refresh is in flight and the served data is stale/absent). Additive only — do not change existing fields. Front-end consumption is out of scope.
4. Cloudflare client timeouts: every request in access-client.ts gets `AbortSignal.timeout(3000)` (make the budget a constant); `listApps` pagination gets a total deadline (~6s) and stops early with what it has (document that the result may be partial via the existing return shape if possible — do not break callers). The detection's Access probe must treat timeout as `unknown`, never as "not covered".
5. Tests: bun:test with mocked fetch/spawn as the existing tests in those files do (read them first): cached-value served while stale + single background refresh (concurrent calls → one detection run), first-call cap returns placeholder then later call returns detection, timeout → unknown, startup warm does not throw on unsupported platform.
6. Verify: `cd apps/gateway && bun test src/tunnel src/api/tunnel-routes.test.ts` all green; `bunx tsc --noEmit -p .` (baseline 21 errors, no increase); biome on changed files; `bun scripts/complexity/gate.ts` from repo root — if a function you touched exceeds its lock, refactor rather than raising; report any remaining failure.

Result file: /Users/konata/code/tmex-enhanced-wt-r8/prompt-archives/2026083101-onboarding-remote-access-round8/sub/B3-result.md
