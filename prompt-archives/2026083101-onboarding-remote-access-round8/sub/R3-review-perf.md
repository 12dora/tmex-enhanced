You are a senior code reviewer. READ-ONLY. Deliver the review as your FINAL MESSAGE (Markdown, English). Do not attempt to write files.

Review the diff at prompt-archives/2026083101-onboarding-remote-access-round8/sub/R3-perf.diff (Bun/TypeScript gateway + CLI runtime). Purpose: make settings-page status endpoints fast. (1) apps/gateway/src/tunnel/external-detect.ts: external cloudflared tunnel detection becomes stale-while-revalidate (serve cached even if expired, single in-flight background refresh, first uncached call capped at 1.5s returning `{detected:false, probing:true}`, `force` path for adopt/sync actions); manager.ts warms the cache fire-and-forget at start; contract gains optional `external.probing`. (2) apps/gateway/src/tunnel/access-client.ts: 3s per-request AbortSignal timeouts, 6s total deadline for listApps pagination with `truncated` flag, Access probe treats timeout/truncated-without-match as unknown. (3) packages/app/src/runtime/local-routes.ts: Promise.all for local status + TLS status. (4) packages/app/src/tls/tls-service.ts: per-instance 10s status projection cache with invalidation on mutations + generation guard. (5) apps/gateway/src/mesh/auth-routes.ts + new auth-mode-cache.ts: 5s cache of request-independent parts of GET /api/auth/mode (TLS info, local-auth payload, primary user, hub meta), per-origin passkey flags recomputed, invalidated on local-auth mutations / key-log apply / setTlsInfo / setLocalAuthStore.

Focus on real defects only, ranked BLOCKER / SHOULD-FIX / NIT with file:line and concrete fix: correctness of the SWR state machine (races between force refresh and background refresh, error handling that could wedge `inFlight` forever, cache timestamp semantics), any path where stale/placeholder data could cause a wrong *security* decision (e.g. exposure protection, Access "covered" vs "unknown", adopt actions using stale data), timeout handling (AbortSignal on Bun fetch, partial results), cache invalidation gaps that would show wrong auth mode after a mutation for longer than the TTL (list the specific mutation paths in the repo that write users/certs/TLS without invalidating), per-instance vs module-level state leaking across tests, and Promise.all error semantics. Verify each claim against the actual source in the worktree. Skip praise and style nits.
