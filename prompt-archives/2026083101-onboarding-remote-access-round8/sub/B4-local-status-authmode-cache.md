## Common rules (read first)

- Worktree: /Users/konata/code/tmex-enhanced-wt-r8 (branch feat/round8-onboarding). Other agents are editing OTHER files in this same worktree in parallel. Touch ONLY the files listed in your scope. NEVER run git commands that change state (no add/commit/stash/checkout/reset). Do not touch files outside your scope; if you believe a change outside scope is required, describe it in your result file instead.
- Runtime is Bun only. Tests: `bun test <path>` inside the package dir (for apps/fe use `bun test src/...`, never bare `bun test` there). Typecheck: `bunx tsc --noEmit -p .` in the package dir; count `error TS` lines before and after your change — must not increase (baselines: apps/fe 0, apps/gateway 21, packages/app 1). Lint only the files you changed: `bunx biome check <files>` from the repo root (fix with `--write`). macOS has no `timeout` command.
- i18n: locale JSON lives in packages/shared/src/i18n/locales/{en_US,zh_CN,ja_JP}.json. Keys must stay in sync across all three. After editing JSON run `bun run --filter @tmex/shared build:i18n` from the repo root to regenerate resources.ts/types.ts (never hand-edit or lint those generated files). Edit JSON with targeted edits; do not reformat the whole file.
- Copy style (all languages): short, professional, no filler. Chinese: use 「本机」 not 「这台机器」, avoid 「你/你的」, full-width Chinese punctuation. English: plain, direct. Japanese: polite です/ます form, concise.
- Code comments: only where logic is non-obvious; write them in Simplified Chinese (project convention). Variable names in English.
- No laziness: no TODOs, no "simplified version", no skipped tests. Finish the whole scope.
- When done, write a concise Markdown result report (English) to the path given in your task: what changed (file list), test/tsc/lint results with numbers, anything out of scope you noticed. Write the result file as your LAST action.

## Task B4 — speed up the settings "Multi-node" tab data: /api/local/status waterfall and /api/auth/mode recomputation

Profiling report: sub/E5-result.md, sections "Nodes" and P1 items "Remove the local-status backend waterfall" and "Cache or snapshot auth mode". Read them first.

Scope (files you may edit): `packages/app/src/runtime/local-routes.ts` (+ test), `packages/app/src/tls/tls-service.ts` (+ test), `apps/gateway/src/tls/tls-config-store.ts` only if needed for invalidation hooks (+ test), `apps/gateway/src/mesh/auth-routes.ts` (+ test), and whatever small module you add next to them for the cache (e.g. `apps/gateway/src/mesh/auth-mode-cache.ts`). Do NOT touch apps/gateway/src/tunnel/**, apps/gateway/src/api/tunnel-routes.ts, or packages/app/src/commands/** (other agents own them).

Required changes:
1. `packages/app/src/runtime/local-routes.ts` (~68-75): `getLocalStatus(deps)` and `deps.tlsStatus()` are independent — run them with `Promise.all`. Keep error semantics (if either rejects, the route's existing error path applies; read the handler).
2. TLS status projection cache: `tlsStatus()` in `packages/app/src/tls/tls-service.ts` (~197-237) re-reads DB rows and parses certificates every call. Add a short-lived in-memory cache (TTL ~10s) of the computed status, invalidated synchronously by every mutation path in the same service (issue/rotate/import/remove/config changes — enumerate them by reading the file; add an `invalidate()` helper and call it from each). The cache must be per service instance (no module-global that leaks across tests/instances). Tests: cached within TTL, invalidated on mutation, expires after TTL (use injectable `now`).
3. `/api/auth/mode` (`apps/gateway/src/mesh/auth-routes.ts` ~157-203, primary-user lookup ~800-814): the handler awaits TLS info and scans certificates/nodes + several DB reads on every request with no cache. Add a short TTL cache (~5s) of the derived mode response parts that are expensive (TLS info + primary-user derivation), keyed appropriately (the response may vary by request context — read carefully: e.g. per-origin passkey flags or per-session fields must NOT be cached; cache only the request-independent derivation). Invalidate on the mutation paths in this file (bootstrap/toggle local auth, user creation/admission, membership changes) — enumerate by reading auth-routes.ts and its neighbors; a conservative approach is a module-level `invalidateAuthModeCache()` exported and called from those handlers plus a TTL safety net. Tests (existing auth-routes.test.ts patterns): cached response reused within TTL, invalidated after a mutation, per-request fields still correct.
4. Verify: `cd packages/app && bun test src/runtime src/tls` and `cd apps/gateway && bun test src/mesh/auth-routes.test.ts src/tls` all green; tsc baselines (app 1, gateway 21) not exceeded; biome on changed files; `bun scripts/complexity/gate.ts` from repo root — refactor rather than raise locks if a function you touched exceeds its lock; report any remaining failure.

Result file: /Users/konata/code/tmex-enhanced-wt-r8/prompt-archives/2026083101-onboarding-remote-access-round8/sub/B4-result.md
