You are exploring a Bun + TypeScript monorepo (tmex — a web terminal/tmux gateway with multi-node "mesh" (hub/node) networking and Cloudflare Tunnel remote access). READ-ONLY. Write your final report (Markdown, in English, concise, with exact file paths/line numbers and exact CLI commands/flags) to /Users/konata/code/tmex-enhanced-wt-r8/prompt-archives/2026083101-onboarding-remote-access-round8/sub/E4-result.md. Finish by writing that file.

Goal: we will write an in-app step-by-step onboarding guide "Connect more devices" with two tabs: (1) "Mobile device (control only)" — how to add tmex as a PWA on iOS (Safari → Share → Add to Home Screen) and Android (Chrome → Install app), and what a mobile device can/can't do (control only — no local tmux); (2) "Server or computer" — how to install the tmex package, then two modes: Mode 1 "join an existing relay/hub": stand up a hub on a public server and enroll THIS machine as a node; Mode 2 "make this machine the hub": expose this machine via Cloudflare Tunnel or direct IP, then let other machines join. The guide must reflect the REAL commands and UI flows in this codebase. Find:
1. Install: how a user installs tmex today (README, packages/app/src/commands/*: `install`, `upgrade`, `service`, flags like `--port`, `--lang`, `--yes`), required prerequisites (Node/bun versions), where it installs (~/Library/Application Support/tmex on macOS; Linux paths?), service manager (launchd/systemd), default port 9883.
2. Hub/mesh: how a machine becomes a hub (Settings → multi-node tab: enable hub? `tmex hub`/`tmex node` CLI commands? packages/app/src/cli-node.ts, commands/node*, apps/gateway/src/mesh/**, hub routes). What is the "public address" of the hub and how it is set (hubHint in remote-access says "set hub public address to the hostname"). How enrollment works: enrollment tokens generated in the UI (Settings → nodes tab → "add node"/"generate token"?), and the node-side command to enroll (e.g. `tmex node enroll --hub https://... --token ...`), and approve/allowlist flow on the hub side (pending nodes, allowlist). Give the exact i18n keys / UI labels of those buttons (packages/shared/src/i18n/locales/zh_CN.json under settings.nodes.* or similar) so the guide can reference the real button names.
3. PWA: is there a web manifest / service worker in apps/fe (vite-plugin-pwa? public/manifest.webmanifest)? What is the app display name? Any existing iOS PWA-specific handling worth mentioning (e.g. "standalone" mode detection) — just to confirm PWA install is supported.
4. Remote access (Cloudflare Tunnel / direct): the API + UI flow summary (Settings → remote access) so Mode 2 can say "open Settings → Remote access → choose Cloudflare Tunnel or Direct connection".
5. Also confirm the login/auth story: when a hub is enabled, does access require login (node login)? Where do users get created (first user bootstrap)?
Provide a proposed guide outline (steps, with exact commands and exact UI paths) at the end. Do not modify files.
