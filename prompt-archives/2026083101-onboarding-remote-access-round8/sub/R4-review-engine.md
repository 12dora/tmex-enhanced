You are a senior code reviewer with a security mindset. READ-ONLY. Deliver the review as your FINAL MESSAGE (Markdown, English). Do not attempt to write files.

Review the diff at prompt-archives/2026083101-onboarding-remote-access-round8/sub/R4-engine.diff (React 19 + Bun front-end of a mesh/hub system). Purpose: the node-enrollment "engine" (certificate watch via mesh push + 5s poll, and the `admit-node` signing pipeline into a hash-chained, sequence-numbered key log) was component-scoped in the Settings → Nodes management page. It is now a module-level singleton (`apps/fe/src/node/enrollment-engine.ts`) so a second UI (the "Connect More Devices" side panel, `apps/fe/src/components/side-panels/connect-devices/join-token.tsx`) can show pending/confirm/admitted state and trigger manual confirmation WITHOUT ever producing two `admit-node` records at the same key-log head (a `seq_gap` fork is unrecoverable). Security invariants (from the design): `admit-node` is signed only when `certificate.enroll_pk` matches a local pending, `cert_sig` verifies, and the pending is unexpired; unknown certificates are alerted and ignored; passkey signers cannot auto-sign (needs a user gesture) and must go through manual confirm; root-key signers auto-sign.

Verify against the actual source (also read `apps/fe/src/node/enrollment.ts`, `enrollment-watch.ts`, `apps/fe/src/auth/key-log-actions.ts`, `credential-prompt.ts`, and the previous implementation via `git show HEAD~1:apps/fe/src/pages/settings/nodes/management/use-admit-action.ts`). Focus on real defects, ranked BLOCKER / SHOULD-FIX / NIT with file:line and concrete fix:
- Any path where two admit records could be produced for one certificate (in-flight lock lifetime vs async signing, lock released on error then retried by the other consumer, manual confirm racing auto-sign, poll and push delivering the same outcome, hubUnconfirmed retry), or where the lock could wedge forever (exception before release, unmount mid-sign).
- Active-context selection: is the context used for signing always the one whose credentials the user actually authenticated (e.g. panel registered last but the user clicks confirm on the management page — which prompt/signer is used?), stale closures captured in the context, contexts of unmounted components still being called (setState on unmounted / prompt dialog gone).
- Refcounted loop lifecycle: start/stop correctness under StrictMode double-mount, hubApi changes, no consumers → loop stopped and timers cleared; expiry sweep timer moved into the engine — no leaks, correct rescheduling.
- State exposure via useSyncExternalStore: snapshot identity stability (infinite re-render risk), reset helper completeness for tests, module state leaking across tests.
- Panel step 6: shows only the pending created in that session — is that mapping robust after a refresh (sessionStorage) or when the token expires; manual confirm button only when the certificate has actually arrived and auto-sign is impossible; error copy correct.
- Behavioural parity with the deleted `use-admit-action.ts`/`useEnrollmentWatch` (toasts, `onDone` refresh, hubUnconfirmed handling, cancelled/expired cleared ids).
Skip praise and style nits.
