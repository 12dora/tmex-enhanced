You are a senior code reviewer. READ-ONLY. Deliver the review as your FINAL MESSAGE (Markdown, English). Do not attempt to write files.

Review the diff at prompt-archives/2026083101-onboarding-remote-access-round8/sub/R2-dist.diff (Bun/TypeScript monorepo). Context: this repository is a fork (github.com/12dora/tmex-enhanced); the npm package `tmex-cli` belongs to upstream and can no longer be used. The change moves distribution to this repo's GitHub Releases: (1) gateway `apps/gateway/src/system/update-check.ts` queries releases/latest and `upgrade.ts` downloads the release tarball, extracts with tar, and runs `package/bin/tmex.js upgrade --apply-current-package` detached; (2) CLI `packages/app`: `upgrade` downloads the release tarball and re-execs the extracted CLI; `init`/`upgrade --apply-current-package` copy the CLI (package.json, bin/, dist/cli-node.js) into `<installDir>/cli/` and write a `~/.local/bin/tmex` shim (+ symlink into ~/.bun/bin), `uninstall` removes them; join command strings become `tmex hub join …`; (3) root `install.sh` (curl | bash) and `.github/workflows/release.yml`; (4) version 1.1.0. Shared constants live in packages/shared/src/release/source.ts.

Focus on real defects, ranked BLOCKER / SHOULD-FIX / NIT with file:line and a concrete fix:
- Correctness of the download/extract/exec flows (redirect handling of GitHub asset URLs, tar invocation and path safety, stage dir lifetime vs detached child, exit-code propagation, error messages), and whether `upgrade --apply-current-package` still works when invoked from the extracted tarball (resolvePackageLayout expectations: package.json name/bin, dist/runtime, resources).
- The shim: PATH handling, quoting, node vs bun selection (the CLI bundle targets node; the installed runtime needs bun ≥1.3), idempotence on re-install, uninstall cleanup safety (never delete a non-managed file), behaviour when ~/.local/bin does not exist.
- install.sh: `set -euo pipefail` placement, `return 0 2>/dev/null || tmex_install "$@"` sourcing trick, TTY re-attachment for `curl | bash`, argument passthrough, temp dir cleanup, failure modes on macOS bash 3.2 (arrays, `local -a`, `${arr[@]+"${arr[@]}"}`), and the GitHub API rate limit / no-jq parsing.
- release.yml: does `bun run build` in CI produce the runtime + resources the tarball needs (check packages/app/scripts and package.json `files`), version==tag check, gh release create vs upload idempotence, permissions block.
- Anything that still points at npm / `npx tmex-cli` in code paths exercised by users (grep the repo, excluding prompt-archives and generated i18n files).
Verify each claim against the actual source in the worktree before reporting. Skip praise and style nits.
