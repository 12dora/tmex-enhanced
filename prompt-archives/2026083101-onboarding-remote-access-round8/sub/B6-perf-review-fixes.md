## Common rules (read first)

- Worktree: /Users/konata/code/tmex-enhanced-wt-r8 (branch feat/round8-onboarding). Other agents are editing OTHER files in this same worktree in parallel. Touch ONLY the files listed in your scope. NEVER run git commands that change state (no add/commit/stash/checkout/reset). Do not touch files outside your scope; if you believe a change outside scope is required, describe it in your result file instead.
- Runtime is Bun only. Tests: `bun test <path>` inside the package dir (for apps/fe use `bun test src/...`, never bare `bun test` there). Typecheck: `bunx tsc --noEmit -p .` in the package dir; count `error TS` lines before and after your change — must not increase (baselines: apps/fe 0, apps/gateway 21, packages/app 1). Lint only the files you changed: `bunx biome check <files>` from the repo root (fix with `--write`). macOS has no `timeout` command.
- i18n: locale JSON lives in packages/shared/src/i18n/locales/{en_US,zh_CN,ja_JP}.json. Keys must stay in sync across all three. After editing JSON run `bun run --filter @tmex/shared build:i18n` from the repo root to regenerate resources.ts/types.ts (never hand-edit or lint those generated files). Edit JSON with targeted edits; do not reformat the whole file.
- Copy style (all languages): short, professional, no filler. Chinese: use 「本机」 not 「这台机器」, avoid 「你/你的」, full-width Chinese punctuation. English: plain, direct. Japanese: polite です/ます form, concise.
- Code comments: only where logic is non-obvious; write them in Simplified Chinese (project convention). Variable names in English.
- No laziness: no TODOs, no "simplified version", no skipped tests. Finish the whole scope.
- When done, write a concise Markdown result report (English) to the path given in your task: what changed (file list), test/tsc/lint results with numbers, anything out of scope you noticed. Write the result file as your LAST action.

## Task B6 — apply the code-review findings on the settings-latency backend work

Read /Users/konata/code/tmex-enhanced-wt-r8/prompt-archives/2026083101-onboarding-remote-access-round8/sub/R3-result.md (10 findings) and the code it references. Apply ALL of them with the pragmatic scope below. Read each file fully before editing.

Scope (files you may edit): `apps/gateway/src/tunnel/external-detect.ts` (+test), `apps/gateway/src/tunnel/manager.ts` (+test), `apps/gateway/src/tunnel/access-client.ts` (+test), `apps/gateway/src/mesh/auth-mode-cache.ts`, `apps/gateway/src/mesh/auth-routes.ts` (+test), `apps/gateway/src/auth/user-key-persistence.ts` (+test, invalidation hook only), `apps/gateway/src/mesh/mesh-runtime.ts` / `apps/gateway/src/mesh/mesh-deps.ts` ONLY to wire an invalidation callback, `packages/app/src/tls/tls-service.ts` (+test), `packages/app/src/runtime/assemble.ts` ONLY to wire TLS-change → auth-mode invalidation (keep the change minimal). Nothing else.

Fixes:
1. (BLOCKER, finding 1) In manager.ts, the acknowledgement waiver for an externally managed tunnel must not rely on a placeholder/stale/error-fallback `lastExternal.running === false`. Before waiving, run a bounded fresh detection (`detect({ force: true })` with a deadline ~3s); if it times out, errors, or reports `probing`, treat the tunnel as possibly running and require `acknowledgeExposure`. Test both branches.
2. (finding 2) Add an invalidation epoch to the detector: `force` starts (or awaits) a scan from the current epoch only; a scan from an older epoch must not commit its result over a newer one; force failures propagate (throw) instead of resolving stale data. `adopt_external` / `sync_access` keep using the force path.
3. (finding 3) On refresh failure keep the last successful timestamp (do not renew `at`); track a separate `lastAttemptAt` with a short retry backoff (~10s) so failures do not hammer, and never cache the initial empty first-call failure as a success (keep `probing: true` until a real result lands).
4. (finding 4) Make detection non-blocking for the event loop: replace `Bun.spawnSync` for `ps` with async `Bun.spawn` + `await new Response(proc.stdout).text()` (with a deadline via `proc.kill()` on timeout), and switch the default fs helpers used by detection (readdir/readFile/stat) to `node:fs/promises`. Keep the injectable deps shape so existing tests still stub them (adapt tests to async stubs). Confirm the 1.5s first-call cap then actually works (test with a slow async stub).
5. (finding 5) In access-client.ts, when wrapping fetch errors into `TunnelError`, preserve the timeout nature: recognise `TimeoutError`/`AbortError` names, `signal.reason`, and messages matching /timed out|timeout|aborted/i; set a `code`/flag on the wrapped error so `isAbortLike` returns true after wrapping. Test with a thrown `DOMException('The operation timed out.', 'TimeoutError')`.
6. (finding 6) Make `listApps` completeness explicit: return `{ apps, truncated }` (or keep the array but ALWAYS set `truncated` including at the 50-page cap) and make every negative decision and mutation path (`upsertBypassApps`, `jobSyncAccess` in manager.ts, any "no matching app → create" logic) reject/abort with a clear TunnelError when the list is truncated. The read-only probe keeps mapping truncated→unknown.
7. (finding 7) Use a separate, longer timeout budget for non-idempotent Cloudflare mutations (POST/PUT/DELETE: ~15s) versus reads (3s). Full reconciliation of ambiguous POSTs is out of scope — just the budget split + a comment.
8. (finding 8) tls-service.ts: invalidate the status cache both before AND after each mutation completes (store upsert, listener apply/stop, trustProxy load), and refuse to cache a projection computed while a mutation is in flight (mutation counter/epoch). Test: status() during an in-flight mutation is not cached.
9. (finding 9) Wire auth-mode cache invalidation to the underlying mutation sources: (a) user/key-log persistence (`user-key-persistence.ts` apply path) — accept an optional `onChange` callback in its deps and call it; wire the AuthRoutes invalidation in the place where both are constructed (mesh-runtime/mesh-deps); (b) TLS changes — TlsService gets an optional `onStatusChange` callback invoked after mutations; assemble.ts passes a callback that updates the auth routes' TLS info (`setTlsInfo`) or invalidates the cache. Hub metadata updates may stay on the 5s TTL (note it in the report).
10. (finding 10) Make the auth-mode cache generation instance-owned (no module-level state): `AuthModeCache` instance holds its own generation; `invalidateAuthModeCache` becomes a method on the routes/cache instance; update callers/tests.

Verify: `cd apps/gateway && bun test src/tunnel src/mesh src/auth src/api/tunnel-routes.test.ts` all green; `cd packages/app && bun test src/tls src/runtime`; tsc baselines (gateway 21, app 1); biome on changed files; `bun scripts/complexity/gate.ts` from repo root — refactor rather than raising locks; report any remaining failure with exact entries.

Result file: /Users/konata/code/tmex-enhanced-wt-r8/prompt-archives/2026083101-onboarding-remote-access-round8/sub/B6-result.md
