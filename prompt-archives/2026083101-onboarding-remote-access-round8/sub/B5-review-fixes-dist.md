## Common rules (read first)

- Worktree: /Users/konata/code/tmex-enhanced-wt-r8 (branch feat/round8-onboarding). Other agents are editing OTHER files in this same worktree in parallel. Touch ONLY the files listed in your scope. NEVER run git commands that change state (no add/commit/stash/checkout/reset). Do not touch files outside your scope; if you believe a change outside scope is required, describe it in your result file instead.
- Runtime is Bun only. Tests: `bun test <path>` inside the package dir (for apps/fe use `bun test src/...`, never bare `bun test` there). Typecheck: `bunx tsc --noEmit -p .` in the package dir; count `error TS` lines before and after your change — must not increase (baselines: apps/fe 0, apps/gateway 21, packages/app 1). Lint only the files you changed: `bunx biome check <files>` from the repo root (fix with `--write`). macOS has no `timeout` command.
- i18n: locale JSON lives in packages/shared/src/i18n/locales/{en_US,zh_CN,ja_JP}.json. Keys must stay in sync across all three. After editing JSON run `bun run --filter @tmex/shared build:i18n` from the repo root to regenerate resources.ts/types.ts (never hand-edit or lint those generated files). Edit JSON with targeted edits; do not reformat the whole file.
- Copy style (all languages): short, professional, no filler. Chinese: use 「本机」 not 「这台机器」, avoid 「你/你的」, full-width Chinese punctuation. English: plain, direct. Japanese: polite です/ます form, concise.
- Code comments: only where logic is non-obvious; write them in Simplified Chinese (project convention). Variable names in English.
- No laziness: no TODOs, no "simplified version", no skipped tests. Finish the whole scope.
- When done, write a concise Markdown result report (English) to the path given in your task: what changed (file list), test/tsc/lint results with numbers, anything out of scope you noticed. Write the result file as your LAST action.

## Task B5 — apply curated code-review fixes to the GitHub-Releases distribution work

Read the review at /Users/konata/code/tmex-enhanced-wt-r8/prompt-archives/2026083101-onboarding-remote-access-round8/sub/R2-result.md for full context, then apply ONLY the items below (the commander rejected the others — do not implement tar path validation, SHA256 verification, ETag caching, or anything not listed). Read each target file fully before editing.

Scope (files you may edit): `packages/app/src/lib/cli-shim.ts` (+test), `packages/app/src/lib/release-fetch.ts` (+test), `packages/app/src/commands/upgrade.ts` (+test), `packages/app/src/commands/enroll.ts` (+test), `packages/app/package.json`, `packages/app/bin/tmex.js`, `packages/app/src/i18n/index.ts` (+test) for new messages, `apps/gateway/src/system/upgrade.ts` (+test), `apps/gateway/src/api/system.ts` (+test, version validation only), `install.sh`, `packages/app/src/lib/install-script.test.ts`, `.github/workflows/release.yml`, docs: `docs/2026021000-tmex-bootstrap/deployment.md`, `docs/release/2026041300-cli-release-process.md` (command/process text only). Nothing else; other agents are not active but keep discipline.

Fixes:
1. **Shim safety (BLOCKER)**: in `installTmexShim`, `lstat` the target `~/.local/bin/tmex` first: proceed only if absent, or a regular file / symlink whose content contains `TMEX_SHIM_MARKER`; otherwise skip writing, keep going, and return a warning string in the result (log it via the CLI i18n; add key). Same rule for the `~/.bun/bin/tmex` symlink (only replace if absent or ours). Write the shim via temp file + rename in the same directory. Record the install dir in the shim (a comment line `# tmex-install-dir: <path>`), and in `removeTmexShims` accept an optional `installDir` and only remove shims whose recorded install dir matches (when provided). Update uninstall call site to pass its installDir. Tests for: foreign file untouched, foreign symlink untouched, managed shim replaced, uninstall ignores other install dir.
2. **node version gate**: in the shim script and in `install.sh` `tmex_run_init`, prefer `node` only when `node --version` reports major ≥ 20; otherwise use the baked-in bun path (shim) / bun (install.sh). Add `"engines": { "node": ">=20" }` to packages/app/package.json.
3. **Version validation**: one strict semver check (`^\d+\.\d+\.\d+(-[0-9A-Za-z.-]+)?$`) applied to explicit `--version` in release-fetch.ts, to `TMEX_VERSION` in install.sh, and to the version accepted by the gateway upgrade API in `apps/gateway/src/api/system.ts` (reject with 400 on mismatch; `latest` stays a sentinel only where it already is). Tests.
4. **install.sh tag resolution**: resolve the latest tag primarily from the redirect of `https://github.com/<repo>/releases/latest` (`curl -sI` → `location:` header → last path segment, case-insensitive header match), falling back to the API JSON; and make the JSON parse non-greedy: `grep -o '"tag_name"[[:space:]]*:[[:space:]]*"[^"]*"' | head -n 1`. Update the sourced-function unit tests.
5. **install.sh TTY probe**: replace `[ -r /dev/tty ]` with an actual open test (`if exec 3</dev/tty 2>/dev/null; then … <&3`), else `--no-interactive`.
6. **CLI upgrade**: forward `bun-path` in the passthrough flag list; propagate the extracted CLI's exit status (set `process.exitCode = code` and throw a typed error that `bin/tmex.js` maps to that code, or simply set exitCode before throwing and make bin/tmex.js keep a non-zero preset exitCode). Adjust the test that claims to check exit code 7.
7. **Gateway upgrade preflight**: before detaching, verify the extracted package has `package.json` (name `tmex-cli` with a `bin` entry), `dist/cli-node.js`, `dist/runtime/server.js`, `resources/fe-dist`, `resources/gateway-drizzle` (see `packages/app/src/lib/install-layout.ts` resolvePackageLayout for the authoritative list); on failure report error and return to idle. Also handle the detached child's `error` event (state back to idle with the message) and wait for the `spawn` event before marking `executing`. Tests.
8. **Join command quoting**: in `packages/app/src/commands/enroll.ts` quote the URL and token with the existing POSIX quoting helper (`quotePosixShellArg` in lib/install.ts) — keep the plain form readable for normal URLs (only quote when the value contains characters outside `[A-Za-z0-9._:/@%+=-]`). Test.
9. **release.yml**: on the existing-release branch run `gh release edit <tag> --title … --notes-file …` before uploading assets so notes converge.
10. **package.json**: remove `publishConfig`; add `"private": true` only if `npm pack` still works with it (verify by running `npm pack --dry-run` in packages/app); otherwise leave non-private and explain in the report.
11. **PATH hint (NIT)**: only warn when neither `~/.local/bin` nor (if the bun symlink was created) `~/.bun/bin` is on PATH; same in install.sh's final hint (check PATH there too).
12. **Docs**: in the two docs files replace `npx tmex-cli …` commands with `install.sh` / `tmex …` equivalents and replace the npm publish steps with tag push → GitHub Actions release (short, factual edits; keep the rest).

Verify: `cd packages/app && bun test` (baseline 453 pass / 1 pre-existing fail), `bunx tsc --noEmit -p .` (baseline 1); `cd apps/gateway && bun test src/system src/api/system.test.ts` and tsc baseline 21; biome on changed files; `bash -n install.sh`; `bun scripts/complexity/gate.ts` from repo root (refactor instead of raising locks).

Result file: /Users/konata/code/tmex-enhanced-wt-r8/prompt-archives/2026083101-onboarding-remote-access-round8/sub/B5-result.md
