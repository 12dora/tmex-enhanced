You are exploring a Bun + TypeScript monorepo (tmex). READ-ONLY. Write your final report (Markdown, in English, concise but with exact file paths and line numbers) to /Users/konata/code/tmex-enhanced-wt-r8/prompt-archives/2026083101-onboarding-remote-access-round8/sub/E3-result.md. Finish by writing that file.

Context: this repository is a fork. `origin` = github.com/12dora/tmex-enhanced, `upstream` = github.com/krhougs/tmex. The npm package `tmex-cli` (packages/app) is owned by the upstream author; this fork cannot publish to it. Goal: make packaging + install + upgrade/auto-update point at THIS repository (12dora/tmex-enhanced, e.g. GitHub Releases tarballs) instead of the upstream npm package, and provide a one-line install script (curl | bash) hosted in this repo.

Report:
1. packages/app: package.json (name, bin, repository, files, publishConfig), scripts (build:cli, build:runtime, release, bundle:resources), scripts/release.ts at repo root — what "release"/"publish" does today.
2. The CLI's install/upgrade/update-check flow: find every place that references the npm registry, `tmex-cli`, `npx`, `npm view`, `registry.npmjs.org`, GitHub URLs, version checks (e.g. `checkForUpdate`, `latestVersion`), `upgrade --apply-current-package`, launchd/systemd service install, install-meta.json, download/tarball handling. Give file:line. Describe how `npx tmex-cli install` and `npx tmex-cli upgrade` obtain the package today.
3. Where the gateway/UI shows "update available" (search apps/gateway and apps/fe for update checks/version fetch, e.g. `/api/version`, `updateAvailable`, `latestVersion`) and where the version constant comes from (TMEX_MONOREPO_VERSION define).
4. Any GitHub Actions workflows (.github/workflows) for release/publish.
5. Docs mentioning install commands (README.md, README.zh-CN.md, docs/**) that reference `npx tmex-cli`.
6. Recommend a concrete minimal design: (a) a GitHub Releases-based distribution (tarball `tmex-cli-<version>.tgz` attached to release `v<version>` on 12dora/tmex-enhanced), (b) how the CLI update check / upgrade should resolve "latest" from GitHub Releases API (`https://api.github.com/repos/12dora/tmex-enhanced/releases/latest`) with fallback, (c) an `install.sh` at repo root (or `scripts/install.sh`) usable as `curl -fsSL https://raw.githubusercontent.com/12dora/tmex-enhanced/main/install.sh | bash` that installs Node/bun prerequisites check, downloads the latest tarball and runs `npx ./tmex-cli-x.tgz install`, and a GitHub Actions workflow to build+attach the tarball on tag push. Note which existing tests cover the update logic (packages/app/src/**/*.test.ts) and their current pass/fail baseline expectations if visible.
Do not modify files.
