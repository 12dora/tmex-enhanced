## Common rules (read first)

- Worktree: /Users/konata/code/tmex-enhanced-wt-r8 (branch feat/round8-onboarding). Other agents are editing OTHER files in this same worktree in parallel. Touch ONLY the files listed in your scope. NEVER run git commands that change state (no add/commit/stash/checkout/reset). Do not touch files outside your scope; if you believe a change outside scope is required, describe it in your result file instead.
- Runtime is Bun only. Tests: `bun test <path>` inside the package dir (for apps/fe use `bun test src/...`, never bare `bun test` there). Typecheck: `bunx tsc --noEmit -p .` in the package dir; count `error TS` lines before and after your change — must not increase (baselines: apps/fe 0, apps/gateway 21, packages/app 1). Lint only the files you changed: `bunx biome check <files>` from the repo root (fix with `--write`). macOS has no `timeout` command.
- i18n: locale JSON lives in packages/shared/src/i18n/locales/{en_US,zh_CN,ja_JP}.json. Keys must stay in sync across all three. After editing JSON run `bun run --filter @tmex/shared build:i18n` from the repo root to regenerate resources.ts/types.ts (never hand-edit or lint those generated files). Edit JSON with targeted edits; do not reformat the whole file.
- Copy style (all languages): short, professional, no filler. Chinese: use 「本机」 not 「这台机器」, avoid 「你/你的」, full-width Chinese punctuation. English: plain, direct. Japanese: polite です/ます form, concise.
- Code comments: only where logic is non-obvious; write them in Simplified Chinese (project convention). Variable names in English.
- No laziness: no TODOs, no "simplified version", no skipped tests. Finish the whole scope.
- When done, write a concise Markdown result report (English) to the path given in your task: what changed (file list), test/tsc/lint results with numbers, anything out of scope you noticed. Write the result file as your LAST action.

## Task B2 — CLI distribution via this repo's GitHub Releases + install script + tmex shim

Context: fork github.com/12dora/tmex-enhanced; the npm package `tmex-cli` is upstream's and cannot be published by us. Shared constants exist in `packages/shared/src/release/source.ts` (RELEASE_REPO, RELEASE_REPO_URL, RELEASE_API_LATEST_URL, INSTALL_SCRIPT_URL, INSTALL_COMMAND, releaseTag, releaseTarballName, releaseTarballUrl, releaseApiUrl). packages/app is Node-compatible and imports shared code by RELATIVE path (see `packages/app/src/commands/upgrade.ts` importing '../../../shared/src/network') — do the same, never `@tmex/shared`.

Scope (files you may edit): everything under `packages/app/` (src, scripts, package.json, CHANGELOG.md, README.md), new root `install.sh`, new `.github/workflows/release.yml`, root `README.md` + `README.zh-CN.md` (install/upgrade sections only), `apps/fe/src/node/enrollment.ts` (ONLY the joinCommand string + its test file `apps/fe/src/node/enrollment.test.ts` if it asserts the string), `docs/hub/2026082800-hub-node-operations.md` (command strings only). Do NOT touch apps/gateway.

Deliverables:
1. **Self-contained CLI check**: build the CLI (`cd packages/app && bun run build:cli`) and verify `node bin/tmex.js help` works from an extracted `npm pack` tarball WITHOUT node_modules (extract to a temp dir under /private/tmp/claude-501/-Users-konata-code-tmex-enhanced/f515c2a8-84b8-4c01-964f-1e2b06d4abc4/scratchpad). Also check `bun bin/tmex.js help`. Report both. If dist/cli-node.js is NOT self-contained, fix `build:cli` (e.g. ensure bundling; keep node target) rather than requiring npm install.
2. **`upgrade` (packages/app/src/commands/upgrade.ts)**: `delegateUpgrade` must no longer run `npx tmex-cli@...`. New flow: resolve target version (`--version` or latest via RELEASE_API_LATEST_URL, tag_name minus 'v'); download releaseTarballUrl(version) to a temp dir; extract with `tar -xzf`; run the extracted `package/bin/tmex.js upgrade --apply-current-package` + passthrough flags using the same runtime that is executing the current CLI (`process.execPath`); propagate exit code. Clear errors for 404 (version not found / no asset), network failure. Keep the existing `--apply-current-package` branch intact except item 3.
3. **CLI self-deploy + shim**: in `init` and `upgrade --apply-current-package` (shared helper in packages/app/src/lib/install.ts or a new lib file), copy the CLI package files (`package.json`, `bin/`, `dist/cli-node.js`) from the running package root (`resolvePackageLayout`) into `<installDir>/cli/`, and write an executable shim `~/.local/bin/tmex`: a bash script that execs `node "<installDir>/cli/bin/tmex.js" "$@"` if node is on PATH, else `bun` (use the bun path recorded in install-meta / run.sh logic). If `~/.bun/bin` exists, also symlink the shim there. Print a PATH hint (i18n via packages/app/src/i18n, both en and zh-CN) when `~/.local/bin` is not in PATH. Add `cliDir` to InstallLayout. `uninstall` must remove the shim(s). Tests for the layout/shim helpers (existing test patterns in packages/app/src/lib/*.test.ts).
4. **Join command strings**: `npx tmex-cli hub join ...` → `tmex hub join ...` in `packages/app/src/commands/enroll.ts` (line ~486), `apps/fe/src/node/enrollment.ts` joinCommand (and its test), and `docs/hub/2026082800-hub-node-operations.md`. Anything else that prints `npx tmex-cli` in packages/app (grep) → `tmex`.
5. **install.sh** (repo root, POSIX bash, `set -euo pipefail`): usable as `curl -fsSL https://raw.githubusercontent.com/12dora/tmex-enhanced/main/install.sh | bash` and as `bash install.sh [init flags...]`. Steps: check curl+tar; detect OS (darwin/linux) else exit; ensure bun ≥ 1.3 (install via `curl -fsSL https://bun.sh/install | bash` if missing, then export PATH=$HOME/.bun/bin:$PATH); resolve latest release via GitHub API (parse tag_name with sed/grep, no jq dependency; honor `TMEX_VERSION` env to pin); download tarball to a mktemp dir; extract; run `node package/bin/tmex.js init "$@"` if node exists else `bun package/bin/tmex.js init "$@"`; when stdin is not a TTY (curl | bash), re-attach `/dev/tty` for the interactive prompts if available, otherwise pass `--no-interactive`. Print the final hint about `tmex` command/PATH. Make it executable.
6. **.github/workflows/release.yml**: on push of tags `v*`: checkout, setup bun (oven-sh/setup-bun) + node 20, `bun install --frozen-lockfile`, `bun run build`, check package version == tag, `cd packages/app && npm pack`, then `gh release create` if the release doesn't exist else `gh release upload --clobber` for `tmex-cli-<version>.tgz` + a `SHA256SUMS` file; release notes from CHANGELOG section if easy, else `--generate-notes`. Also a `workflow_dispatch` input for the tag.
7. **Version + changelog**: bump `packages/app/package.json` version to `1.1.0`; add a 1.1.0 entry to `packages/app/CHANGELOG.md` following the existing bilingual format (read it first) summarizing: distribution moved to GitHub Releases of 12dora/tmex-enhanced, install.sh, tmex shim, remote-access wizard restructure, "Connect more devices" guide, header mesh icon removed. Remove the npm `release: npm publish` script (replace with `pack: npm pack`), add `repository` field pointing to the fork. Check root `scripts/release.ts` for npm assumptions and adjust minimal text if it mentions npm publish.
8. **README.md / README.zh-CN.md**: replace `npx tmex-cli init` instructions with the install.sh one-liner and `tmex upgrade` / `tmex doctor` / `tmex uninstall`; keep the non-interactive example but via `bash install.sh --no-interactive ...`. Keep edits within the install/upgrade sections.
9. Run `cd packages/app && bun test` (baseline 430 pass / 1 pre-existing fail) and `bunx tsc --noEmit -p .` (baseline 1 error), biome on changed files, `bash -n install.sh`, and a dry run of install.sh's download+extract part against the real GitHub API is NOT possible yet (no release exists) — instead unit-test the version parsing by running the relevant function with a sample JSON (e.g. via a `--print-latest` debug flag or by structuring the script so the parse step is a function you can source). Do not touch the production tmex install under ~/Library/Application Support/tmex.

Result file: /Users/konata/code/tmex-enhanced-wt-r8/prompt-archives/2026083101-onboarding-remote-access-round8/sub/B2-result.md
