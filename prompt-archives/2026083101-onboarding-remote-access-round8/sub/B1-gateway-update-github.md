## Common rules (read first)

- Worktree: /Users/konata/code/tmex-enhanced-wt-r8 (branch feat/round8-onboarding). Other agents are editing OTHER files in this same worktree in parallel. Touch ONLY the files listed in your scope. NEVER run git commands that change state (no add/commit/stash/checkout/reset). Do not touch files outside your scope; if you believe a change outside scope is required, describe it in your result file instead.
- Runtime is Bun only. Tests: `bun test <path>` inside the package dir (for apps/fe use `bun test src/...`, never bare `bun test` there). Typecheck: `bunx tsc --noEmit -p .` in the package dir; count `error TS` lines before and after your change — must not increase (baselines: apps/fe 0, apps/gateway 21, packages/app 1). Lint only the files you changed: `bunx biome check <files>` from the repo root (fix with `--write`). macOS has no `timeout` command.
- i18n: locale JSON lives in packages/shared/src/i18n/locales/{en_US,zh_CN,ja_JP}.json. Keys must stay in sync across all three. After editing JSON run `bun run --filter @tmex/shared build:i18n` from the repo root to regenerate resources.ts/types.ts (never hand-edit or lint those generated files). Edit JSON with targeted edits; do not reformat the whole file.
- Copy style (all languages): short, professional, no filler. Chinese: use 「本机」 not 「这台机器」, avoid 「你/你的」, full-width Chinese punctuation. English: plain, direct. Japanese: polite です/ます form, concise.
- Code comments: only where logic is non-obvious; write them in Simplified Chinese (project convention). Variable names in English.
- No laziness: no TODOs, no "simplified version", no skipped tests. Finish the whole scope.
- When done, write a concise Markdown result report (English) to the path given in your task: what changed (file list), test/tsc/lint results with numbers, anything out of scope you noticed. Write the result file as your LAST action.

## Task B1 — gateway update check + one-click upgrade must use this repo's GitHub Releases

Context: this repo is a fork (github.com/12dora/tmex-enhanced). The npm package `tmex-cli` belongs to upstream and can no longer be used for updates. Shared constants already exist in `packages/shared/src/release/source.ts` (exported from `@tmex/shared`): RELEASE_REPO, RELEASE_API_LATEST_URL, releaseTarballUrl(version), releaseTarballName(version), releaseApiUrl(version), releaseTag(version). Use them; do not hardcode URLs.

Scope (files you may edit): `apps/gateway/src/system/update-check.ts`, new `apps/gateway/src/system/update-check.test.ts`, `apps/gateway/src/system/upgrade.ts`, new/updated `apps/gateway/src/system/upgrade.test.ts` (if practical), and `apps/gateway/src/system/managed.ts`/`managed-endpoint.ts` ONLY if they reference npm/tmex-cli (check first). Nothing else.

1. `checkForUpdate()` (update-check.ts): replace the npm registry query with GitHub Releases API `RELEASE_API_LATEST_URL` (headers: accept application/vnd.github+json, X-GitHub-Api-Version 2022-11-28, no-store, 10s timeout). latestVersion = tag_name with a leading 'v' stripped; publishedAt = published_at; changelog = release body (markdown, null if empty); hasUpdate uses existing compareVersions. Keep the `UpdateCheckResult` contract shape unchanged (check packages/shared for its definition; do not change it). Drop the jsDelivr changelog fetch. Handle 403/404/rate-limit by throwing a clear Error (callers already surface errors) — never fall back to npm. Also verify that a tarball asset named releaseTarballName(latest) exists in the release `assets`; if the release has no such asset, treat as "no update" (hasUpdate=false, latestVersion still reported) so the UI never offers an impossible upgrade. Write unit tests with a mocked global fetch (bun:test, mock.module or replace globalThis.fetch and restore) covering: newer version → hasUpdate true + changelog; same version → false; missing asset → false; HTTP 403 → throws.
2. `UpgradeController.run()` (upgrade.ts): replace `bun add tmex-cli@<version>` staging with: download releaseTarballUrl(version) into the stage dir (fetch → file, follow redirects — GitHub asset URLs redirect to objects.githubusercontent.com; `fetch` follows by default), extract with `tar -xzf <tgz> -C <stageDir>` (spawn, wait for exit 0), then the CLI entry is `<stageDir>/package/bin/tmex.js`. Keep the existing detached execution stage (node/bun invocation, KillMode notes, error reporting) — read the whole file first and preserve behavior: the same binPath check, the same `upgrade --apply-current-package` arguments, same state machine. The CLI bundle is self-contained (dist/cli-node.js is a bun-bundled file; no npm install needed) — confirm by inspecting packages/app/scripts and package.json `build:cli`, and note in your report whether `bin/tmex.js` needs node or bun to run (look at how the current code launches it and keep that).
3. Update the file header comments to describe the new flow (Chinese).
4. Run: `cd apps/gateway && bun test src/system` and `bunx tsc --noEmit -p .` (baseline 21 errors), biome on changed files.

Result file: /Users/konata/code/tmex-enhanced-wt-r8/prompt-archives/2026083101-onboarding-remote-access-round8/sub/B1-result.md
