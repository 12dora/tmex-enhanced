# Task B1 — shared package: relay protocol codec, tenant cipher, join token v3, new key-log record types, roles

Result file (absolute): /Users/konata/code/tmex-r23/prompt-archives/2026090304-round23-relay-legacy-removal/sub/B1-result.md

READ FIRST: /Users/konata/code/tmex-r23/prompt-archives/2026090304-round23-relay-legacy-removal/plan-00.md — section 一 (中继设计) is the contract you implement; sections 1.3, 1.4, 1.5, 1.8 are yours. The hub map in sub/EX1-hub-map.md tells you where the existing hub uplink codec / key-log framework lives.

## Scope (files you may edit)
- NEW `packages/shared/src/relay/` (codec.ts, tenant-cipher.ts, join-token.ts, index.ts, tests)
- `packages/shared/src/auth/key-log.ts`, `packages/shared/src/auth/encoding.ts`, and the payload schema files they use (add the new record types; do NOT change existing record formats), plus their tests
- `packages/shared/src/roles.ts` + test
- `packages/shared/package.json` exports: add `"./relay": "./src/relay/index.ts"` (this is the ONE package.json edit you may make; no dependency changes)
- `packages/shared/src/index.ts` only if the roles change needs re-export (roles are probably already exported)
Do NOT edit apps/gateway, packages/app, apps/fe, packages/api-client, or any other shared module (another agent, L1a, is editing packages/shared/src/ws-borsh/* and packages/shared/src/capabilities.ts right now — do not touch those).

## Deliverables
1. **roles.ts**: `TmexRoleName` gains `'relay' | 'relay,node'`; `TmexRoles` gains `relay: boolean`; update `isTmexRoleName`, `rolesFromName`, `roleNameFromFlags`, `isStandaloneRoles` (standalone = no hub, no node, no relay). Combining relay with hub is invalid — add `validateRoles(roles): string | null` returning an error message for `hub && relay`. Fix all existing call sites INSIDE packages/shared only (grep `TmexRoles`); list call sites outside shared that construct `TmexRoles` literals in the result file for the commander (do not edit them).
2. **tenant-cipher.ts** (browser + Bun compatible; use WebCrypto `crypto.subtle` AES-GCM and `@noble/curves` x25519 + `@noble/hashes` HKDF/SHA-256 as already used under packages/shared/src/auth and packages/shared/src/link/secure-channel-link.ts — check what is imported there and reuse the same libraries):
   - `generateTenantKey(): Uint8Array(32)`
   - `sealEnvelope(key, kind: string, plaintext: Uint8Array, epoch?: number): RelayEnvelope` / `openEnvelope(key, kind, env): Uint8Array` where AAD = `tmex-relay/<kind>/v1`, nonce 12 random bytes; `RelayEnvelope = { v: 1, epoch?: number, n: string(b64url), ct: string(b64url) }`; wrong key/AAD → throws `RelayCipherError`.
   - `wrapKeyForNode({ key, nodeId(hex), nodeX25519Pk }): WrapEntry` / `unwrapKeyForNode({ entry, nodeX25519Sk }): Uint8Array` per plan 1.3 (ephemeral X25519 → HKDF-SHA-256 salt `tmex-relay-wrap/v1`, info = node_id bytes → AES-256-GCM). `WrapEntry = { node_id: string, eph_pk: string(b64url), nonce: string(b64url), ct: string(b64url) }`.
   - Fixed test vectors + round-trip tests + tamper tests.
3. **join-token.ts**: v3 join string per plan 1.5 (`"r3." + base64url(enroll_sk32 ‖ root_pk32 ‖ head_hash32 ‖ K_log32 ‖ tenant_id16 ‖ token32 ‖ n(u8) ‖ [len u16 LE ‖ url utf8]×n)`, optional `.<64hex>` CA fingerprint suffix exactly like the existing hub join token — find the existing encoder in apps/fe/src/node/enrollment.ts ~699-755 and the CLI parser in packages/app/src/commands/hub.ts ~446-482 (read-only) and mirror their conventions). Export `encodeRelayJoinToken`, `decodeRelayJoinToken`, `isRelayJoinToken`, `RelayJoinToken` type. Reject: wrong prefix, bad length, n=0, url > 512 chars, non-https urls (allow `http://localhost` and `http://127.0.0.1` for tests — check how hub-url.ts normalizes and reuse it).
4. **codec.ts**: relay/v1 ctl message types + `encodeRelayCtl` / `decodeRelayCtl` with the same defensive bounds style as packages/shared/src/uplink/codec.ts (64 KiB, depth, string length, arrays). Message set exactly per plan table 1.8: `auth.challenge`, `relay.auth`, `auth.ok`, `ping`, `pong`, `relay.status`, `relay.list`, `relay.keylog.append`, `relay.keylog.ack`, `relay.keylog.req`, `relay.keylog.res`, `relay.keylog.push`, `relay.rtc`, `relay.enroll.create`, `relay.enroll.ack`, `enroll.redeemed`, `relay.quota`, `relay.kicked`. Also export `RELAY_PROTO_VERSION = 1`, `MIN_RELAY_CLIENT_VERSION = '1.1.23'`, `RELAY_OPEN_STREAM` type `{ to: string }` helpers (encode/decode of the relay stream OPEN first frame identical to the hub's `{to}` JSON — see apps/gateway/src/mesh/uplink-client.ts openRelay, read-only), the status blob plaintext type `RelayStatusBlob = { name, version, tmux, inventory, endpoints }` with encode/decode (JSON, bounded), the rtc blob `{ sdp?, candidate? }`, and the enroll proof structure `{ domain: 'tmex/relay-enroll/v1', relay_host, root_public_key, ts }` Borsh-encoded (reuse packages/shared/src/auth/encoding.ts helpers for Borsh + ed25519 sign/verify) with `signRelayEnrollProof` / `verifyRelayEnrollProof` (ts window ±5 min checked by caller; expose `RELAY_ENROLL_PROOF_MAX_SKEW_MS`).
5. **key-log new record types** `set-relays` and `meta-key` per plan 1.4: add to the type enum/encoding map, `KEY_LOG_SIGNER_MATRIX` (both `['root','passkey']`), `KEYLOG_RECORD_COMPAT` (`minVersion: '1.1.23'`, `allowForce: false`, export `MIN_RELAY_RECORD_VERSION`), Borsh payload schemas + `encode/decodeSetRelaysPayload`, `encode/decodeMetaKeyPayload`, and wire them into the state-apply function (`applyKeyLogRecord` or whatever key-log.ts uses — find where `admit-node` is handled ~line 599) so state tracks `relays: { mode, relays[] } | null`, `metaKeyEpoch`, and exposes the raw entries for the node to unwrap (the node side will consume them). `meta-key.epoch` must be strictly greater than the previous applied epoch else the record is rejected as invalid; `set-relays.meta_key.epoch` may equal the current epoch (re-distribution) or exceed it. Tests: encode/decode round trips, signer matrix, compat gate, apply-state transitions, rejection of non-increasing epoch.
6. Verify: `bun test` in packages/shared (baseline 534 + L1a's additions may be landing concurrently — only your own files matter; if an unrelated ws-borsh test fails, note it and move on), `bunx tsc --noEmit -p packages/shared`, biome on your files.

In the result file: exported API surface (names + signatures), wire examples (one JSON per ctl type), the exact record payload field order, and the list of out-of-scope call sites that must be updated for the roles change.

## Common rules (apply to every task)

- Repo: Bun-only TypeScript monorepo at /Users/konata/code/tmex-r23 (git worktree, branch feat/round23-relay-legacy-removal). Bun is at /opt/homebrew/bin/bun (if not on PATH, read ~/.zshrc). Never use Node APIs that Bun lacks; runtime code runs on Bun 1.3.x.
- OTHER AGENTS ARE EDITING THIS SAME WORKTREE IN PARALLEL. Touch ONLY the files listed in your scope (plus new files you create inside your scope directories). Do not reformat or "clean up" files outside your scope. If you believe a change outside your scope is unavoidable, do NOT make it — describe it precisely in your result file under "需要指挥官处理".
- NO git operations at all (no add/commit/stash/checkout/reset). The commander commits.
- Do not run `bun install`, do not edit lockfiles, do not edit package.json dependencies unless your scope says so.
- Do not touch generated files: packages/shared/src/i18n/resources.ts, packages/shared/src/i18n/types.ts, resources/fe-dist/*, dist/*. i18n keys are edited in the locale JSON sources (find them under packages/shared/src/i18n/ — look for locales/*.json) and regenerated with `bun run build:i18n` at the repo root (you MAY run that if your scope includes i18n changes).
- Never touch the production tmex service (port 9883, ~/Library/Application Support/tmex) and never touch a tmux session named `tmex`. If you need tmux in tests, use a separate socket (`tmux -L tmex-e2e` or a unique name). Never run `tmux kill-server` on the default socket. Temporary gateway instances must set TMEX_TMUX_SOCKET to an isolated socket and use ports other than 9883/9663/19883/19663.
- Do not run the Playwright e2e suite (apps/fe/tests) — the commander runs it. In apps/fe, unit tests are `bun test src/`.
- Code style: TypeScript, biome. No unnecessary comments; comments only for genuinely complex logic, written in Simplified Chinese. Identifiers in English. Follow the existing patterns in neighbouring files. Keep files under 600 lines (complexity gate; run `bun run lint` at repo root to check — it runs biome + the complexity gate). Do not add entries to the complexity allowlist.
- Verify before finishing: (1) `bunx tsc --noEmit -p <package>` for each package you touched must not add errors versus baseline (baseline: packages/app 1 pre-existing error, everything else 0); (2) `bun test` inside each touched package (apps/fe: `bun test src/`) must pass — baselines: shared 534, ws-client 408, stores 440, panels 911, ui 110, api-client 175, app 690 (+1 known cpu-features fail), terminal-ui 400, theme 52, fe 1783, gateway 4046 (4 known load flakes under full parallel run: stream failover legacy 0x305, large raw-body push ×2, RtcPeerManager ice summary — rerun those files in isolation if they fail); (3) `bunx biome check <your files>`.
- Write tests for new behaviour (bun test, colocated `*.test.ts`). Prefer deterministic tests; no sleeps over 100 ms.
- When completely done, write your result report (Simplified Chinese, concise, technical) to the ABSOLUTE result path given in your task — list files changed, tests added, verification output summary, anything the commander must handle — and only then exit. The result file must be the LAST thing you write.
