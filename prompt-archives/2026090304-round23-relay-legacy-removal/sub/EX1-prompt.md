You are exploring the tmex monorepo (Bun runtime, TypeScript) at the current directory, READ-ONLY. Output your complete report as your FINAL MESSAGE (you cannot write files). Write the report in Simplified Chinese (zh-CN), technical, concise, with exact file paths and line numbers.

Goal: produce a precise architecture map of the existing hub / mesh implementation so that a planner can design a NEW role called `relay` — a public, multi-tenant, "blind" hub: it provides everything the hub provides to nodes (registry, node.list broadcast, WebRTC signaling, enrollment redemption, ciphertext relay streams, STUN/TURN config), but MUST NOT be able to read tenant metadata (node names, device inventory, endpoints, key log payloads, rtc signal SDP) and serves ~100 independent tenants (each tenant = one user's mesh with its own root key). Tenants enroll to the relay with a relay-wide password; the operator can rotate the password and choose to kick all existing tenants or keep them.

Read the design doc docs/hub/2026082700-hub-node-architecture.md and docs/hub/2026090104-multi-hub-standby.md first, then the code. Report, with file:line references:

1. HubRuntime: where it lives, how it is constructed (packages/app/src/runtime/server.ts role assembly, TMEX_ROLES parsing), every HTTP route it serves (/api/hub/*, /hub/uplink, /api/hub/status, enrollments redeem, tokens, role, etc.), and what each does. Which of those routes are used by the FE (grep apps/fe and packages/api-client) vs CLI vs other hubs.
2. Uplink protocol: the exact ctl message types on the uplink (auth.challenge/response, node.status, node.list, key.log, key.log.req, rtc.signal, relay OPEN, hub.write-forward, hub.tokens, hub-relay, ping/pong, anything else). For each: which side sends, the payload schema (TS types, where defined), and what fields the hub READS/INTERPRETS vs merely forwards. Point out every place where the hub reads plaintext tenant metadata (names, inventory, endpoints, versions, SDP, key log records).
3. Hub-side persistence: tables the hub writes (nodes, enrollment_tokens, users, user_key_log, node_certs, mesh_hubs, gateway_kv, ...), the schema files (apps/gateway/src/db/schema/* after round22 split), migrations dir and naming convention, and how user_id scoping works today (single user? where is uid derived on the hub).
4. Uplink authentication on the hub: how the hub verifies a connecting node (node_certs lookup, ed_pk, revoked), how node_certs get to the hub (redeem flow, admit-node key log records applied on hub?), and how `relay` streams are authorized (same user_id check) — exact code.
5. Node side: UplinkClient and UplinkPool (multi-hub failover), how TMEX_HUB_URL/TMEX_HUB_URLS are configured, node_identity table (hub_url etc.), how node.list is consumed into peer_cache, how key.log catch-up works, how rtc.signal is routed (browser→entry→hub→node), how relay streams open SecureChannelLink. Where would a "relay" uplink target differ.
6. Enrollment/join flow end-to-end: CLI commands (packages/app/src/commands/: enroll, hub join, hub leave, hub user add, hub standby/promote/...), the join string format, `POST /api/hub/enrollments/redeem` handler, admit-node signing on the FE (apps/fe or packages/*), the enrollment token table. Which steps require the hub to read the user's root public key / authorization.
7. FE: the Nodes / 多节点互联 page (settings tab 'nodes', NodesPage, HubStrip, hub status badges, multi-hub switch UI), which API endpoints feed it (/api/mesh/nodes, /api/mesh/hubs?, /api/hub/status, node.list projection), the stores in packages/stores for mesh, and i18n key namespaces used (packages/shared/src/i18n, locale JSON files location, build:i18n).
8. CLI structure: how commands are registered (packages/app/src/commands, cli entry), how `tmex hub ...` subcommands are implemented (Node bootstrap → Bun cli-auth.js pattern), where API calls to the local gateway are made from CLI, how config env vars are written (app.env editing helpers).
9. Existing per-user scoping: where `user_id` appears in hub code paths; is there anything that assumes a single user (e.g. `users` first row, constants). List the hardest spots for multi-tenancy.
10. Tests: existing mesh integration tests (apps/gateway/src/mesh/**/*.test.ts, multi-hub integration tests, test fixtures such as stampHubCtlVersions, in-process hub+node harness helpers), so new relay tests can reuse them. Give the exact helper names and files.
11. Version gates: how min-version checks are implemented (semver helper after round22 dedupe, `MIN_*` constants), the current package version.

Be exhaustive on 2, 4, 5, 6 — these determine the E2EE design. Do not propose the design; just map the facts, but at the end add a short section "对 relay 设计的硬约束" listing concrete facts that constrain the design (e.g. "hub 用 nodes.inventory_json 生成 node.list，节点侧 peer_cache 依赖它做 X").
