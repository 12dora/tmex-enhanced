# R1b prompt（修复批次：canonical v1.1 客户端/shared 复审 + e2e）

> 存档时间：2026-09-04。原始 prompt 原文如下。

You are an autonomous senior engineer executing fix batch **R1b** in the git worktree `/Users/konata/code/tmex-r23` (branch `feat/round23-relay-legacy-removal`, Bun-only TypeScript monorepo). Bun is at `/Users/konata/.bun/bin/bun` (on PATH). Result file (write LAST, Simplified Chinese): `/Users/konata/code/tmex-r23/prompt-archives/2026090304-round23-relay-legacy-removal/sub/R1b-result.md`.

Background: this round deleted the legacy terminal state stream end to end and moved to canonical v1.1 (read `prompt-archives/2026090304-round23-relay-legacy-removal/sub/L1a-result.md`, `sub/L1b-result.md`, `sub/L1c-result.md`, `sub/L1d-result.md`; design `plan-00.md` §二). Code reviews and a full Playwright run produced the items below. For each review finding: verify it against the code first; if real, fix with a test; if not, explain why in the result file.

## A. Review findings (client / shared)
1. **Tree order: after `Unset`, the client cannot restore tmux's original order** — `packages/shared/src/ws-borsh/canonical-tree-order.ts` ~67 and `packages/ws-client/src/canonical-metadata-identity.ts` ~192: the client keeps only the already-sorted snapshot; when all order entries are unset, the order map is empty and `sortSnapshotByCanonicalTreeOrder()` returns the previously sorted snapshot, so the UI stays in the old custom order; partial edits likewise let unordered entities inherit the previous custom order instead of tmux index order. Fix: keep the unsorted canonical source snapshot and derive display order from it each time, or make the sort restore base order by window/pane `index` first. Add tests: set order → partial change → full Unset.
2. **Old-node rejection is not surfaced as `server-too-old`** — the entry gateway rejects an old node with `ERROR_UNSUPPORTED_PROTOCOL` whose message starts with `canonical-state-v1.1 required` (a gateway agent is editing apps/gateway concurrently — do NOT change the gateway; keep that prefix as the contract). In `packages/ws-client/src/transport-message-decoder.ts` ~124 / client.ts, map that ERROR to the typed `server-too-old` event (so the stores toast fires) and stop auto-reconnect for this non-retryable error. Put the prefix/matcher constant in `packages/shared` (e.g. next to `canonical-version.ts`) so both sides can share it later. Tests.
3. **Shared still exports the deleted legacy wire contract** — `packages/shared/src/ws-borsh/kind.ts` ~26, `packages/shared/src/ws-borsh/index.ts` ~26/~151: `STATE_SNAPSHOT*`, `TERM_OUTPUT/HISTORY`, `TERM_RESIZE/SYNC_SIZE`, `SWITCH_ACK/LIVE_RESUME`, legacy subscribe/fetch kinds, their payload schemas, codecs and barrel exports. Delete them (grep the whole repo for remaining references — apps/gateway, packages/ws-client, stores, terminal-ui, apps/fe/tests, scripts, bench — and fix those references; the gateway must still return UNKNOWN_KIND for unknown numeric kinds). Keep any internal snapshot-projection helpers that canonical clients still use. Update the spec doc `docs/ws-protocol/2026021402-ws-borsh-v1-spec.md` to mark those kinds removed in 1.1.23 (keep the v1 text otherwise frozen). NOTE: `apps/gateway/src/ws/**` is being edited concurrently by agent R1a — if a gateway file still references a deleted symbol, make the minimal import/constant removal in that file and list it in the result (do not restructure gateway code).
4. **Per-pane `sizeEpoch` entries never pruned on pane removal** — `packages/ws-client/src/canonical-metadata-identity.ts` ~382 / `canonical-size-epochs.ts`: add a per-pane delete on the `pane-removed` branch; test.

## B. e2e (Playwright) items — you MAY run targeted specs
The commander ran the full suite: 97 passed / 14 failed / 1 skipped. Run targeted specs from `apps/fe` with **`TMEX_E2E_GATEWAY_PORT=9765 TMEX_E2E_FE_PORT=9985 bun run scripts/run-e2e.ts <spec files...>`** (isolated tmux socket `tmex-e2e`; NEVER touch the tmux session named `tmex` or port 9883; `tmux -L tmex-e2e kill-server` is safe to clean leftovers; if a run hangs, kill only processes you started). Do not run the full suite. Failure artifacts from the full run are in `apps/fe/test-results/<case>/` (error-context.md, screenshots, trace.zip).
5. **Theme specs (10 failures + `ws-borsh-theme-resize:89`)**: `apps/fe/tests/helpers/site-theme.ts` ~51 sends `clientVersion: '0.0.0'` in HELLO; the gateway's fail-closed gate now rejects it. Use a real version (e.g. `CANONICAL_V11_MIN_PEER_VERSION` from `@tmex/shared/ws-borsh` or the same source `apps/fe/tests/helpers/ws-borsh.ts` uses). Then rerun: `theme-broadcast.spec.ts theme-notify-2031.spec.ts theme-presets.spec.ts theme-propagation.spec.ts ws-borsh-theme-resize.spec.ts` and make them pass.
6. **`viewport-policy.spec.ts:77` and `:128` failed** ("a smaller second client does not shrink the tmux window and pans locally", "closing the owner hands the window size to the remaining client"). Diagnose from the artifacts, then from the code: likely candidates are the canonical v1.1 resize mapping in `packages/stores/src/tmux-viewport-actions.ts` / `packages/ws-client` (change vs resend, epoch reuse) or the gateway select path still passing `distrustLive` (R1a is fixing that gateway side right now in `apps/gateway/src/ws/tmux-selection-handlers.ts` — re-run after your client fixes; if the failure is purely that gateway bug, say so). Do not edit gateway files for this.
7. **`terminal-mouse-recovery.spec.ts:411`** ("desktop: focus restore repaints a cleared terminal canvas even when terminal size is unchanged") failed. Diagnose from artifacts; it may be load-sensitive (rerun in isolation twice) or a real regression from the legacy history-restore removal in `packages/terminal-ui` (L1d). Fix if real.
8. Strengthen two rewritten specs (review minors): `apps/fe/tests/ws-borsh-switch-barrier.spec.ts` ~130 ("land on the final pane" passes even if the second click fails because the first pane stays subscribed during the 60 s keep-alive) — also assert the final selected window/URL or the visible terminal; `apps/fe/tests/ws-borsh-resize.spec.ts` ~310 — capture the epoch of a controlled `change`, then assert every `resend` has exactly that `sizeEpoch` (filter by reason === RESEND).

Rules: scope = `packages/shared/src/ws-borsh/**` (+ spec doc), `packages/ws-client/src/**`, `packages/stores/src/**`, `packages/terminal-ui/src/**`, `apps/fe/tests/**`, `apps/fe/src/**` only where a removed symbol forces it, plus minimal import removals in apps/gateway as described. Do NOT touch `packages/shared/src/relay`, `packages/shared/src/auth`, `apps/gateway/src/relay`, `apps/gateway/src/mesh/relay-*`, `packages/app`, `packages/api-client`, `apps/fe/src/node/**`, `apps/fe/src/pages/settings/**` (other agents). Read-only git only; no `bun install`; no TODOs; files <=600 lines; no allowlist additions; do not run `biome --write` on apps/fe/tests.
Verify: `bun test` in packages/shared (632+), ws-client (380), stores (411), terminal-ui (394); `cd apps/fe && bun test src/`; tsc for shared, ws-client, stores, terminal-ui, apps/fe, and `bunx tsc --noEmit -p apps/gateway` read-only (report errors you caused, none allowed); `bunx biome check` on changed files; `bun run lint`; the targeted e2e specs above passing (give the exact commands and results).
Result file: per finding verdict + fix + tests; per e2e spec the root cause and outcome; anything left for the commander. Then reply with a short summary.
