# Task B4 — CLI: `tmex relay ...` (operator + tenant), join-token r3 support, `init --role relay|relay,node`

Result file (absolute): /Users/konata/code/tmex-r23/prompt-archives/2026090304-round23-relay-legacy-removal/sub/B4-result.md

READ FIRST: /Users/konata/code/tmex-r23/prompt-archives/2026090304-round23-relay-legacy-removal/plan-00.md section 一 (the relay design — it is the contract; do not deviate from message names, routes, table names or field names), /Users/konata/code/tmex-r23/prompt-archives/2026090304-round23-relay-legacy-removal/sub/EX1-hub-map.md (where the existing hub/mesh code lives, with line numbers), and /Users/konata/code/tmex-r23/prompt-archives/2026090304-round23-relay-legacy-removal/sub/B1-result.md (the shared relay API another agent just finished: @tmex/shared/relay codec / tenant-cipher / join-token, new key-log record types set-relays & meta-key, roles with relay).
Also read /Users/konata/code/tmex-r23/prompt-archives/2026090304-round23-relay-legacy-removal/sub/B2-result.md and /Users/konata/code/tmex-r23/prompt-archives/2026090304-round23-relay-legacy-removal/sub/B3-result.md when they exist (server routes you call); if not yet present, code against plan-00 §1.7 / §1.9 exactly and note assumptions.

## Scope (files you may edit / create)
- NEW packages/app/src/commands/relay.ts (tenant commands), relay-admin.ts (operator commands), relay-shared.ts, tests
- packages/app/src/index.ts and packages/app/src/lib/args.ts ONLY to register the `relay` command group; packages/app/src/cli-auth-entry.ts to load it via the Bun auth runtime like hub/enroll commands
- packages/app/src/commands/hub.ts: `hub join` recognises `r3.` join tokens (decodeRelayJoinToken from @tmex/shared/relay) and redeems against the relay (`POST /api/relay/tenants/:tenantId/enrollments/redeem` with header x-tmex-relay-token) instead of a hub; commits node identity with uplink_kind='relay', writes mesh_relays rows + K_log into mesh_secrets (use B3's mesh-relay-store if present in apps/gateway/src/auth/mesh-relay-store.ts — import it the same way hub.ts imports other gateway stores), verifies the downloaded key log chain from the join token's root_pk/head_hash after opening each blob with K_log. `TMEX_HUB_URL` must not be required for relay joins.
- packages/app/src/commands/init.ts (or wherever `--role` is parsed): accept `relay` and `relay,node`; `relay` alone skips frontend/tmux checks and prints the generated admin token location.
- packages/app/src/lib/install.ts ONLY if a new env key helper is needed (TMEX_RELAY_PUBLIC_URL, TMEX_RELAY_ADMIN_TOKEN)
- i18n for CLI strings: follow how existing commands localise (grep packages/app/src for the i18n helper); if CLI strings live in the shared locale JSON, add keys under `cli.relay.*` with targeted edits and run `bun run build:i18n`.
Do NOT edit apps/gateway/**, apps/fe/**, packages/shared/**.

## Commands (plan 1.10)
Operator (relay machine; read TMEX_RELAY_ADMIN_TOKEN + GATEWAY_PORT from the installed app.env via install.ts helpers; call http://127.0.0.1:<port>/api/relay/*):
- `tmex relay status [--json]`, `tmex relay tenants [--json]`, `tmex relay passwd [--clear] [--kick|--keep]` (interactive hidden input twice unless --clear; default --keep; print a summary of what mode does), `tmex relay kick <tenantId>`, `tmex relay remove <tenantId>` (confirm), `tmex relay quota <tenantId|default> [--max-nodes N] [--max-streams N] [--bandwidth <KBps>|unlimited] [--inherit]`, `tmex relay label <tenantId> <text>`.
Tenant (any node; needs local auth like `tmex enroll`): 
- `tmex relay enroll <url> [--password <p>]` : prompt relay password (hidden) if not given and the relay reports it requires one (call GET /api/relay/health first — add `hasPassword` to what you read if B2 exposes it; otherwise just try and handle 401), prompt local user password (derive root key exactly like enroll.ts does; if no local user exists, create one like `hub user add` does — reuse that code path), then: POST local `/api/mesh/relay/enroll/proof-material` → sign proof with root key (signRelayEnrollProof) → POST local `/api/mesh/relay/enroll` → receive unsigned set-relays payload → sign a key-log record with the root key (reuse the record-signing helpers enroll.ts uses for admit-node) → submit via the local `POST /api/auth/keylog?hub=sync` path (see packages/api-client/src/auth/auth-api.ts ~327-356 for the body shape) → poll `/api/mesh/relay/status` until mode = relay and the new relay is online (≤30 s) → print result.
- `tmex relay reauth <url>` (same as enroll for an existing relay: new token, same tenant), `tmex relay leave` (sign set-relays with empty list), `tmex relay list [--json]` (prints /api/mesh/relay/status).
- The CLI runs on Bun via cli-auth.js for these commands (they need crypto); keep the Node bootstrap untouched.
Tests: arg parsing, output formatting, request bodies against a mocked fetch; join-token r3 path with a mocked relay redeem.

Verify: `cd packages/app && bun test` (baseline 690 + 1 known cpu-features fail), `bunx tsc --noEmit -p packages/app` (baseline 1 error), biome, `bun run lint`. Then build the CLI (`bun run build:cli` in packages/app) to ensure it still bundles.

Result file: command matrix with examples, exact HTTP calls, assumptions about B2/B3 that must be confirmed.
## Common rules (apply to every task)

- Repo: Bun-only TypeScript monorepo at /Users/konata/code/tmex-r23 (git worktree, branch feat/round23-relay-legacy-removal). Bun is at /opt/homebrew/bin/bun (if not on PATH, read ~/.zshrc). Never use Node APIs that Bun lacks; runtime code runs on Bun 1.3.x.
- OTHER AGENTS ARE EDITING THIS SAME WORKTREE IN PARALLEL. Touch ONLY the files listed in your scope (plus new files you create inside your scope directories). Do not reformat or "clean up" files outside your scope. If you believe a change outside your scope is unavoidable, do NOT make it — describe it precisely in your result file under "需要指挥官处理".
- NO git operations at all (no add/commit/stash/checkout/reset). The commander commits.
- Do not run `bun install`, do not edit lockfiles, do not edit package.json dependencies unless your scope says so.
- Do not touch generated files: packages/shared/src/i18n/resources.ts, packages/shared/src/i18n/types.ts, resources/fe-dist/*, dist/*. i18n keys are edited in the locale JSON sources (find them under packages/shared/src/i18n/ — look for locales/*.json) and regenerated with `bun run build:i18n` at the repo root (you MAY run that if your scope includes i18n changes).
- Never touch the production tmex service (port 9883, ~/Library/Application Support/tmex) and never touch a tmux session named `tmex`. If you need tmux in tests, use a separate socket (`tmux -L tmex-e2e` or a unique name). Never run `tmux kill-server` on the default socket. Temporary gateway instances must set TMEX_TMUX_SOCKET to an isolated socket and use ports other than 9883/9663/19883/19663.
- Do not run the Playwright e2e suite (apps/fe/tests) — the commander runs it. In apps/fe, unit tests are `bun test src/`.
- Code style: TypeScript, biome. No unnecessary comments; comments only for genuinely complex logic, written in Simplified Chinese. Identifiers in English. Follow the existing patterns in neighbouring files. Keep files under 600 lines (complexity gate; run `bun run lint` at repo root to check — it runs biome + the complexity gate). Do not add entries to the complexity allowlist.
- Verify before finishing: (1) `bunx tsc --noEmit -p <package>` for each package you touched must not add errors versus baseline (baseline: packages/app 1 pre-existing error, everything else 0); (2) `bun test` inside each touched package (apps/fe: `bun test src/`) must pass — baselines: shared 534, ws-client 408, stores 440, panels 911, ui 110, api-client 175, app 690 (+1 known cpu-features fail), terminal-ui 400, theme 52, fe 1783, gateway 4046 (4 known load flakes under full parallel run: stream failover legacy 0x305, large raw-body push ×2, RtcPeerManager ice summary — rerun those files in isolation if they fail); (3) `bunx biome check <your files>`.
- Write tests for new behaviour (bun test, colocated `*.test.ts`). Prefer deterministic tests; no sleeps over 100 ms.
- When completely done, write your result report (Simplified Chinese, concise, technical) to the ABSOLUTE result path given in your task — list files changed, tests added, verification output summary, anything the commander must handle — and only then exit. The result file must be the LAST thing you write.
