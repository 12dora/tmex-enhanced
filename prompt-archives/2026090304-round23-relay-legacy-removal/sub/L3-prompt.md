# Task L3 — delete three routes that only tests reference

Result file (absolute): /Users/konata/code/tmex-r23/prompt-archives/2026090304-round23-relay-legacy-removal/sub/L3-result.md

## Scope (files you may edit)
- apps/gateway/src/api/tmux-tree.ts (delete), apps/gateway/src/api/tmux-tree.test.ts (delete), apps/gateway/src/api/system-routes.ts, apps/gateway/src/api/index.ts
- apps/gateway/src/api/theme.ts (delete), apps/gateway/src/api/theme.test.ts (move the DB-constraint tests at lines ~106-122 into a new test next to the site-settings service they exercise, then delete the file), apps/gateway/src/api/settings-routes.ts
- apps/gateway/src/hub/hub-runtime.ts (ONLY the `POST /api/hub/nodes/:id/revoke` route: routing at ~790-795, forwarded dispatch ~573-586 and ~625-630, handler ~908-941 — nothing else in that file), apps/gateway/src/hub/hub-runtime.test.ts, apps/gateway/src/hub/writer-forward.test.ts, apps/gateway/src/mesh/integration/mesh.integration.test.ts
- scripts/hub-e2e/driver/files.ts (remove the `tmux-tree` driver command), and any hub-e2e driver README/usage mentioning it
- apps/fe/tests/helpers/*.ts (add a helper), apps/fe/tests/theme-broadcast.spec.ts, apps/fe/tests/theme-notify-2031.spec.ts, apps/fe/tests/ws-borsh-theme-resize.spec.ts, apps/fe/tests/theme-propagation.spec.ts, apps/fe/tests/theme-presets.spec.ts
- docs/ws-protocol/2026070402-site-theme-update.md, docs/hub/2026082700-hub-node-architecture.md (line ~200 only), docs/hub/2026090104-multi-hub-standby.md (line ~133 only)

## What to do
1. `GET /api/tmux/tree`: delete handler + test + mounting. Keep `apiError.deviceNotFound` (used elsewhere). Remove the hub-e2e driver command that called it.
2. `GET|POST /api/settings/theme`: delete handler + mounting. Keep `broadcastThemeChange`, `broadcastSiteThemeUpdateS2C`, `broadcastSettingsUpdate`, `getSiteSettings`, `updateSiteSettings` (still used). Preserve the DB constraint tests by moving them. The five FE e2e specs use `request.post('/api/settings/theme')` as setup/cleanup — replace with a shared helper in apps/fe/tests/helpers/ that sets the site theme the way the real UI does. Find how the theme menu (apps/fe/src/components/page-layouts/components/theme-menu.tsx) applies a theme (likely a WS `KIND_SITE_THEME_UPDATE` message or a site-settings API call) and implement the helper on the same path (an existing helper in apps/fe/tests/helpers/ws-borsh.ts shows how specs open a raw WS and encode frames; there is `isGatewayWsUrl`). `theme-broadcast.spec.ts` has a negative test asserting the HTTP route does not broadcast — delete that specific test. Keep every other assertion intact. You cannot run Playwright; make the change carefully and self-consistently, and in the result file list every spec and the exact helper call replacing each HTTP call so the commander can run them.
3. `POST /api/hub/nodes/:id/revoke`: delete the route, forwarded dispatch and handler in hub-runtime.ts. Do not remove `revokeNodeRecord`, the `revoke-node` key-log type, hub authorization, or node revoked status. Tests that used the route to exercise revoke semantics must be rewritten to go through the surviving path `POST /api/auth/keylog?hub=sync` (see packages/api-client/src/auth/auth-api.ts ~327-356 for the client-side shape) so the behavioural coverage stays; only drop tests that tested the deleted route's own plumbing.
4. Update the three docs so they no longer mention the deleted routes.
5. Verify: gateway `bun test src/api src/hub src/mesh` (isolate flaky load tests), `bunx tsc --noEmit -p apps/gateway`, biome on changed files, and `bun run lint` at root.

## Common rules (apply to every task)

- Repo: Bun-only TypeScript monorepo at /Users/konata/code/tmex-r23 (git worktree, branch feat/round23-relay-legacy-removal). Bun is at /opt/homebrew/bin/bun (if not on PATH, read ~/.zshrc). Never use Node APIs that Bun lacks; runtime code runs on Bun 1.3.x.
- OTHER AGENTS ARE EDITING THIS SAME WORKTREE IN PARALLEL. Touch ONLY the files listed in your scope (plus new files you create inside your scope directories). Do not reformat or "clean up" files outside your scope. If you believe a change outside your scope is unavoidable, do NOT make it — describe it precisely in your result file under "需要指挥官处理".
- NO git operations at all (no add/commit/stash/checkout/reset). The commander commits.
- Do not run `bun install`, do not edit lockfiles, do not edit package.json dependencies unless your scope says so.
- Do not touch generated files: packages/shared/src/i18n/resources.ts, packages/shared/src/i18n/types.ts, resources/fe-dist/*, dist/*. i18n keys are edited in the locale JSON sources (find them under packages/shared/src/i18n/ — look for locales/*.json) and regenerated with `bun run build:i18n` at the repo root (you MAY run that if your scope includes i18n changes).
- Never touch the production tmex service (port 9883, ~/Library/Application Support/tmex) and never touch a tmux session named `tmex`. If you need tmux in tests, use a separate socket (`tmux -L tmex-e2e` or a unique name). Never run `tmux kill-server` on the default socket. Temporary gateway instances must set TMEX_TMUX_SOCKET to an isolated socket and use ports other than 9883/9663/19883/19663.
- Do not run the Playwright e2e suite (apps/fe/tests) — the commander runs it. In apps/fe, unit tests are `bun test src/`.
- Code style: TypeScript, biome. No unnecessary comments; comments only for genuinely complex logic, written in Simplified Chinese. Identifiers in English. Follow the existing patterns in neighbouring files. Keep files under 600 lines (complexity gate; run `bun run lint` at repo root to check — it runs biome + the complexity gate). Do not add entries to the complexity allowlist.
- Verify before finishing: (1) `bunx tsc --noEmit -p <package>` for each package you touched must not add errors versus baseline (baseline: packages/app 1 pre-existing error, everything else 0); (2) `bun test` inside each touched package (apps/fe: `bun test src/`) must pass — baselines: shared 534, ws-client 408, stores 440, panels 911, ui 110, api-client 175, app 690 (+1 known cpu-features fail), terminal-ui 400, theme 52, fe 1783, gateway 4046 (4 known load flakes under full parallel run: stream failover legacy 0x305, large raw-body push ×2, RtcPeerManager ice summary — rerun those files in isolation if they fail); (3) `bunx biome check <your files>`.
- Write tests for new behaviour (bun test, colocated `*.test.ts`). Prefer deterministic tests; no sleeps over 100 ms.
- When completely done, write your result report (Simplified Chinese, concise, technical) to the ABSOLUTE result path given in your task — list files changed, tests added, verification output summary, anything the commander must handle — and only then exit. The result file must be the LAST thing you write.
