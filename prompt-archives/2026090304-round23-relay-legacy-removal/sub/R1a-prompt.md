# R1a — 网关侧 code review 修复批次（prompt 存档）

> 存档说明：本批次由指挥者以英文 prompt 下发，原文照录。执行结果见 `R1a-result.md`。

You are an autonomous senior engineer executing fix batch **R1a** in the git worktree `/Users/konata/code/tmex-r23` (branch `feat/round23-relay-legacy-removal`, Bun-only TypeScript monorepo). Bun is at `/Users/konata/.bun/bin/bun` (on PATH). Result file (write LAST, Simplified Chinese): `/Users/konata/code/tmex-r23/prompt-archives/2026090304-round23-relay-legacy-removal/sub/R1a-result.md`.

Background: this round deleted the legacy terminal state stream on the gateway and moved to canonical v1.1 (see `prompt-archives/2026090304-round23-relay-legacy-removal/sub/L1a-result.md`, `sub/L1b-result.md`, `sub/L1c-result.md`; design in `plan-00.md` §二). A code review of the gateway diff (base fc7fdba3..HEAD) produced the findings below. For EACH: first verify it against the code (read the cited lines and their callers); if it is real, fix it with a test; if it is not real, say precisely why in the result file. Do not add hardening beyond what the finding needs.

Findings to handle (all in apps/gateway):

1. **`TMUX_SELECT` still bypasses live-geometry dedup** — `apps/gateway/src/ws/tmux-selection-handlers.ts` ~92/102: canonical clients now always send `wantHistory=false`, and the select path passes `distrustLive: true`. Per the v1.1 contract only `ResizePaneV11` with `geometryReason=resend` may distrust the live geometry. Remove `distrustLive` from the select path (keep the underlying tmux select). Test: select with size equal to live → no tmux resize; a following resend → forced resize.
2. **Failover re-negotiation fails open on missing/malformed HELLO** — `apps/gateway/src/mesh/stream-replay-state.ts` ~156/188 and `apps/gateway/src/mesh/forwarder-failover.ts` ~289: (a) if the new node stream does not answer HELLO within the timeout, the code still sends device connect / subscriptions and flushes the browser queue; (b) `beginResume()` does not reset `peerVersion`, and a HELLO decode failure keeps the previous version, so a malformed HELLO inherits the old "supported" verdict. Required: reset `peerVersion` to null at every resume; a decode failure marks the peer unsupported; a HELLO timeout sends ERROR to the browser and closes both sides, never replays/flushes. Tests for missing HELLO, malformed HELLO, and stale-state carry-over during a real failover.
3. **Rejecting an old node leaks the upstream mesh stream and the remote GatewaySession** — `apps/gateway/src/mesh/stream-replay-state.ts` ~33 and `apps/gateway/src/mesh/forwarder.ts` ~516: the rejection path only calls `closeBrowser()`, which removes the pump record and closes the browser socket but never closes `pump.stream` (and the browser close callback then cannot find the pump). Fix: close/discard the current and any in-flight stream before closing the browser, preferably by reusing the full pump teardown. Test asserts the upstream stream is closed after rejection.
4. **entry↔node failover does not restore viewport claims / latest geometry** — `apps/gateway/src/mesh/stream-replay-state.ts` ~105/210: failover creates a fresh GatewaySession on the node; the old session's viewport claims die with it, but replay only restores canonical subscriptions and agent subscriptions, not `TERM_VIEWPORT` / `TMUX_SELECT` / the latest `ResizePaneV11`. The browser's physical WS did not reconnect, so nothing re-declares. Fix: cache the latest viewport declaration and the latest resize per pane (and latest select if focus depends on it) per browser session and replay them after device connect, with the same `sizeEpoch` and `geometryReason=resend`. Test: two browsers, failover, owner/geometry unchanged afterwards. (Legacy replay used to re-send select/resize; this regressed when it was deleted.)
5. **`paneSizeEpochs` grows without bound with pane churn** — `apps/gateway/src/ws/gateway-session.ts` ~23, `apps/gateway/src/ws/tmux-geometry-handlers.ts` ~133: entries are only cleared on device disconnect / session close. Prune on canonical metadata pane removal (or snapshot reconcile). Test with pane churn.
6. **`stream-failover.integration.test.ts` bypasses the canonical feed under test** (~524/558): it calls `sendChunked(PaneData)` directly on all negotiated GatewaySessions, skipping pane retention / subscription acceptance / cursor replay, and collapses duplicate seqs before asserting continuity. Rewrite so a fake runtime ingests output through retention, only sessions with an accepted subscription receive data, and assert no duplicates, no gaps, and a correct cursor start across failover.

Rules:
- Scope: `apps/gateway/src/ws/**`, `apps/gateway/src/mesh/stream-replay-state*.ts`, `apps/gateway/src/mesh/forwarder*.ts`, `apps/gateway/src/mesh/failover-log*.ts`, `apps/gateway/src/mesh/integration/stream-failover.integration.test.ts`, `apps/gateway/src/tmux-client/**` only if a metadata hook is needed. Another agent (B5) is concurrently editing `apps/gateway/src/relay/**`, `apps/gateway/src/mesh/relay-*.ts`, `uplink-pool*.ts`, `relay-wiring.ts`, `packages/shared/src/relay/**`, `packages/app/**` — do not touch those. Do NOT edit `packages/shared`, `packages/ws-client`, `apps/fe` (a Playwright e2e run is in progress; edits there would corrupt it) — if finding 2/3's ERROR needs a shared error code to become user-visible on the client, keep the existing `ERROR_UNSUPPORTED_PROTOCOL` + `canonical-state-v1.1 required` message prefix on the gateway and note that the client-side mapping is handled in a later batch.
- Read-only git only. No `bun install`. Never touch production tmex (9883, ~/Library/Application Support/tmex) or tmux session `tmex`; tests must not need tmux or must use an isolated socket. No TODOs. Files ≤600 lines; no complexity-allowlist additions.
- Verify: `cd apps/gateway && bun test src/ws src/mesh/stream-replay-state.test.ts src/mesh/forwarder.test.ts src/mesh/integration/stream-failover.integration.test.ts src/tmux-client`, then full gateway `bun test` (baseline 4077 pass + up to 4 known load flakes; B5 is adding tests concurrently — report your own), `bunx tsc --noEmit -p apps/gateway` (0 errors), `bunx biome check` on changed files, `bun run lint`.
- Result file: per finding — verdict (real / not real + why), fix summary with file:line, tests added; final test counts; anything left for the commander. Then reply with a short summary.
