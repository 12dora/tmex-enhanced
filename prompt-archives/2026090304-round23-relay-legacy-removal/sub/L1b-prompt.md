# Task L1b — gateway: delete the legacy terminal state stream, adopt canonical v1.1 resize semantics, gate old peers

Result file (absolute): /Users/konata/code/tmex-r23/prompt-archives/2026090304-round23-relay-legacy-removal/sub/L1b-result.md

READ FIRST: /Users/konata/code/tmex-r23/prompt-archives/2026090304-round23-relay-legacy-removal/plan-00.md (the round plan) and /Users/konata/code/tmex-r23/prompt-archives/2026090304-round23-relay-legacy-removal/sub/EX2-leftovers-map.md §A (the exact legacy inventory with line numbers) and /Users/konata/code/tmex-r23/prompt-archives/2026090304-round23-relay-legacy-removal/sub/L1a-result.md (the shared-package API another agent just added: canonical v1.1 ResizePane variant, geometry reason, sizeEpoch, capability 'canonical-state-v1.1', peerSupportsCanonicalV11(), canonical metadata fields for tree order/custom names).

## Scope (files you may edit)
- apps/gateway/src/ws/** (all files, including tests)
- apps/gateway/src/mesh/stream-replay-state.ts, stream-replay-state.test.ts, apps/gateway/src/mesh/integration/stream-failover.integration.test.ts, apps/gateway/src/mesh/forwarder.test.ts (ONLY the legacy snapshot/replay parts), apps/gateway/src/mesh/forwarder.ts ONLY if the legacy replay branch lives there
- apps/gateway/src/tmux-client/runtime/event-bridge.ts (the metadata projection producer) if needed to emit the new canonical metadata fields
Do NOT touch packages/*, apps/fe, apps/gateway/src/hub, apps/gateway/src/relay, other apps/gateway/src/mesh files (relay agents are editing mesh/uplink-*, mesh-runtime, peer-*, mesh-routes and new relay-* files concurrently).

## What to do
1. **Canonical v1.1 resize**: consume the new `ResizePaneV11` (or whatever L1a named it) in canonical-feed-session.ts; extend the gateway resize callback (apps/gateway/src/ws/index.ts ~370-374, tmux-geometry-handlers.ts ~143-192, ~251-280, viewport-policy.ts) with `{ reason: 'change'|'resend', sizeEpoch }`: `change` = browser viewport declaration (current `TERM_RESIZE` semantics), `resend` = warm switch / reconnect re-declaration (current `TERM_SYNC_SIZE` semantics, the `distrustLive`/`skipResize` path of commit 4c7b5274). Drop stale epochs (an epoch lower than the last accepted `change` epoch for that pane/session is ignored). Keep accepting v1 `ResizePane` as `change` with epoch 0 for the transition (it will be removed later — note it).
2. **Capability & gate**: advertise `canonical-state-v1.1` in HELLO_S2C capabilities (apps/gateway/src/ws/index.ts ~519-553). Record `HELLO_C2S.clientVersion`; if it fails `peerSupportsCanonicalV11` (fail-closed) reject the session with an ERROR frame whose code the FE can show (add a constant, e.g. `ERROR_CLIENT_TOO_OLD` — check how error kinds are defined in the ws borsh kind table; if adding a kind requires editing packages/shared (out of scope) then reuse an existing generic error kind with a distinct message string and note it). Never fall back to legacy.
3. **Delete legacy on the gateway**: everything in EX2 §A.3 "gateway" — switch-barrier.ts (+test), legacy-feed-broadcaster legacy segments (keep generic events/errors/clipboard; rename the file if 'legacy' no longer fits — but only within apps/gateway/src/ws), tmux-command-handlers subscribe/history, tmux-kind-handlers, tmux-selection-handlers barrier/observer parts (keep underlying tmux select; migrate to canonical SelectPane if the ws-client already sends it — check canonical-state.ts command enum for SelectPane; if none exists, keep `TMUX_SELECT` handling since it is not a legacy STATE kind), tmux-viewport-handlers TERM_RESIZE/TERM_SYNC_SIZE, device-connection-registry legacy observers, index.ts legacy output/snapshot/observer wiring (~342-362, 609-640, 729-744, 770-775), borsh/codec-borsh legacy kinds, websocket-send-guard TERM_OUTPUT branch, borsh/session-state select-transaction/output buffering (keep bell/notification throttle). The gateway must no longer send STATE_SNAPSHOT / STATE_SNAPSHOT_DIFF / TERM_HISTORY / LIVE_RESUME / SWITCH_ACK / TERM_OUTPUT nor accept TMUX_FETCH_PANE_HISTORY / TMUX_SUBSCRIBE(legacy) / TERM_RESIZE / TERM_SYNC_SIZE — unknown legacy kinds get the existing ERROR_UNKNOWN_KIND path. The shared kind constants still exist (another agent deletes them last); just stop referencing them.
4. **Legacy overlay → canonical metadata**: the canonical feed currently also sends a legacy STATE_SNAPSHOT overlay for tree order / custom names (index.ts ~342-362). Emit those fields inside the canonical SourceMetadataRecord/Patch instead, using the new fields L1a added, from the metadata projection producer (event-bridge.ts + wherever the canonical feed session builds records). Then delete the overlay send.
5. **mesh replay**: stream-replay-state.ts legacy subscribe/select/replay (107-120, 168-223, 587-626) → delete; only canonical cursor replay remains. entry↔node: if the peer node's version fails `peerSupportsCanonicalV11`, mark the stream/peer as unsupported with a clear error to the browser (no legacy fallback). Update stream-failover.integration.test.ts (delete the legacy failover case) and forwarder.test.ts legacy helpers.
6. Rewrite/delete tests per EX2 §A.3 gateway test list; add tests for reason/epoch handling and the version gate.
7. Verify: `cd apps/gateway && bun test src/ws src/mesh/stream-replay-state.test.ts src/mesh/integration/stream-failover.integration.test.ts src/mesh/forwarder.test.ts src/tmux-client` then the full gateway `bun test` (baseline 4046 + 4 known flakes; the count will drop as you delete tests — report the new count), `bunx tsc --noEmit -p apps/gateway`, biome, `bun run lint`.

Result file: list of deleted kinds/handlers, the new callback signature, the gate error code, the new test count, and anything left for the ws-client/stores agent (L1c) or terminal-ui agent (L1d).
## Common rules (apply to every task)

- Repo: Bun-only TypeScript monorepo at /Users/konata/code/tmex-r23 (git worktree, branch feat/round23-relay-legacy-removal). Bun is at /opt/homebrew/bin/bun (if not on PATH, read ~/.zshrc). Never use Node APIs that Bun lacks; runtime code runs on Bun 1.3.x.
- OTHER AGENTS ARE EDITING THIS SAME WORKTREE IN PARALLEL. Touch ONLY the files listed in your scope (plus new files you create inside your scope directories). Do not reformat or "clean up" files outside your scope. If you believe a change outside your scope is unavoidable, do NOT make it — describe it precisely in your result file under "需要指挥官处理".
- NO git operations at all (no add/commit/stash/checkout/reset). The commander commits.
- Do not run `bun install`, do not edit lockfiles, do not edit package.json dependencies unless your scope says so.
- Do not touch generated files: packages/shared/src/i18n/resources.ts, packages/shared/src/i18n/types.ts, resources/fe-dist/*, dist/*. i18n keys are edited in the locale JSON sources (find them under packages/shared/src/i18n/ — look for locales/*.json) and regenerated with `bun run build:i18n` at the repo root (you MAY run that if your scope includes i18n changes).
- Never touch the production tmex service (port 9883, ~/Library/Application Support/tmex) and never touch a tmux session named `tmex`. If you need tmux in tests, use a separate socket (`tmux -L tmex-e2e` or a unique name). Never run `tmux kill-server` on the default socket. Temporary gateway instances must set TMEX_TMUX_SOCKET to an isolated socket and use ports other than 9883/9663/19883/19663.
- Do not run the Playwright e2e suite (apps/fe/tests) — the commander runs it. In apps/fe, unit tests are `bun test src/`.
- Code style: TypeScript, biome. No unnecessary comments; comments only for genuinely complex logic, written in Simplified Chinese. Identifiers in English. Follow the existing patterns in neighbouring files. Keep files under 600 lines (complexity gate; run `bun run lint` at repo root to check — it runs biome + the complexity gate). Do not add entries to the complexity allowlist.
- Verify before finishing: (1) `bunx tsc --noEmit -p <package>` for each package you touched must not add errors versus baseline (baseline: packages/app 1 pre-existing error, everything else 0); (2) `bun test` inside each touched package (apps/fe: `bun test src/`) must pass — baselines: shared 534, ws-client 408, stores 440, panels 911, ui 110, api-client 175, app 690 (+1 known cpu-features fail), terminal-ui 400, theme 52, fe 1783, gateway 4046 (4 known load flakes under full parallel run: stream failover legacy 0x305, large raw-body push ×2, RtcPeerManager ice summary — rerun those files in isolation if they fail); (3) `bunx biome check <your files>`.
- Write tests for new behaviour (bun test, colocated `*.test.ts`). Prefer deterministic tests; no sleeps over 100 ms.
- When completely done, write your result report (Simplified Chinese, concise, technical) to the ABSOLUTE result path given in your task — list files changed, tests added, verification output summary, anything the commander must handle — and only then exit. The result file must be the LAST thing you write.
