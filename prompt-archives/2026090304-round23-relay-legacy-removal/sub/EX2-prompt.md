You are exploring the tmex monorepo (Bun runtime, TypeScript) at the current directory, READ-ONLY. Output your complete report as your FINAL MESSAGE (you cannot write files). Write the report in Simplified Chinese (zh-CN), technical, concise, with exact file paths and line numbers.

Four leftover tasks from the previous round need precise implementation maps. Report for each:

### A. Remove the legacy terminal state stream, after adding canonical v1.1 size semantics
Context: the WS terminal protocol has a "canonical" state stream (borsh `PaneData` etc., wired up in round 21/22, see packages/shared/src/ws-borsh, apps/gateway/src/ws/*, packages/ws-client, packages/terminal-ui, apps/fe) and a "legacy" control/state stream (TERM_HISTORY / LIVE_RESUME / resize replay etc.). Recent commits: `39318f94 fix(canonical): 尺寸补发与尺寸变更仍走 legacy 控制面（canonical v1 无法区分二者）`, `4c7b5274 fix(ws): 区分「浏览器上报视口」与「暖切换窗口」两条 resize 语义`, `b220286d refactor(ws): liveWindowGeometry 迁入 viewport-policy`. Read those commits (`git show`) and the docs under docs/ (grep canonical, legacy, viewport-policy, ws-borsh spec).
Report: (1) exactly what "canonical v1 cannot distinguish size re-send vs size change" means in code (which messages/fields; where the gateway decides; where the client consumes); (2) a concrete proposal for canonical v1.1 fields (e.g. a `geometryReason`/`sizeEpoch` field) with the borsh schema location and the spec doc to update, and the version negotiation mechanism (how clients advertise canonical version / how min version 1.1.22 can be enforced — find the existing min-version / protocol-version negotiation code); (3) the complete inventory of legacy-only code paths to delete on gateway, ws-client, terminal-ui, fe, shared (kinds in the borsh kind table, handlers, tests, e2e helpers that rely on TERM_HISTORY like `writeRestoredHistory`, the `forceFullRepaint` legacy history backfill path), with line counts; (4) what the FE e2e tests (apps/fe/tests) assume about legacy frames and which specs will need rewriting; (5) risks (older nodes in the mesh: how entry↔node WS forwarding interacts — a 1.1.22 entry talking to an older node; the minimum-version gate the user decided: 1.1.22).

### B. Replace `tailwind-merge`
Find every use of `cn()` / `twMerge` / `clsx` in packages/ui, packages/panels, packages/terminal-ui, apps/fe (count per package), where `cn` is defined, the bundle cost (check apps/fe build stats or node_modules size of tailwind-merge), and whether the class merging semantics are actually relied upon (cases where two conflicting tailwind classes are passed and the later must win, e.g. `cn('p-2', className)` with className containing `p-4`). Grep for patterns where a caller passes a className that overrides padding/margin/color of the base classes. Propose the replacement approach (plain `clsx` concatenation vs a tiny custom merge) and list the components at highest risk of visual regression, so a per-page visual check list can be built.

### C. Delete three routes that only tests reference
`GET /api/tmux/tree`, `/api/settings/theme` (GET/PUT?), `POST /api/hub/nodes/:id/revoke`. For each: the handler file:line, the service functions that become dead once removed, tests referencing them, api-client functions, FE callers (should be none — verify), i18n keys, docs mentioning them.

### D. Delete the 7 old bench files without a `bench` script
Find all bench files (`*.bench.ts`, `bench/` dirs) across packages; identify which have a package.json script or README referencing them and which were added in round 22 (git log --diff-filter=A --since=2026-09-02) vs older. List the 7 old ones to delete and the 4 new ones to keep (plus render-bridge bench dir).

For each section give exact file lists so tasks can be dispatched to coding agents with non-overlapping file sets.
