You are a read-only code explorer. Repo (Bun + TypeScript monorepo): the current directory. Do NOT modify files. Produce a precise, file:line-referenced report (markdown, English) that a backend engineer can implement from without re-exploring. Be concrete; quote signatures.

Context: we are adding graphical (browser) management for hub/node features and built-in TLS. Backend work will be split into: (A) "setup wizard" HTTP API that, from a standalone gateway process, can (1) create the first user + root key + self-admit the local node cert (today: CLI `hub user add` in packages/app/src/commands/hub.ts runHubUserAdd), (2) join a hub (today: CLI `hub join` runHubJoin), (3) run `direct enable|disable` (packages/app/src/commands/direct.ts), (4) rewrite app.env keys (TMEX_ROLES, TMEX_HUB_PUBLIC_URL, TMEX_HUB_URL, and new TLS keys) and then `process.exit(0)` so the supervisor restarts it. (B) TLS: a second `Bun.serve` https listener with cert material stored encrypted in DB; self-signed local CA; ACME (acme-client) with http-01 and Cloudflare dns-01; join-token carrying CA fingerprint; uplink/hub-client trusting that CA via `tls.ca`.

Report on:
1. Process/runtime assembly: packages/app/src/runtime/server.ts and apps/gateway/src/index.ts — how config is loaded (loadEnv, config.ts), how HubRuntime/MeshRuntime/GatewayRuntime are built by role, how `onRestartRequested` works, where a second listener could be added, shutdown order. Which process runs in production (`runtime/server.js` built from which entry?) — check packages/app build scripts.
2. How the gateway process can locate app.env / installDir at runtime: env vars available (TMEX_INSTALL_DIR? TMEX_NATIVE_DIR? others) — grep packages/app/src/lib/install-layout.ts, run.sh generation in service.ts / init.ts, and the env loader packages/shared/src/env/load-env.ts. Is there an existing helper to read/modify/write app.env preserving other keys (init.ts / upgrade.ts / hub.ts writeEnv...)? Name it.
3. The `withAuth` / HubIo / ctx (userStore, userKeys, identityStore, keyLog...) used by runHubUserAdd/runHubJoin: what does `withAuth` construct (DB connection, master key) and can the same stores be obtained from inside the gateway process (which already has DB + stores wired in apps/gateway/src/auth/* and mesh-deps.ts)? Identify the minimal seam to reuse `bootstrapUserWithSelfAdmit` and the join commit logic (the function that atomically writes users/keylog/certs/node_identity) in-process. List dependency directions: can apps/gateway import from packages/app? (check package.json deps / tsconfig paths / existing imports) or must the logic move to packages/shared / a new package? Recommend a placement.
4. direct.ts: signature of enable/disable functions, what they need (installDir/nativeDir, fetch), how `direct_capable` is computed at runtime (TMEX_NATIVE_DIR? mesh-runtime), whether the native module can be loaded without process restart after download.
5. Existing HTTP route registration style for /api (mesh-routes.ts, auth-routes.ts, gateway routes): how to add new routes, session/auth middleware usage (session-middleware.ts), how "standalone vs mesh" mode is detected server-side (`/api/auth/mode`), how JSON errors are shaped.
6. DB: schema.ts + drizzle migrations dir & journal (latest number 0019), managed-migrations.ts registration; how a new table (e.g. `tls_config`) would be added; the encrypt/decrypt helper (apps/gateway/src/crypto/index.ts) usage pattern.
7. Join token: decodeJoinToken/encodeJoinToken location and format (128 chars base64url = enroll_sk || root pk || head hash); where token is produced in the UI path (apps/fe NodesPage → which API) and CLI enroll path; how to add an optional CA-fingerprint segment backward-compatibly.
8. Test conventions: bun test locations, test-support helpers for mesh (apps/gateway/src/mesh/test-support.ts), how to spin an in-process gateway for route tests; tsc command per package; biome config.

Also list the exact current numbers: run `bun test src/ 2>&1 | tail -3` in apps/gateway and packages/app is NOT required (the commander will do it). Just report code facts.
