## Findings

| Severity | File:line | Rationale | Concrete fix |
|---|---|---|---|
| blocker | [validation.ts:11](/Users/konata/code/tmex-enhanced-wt-merge/apps/fe/src/pages/settings/nodes/setup/validation.ts:11)<br>[enrollment.ts:603](/Users/konata/code/tmex-enhanced-wt-merge/apps/fe/src/node/enrollment.ts:603)<br>[hub-api.ts:26](/Users/konata/code/tmex-enhanced-wt-merge/apps/fe/src/node/hub-api.ts:26) | CA-pinned v2 join strings are unsupported in both directions. The wizard rejects the required `128-base64url.64-hex` form because `.` is forbidden, while browser-created enrollments ignore `ca_fingerprint` and always emit v1. A self-signed hub therefore cannot provide the pin needed for a joining node to fetch and trust its CA. | Add `ca_fingerprint` to `HubEnrollmentCreated`, append it to the browser-generated token after validating 64 lowercase hex, and accept exactly `/^[A-Za-z0-9_-]{128}(?:\.[0-9a-f]{64})?$/` in the wizard. Update the placeholder and add generation/submission tests for v1 and v2. |
| major | [https-section.tsx:54](/Users/konata/code/tmex-enhanced-wt-merge/apps/fe/src/pages/settings/nodes/https/https-section.tsx:54)<br>[acme-panel.tsx:246](/Users/konata/code/tmex-enhanced-wt-merge/apps/fe/src/pages/settings/nodes/https/acme-panel.tsx:246)<br>[acme-panel.tsx:315](/Users/konata/code/tmex-enhanced-wt-merge/apps/fe/src/pages/settings/nodes/https/acme-panel.tsx:315) | TLS mutations are not serialized. Save and renew have independent pending flags; ACME renew remains enabled during save, save remains enabled during renew, and all mode controls become available again while background ACME issuance is still `pending`. This can overlap issuance/listener changes, waste ACME attempts, or leave the listener inconsistent with the selected mode. | Use one mutation lock for save/renew and include `status.acme?.status === 'pending'`. Guard handlers as well as disabling every mode, form, save, and renew control until the operation completes. |
| major | [https-section.tsx:211](/Users/konata/code/tmex-enhanced-wt-merge/apps/fe/src/pages/settings/nodes/https/https-section.tsx:211)<br>[external-panel.tsx:51](/Users/konata/code/tmex-enhanced-wt-merge/apps/fe/src/pages/settings/nodes/https/external-panel.tsx:51) | Switching a running built-in listener to `none` or `external` stops the HTTPS endpoint serving the current page, with no confirmation. Although plain HTTP remains running, it may not be exposed through the firewall/router, so a remote administrator can lock themselves out immediately. | Before saving `none` or `external` while `status.listener.running`, require an explicit confirmation explaining that the current HTTPS endpoint will stop and that another reachable HTTP/proxy endpoint must already exist. |
| major | [setup-api.ts:55](/Users/konata/code/tmex-enhanced-wt-merge/packages/api-client/src/local/setup-api.ts:55)<br>[use-restart-waiter.ts:57](/Users/konata/code/tmex-enhanced-wt-merge/apps/fe/src/pages/settings/nodes/setup/use-restart-waiter.ts:57)<br>[local-machine-card.tsx:230](/Users/konata/code/tmex-enhanced-wt-merge/apps/fe/src/pages/settings/nodes/local-machine-card.tsx:230)<br>[use-restart-now.ts:25](/Users/konata/code/tmex-enhanced-wt-merge/apps/fe/src/pages/settings/nodes/https/use-restart-now.ts:25) | The advertised 60-second restart timeout does not bound an individual health request. A proxy or tunnel can hold `/healthz` open beyond the deadline, and unmount only suppresses later state updates—it cannot cancel the outstanding fetch. The duplicated card implementations also omit `cache: 'no-store'`. | Keep one neutral `waitForRestart` core using `startedAt`, `cache: 'no-store'`, and an abort signal capped by the remaining deadline. Move one shared `useRestartNow` above `setup/` and `https/`; let both cards use it while the setup hook wraps the same core for navigation. |
| minor | [https-section.tsx:78](/Users/konata/code/tmex-enhanced-wt-merge/apps/fe/src/pages/settings/nodes/https/https-section.tsx:78) | A failed renewal does not refresh status. Self-signed renewal can store a new certificate and then return `port_in_use`; the UI consequently keeps displaying the old certificate and listener state even though server state changed. | Invalidate/refetch TLS status in the renewal error path, as the save path already does. |
| minor | [error-messages.ts:7](/Users/konata/code/tmex-enhanced-wt-merge/apps/fe/src/pages/settings/nodes/setup/error-messages.ts:7)<br>[validation.ts:135](/Users/konata/code/tmex-enhanced-wt-merge/apps/fe/src/pages/settings/nodes/setup/validation.ts:135) | Known `join_failed` errors discard the contractually meaningful server message. Causes such as `ca_fingerprint_mismatch` are reduced to “The hub rejected the join request,” preventing useful diagnosis. | Let localized `join_failed` and other cause-bearing errors interpolate or append `SetupApiError.message`; keep static text for codes whose message adds nothing. |
| minor | [local-machine-card.tsx:142](/Users/konata/code/tmex-enhanced-wt-merge/apps/fe/src/pages/settings/nodes/local-machine-card.tsx:142) | After a successful direct-link restart, `restartRequired` is never cleared. Status is refreshed, but the panel and enabled “Restart now” button remain, inviting an unnecessary second restart. | Clear the local flag in the restart-success callback, then refresh local status. |
| minor | [tls-form.ts:18](/Users/konata/code/tmex-enhanced-wt-merge/apps/fe/src/pages/settings/nodes/https/tls-form.ts:18) | The IPv6 regex accepts malformed values such as `::::` and `1:2:3:4:5:6:7:8:9`, allowing inputs that the backend rejects as `invalid_sans`. | Replace the character-class check with a strict, browser-safe IPv6 parser and add malformed compression/group-count cases. |
| minor | [https-section.test.tsx:1](/Users/konata/code/tmex-enhanced-wt-merge/apps/fe/src/pages/settings/nodes/https/https-section.test.tsx:1)<br>[nodes-tab.test.tsx:1](/Users/konata/code/tmex-enhanced-wt-merge/apps/fe/src/pages/settings/nodes/nodes-tab.test.tsx:1) | The new frontend tests are mostly static rendering. They never execute TLS save/renew, direct restart, listener-disable confirmation, ACME polling transitions, or the actual Settings tab trigger, which is why the mutation races and stale state are uncovered. | Add injected-client interaction/state-machine tests for mutation serialization, `port_in_use` refresh, restart abort/timeout, v2 tokens, and listener shutdown confirmation; add one SettingsPage tab-selection test. |

## What is fine

- API paths, methods, bodies, response types, and nested error-envelope parsing match the supplied contracts.
- The `NodesPage` extraction preserves the original hook pipeline, enrollment expiry cleanup, management actions, props, and page behavior; the moved component bodies are otherwise unchanged.
- Settings tab placement/wiring and mesh-only sidebar visibility are correct, and the tightened title spacing retains the existing right-edge alignment.
- Passwords and Cloudflare tokens are neither logged nor stored in browser persistence. The token is omitted when blank, and the CA link correctly targets `/api/tls/ca.crt` with `tmex-ca.crt`.
- ACME query polling stops outside `pending` and is cleaned up by React Query; the hard navigation to `/login` after role-changing restarts is appropriate.

Verification: frontend tests passed `413/413`, API-client tests passed `128/128`, and frontend TypeScript passed. API-client TypeScript retains the five documented pre-existing test errors.