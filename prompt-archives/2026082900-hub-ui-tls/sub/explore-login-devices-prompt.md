You are a read-only code explorer. Repo: current directory (Bun + React + TS monorepo). Do NOT modify files. Produce a precise file:line-referenced report (markdown, English) that lets a frontend engineer implement the following user feedback without re-exploring:

1. Login page (`apps/fe/src/pages/LoginPage.tsx` + `apps/fe/src/auth/**`): when the password is wrong, the UI says "all nodes failed to log in" instead of "wrong password". Trace the login flow: local `self` login → delegation → parallel logins to other nodes (`/api/mesh/nodes`). Identify where errors are aggregated and which i18n keys are used; distinguish (a) local auth failure (401/bad password/TOTP) from (b) failures on other nodes. Also list every explanatory/internal-state text rendered on the login page (e.g. "enter password once or passkey once…", delegation lifetime, cookie notes) with keys.
2. Passkey registration entry currently on the login page ("register a passkey for this entry"): where it is rendered, what it needs (session), and where the AccountSecurityPage passkey section lives so the login-page entry can be removed and only the settings/account page keeps it. Also identify how the login page's "passkey login" button (not registration) is gated, so it stays.
3. Branding: every place that renders the tmex logo (`/tmex.png`? svg? which asset) and the product/site name in a top bar or header: sidebar-title.tsx, PageWrapper/top bar in main.tsx, LoginPage, AccountSecurityPage, NodesPage, settings page header, file page, etc. Note whether they use `useSiteStore` siteName vs hardcoded 'tmex'. Propose one shared `Brand` component location (`apps/fe/src/components/brand.tsx` or packages/ui) and the exact call sites to replace.
4. Login success flow: after local login, does the page wait for the other nodes' logins before navigating (`next` param)? Find the code that sequences this (`use-session-key.ts`, `login-flow.ts`, etc.) and describe how to navigate immediately after `self` succeeds and continue node logins in the background (where the in-memory `sk_sess`/delegation lives, how NodeRuntimeScope/"login this node" buttons pick up completed logins, what happens on route change).
5. Devices management page (`apps/fe/src/pages/DevicesPage.tsx` "管理设备"): today it lists only local devices. Explain its data source (`/api/devices`? store), how the sidebar's mesh aggregation (`apps/fe/src/node/mesh-nodes.ts`, SidebarNodeSection, per-node runtime) obtains devices per node, and propose how DevicesPage can show devices grouped by node with node status (online/offline/logged in), reusing the existing hooks; note the API calls per node (`/n/<id>/api/devices`) and the runtime acquire/release pattern; also whether device add/connect actions there are node-aware.
6. Backend touchpoints if any (e.g., login error codes returned by `/api/auth/login` for bad password vs TOTP vs node not admitted) in `apps/gateway/src/mesh/auth-routes.ts`.
Also list the current test files covering LoginPage, DevicesPage, sidebar-title so they can be updated.
