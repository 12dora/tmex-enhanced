You are a read-only code explorer. Repo (Bun + TypeScript + React monorepo): the current directory. Do NOT modify files. Produce a precise, file:line-referenced report (markdown, English) that a frontend engineer can implement from without re-exploring.

Context: we are adding (1) a new "Nodes" tab in the Settings page (apps/fe/src/pages/SettingsPage.tsx), (2) a nodes shortcut icon in the sidebar title next to the settings gear (apps/fe/src/components/page-layouts/components/sidebar-title.tsx) with tighter spacing between the top buttons, (3) a standalone-mode "enable hub" wizard (become hub / join hub) inside that tab, (4) an HTTPS configuration section (external proxy / self-signed CA / Let's Encrypt) inside that tab, (5) embedding the existing NodesPage management (apps/fe/src/pages/NodesPage.tsx) into the tab by extracting a shared component, plus a "this machine" block (role, hub url, direct enable/disable) and a link to /account/security.

Report on:
1. SettingsPage structure: tabs type, pill tab triggers, how each tab component receives `form`, how save works (settings-save-button, use-site-settings-form) and whether the new tab should be outside the site-settings form model. i18n keys used for tab labels and where they live.
2. sidebar-title.tsx: full component structure, the buttons rendered, their spacing classes (gap/px), the settings Link, how to conditionally render by mesh mode. Any tests covering it.
3. NodesPage.tsx (and NodesPage.test.tsx): component structure, hooks used (data fetching for /api/mesh/nodes, /mesh/ws events, enrollment flow, credential dialog, pending storage), which parts are page chrome (header/back button/withSidebar=false wrapper) vs reusable body; what a clean extraction boundary looks like (e.g. `NodesManagement` component + props). Route registration in apps/fe/src/main.tsx (nodesModule loader, PageWrapper).
4. How the FE learns mode: `/api/auth/mode` client + store/hook (name, shape: mode 'none'|'mesh', nodeId, username, hubPublicUrl, passkeyAvailable, isHub?), how standalone hides pages, how `self` vs `/n/:id` api clients are obtained (resolveNodeUrl, ApiClient usage, useNodeRuntime or similar).
5. api-client package: how to add new endpoints (packages/api-client), error handling, 401 interception; fetch helper for raw JSON.
6. AccountSecurityPage credential dialog / signing helpers (packages/shared/src/auth) that the "become hub" wizard would NOT need (it creates the user server-side) but the mesh "this machine" block might (direct toggle requires session only).
7. UI kit: available primitives in packages/ui (Button, Input, Tabs, Dialog, Select, Switch, Alert/Callout, Badge, Table, Code/copyable block), and toast (sonner) usage.
8. i18n: where locale JSON files live (three locales), the build command (bun run build:i18n), key naming conventions in settings/nodes namespaces, and the rule that resources.ts/types.ts are generated (never hand-edit).
9. Health polling / reload pattern: is there any existing "server restarting, wait for /healthz" UX (e.g. after upgrade) to reuse?
10. Test conventions for apps/fe (bun test src/, testing-library, msw or fetch mocks), and the current tsc/biome commands.
