## Findings

| Severity | File:line | Rationale | Concrete fix |
|---|---|---|---|
| blocker | [cert-authority.ts:93](/Users/konata/code/tmex-enhanced-wt-merge/packages/app/src/tls/cert-authority.ts:93), [hub.ts:136](/Users/konata/code/tmex-enhanced-wt-merge/packages/app/src/commands/hub.ts:136), [hub-client.ts:69](/Users/konata/code/tmex-enhanced-wt-merge/packages/app/src/lib/hub-client.ts:69), [mesh-runtime.ts:775](/Users/konata/code/tmex-enhanced-wt-merge/apps/gateway/src/mesh/mesh-runtime.ts:775) | The fingerprint covers only the first certificate in the unauthenticated PEM response, but the entire response is persisted and passed to `tls.ca`. A MITM can return `real pinned CA || attacker CA`; the first SPKI matches, while subsequent join HTTP and uplink TLS also trust the attacker CA. | Bound the response size, require exactly one PEM certificate with no trailing data, validate CA constraints/key usage, and persist only that canonical certificate. Add a matching-first-plus-rogue-second regression test. |
| major | [hub-trust-store.ts:19](/Users/konata/code/tmex-enhanced-wt-merge/apps/gateway/src/auth/hub-trust-store.ts:19), [hub-client.ts:67](/Users/konata/code/tmex-enhanced-wt-merge/packages/app/src/lib/hub-client.ts:67), [mesh-runtime.ts:766](/Users/konata/code/tmex-enhanced-wt-merge/apps/gateway/src/mesh/mesh-runtime.ts:766) | Trust keys only strip trailing slashes. Equivalent URLs such as uppercase hosts or explicit default ports miss the record, and lookup silently falls back to system trust. Manual env edits or migrations can therefore downgrade pinning. | Use one canonical URL routine for validation, env persistence, DB keys, HTTP lookup, and uplink lookup. Normalize host/default port/path consistently, reject credentials/query/fragment, migrate old keys, and fail closed when pinned trust is expected. |
| major | [tls-service.ts:217](/Users/konata/code/tmex-enhanced-wt-merge/packages/app/src/tls/tls-service.ts:217), [tls-config-store.ts:133](/Users/konata/code/tmex-enhanced-wt-merge/apps/gateway/src/tls/tls-config-store.ts:133), [acme-service.ts:170](/Users/konata/code/tmex-enhanced-wt-merge/packages/app/src/tls/acme-service.ts:170) | TLS mutations are unsynchronized. An old ACME job writes certificate/key/status before its generation check, and mode changes or shutdown do not invalidate it. It can overwrite newer self-signed material or restart HTTPS after `none`/`external`. Concurrent first-time self-signed requests can also interleave two different CAs and leaves. | Serialize TLS mutations. Make ACME issuance return material rather than commit it; atomically commit only when epoch, mode, domain, challenge, and directory still match. Invalidate active work on every mode change and shutdown. |
| major | [https-listener.ts:39](/Users/konata/code/tmex-enhanced-wt-merge/packages/app/src/tls/https-listener.ts:39), [server.ts:39](/Users/konata/code/tmex-enhanced-wt-merge/packages/app/src/runtime/server.ts:39) | Bun 1.3 declares `Server.stop(true): Promise<void>`, but both HTTPS and plain-listener shutdown discard it. Same-port certificate reload can race the old listener and falsely report `EADDRINUSE`; asynchronous stop failures also bypass the `try/catch`. | `await server.stop(true)` in both places. Add same-port reapply and deferred shutdown-order tests. |
| major | [acme-service.ts:170](/Users/konata/code/tmex-enhanced-wt-merge/packages/app/src/tls/acme-service.ts:170), [tls-service.ts:335](/Users/konata/code/tmex-enhanced-wt-merge/packages/app/src/tls/tls-service.ts:335), [https-listener.ts:45](/Users/konata/code/tmex-enhanced-wt-merge/packages/app/src/tls/https-listener.ts:45) | ACME persists `status=ok` before listener activation. Bind errors are stored only in listener state and never thrown, so the scheduler treats the operation as successful, resets its backoff, and may leave HTTPS down until the next certificate renewal date. | Treat listener activation as part of completion. Persist an ACME/listener error and retry listener activation with backoff without unnecessarily reissuing the certificate. |
| major | [tls-service.ts:117](/Users/konata/code/tmex-enhanced-wt-merge/packages/app/src/tls/tls-service.ts:117), [tls-service.ts:273](/Users/konata/code/tmex-enhanced-wt-merge/packages/app/src/tls/tls-service.ts:273), [tls-service.ts:324](/Users/konata/code/tmex-enhanced-wt-merge/packages/app/src/tls/tls-service.ts:324) | Initial and manual issuance bypass the renewal scheduler. Their failures are swallowed by `queueAcmeIssue`; with `nextRenewAt=null`, later checks never retry. Restarting while the row is `pending` or failed before the first certificate also leaves it permanently stuck. | Route all issuance through one single-flight retry path. Resume pending/no-certificate ACME state on startup and arm the promised 1h→24h backoff after failures. |
| major | [acme-service.ts:104](/Users/konata/code/tmex-enhanced-wt-merge/packages/app/src/tls/acme-service.ts:104), [tls-service.ts:265](/Users/konata/code/tmex-enhanced-wt-merge/packages/app/src/tls/tls-service.ts:265) | Switching between Let’s Encrypt staging and production preserves the prior directory’s account URL, passes it to the new directory, and skips account creation. Issuance then fails persistently. | Associate the account URL with its directory, or clear it when `acmeStaging` changes while retaining the encrypted account key. Test both direction changes. |
| major | [acme-service.ts:129](/Users/konata/code/tmex-enhanced-wt-merge/packages/app/src/tls/acme-service.ts:129) | DNS-01 returns immediately after Cloudflare accepts the TXT write, while `acme-client` immediately performs a one-shot DNS lookup. This races DNS visibility. Additionally, `acme-client` suppresses removal-callback errors, allowing stale TXT records to remain while issuance reports success. | Poll authoritative DNS for the exact TXT value with a bounded timeout. Track outstanding record IDs outside callbacks and retry cleanup in an outer `finally`, logging/persisting cleanup warnings. |
| major | [direct.ts:193](/Users/konata/code/tmex-enhanced-wt-merge/packages/app/src/commands/direct.ts:193) | The claimed atomic replacement deletes the existing non-empty `native/` before renaming staging. If promotion fails or the process crashes between operations, the previously working addon is lost. | Rename the old directory to a backup, promote staging, restore the backup on failure, then delete it after success; alternatively use versioned directories plus an atomically replaced pointer. |
| major | [setup-service.ts:524](/Users/konata/code/tmex-enhanced-wt-merge/packages/app/src/runtime/setup-service.ts:524), [env-file.ts:41](/Users/konata/code/tmex-enhanced-wt-merge/packages/app/src/lib/env-file.ts:41) | Join’s staged promotion bypasses the env helper’s symlink-resolution behavior. With symlinked `app.env`, rename replaces the symlink rather than updating its target, leaving the persistent volume copy stale. | Resolve the actual write target before constructing the sibling staging path, then rename onto that target while preserving the symlink. Test existing and dangling absolute/relative links. |
| major | [tls-service.ts:224](/Users/konata/code/tmex-enhanced-wt-merge/packages/app/src/tls/tls-service.ts:224), [setup-service.ts:596](/Users/konata/code/tmex-enhanced-wt-merge/packages/app/src/runtime/setup-service.ts:596) | External mode is persisted and HTTPS stopped before the env write; a write failure returns 500 after already disabling HTTPS. Conversely, join promotes an old env snapshot and can overwrite a concurrent successful `TMEX_TRUST_PROXY` update. | Use a shared env-mutation lock. Stage external env first, then commit mode/listener state; re-read and merge unrelated keys immediately before join promotion. Add rollback and concurrent-writer tests. |
| minor | [cert-authority.ts:31](/Users/konata/code/tmex-enhanced-wt-merge/packages/app/src/tls/cert-authority.ts:31), [tls-service.ts:368](/Users/konata/code/tmex-enhanced-wt-merge/packages/app/src/tls/tls-service.ts:368) | CA and leaf validity starts exactly at issuer time, so a joining node slightly behind can reject a freshly issued leaf. The CA is also reused without expiry checks, allowing late-life leaves to outlive it. | Backdate `notBefore` by a small skew allowance while preserving the 398-day maximum; cap leaf expiry to CA expiry and define CA rotation behavior. |
| minor | [acme-challenge.ts:14](/Users/konata/code/tmex-enhanced-wt-merge/packages/app/src/tls/acme-challenge.ts:14) | Malformed percent encoding makes `decodeURIComponent` throw before route error handling. The public challenge endpoint therefore returns an uncontrolled 500 rather than 404. | Catch decoding failures and accept only the ACME base64url token alphabet before map lookup. |
| minor | [hub.ts:427](/Users/konata/code/tmex-enhanced-wt-merge/packages/app/src/commands/hub.ts:427) | All pinned `/api/auth/mode` failures are discarded and rewritten as “unable to resolve hub uid.” Network failure, v1 against a self-signed hub, and pinned hostname/certificate failure therefore give the same non-actionable CLI message. | Preserve/classify the cause and advise generating a v2 token for self-signed hubs or checking the pinned CA/hostname as appropriate. |
| minor | [join.test.ts:909](/Users/konata/code/tmex-enhanced-wt-merge/packages/app/src/commands/join.test.ts:909), [tls-service.test.ts:91](/Users/konata/code/tmex-enhanced-wt-merge/packages/app/src/tls/tls-service.test.ts:91), [uplink-client.test.ts:36](/Users/konata/code/tmex-enhanced-wt-merge/apps/gateway/src/mesh/uplink-client.test.ts:36) | Pinning is verified with injected fetches and option-shape assertions only. Tests also omit stale/out-of-order issuance, same-port awaited replacement, startup recovery, account-directory switching, failed native promotion, and symlinked env promotion. | Add a real ephemeral self-signed HTTPS/WSS join-and-restart integration test plus deterministic deferred-promise transition tests. Live ACME remains intentionally out of scope. |

## What is fine

- The current CA download route is publicly readable as required for pre-session join, while TLS status/mutation/renewal remain authenticated. No unauthenticated mesh mutation path was found.
- CA/leaf generation otherwise uses P-256/SHA-256 with appropriate CA constraints, signing usages, non-CA leaf constraints, `serverAuth`, and correct DNS/IP SAN classification.
- SPKI fingerprinting hashes the correct DER `SubjectPublicKeyInfo`. V2 parsing is strict lowercase hex, and a network MITM cannot strip the locally supplied suffix into v1.
- CA, leaf, ACME account keys, and Cloudflare tokens are encrypted at rest. Public APIs and reviewed logs do not expose private material.
- With a clean single-certificate CA response, all post-bootstrap join requests use `tls.ca`, trust is persisted only after verified join commit, and node uplink loads it after restart. A hub’s own uplink uses the in-memory path.
- ACME uses the correct production/staging directories, P-256 CSR key, `auto()` authorization/finalize flow, and the already-hashed DNS-01 value supplied by `acme-client`.
- The setup transition latch, abort propagation/body cancellation, typed direct errors, staged-file cleanup, manual partial-join recovery message, and setup exit-code fix are present.
- Migrations `0021`/`0022`, schema, snapshots, journal order, and managed migration list are consistent.