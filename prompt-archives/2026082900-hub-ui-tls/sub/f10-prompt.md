# Task F10 — Diagnose and fix: React #185 (infinite update loop) when opening a remote node's pane

Symptom (reproduced by the commander with Playwright against a 3-node mesh): after login on the entry node, clicking a pane of ANOTHER node in the sidebar navigates to `/n/<nodeId>/devices/<deviceId>/windows/@0/panes/%250` and the React Router error boundary shows "Unexpected Application Error! Minified React error #185" (Maximum update depth exceeded). Opening `/n/<nodeId>/devices` (the device list) works. Note the pane id in the URL is `%250` (the pane id `%0` percent-encoded) — check whether double-encoding/decoding of `paneId` (`%0` → `%250` → `%0`) or the window id `@0` plays a role, but the primary suspect is a hook whose `useSyncExternalStore`/`useEffect` produces a new snapshot object every render or a setState-in-render loop that only occurs for non-self node runtimes (per-node `NodeRuntimeBoundary` / `NodeRuntimeScope` / `DeviceIntentStore` / connection provider). Recently changed areas: `apps/fe/src/node/**`, `packages/stores/src/node-connection-manager.ts`, `packages/stores/src/**device**`, the DeviceConsole (`packages/panels/src/device-console/**`) and the sidebar's per-node sections.

How to reproduce locally without touching production (mandatory — never use port 9883, never the tmux session named `tmex`; use a separate tmux socket `tmux -L tmex-f10` if you need panes):
1. Build an unminified frontend: from repo root `cd apps/fe && bunx vite build --minify false --outDir /private/tmp/claude-501/-Users-konata-code-tmex-enhanced/aa9de10f-9bbe-4a6a-8bda-48566133bc05/scratchpad/f10-fe-dist` (vite config outDir is relative; pass an absolute path) so stacks show component names.
2. Start a local hub from source: copy the pattern in `/private/tmp/claude-501/-Users-konata-code-tmex-enhanced/aa9de10f-9bbe-4a6a-8bda-48566133bc05/scratchpad/smoke2/run.sh` and `/private/tmp/claude-501/-Users-konata-code-tmex-enhanced/aa9de10f-9bbe-4a6a-8bda-48566133bc05/scratchpad/smoke2/app.env` (production-mode scratch install: `app.env` + `resources/fe-dist` symlink + `resources/gateway-drizzle` symlink, run `bun packages/app/src/runtime/server.ts` in a restart loop). Use fresh dirs `/private/tmp/claude-501/-Users-konata-code-tmex-enhanced/aa9de10f-9bbe-4a6a-8bda-48566133bc05/scratchpad/f10-hub` (GATEWAY_PORT 22101, TMEX_PEER_PORT 29101, TMEX_MASTER_KEY = a base64 32-byte value) and `/private/tmp/claude-501/-Users-konata-code-tmex-enhanced/aa9de10f-9bbe-4a6a-8bda-48566133bc05/scratchpad/f10-node` (22102 / 29102). Point `TMEX_FE_DIST_DIR` at the unminified dist. Turn the hub into a hub: `PUT /api/tls {"mode":"selfsigned","sans":["localhost","127.0.0.1"],"tlsPort":29143,"bindHost":"127.0.0.1"}` then `POST /api/setup/hub {"hubPublicUrl":"https://127.0.0.1:29143","username":"u","password":"tmex-test-pass","directEnable":false}` (process exits, loop restarts it). Generate a join code with `bun packages/app/src/cli-auth-entry.ts enroll --install-dir /private/tmp/claude-501/-Users-konata-code-tmex-enhanced/aa9de10f-9bbe-4a6a-8bda-48566133bc05/scratchpad/f10-hub --ttl 30m` (env from that app.env + `TMEX_MIGRATIONS_DIR`, `TMEX_PASSWORD=tmex-test-pass`, run in background — it waits for the join), then on the node: `POST /api/setup/join {"hubUrl":"https://127.0.0.1:29143","token":"<code>","name":"f10-node","directEnable":false}`. Create a tmux session on the node's default socket? NO — the node lists the machine's tmux; to have a pane to click, make sure a session exists on a dedicated socket only if the gateway supports `TMEX_TMUX_SOCKET` (grep config.ts); otherwise rely on existing sessions being listed (read-only) — do not send keys to them.
3. Playwright (already a devDependency in apps/fe; see `/private/tmp/claude-501/-Users-konata-code-tmex-enhanced/aa9de10f-9bbe-4a6a-8bda-48566133bc05/scratchpad/repro2.ts` for the exact login + click script; adapt base URL to http://127.0.0.1:22101): log in, click a pane of the OTHER node in the sidebar, capture `pageerror` with the full component stack.

Then fix the root cause with a regression test (unit test reproducing the loop at hook/store level, e.g. snapshot identity stability). Verify with the same Playwright script that the pane opens and renders terminal output.

File ownership caveat: tasks F7 (apps/fe/src/auth/**, apps/fe/src/node/node-runtime-boundary.tsx, sidebar-node-section.tsx, LoginPage), F8 (brand.tsx, sidebar-title.tsx, main.tsx, packages/stores/src/site*.ts), F9 (DevicesPage, apps/fe/src/pages/devices/**, packages/panels/src/device-management/**) run in parallel. You may edit packages/stores/src/** (except site*.ts), packages/panels/src/device-console/**, apps/fe/src/node/** except node-runtime-boundary.tsx, apps/fe/src/pages/DevicePage.tsx. If the root cause lives in a file owned by another task, do not edit it — put the exact patch (unified diff) in your result file and the commander will apply it.

Clean up: kill your scratch servers and background processes before finishing; leave the scratch dirs.
Baselines: apps/fe 470/0 tsc 0; packages/stores 257/0 tsc 1; packages/panels 368/0 tsc 0.
Result: prompt-archives/2026082900-hub-ui-tls/sub/f10-result.md (root cause, fix, repro evidence before/after).
## Ground rules (apply to every task)

- Repo: /Users/konata/code/tmex-enhanced-wt-merge (branch chore/merge-hub-tabs). Bun monorepo (Bun 1.3.14); NOT Node-compatible. If `bun` is not on PATH, `source ~/.zshrc`.
- Other agents are editing this same worktree IN PARALLEL. Touch ONLY the files/directories listed in your scope. If you believe you need to change a file outside your scope, do not edit it — describe the needed change in your result file instead.
- NEVER run any git command that changes state (no add/commit/stash/checkout/reset). Read-only git (status/diff/log) is fine. The commander commits.
- NEVER touch the production tmex service (launchd, port 9883, ~/Library/Application Support/tmex/) nor the tmux session named `tmex`. Do not run e2e (Playwright). Any ad-hoc server you start must use a scratch DB and ports in 20000-29999 and must be killed before you finish.
- Never lint/format generated files: packages/shared/src/i18n/resources.ts, types.ts, resources/fe-dist/*, dist/*. i18n: edit the three locale JSON sources, then run `bun run build:i18n` from the repo root.
- Code comments only where logic is non-obvious. Variable names in standard English. No TODOs, no stubs, no "simplified version" — finish the task fully. Do not restructure unrelated code.
- Verify before finishing: inside each package you touched run `bun test` (apps/fe: `bun test src/`), `bunx tsc --noEmit -p .` (error count must not exceed the baseline given to you), and `bunx biome check <changed files>`. macOS has no `timeout` command. Strip ANSI when parsing test summaries: `sed 's/\x1b\[[0-9;]*m//g'`.
- Follow the exploration report(s) given to you; if the code differs from the report, trust the code and note the discrepancy.
- Write your final report (English, markdown) to the result path given: what you changed (file list), how to verify, test/tsc numbers before/after, open issues, and any out-of-scope changes you need from others. The result file is the completion signal — write it last.
