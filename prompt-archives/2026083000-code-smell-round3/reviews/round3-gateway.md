- **BLOCKER** — [ssh-external-connection.ts:363](/Users/konata/code/tmex-enhanced-wt-smell/apps/gateway/src/tmux-client/ssh-external-connection.ts:363), [ssh-control-channel.ts:103](/Users/konata/code/tmex-enhanced-wt-smell/apps/gateway/src/tmux-client/ssh-control-channel.ts:103): old reconnect logic read live `this.sessionName` after the 500 ms backoff; HEAD captures it before waiting. If session A closes, the connection is reused for session B before the timer fires, old code probes B; HEAD probes stale A and can emit `session_closed` and shut down the fresh B connection. The helper test hard-codes one session name and misses this.

- **BLOCKER** — [session-commands.ts:142](/Users/konata/code/tmex-enhanced-wt-smell/apps/gateway/src/tmux-client/external/session-commands.ts:142), [session-commands.ts:325](/Users/konata/code/tmex-enhanced-wt-smell/apps/gateway/src/tmux-client/external/session-commands.ts:325), [session-commands.ts:336](/Users/konata/code/tmex-enhanced-wt-smell/apps/gateway/src/tmux-client/external/session-commands.ts:336): old `requestPaneHistory()` and `selectPaneInternal()` dispatched through `this.capturePaneHistory()`, which dispatched through `this.fetchPaneHistory()`. HEAD calls free helpers directly. A `SessionCommands` subclass overriding either method previously intercepted capture; HEAD instead executes base tmux queries and emits terminal history.

- **BLOCKER** — [session-pane-query.ts:40](/Users/konata/code/tmex-enhanced-wt-smell/apps/gateway/src/tmux-client/external/session-pane-query.ts:40), [session-lifecycle-commands.ts:13](/Users/konata/code/tmex-enhanced-wt-smell/apps/gateway/src/tmux-client/external/session-lifecycle-commands.ts:13), [session-command-runner.ts:26](/Users/konata/code/tmex-enhanced-wt-smell/apps/gateway/src/tmux-client/external/session-command-runner.ts:26): extracted helpers bypass public overrides of `SessionCommands.runTmux()` and `recoverFromTargetMissingError()`. For example, a subclass returning fixture output from `runTmux()` previously controlled `capturePaneText('%1')`; HEAD calls `host.runTmuxAllowFailure()` instead. Reproduced on HEAD: override calls `0`, host calls `1`.

- **BLOCKER** — [ws/index.ts:187](/Users/konata/code/tmex-enhanced-wt-smell/apps/gateway/src/ws/index.ts:187): old `sendEnvelope()` called `this.sendChunked()`. HEAD calls free `sendClientEnvelope()`, which calls free `sendClientChunked()`, bypassing subclass or instance overrides. With an overridden `sendChunked`, old code invokes the override; HEAD performs the real socket send. The new helper tests do not test this class delegation.

- **BLOCKER** — [ws/index.ts:195](/Users/konata/code/tmex-enhanced-wt-smell/apps/gateway/src/ws/index.ts:195): old `sendError()` encoded the error and called `this.sendEnvelope()`. HEAD calls free `sendClientError()`, bypassing overridden `sendEnvelope` and `sendChunked`. Reproduced on HEAD: both override counters remain `0`.

- **MINOR** — [ssh-external-connection.ts:218](/Users/konata/code/tmex-enhanced-wt-smell/apps/gateway/src/tmux-client/ssh-external-connection.ts:218): `configureSshWindowStyle()` now snapshots `sessionName` and `tmuxBin`. Old code reread them after its awaits. A concurrent direct `connect()` switching from session A to B while `set-hook` is pending makes old code continue against B; HEAD continues against captured A. Production runtime connect deduplication limits reachability.

- **MINOR** — [session-commands.ts:141](/Users/konata/code/tmex-enhanced-wt-smell/apps/gateway/src/tmux-client/external/session-commands.ts:141): disconnected `requestPaneHistory()` gains an observable microtask. Old code returned before any `await`; HEAD awaits an already-resolved helper. For `p.then(done); queueMicrotask(marker)`, ordering changes from `done, marker` to `marker, done`.

Focused verification: 239 changed-area tests passed; an additional 165-test integration-focused run passed. Repository TypeScript checks remain inconclusive due unrelated pre-existing errors.