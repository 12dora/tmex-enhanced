- **MINOR** — [canonical-screen-capture.ts:67](/Users/konata/code/tmex-enhanced-wt-smell/apps/gateway/src/tmux-client/runtime/canonical-screen-capture.ts:67): The old checkpoint literal called `createHistoryCursor` before evaluating `capturedAt`. The refactor evaluates `host.now()` first, then `buildCanonicalCheckpoint` calls `createHistoryCursor` at [canonical-screen-checkpoint.ts:125](/Users/konata/code/tmex-enhanced-wt-smell/apps/gateway/src/tmux-client/runtime/canonical-screen-checkpoint.ts:125). For a primary-screen capture where `now()` advances observable state and cursor creation depends on it, the old code can return a cursor while the new code returns `null`; normally, the timestamp now also precedes cursor-session creation instead of following it. The added helper tests inject a completed timestamp and do not cover this ordering.

Verification: 108/108 helper tests passed. The broader run passed 91 tests; five upload tests could not start because the read-only sandbox rejects their `mkdtemp` setup.