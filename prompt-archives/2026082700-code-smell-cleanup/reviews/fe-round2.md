- **Low · confident — 上传校验改变了文件处理顺序。** [use-directory-upload.ts:54](/Users/konata/code/tmex-enhanced-wt-smell/packages/panels/src/files/use-directory-upload.ts:54) 先把文件拆成 `oversized`/`accepted` 两组，再先显示全部超限错误、后上传全部合法文件。原实现 `bb9d84f:packages/panels/src/files/files-tab.tsx:356` 严格按用户选择顺序逐个处理，并等待每次合法上传结束后才检查下一个文件。例如 `[合法文件, 超限文件]` 现在会先报告第二个文件超限，再开始上传第一个文件；原来则在第一个文件上传完成后才报告超限，违反行为保持要求。可用一个未决的 `uploadFileChunked` mock 加 `toast.error` spy 验证调用顺序。

- **Low · confident — formatter 释放测试的关键断言实际为空。** [ghostty-wasm.alloc.test.ts:110](/Users/konata/code/tmex-enhanced-wt-smell/packages/ghostty-terminal/src/ghostty-wasm.alloc.test.ts:110) 返回硬编码 formatter handle `4096`，但没有把它加入 `liveAllocations`；[ghostty-wasm.alloc.test.ts:117](/Users/konata/code/tmex-enhanced-wt-smell/packages/ghostty-terminal/src/ghostty-wasm.alloc.test.ts:117) 的 `ghostty_formatter_free` 又是空函数。因此名为“释放 selection 与 formatter”的正常路径测试即使删除生产代码 [ghostty-wasm.ts:931](/Users/konata/code/tmex-enhanced-wt-smell/packages/ghostty-terminal/src/ghostty-wasm.ts:931) 的 `freeFormatter()` 仍会通过，无法防止 formatter handle 泄漏回归。让 fake formatter 创建/释放也通过同一记账表，并做一次删除 `freeFormatter()` 的 mutation run，即可验证测试应当失败。