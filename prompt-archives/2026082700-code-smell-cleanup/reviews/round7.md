## High

- **[高][确定] `stop()` 超时后的旧 run 会跨同进程重启占位，导致新消息永久滞留。** [supervisor.ts:147](/Users/konata/code/tmex-enhanced-wt-smell/apps/gateway/src/agent/supervisor.ts:147) 在 `start()` 时立即恢复接收请求，但 [supervisor.ts:175](/Users/konata/code/tmex-enhanced-wt-smell/apps/gateway/src/agent/supervisor.ts:175) 的恢复调用会被 [supervisor.ts:463](/Users/konata/code/tmex-enhanced-wt-smell/apps/gateway/src/agent/supervisor.ts:463) 中仍存在的旧 `activeRuns` entry 跳过。网关确实会在同一进程内执行 `stop → start`（[index.ts:13](/Users/konata/code/tmex-enhanced-wt-smell/apps/gateway/src/index.ts:13)）。此后提交会被当作旧 run 的队列消息；旧 promise 最终 settle 时只删除 entry，不会启动新 run 或消费队列。实际复现结果为 `{"creates":1,"secondKind":"queued","active":false,"queued":["second"]}`。新增测试 [supervisor.test.ts:589](/Users/konata/code/tmex-enhanced-wt-smell/apps/gateway/src/agent/supervisor.test.ts:589) 从未在超时后调用 `start()`，因此遗漏了真实重启路径。验证方式：让首个 run 超时未 settle，依次调用 `stop()`、`start()`、提交第二条消息，再 settle 首个 run；应确认第二个 run 被创建或 supervisor 继续拒绝输入，而不是留下无消费者的队列。