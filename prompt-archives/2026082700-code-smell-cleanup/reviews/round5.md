1. **中等严重度｜确定** — pending sweep 会重试已进入 WebSocket 背压队列的 `SourceGap`，导致重复 gap 或断开连接。  
   [pane-stream.ts:150](/Users/konata/code/tmex-enhanced-wt-smell/apps/gateway/src/ws/canonical/pane-stream.ts:150) 将任何 `sendPaneGap() === false` 都加入重试队列，[canonical-feed-session.ts:234](/Users/konata/code/tmex-enhanced-wt-smell/apps/gateway/src/ws/canonical-feed-session.ts:234) 又在 250ms 后直接调用 `onDrain()`。但生产 `sendEvent` 在 Bun `ws.send()` 返回 `-1` 时同样返回 `false`；此时发送守卫已经记录背压，帧并非普通的“未发送”。如果真实 drain 先到，queued gap 会再次发送；如果 sweep 先到，重试会把 `skippedFrame` 置为 true，随后真实 drain 在 [websocket-send-guard.ts:133](/Users/konata/code/tmex-enhanced-wt-smell/apps/gateway/src/ws/websocket-send-guard.ts:133) 以 `backpressure_gap` 终止连接。最小复现得到 `pendingPaneGaps=1`，sweep 后真实 drain 产生 `backpressure_gap`；反向顺序则观察到两次发送。  
   [canonical-feed-session.test.ts:449](/Users/konata/code/tmex-enhanced-wt-smell/apps/gateway/src/ws/canonical-feed-session.test.ts:449) 的测试把 `false` 模拟成完全未发送，并未经过 `WebSocketSendGuard`，因此无法覆盖生产中的 `-1` 状态。验证时应让伪 WebSocket 首次 `send()` 返回 `-1`，分别测试“sweep 先于 drain”和“drain 先于 sweep”。

2. **低严重度｜确定** — name-only `%session-renamed` 修复会错误解析以 `$数字 ` 开头的会话名。  
   [metadata.ts:25](/Users/konata/code/tmex-enhanced-wt-smell/apps/gateway/src/tmux-client/control-mode/metadata.ts:25) 在参数首词匹配 `/^\$\d+$/` 时，将其猜测为 session ID；但 tmux 的该通知参数整体就是会话名。收到 `%session-changed $0 old` 后，如果会话重命名为 `$3 renamed`，当前代码会生成 `{sessionId: '$3', name: 'renamed'}` 并覆盖缓存的 session ID，而正确事件应为 `{sessionId: '$0', name: '$3 renamed'}`。这会让 metadata projection 更新错误或不存在的 session。  
   [metadata.test.ts:46](/Users/konata/code/tmex-enhanced-wt-smell/apps/gateway/src/tmux-client/control-mode/metadata.test.ts:46) 反而把这种错误假设固化成测试。用上述两条通知即可复现。