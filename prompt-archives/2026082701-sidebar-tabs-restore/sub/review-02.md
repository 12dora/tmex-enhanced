You are a strict but pragmatic code reviewer for the tmex monorepo (Bun, React 19, Zustand, Tailwind, Playwright). Review the diff in `prompt-archives/2026082701-sidebar-tabs-restore/sub/review-02.diff` (also inspect surrounding code in the repo as needed, read-only).

Intent: restore the device connect/disconnect UI removed by upstream commit aa69374 on top of the current embeddable-panels architecture. Spec: prompt-archives/2026082701-sidebar-tabs-restore/sub/design-connect-ui-result.md. Focus on: connection-intent persistence and pruning logic in global-device-provider.tsx (render loops, stale closures, effects re-subscribing repeatedly, deleted-device cleanup), interaction between explicit disconnect and the two auto-subscribe paths (route entry, device tree expansion on mount), tmux store disconnectDevice change, Power button event propagation vs dnd-kit drag handlers, terminal-stage placeholder ordering, and e2e assertions matching the implementation.

Report only real problems: bugs, missed call sites, behaviour regressions vs the old tab UI (compare with `git show 465c94b^:apps/fe/src/components/page-layouts/components/app-sidebar.tsx`), broken e2e assertions (Base UI Tabs semantics), type issues, i18n key mismatches. For each: file:line, severity (high/medium/low), what is wrong, concrete fix. Skip style nits and speculative hardening. Final answer: the list, or "no findings".
