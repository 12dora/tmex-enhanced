You are a backend/devops engineer working in the tmex monorepo worktree at the current directory (Bun runtime; the code does not run on Node). Other agents are editing FRONTEND files (apps/fe/src, packages/panels, packages/stores, packages/shared/src/i18n, apps/fe/tests) in this same worktree at the same time. Do NOT touch those paths, do NOT run git commands that change state (no add/commit/checkout/stash/reset), do NOT run biome/lint on generated files, and do NOT build the frontend yet (their edits are in flight).

Task: make sure the Docker deployment path of this repo works with the CURRENT monorepo layout, so the commander can later run `docker compose up -d --build` and reach the app.

Files: `docker-compose.yml`, `apps/gateway/Dockerfile`, `apps/fe/Dockerfile`, any `.dockerignore`, `apps/fe/nginx*.conf` or similar, `packages/app` (the packaged runtime: `packages/app/src/runtime/server.ts`, `scripts/build-runtime.ts`, `scripts/copy-runtime-assets.sh`), root `package.json` scripts, `apps/gateway/package.json`, and `docs/` for any deploy docs.

Steps:
1. Read the Dockerfiles and compose file. Known issue already spotted: `docker-compose.yml` declares the `environment:` key twice on the gateway service (list form, then mapping form) — YAML duplicate key; fix so both SSH_AUTH_SOCK and the other vars survive. Also `version: '3.8'` is obsolete for compose v2 (warning only).
2. Statically verify each Dockerfile against the real repo: workspace packages it must copy for `bun install --frozen-lockfile` (check bun.lock / root package.json `workspaces`), the entrypoint file paths, the migrations dir (gateway uses drizzle migrations — find where they live and how `TMEX_MIGRATIONS_DIR` / `TMEX_FE_DIST_DIR` are resolved in `packages/shared/src/env/load-env.ts` and the gateway bootstrap), NODE_ENV=production semantics (production does NOT read repo env files — confirm required env vars are all supplied by compose), healthcheck URL/port, the fe image's nginx config proxying `/api` and websocket paths to the gateway service and serving the vite dist.
3. Decide whether the fe container should proxy to the gateway or whether the gateway already serves fe-dist itself in production (check `TMEX_FE_DIST_DIR` handling). Prefer the simplest working topology; if the gateway can serve the static fe-dist, consider whether the separate fe container is still needed — but do not delete the fe service unless it is clearly broken; report instead.
4. Build ONLY the gateway image to validate: `docker build -f apps/gateway/Dockerfile -t tmex-gateway:verify .` (the frontend image is built later by the commander). If the gateway Dockerfile builds the frontend too, stop before that step and report. Fix Dockerfile problems you find (missing workspace copies, wrong paths, wrong bun version) — these Docker/deploy files are yours to edit.
5. Check the gateway needs `tmux` and `ssh` client binaries inside the image (it drives tmux via control mode and ssh2 for remote devices) — verify they are installed in the Dockerfile; add if missing.
6. Run the gateway image once with a throwaway env to hit `/healthz`: use a random high port (e.g. `-p 18080:8080`), `TMEX_MASTER_KEY`/`JWT_SECRET`/`TMEX_ADMIN_PASSWORD` dummy values, tmpfs data dir; stop and remove the container afterwards. NEVER touch the host's production tmex service (port 9883, ~/Library/Application Support/tmex) or the tmux session named `tmex`.

Output at the end (plain text): what was wrong, what you changed (file list), the exact commands and their results (image build ok/fail, healthz ok/fail), and anything the commander must do when building the fe image. Also write the same report to `prompt-archives/2026082701-sidebar-tabs-restore/sub/grok-docker-result.md`.
