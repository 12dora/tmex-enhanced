- [apps/gateway/src/ws/device-connection-registry.ts:192](/Users/konata/code/tmex-enhanced-wt-tabs/apps/gateway/src/ws/device-connection-registry.ts:192) — **high**：连接创建期间点击断开不会取消 pending 建连。断开先发送 `device-disconnected`，随后 pending `getOrCreate()` 完成，仍会重新添加客户端并发送 `device-connected`。前端因此再次将 `deviceConnected` 设为 `true`，断开占位消失并恢复可交互终端，尽管连接意图仍为主动断开。为每个 websocket/device 记录连接请求 generation 或 desired set；断开时使 pending 请求失效，`await getOrCreate()` 后仅在请求仍有效时挂载客户端，否则释放无客户端 entry。增加 deferred-connect → disconnect 的竞态测试。

- [packages/stores/src/tmux.ts:166](/Users/konata/code/tmex-enhanced-wt-tabs/packages/stores/src/tmux.ts:166) — **medium**：`disconnectDevice` 没有清除 `lastConnectSentAt`。连接后 500ms 内断开并立即重连时，第二个 `connectDevice` 会把设备重新加入 `connectedDevices`，但被去重逻辑吞掉，不发送 `connect-device`，UI 将永久停在 `connecting`。断开时执行 `lastConnectSentAt.delete(deviceId)`，并增加 connect → disconnect → immediate connect 应发送两次连接命令的回归测试。