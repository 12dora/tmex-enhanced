You are a backend engineer in the tmex monorepo worktree at the current directory (Bun runtime; not Node). Other agents are editing frontend files concurrently; touch ONLY `apps/gateway/src/ws/**` (source + tests). No git state-changing commands. Minimal comments (repo rule). Never touch the host's production tmex service (port 9883) or the tmux session named `tmex`; gateway unit tests use their own fakes.

Bug (high, from code review): in `apps/gateway/src/ws/device-connection-registry.ts` (around line 192) a `disconnect-device` arriving while the same websocket's `connect-device` is still awaiting `getOrCreate()` does not cancel the pending connect. The disconnect sends `device-disconnected`, then the pending `getOrCreate()` resolves, re-attaches the client and emits `device-connected`, so the frontend flips back to connected although the user explicitly disconnected.

Fix: track per (socket, deviceId) a connect generation or a "desired" set; disconnect invalidates any pending connect; after `await getOrCreate()` attach the client only if the request is still valid, otherwise release the entry when it has no clients (reuse the existing release/refcount path — read how entries are released on disconnect and on socket close so you do not leak connections). Add a regression test (deferred connect → disconnect → connect resolves → no `device-connected`, entry released) next to the existing registry tests (find them: `ls apps/gateway/src/ws/*.test.ts`), following their fake/harness style.

Verify: `cd apps/gateway && bun test src/ws` and `bunx tsc --noEmit -p apps/gateway` (report the baseline error count first if it is non-zero, then confirm you added none); `bunx biome check` on edited files. Final answer: files changed, the mechanism, test output. Also write it to `prompt-archives/2026082701-sidebar-tabs-restore/sub/grok-ws-race-result.md`.
