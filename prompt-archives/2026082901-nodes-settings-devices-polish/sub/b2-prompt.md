You are a senior backend engineer working in the tmex monorepo at the current directory (Bun runtime, `export PATH=$HOME/.bun/bin:$PATH`; not Node-compatible). Other agents edit FRONTEND files concurrently; you may ONLY edit `packages/app/src/**` and `apps/gateway/src/**`. No git commands. Never touch the production tmex (~/Library/Application Support/tmex, port 9883) or the tmux session `tmex`.

Context: you (a previous run) implemented `leaveMesh()` in `packages/app/src/runtime/membership-reset.ts`, `POST /api/local/leave` in `local-routes.ts`, `quiesceMesh` wiring in `assemble.ts`, and CLI `hub leave` reuse in `commands/hub.ts` (see `prompt-archives/2026082901-nodes-settings-devices-polish/sub/b1-result.md`). A code review (`sub/review-backend.md`) found three issues; the commander judged all three valid. Fix them:

1. [blocker] Ordering: the DB `clearAll()` transaction commits before the fallible env write; if `patchOwnedEnvKeys()` throws, membership is gone while `app.env` still says a mesh role. Reuse the staged-env pattern already used by `joinHub()` in `setup-service.ts` (`newStagedEnvPath` / `resolveEnvWriteTarget` / atomic promote): write the standalone env to a staged file FIRST, then quiesce + `clearAll()`, then atomically promote the staged file (rename). If the DB step throws, delete the staged file and leave env untouched. Update `membership-reset.test.ts`: the env-write-failure test must assert membership remains intact (no rows deleted) when staging fails; add a test that a DB failure leaves env untouched.
2. [major] Quiesce must drain: make `quiesceMesh` in `assemble.ts` await `mesh.stop()` and an async `hub.stop()`; in `apps/gateway/src/hub/uplink-server.ts` (and the hub runtime it belongs to) reject new control work after stop is requested and await in-flight control handlers / key-log append tasks (track them in a Set of promises) with a bounded wait (e.g. 5 s) before resolving. Keep it contained: no protocol changes. If the drain times out, log and continue (the restart follows anyway) — do not throw. Add a focused unit test that an in-flight append completes before `stop()` resolves.
3. [major] CLI `hub leave` vs a running service: when a service manager is detected (the same detection `maybeRestart` uses), STOP the service before opening the DB and resetting, then START it afterwards (with `--no-restart`, leave it stopped and print a hint); without a service manager keep the current behaviour but print the existing "restart manually" hint. Update `join.test.ts` expectations accordingly.

Verification (report exact numbers): `cd packages/app && bun test` (baseline 406 pass / 0 fail; tsc 1 pre-existing error), `cd apps/gateway && bun test` (baseline 2454 / 0; tsc 21 pre-existing). `bunx biome check <changed files>` from repo root. No TODOs.
Write a concise English report to `prompt-archives/2026082901-nodes-settings-devices-polish/sub/b2-result.md`: files changed, how each finding was fixed, test/tsc numbers.
