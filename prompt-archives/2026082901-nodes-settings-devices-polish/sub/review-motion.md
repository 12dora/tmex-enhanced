1. **[major] `packages/theme/src/motion.css:64-74`** — Reduced-motion mode shortens animation duration but does not remove `animation-delay`. Delayed `<Reveal>` instances and staggered items remain invisible for up to 120/385 ms because `animation-fill-mode: both` applies the zero-opacity starting frame during the unchanged delay. This does not honor reduced-motion preferences. Disable the custom reveal animations under the media query, or at minimum force `animation-delay: 0ms !important`.

2. **[major] `apps/fe/src/pages/LoginPage.tsx:243`; `apps/fe/src/auth/credential-prompt.tsx:269`** — `empty:hidden` sets the empty live region to `display: none`, removing it from the accessibility tree. When an error arrives, the region and its content appear simultaneously, which screen readers do not reliably announce. Keep a persistent live region in the accessibility tree, such as an `sr-only` status node updated with the error, while rendering the visible error separately.

3. **[major] `apps/fe/src/pages/settings/nodes/https/parts.tsx:169`; `apps/fe/src/pages/settings/nodes/local-machine-card.tsx:600`; `apps/fe/src/pages/settings/nodes/management/enrollment-section.tsx:252`** — The whole visible button label is an `aria-live` region. After copying, it announces “Copied”, but the existing two-second reset also announces “Copy” without user action; changing the focused button’s accessible name may additionally duplicate the announcement. Keep the visible label outside the live region and add a persistent status node whose content is only populated while `copied` is true.

4. **[major] `packages/panels/src/connection-indicator.tsx:33-48`** — If `prefers-reduced-motion` becomes enabled while the indicator is in `exiting`, the effect does nothing because it only handles `phase === 'visible'`. The inline transition becomes `none`, so no `transitionend` arrives and the invisible fixed element remains mounted indefinitely. Handle reduced motion first—for example, immediately set `shouldShow ? 'visible' : 'hidden'`—regardless of the current phase.

No blockers found.