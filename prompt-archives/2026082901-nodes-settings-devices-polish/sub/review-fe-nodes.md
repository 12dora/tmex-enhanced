1. **[major]** `apps/fe/src/pages/settings/nodes/membership/use-leave-mesh.ts:159` — After `POST /api/local/leave` clears all node sessions, the existing page remains mounted while polling for restart. Any concurrent self request or mesh WebSocket reconnect that receives 401 triggers the global interceptor’s `/login` navigation, unmounting this hook and aborting the restart waiter before it can hard-navigate to Settings. Pause mesh queries/sockets and suppress or redirect expected self-401s during this intentional auth transition; add an integration test that injects a 401 between the leave response and restart detection.

2. **[major]** `apps/fe/src/pages/settings/nodes/membership/use-leave-mesh.ts:128` — `run()` has no synchronous in-flight guard. Two clicks before React commits `phase='leaving'` start two workflows; the credential prompt replaces the first pending request, making the first workflow interpret it as cancellation and continue leaving without revocation, while the second may submit another leave. Guard entry with an atomic `useRef`, and release it only after a definitive pre-submit failure/reset.

3. **[major]** `apps/fe/src/pages/settings/nodes/membership/use-leave-mesh.ts:137` — The restart baseline is sampled before the potentially long credential prompt. If the gateway restarts while the prompt is open, the subsequent waiter sees the already-changed `startedAt` and navigates immediately after the leave response, before the leave-triggered restart occurs. The imported probe also has no request timeout, so a hung `/healthz` leaves the modal permanently busy. Take a bounded baseline immediately after self-revoke and immediately before `leaveApi.leave`, using the timeout-aware restart probe.

4. **[major]** `apps/fe/src/pages/settings/nodes/membership/use-leave-mesh.ts:134` — A switch/change-hub intent is written before any work and is not cleared when `leave()` returns an error. A later unrelated transition to standalone will consume that stale marker and unexpectedly open the old wizard path. Write it only immediately before the leave request, clear it on definitive rejection/cancellation, and store a short-lived timestamp for ambiguous transport failures.

5. **[major]** `apps/fe/src/pages/settings/nodes/membership/leave-dialog.tsx:103` — `phase === 'timeout'` makes both `busy` and `done` false, re-enabling “Leave and restart” even though the destructive leave request already succeeded. Clicking it repeats the whole flow against cleared membership; cancelling leaves the stale mesh SPA mounted after auth has changed. Treat timeout as a post-commit terminal state and show only explicit “Reload”/“Check again” recovery actions.

6. **[major]** `apps/fe/src/pages/settings/nodes/nodes-tab.tsx:79` — While local status is loading, `local.status?.role === 'node'` is false, so `HttpsSection` mounts enabled and starts its TLS query. If TLS returns first, a pure node briefly exposes actionable HTTPS controls that should never be available. Delay mounting/enabling the section until the role is known, then enable it only for `hub,node`.

7. **[major]** `apps/fe/src/pages/SettingsPage.tsx:67` — URL synchronization ignores missing or invalid `tab` values after mount. Navigating from `/settings?tab=nodes` to `/settings` or `?tab=bogus` without remounting leaves the Nodes panel selected despite the URL’s documented general-tab fallback. Set `activeTab` to `general` in the invalid branch, or derive the active tab directly from `searchParams`.

8. **[major]** `packages/shared/src/i18n/locales/en_US.json:1197` (also `ja_JP.json:1197`, `zh_CN.json:1197`) — The destructive confirmation always says an old hub retains an offline record. That is false after successful pure-node self-revocation, and nonsensical for `hub,node`, where this machine is the hub and leaving disrupts its dependent nodes. Use role-specific consequence text in all three locales, including the hub shutdown impact and conditional wording for failed automatic revocation.

9. **[major]** `apps/fe/src/pages/settings/nodes/membership/use-leave-mesh.test.ts:10` — The new leave test only exercises `describeLeaveError`; the local-machine and NodesTab tests are static renders. The 55 selected tests pass, but none invokes `run()`, resolves/cancels the signer prompt, double-submits, advances restart polling, observes navigation, or verifies intent cleanup. Add an injectable orchestration/controller test covering exact call order, cancellation, duplicate confirmation, explicit leave failure, timeout, auth-transition 401, and successful hard navigation.

No blockers found. All nine role-pair classifications are correct, and the newly referenced i18n keys exist in all three locales.