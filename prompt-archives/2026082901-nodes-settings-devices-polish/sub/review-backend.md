1. **[blocker]** [packages/app/src/runtime/membership-reset.ts:62](/Users/konata/code/tmex-enhanced-wt-merge/packages/app/src/runtime/membership-reset.ts:62) — The membership transaction commits before the fallible env write. If `patchOwnedEnvKeys()` fails, every user, session, certificate, and node identity is permanently deleted, while `app.env` still says `node`/`hub,node`; `withSetupTransition()` also skips restart on the thrown error. The current process is already quiesced, and the next start boots a mesh role with no identity or usable login. Make the cross-resource transition recoverable: persist a durable reset-intent marker and complete it during startup, or otherwise keep the DB deletion rollback-able until the standalone env is committed. Once deletion commits, restart/recovery must be guaranteed. The `env_write_failed` test should assert membership remains intact or that automatic recovery completes the standalone transition.

2. **[major]** [packages/app/src/runtime/assemble.ts:246](/Users/konata/code/tmex-enhanced-wt-merge/packages/app/src/runtime/assemble.ts:246) — This is not a draining quiesce. `hub.stop()` is synchronous and only closes links; existing `UplinkServer` control-queue tails remain active, including an already-running `handleKeyLogAppend()` awaiting `keyLogSource.append()`. `leaveMesh()` can therefore begin `clearAll()` while an old uplink membership write is still executing. Make hub shutdown asynchronous, track and await all active control handlers/append tasks after rejecting new work, and only then clear the database. Do not silently proceed with deletion if the drain fails; use a reset/write barrier or abort safely.

3. **[major]** [packages/app/src/commands/hub.ts:796](/Users/konata/code/tmex-enhanced-wt-merge/packages/app/src/commands/hub.ts:796) — CLI leave opens the installed database directly and supplies a newly created in-memory setup lock. That lock cannot coordinate with the running service’s process lock, and this path supplies no `quiesceMesh`; the service can perform setup or uplink writes while the CLI deletes its membership. Restart happens only afterward, so restart failure, no service manager, or `--no-restart` leaves the old mesh process running against a wiped database. Prefer calling the local leave endpoint so the service owns locking and shutdown. For an offline fallback, stop the service before opening/resetting the DB and use a real cross-process lock; `--no-restart` should leave the service stopped.

A blocker was found: item 1.