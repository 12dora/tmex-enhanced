## Ground rules (read first)
- Repo: Bun monorepo, worktree branch `chore/merge-hub-tabs`. Bun only (no Node APIs assumed unless the file already is a Node CLI). Read `AGENTS.md`.
- Other agents edit other files in this same worktree concurrently. Touch ONLY the files listed in your scope. Do NOT run any git command that changes state (no add/commit/stash/checkout). Do not run lint/format on generated files (`packages/shared/src/i18n/*`, `resources/fe-dist`, `dist`).
- Never touch the production tmex on this Mac (launchd service on 9883, `~/Library/Application Support/tmex/`) nor the tmux session named `tmex`. Tests use their own tmux sockets / temp dirs.
- No credentials in files. No hardcoded passwords.
- No comments in code unless the logic is genuinely non-obvious. Variable names in English.
- Before claiming done: run the package's `bun test` (for `apps/fe` use `bun test src/`) and `bunx tsc --noEmit -p .` — the tsc error count must not exceed the existing baseline (gateway 21, theme 9, api-client 5, stores 1, app 1, others 0); `bunx biome check <changed files>` clean.
- Finish the whole task; no TODOs, no "simplified version".
- Write your final report (what changed, how verified, any open issues) to the result file path given below. Keep it concise, in English.

# Task: make the cross-NAT RTC direct path actually establish, expose the real transport, add ICE diagnostics

Scope (only these): `apps/gateway/src/mesh/peer-manager.ts`, `apps/gateway/src/mesh/peer-manager*.test.ts`, `apps/gateway/src/mesh/rtc/**`, `apps/gateway/src/mesh/mesh-routes.ts` (+ its test), `apps/gateway/src/mesh/mesh-deps.ts`, `apps/gateway/src/mesh/forwarder.ts` only if strictly needed, `apps/gateway/src/mesh/uplink-protocol.ts` / `apps/gateway/src/hub/**` only if a new ctl frame is needed, the shared DTO type file that defines the mesh node DTO (grep `MeshNodeDto` / `direct_capable` under `packages/shared/src` and `packages/api-client/src`) and `docs/hub/2026082800-hub-node-operations.md` (small additions). Do NOT touch `scripts/**`, `apps/fe/**`, `packages/app/**`.

Read first: the exploration report `/Users/konata/code/tmex-enhanced-wt-merge/prompt-archives/2026082802-hub-live-e2e/sub/explore-rtc.md` (full; it has file:line references) and the design doc `docs/hub/2026082700-hub-node-architecture.md` sections on direct path. Real-world symptom: public hub node (`hub,node`) and a NAT'd node, both `direct_capable=true`, reachable STUN, streams stay on hub relay, zero RTC logs.

Implement, with TDD (unit tests in the existing test files / `rtc-peer-manager.test.ts` using `rtc/test-fakes.ts`):

1. **Offerer wake-up (report §6.1, P0).** Today only the node that services a request calls `getLink()`, and `RtcPeerManager.connectToPeer()` makes the smaller node id the offerer, so when the requesting side is the answerer no SDP is ever sent. Fix so that a single-sided `getLink()` from the *larger*-id node still yields a DataChannel: send a signed/authenticated wake request over the existing hub ctl signaling channel (reuse the `rtc.signal` envelope with a new `kind`, or add a minimal ctl frame — pick whichever keeps the hub relay code untouched or nearly so; the hub must only forward, never interpret) so that the peer with the smaller id calls its own dial path toward the requester. Guard against storms (one pending wake per peer, cooldown, ignore when already `dc`). Also make `node.list`/presence changes that flip a peer to `direct_capable=true` schedule `maybeUpgrade()` on both sides (check whether that already happens via `notifyPeerEndpointsChanged`; if only endpoints changes trigger it, extend it to capability changes). Unit test: only the larger-id node calls `getLink()`; both ends reach `transportOf(peer) === 'dc'`.

2. **Upgrade awaitable + transport exposed (§6.2/§7).** Add `PeerManager.waitForTransport(nodeId, 'dc', timeoutMs)` (resolves early when already dc; rejects/false on timeout). Add `transport: 'ws-secure' | 'relay' | 'dc' | null` to the mesh node DTO returned by `GET /api/mesh/nodes` (`collectNodes()` → `peers.transportOf(id)`), keep `reach` unchanged for compatibility, update the shared DTO type and its tests. Do not block normal stream opening on the upgrade (streams keep using the current link; carrier-switch already migrates live sessions — verify by reading `rtc/carrier-switch.ts` and note in the result whether an already-open stream migrates to dc or only new streams use it).

3. **Diagnostics (§6.3).** Structured, rate-limited console logs prefixed `[mesh][rtc]`: dial start (peer, role offerer/answerer, stun count, turn enabled), signal send/recv (kind, sdp type, candidate type host/srflx/prflx/relay — never full SDP, never credentials, mask candidate IPs to /24 or last octet), gathering/ice/peer state changes, DataChannel created/open/error/closed, dial failure reason + fallback chosen. Extend `PeerConnectionLike` in `rtc/native.ts` with the optional state accessors/callbacks and wire them from node-datachannel (check the node-datachannel API in `node_modules/node-datachannel` — `onStateChange`, `onIceStateChange`, `onGatheringStateChange`, `getSelectedCandidatePair`) and from `test-fakes.ts`. Also log a one-line summary when ICE fails: `[mesh][rtc] ice failed peer=… local_types=[host,srflx] remote_types=[…]` so a reader can tell "no srflx" (STUN unreachable) from "srflx both sides but no pair" (needs TURN).

4. Update `docs/hub/2026082800-hub-node-operations.md`: the `transport` field, the wake mechanism in one paragraph, and how to read the new log lines in 常见排障.

Verification: `cd apps/gateway && bun test src/mesh` all green plus the full `bun test` (baseline 2311 pass, 0 fail), `bunx tsc --noEmit -p .` ≤ 21 errors, biome clean on changed files. Do NOT run the docker harness. Report which node id ordering the fix was tested under.

Result file: `/Users/konata/code/tmex-enhanced-wt-merge/prompt-archives/2026082802-hub-live-e2e/sub/grok-rtc-result.md`
