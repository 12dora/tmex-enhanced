## Ground rules (read first)
- Repo: Bun monorepo, worktree branch `chore/merge-hub-tabs`. Bun only (no Node APIs assumed unless the file already is a Node CLI). Read `AGENTS.md`.
- Other agents edit other files in this same worktree concurrently. Touch ONLY the files listed in your scope. Do NOT run any git command that changes state (no add/commit/stash/checkout). Do not run lint/format on generated files (`packages/shared/src/i18n/*`, `resources/fe-dist`, `dist`).
- Never touch the production tmex on this Mac (launchd service on 9883, `~/Library/Application Support/tmex/`) nor the tmux session named `tmex`. Tests use their own tmux sockets / temp dirs.
- No credentials in files. No hardcoded passwords.
- No comments in code unless the logic is genuinely non-obvious. Variable names in English.
- Before claiming done: run the package's `bun test` (for `apps/fe` use `bun test src/`) and `bunx tsc --noEmit -p .` — the tsc error count must not exceed the existing baseline (gateway 21, theme 9, api-client 5, stores 1, app 1, others 0); `bunx biome check <changed files>` clean.
- Finish the whole task; no TODOs, no "simplified version".
- Write your final report (what changed, how verified, any open issues) to the result file path given below. Keep it concise, in English.

# Task: parameterize the split harness for a second remote host (no domain, private CA, non-root ssh user)

Scope (only): `scripts/hub-e2e/split/run.sh`, `scripts/hub-e2e/split/setup-remote.sh`, `scripts/hub-e2e/split/setup-local.sh`, `scripts/hub-e2e/split/docker-compose.remote.yml`, `scripts/hub-e2e/split/docker-compose.local.yml`, `scripts/hub-e2e/split/Caddyfile` (may become a template), `scripts/hub-e2e/split/browser.ts`, `docs/hub/2026082801-hub-docker-e2e.md` (分体拓扑 section). No app code, no driver code, no docker builds/runs.

Today the split harness hardcodes: hub host `ai.jiefakj.com`, IP `43.248.129.233`, remote user `root`, remote dir `/root/tmex-e2e`, Let's Encrypt cert files at `/root/tmex-e2e/certs/{fullchain,privkey}.pem`, and `browser.ts` maps the domain to the IP with `ignoreHTTPSErrors:false`. We now also have a throwaway Tencent Cloud VM `118.195.194.170` (Ubuntu 24.04, user `ubuntu` with passwordless sudo, docker installed via apt, **no DNS name, no Docker Hub**, all inbound ports open) to run hub + coturn with real inbound UDP.

Make everything configurable by env with the current values as defaults so the existing invocation keeps working:
- `TMEX_E2E_HUB_HOST` (default `ai.jiefakj.com`), `TMEX_E2E_HUB_IP` (default `43.248.129.233`), `TMEX_E2E_HUB_PORT` (default 18443) → `HUB_PUBLIC_URL`, `extra_hosts` in the local compose (both nodes + driver), `browser.ts` `--map-host/--map-ip` defaults, Caddyfile site address, `TMEX_E2E_TURN_EXTERNAL_IP` default.
- `TMEX_E2E_REMOTE_USER` (default `root`) and `TMEX_E2E_REMOTE_DIR` (default `/root/tmex-e2e`): every `rssh`/rsync path and the remote compose volume paths must derive from these; when the user is not root, prefix docker/compose commands on the remote with `sudo` (add a `TMEX_E2E_REMOTE_SUDO` (default empty; `sudo` when user≠root) used consistently in `setup-remote.sh` and in every `rssh "docker …"` call in `run.sh`).
- `TMEX_E2E_TLS_MODE` = `letsencrypt` (default; unchanged behavior) | `private-ca`: in private-ca mode use `scripts/hub-e2e/ca/` (already generated by `gen-ca.sh`; `hub.crt` has SAN `hub.tmex.test` + `entry.tmex.test` — verify with openssl; if the chosen `TMEX_E2E_HUB_HOST` is not in the SAN, regenerate via `gen-ca.sh` with the host — read that script), rsync `ca/` to the remote (it is currently excluded), mount `ca.crt`/`hub.crt`/`hub.key` into caddy, set `NODE_EXTRA_CA_CERTS=/ca/ca.crt` for hub/node-a/node-b/driver by mounting `ca/ca.crt` (the Dockerfile/entrypoint already default `NODE_EXTRA_CA_CERTS=/ca/ca.crt` — check `entrypoint.sh`), make `curl_hub` use `--cacert`, and let `browser.ts` accept `--ca-file` → launch Chromium with the CA trusted: simplest is `ignoreHTTPSErrors: true` ONLY when `--insecure-tls` is passed (document that F is weaker in this mode).
- `TMEX_E2E_HUB_IP` also drives `sync_clocks`, `preflight_udp`, and the `.compose-bind.yml` fallback.

Keep `set -u` safety, keep the report format, keep all existing scenarios (including the LAN-DC variant another agent may have just added — re-read `run.sh` right before editing since it may have changed). `bash -n` all scripts; `docker compose config` on both compose files with default env and with the new-host env (`TMEX_E2E_HUB_HOST=hub.tmex.test TMEX_E2E_HUB_IP=118.195.194.170 TMEX_E2E_REMOTE_USER=ubuntu TMEX_E2E_REMOTE_DIR=/home/ubuntu/tmex-e2e TMEX_E2E_TLS_MODE=private-ca`); biome on `browser.ts`. Document the new variables and an example invocation for the second host.

Result file: `/Users/konata/code/tmex-enhanced-wt-merge/prompt-archives/2026082802-hub-live-e2e/sub/grok-harness-param-result.md`
