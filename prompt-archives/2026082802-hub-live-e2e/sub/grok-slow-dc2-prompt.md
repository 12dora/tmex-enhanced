## Ground rules (read first)
- Repo: Bun monorepo, worktree branch `chore/merge-hub-tabs`. Bun only (no Node APIs assumed unless the file already is a Node CLI). Read `AGENTS.md`.
- Other agents edit other files in this same worktree concurrently. Touch ONLY the files listed in your scope. Do NOT run any git command that changes state (no add/commit/stash/checkout). Do not run lint/format on generated files (`packages/shared/src/i18n/*`, `resources/fe-dist`, `dist`).
- Never touch the production tmex on this Mac (launchd service on 9883, `~/Library/Application Support/tmex/`) nor the tmux session named `tmex`. Tests use their own tmux sockets / temp dirs.
- No credentials in files. No hardcoded passwords.
- No comments in code unless the logic is genuinely non-obvious. Variable names in English.
- Before claiming done: run the package's `bun test` (for `apps/fe` use `bun test src/`) and `bunx tsc --noEmit -p .` — the tsc error count must not exceed the existing baseline (gateway 21, theme 9, api-client 5, stores 1, app 1, others 0); `bunx biome check <changed files>` clean.
- Finish the whole task; no TODOs, no "simplified version".
- Write your final report (what changed, how verified, any open issues) to the result file path given below. Keep it concise, in English.

# Task: slow-DataChannel 8 MiB stall STILL reproduces after 9bfecf0 — fix it against the live docker reproduction

Scope: `packages/shared/src/link/**`, `apps/gateway/src/mesh/rtc/**`, `apps/gateway/src/mesh/stream-targets.ts`, `apps/gateway/src/mesh/link-stream-carrier.ts`, `apps/gateway/src/mesh/forwarder.ts` (+tests), `apps/gateway/src/mesh/integration/dc-http-bulk.integration.test.ts`. No other agents are running now. You MAY use the local docker containers described below (they are test containers, not the production tmex; never touch the tmux session named `tmex` or the Mac's production install).

## Live reproduction (local Docker, no credentials needed)
Wait until `grep -q 'report at' /private/tmp/claude-501/-Users-konata-code-tmex-enhanced/a9d1822d-746f-46fa-a716-2a837fae36dc/scratchpad/split-run9b.log` (the harness run must finish first; poll every 30 s). Then containers `tmex-split-node-a` (entry) and `tmex-split-node-b` (target) stay up, joined to a remote hub, connected over the docker `lan` network with `tc netem delay 80ms rate 16mbit` on their `eth1` (check with `docker exec tmex-split-node-b tc qdisc show`; re-add with `docker exec <c> tc qdisc add dev eth1 root netem delay 80ms rate 16mbit` if missing). node-b's id is `ee647932c9c347ec443eabfd19e0d6e8`; the driver container `tmex-split-driver` has cookies at `/out/cookies-entry.json` (if 401, re-login: `docker exec -w /workspace tmex-split-driver bun /workspace/scripts/hub-e2e/driver-dist/login.js --base-url http://node-a:9883 --username alice --password 'TmexE2e!alice-2026' --target-node-id ee647932c9c347ec443eabfd19e0d6e8 --out /out/cookies-entry.json`). Reproduce:
```
docker exec -w /workspace tmex-split-driver bun /workspace/scripts/hub-e2e/driver-dist/nodes.js wait-transport --base-url http://node-a:9883 --cookie-file /out/cookies-entry.json --name ee647932c9c347ec443eabfd19e0d6e8 --transport dc --timeout 90000
docker exec -w /workspace tmex-split-driver bun /workspace/scripts/hub-e2e/driver-dist/files.js sha256 --base-url http://node-a:9883 --cookie-file /out/cookies-entry.json --node-id ee647932c9c347ec443eabfd19e0d6e8 --root-id $(cat /Users/konata/code/tmex-enhanced-wt-merge/scripts/hub-e2e/split/out/file-root-lan.json | bun -e 'const j=await Bun.file(process.argv[1]).json(); console.log(j.root?.id ?? j.id)' /dev/stdin) --path /e2e/bulk.bin
```
(read `scripts/hub-e2e/split/run.sh` `run_direct_scenarios` for the exact I1 invocation and `out/*-lan.json` names). Observed today: node-a logs `[mesh][http] forward aborted status=200 sent=1048508 expected=- reason=http stream aborted` ~13 s after DC open; node-b logs `[mesh][rtc] liveness timeout … idle_ms=10005` ~35–50 s after open. Both nodes' logs: `docker logs -t tmex-split-node-a`. Rebuild loop after a code change: `cd packages/app && bun run build:runtime && bun run build:cli && npm pack --pack-destination /private/tmp/claude-501/-Users-konata-code-tmex-enhanced/a9d1822d-746f-46fa-a716-2a837fae36dc/scratchpad && cp /private/tmp/claude-501/-Users-konata-code-tmex-enhanced/a9d1822d-746f-46fa-a716-2a837fae36dc/scratchpad/tmex-cli-1.0.2.tgz /Users/konata/code/tmex-enhanced-wt-merge/scripts/hub-e2e/build/tmex-cli.tgz && cd /Users/konata/code/tmex-enhanced-wt-merge && docker build --platform linux/arm64 -t tmex-e2e:split -f scripts/hub-e2e/Dockerfile scripts/hub-e2e && docker restart tmex-split-node-a tmex-split-node-b` (volumes persist; wait for `/healthz` on both, re-add netem, re-login, re-run). Add temporary verbose logging as needed but remove it before finishing.

Find the real cause of the 1 MiB stall (why node-a aborts the mux stream at exactly one `INITIAL_STREAM_WINDOW` after HEAD; whether WINDOW frames are sent by node-a and received by node-b; whether node-b's sender honours them; whether the abort comes from `LINK_STREAM_BACKPRESSURE_BYTES`/carrier, from mux receive-window overflow RST (check the RST payload text), or from the HTTP writer). Fix it, keep the fake-DC integration test but make it actually model the failure (rate-limited + delayed channel, receiver window updates), and prove with the live reproduction that the 8 MiB sha256 matches over the shaped DC and that L8 (`iptables -I OUTPUT -p udp -j DROP` on node-a mid-transfer, then read again over ws-secure) also passes.

Verification: unit/integration suites in scope 0 fail; `cd apps/gateway && bun test src/mesh` 0 fail; tsc ≤ 21; biome clean; live sha256 match logged in the result with the node logs.

Result file: `/Users/konata/code/tmex-enhanced-wt-merge/prompt-archives/2026082802-hub-live-e2e/sub/grok-slow-dc2-result.md`
