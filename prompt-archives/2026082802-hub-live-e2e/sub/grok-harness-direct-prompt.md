## Ground rules (read first)
- Repo: Bun monorepo, worktree branch `chore/merge-hub-tabs`. Bun only (no Node APIs assumed unless the file already is a Node CLI). Read `AGENTS.md`.
- Other agents edit other files in this same worktree concurrently. Touch ONLY the files listed in your scope. Do NOT run any git command that changes state (no add/commit/stash/checkout). Do not run lint/format on generated files (`packages/shared/src/i18n/*`, `resources/fe-dist`, `dist`).
- Never touch the production tmex on this Mac (launchd service on 9883, `~/Library/Application Support/tmex/`) nor the tmux session named `tmex`. Tests use their own tmux sockets / temp dirs.
- No credentials in files. No hardcoded passwords.
- No comments in code unless the logic is genuinely non-obvious. Variable names in English.
- Before claiming done: run the package's `bun test` (for `apps/fe` use `bun test src/`) and `bunx tsc --noEmit -p .` — the tsc error count must not exceed the existing baseline (gateway 21, theme 9, api-client 5, stores 1, app 1, others 0); `bunx biome check <changed files>` clean.
- Finish the whole task; no TODOs, no "simplified version".
- Write your final report (what changed, how verified, any open issues) to the result file path given below. Keep it concise, in English.

# Task: split harness — assert a real DataChannel, direct-link interruption without data loss, file bulk over DC

Scope (only): `scripts/hub-e2e/split/run.sh`, `scripts/hub-e2e/driver/nodes.ts`, `scripts/hub-e2e/driver/terminal.ts`, `scripts/hub-e2e/driver/files.ts`, new files under `scripts/hub-e2e/driver/`, `scripts/hub-e2e/split/docker-compose.local.yml` (only to add capabilities if needed), `docs/hub/2026082801-hub-docker-e2e.md` (only the 分体拓扑 section). Do NOT touch `scripts/hub-e2e/run.sh`, `scripts/hub-e2e/driver/login.ts`, `driver/totp*.ts`, `driver/lib.ts`, or any app source. Do NOT run the harness (it needs remote credentials the commander holds); the commander runs it and feeds results back. Do NOT run docker builds.

Context: read `docs/hub/2026082801-hub-docker-e2e.md` (分体拓扑), `scripts/hub-e2e/split/run.sh` scenario D (lines ~785–848) and how `driver terminal.ts` / `files.ts` / `nodes.ts` work. A backend engineer is concurrently changing `apps/gateway/src/mesh/**` with this contract, which you must code against (do not read their in-progress files for anything else):
- `GET /api/mesh/nodes` rows gain `transport: 'ws-secure' | 'relay' | 'dc' | null` (actual link transport to that peer from the entry); `reach` unchanged.
- gateway logs gain lines prefixed `[mesh][rtc]` (dial start / signal / ice state / datachannel open / ice failed with local/remote candidate types / fallback).
- a one-sided `getLink()` from either node now results in a DataChannel when both are `direct_capable` and ICE succeeds.

Implement:
1. **Scenario D** (rewrite the assertions): after `direct enable` + restarts, from entry node-a: `driver nodes.ts wait-transport --name <hubNodeId> --transport dc --timeout 90000` (new subcommand polling `/api/mesh/nodes`; also usable for `--name node-b`). Open the terminal stream node-a→hub (existing marker check) *after* transport=dc and record `transport` into `out/direct-path.json`. D1 = both rows direct_capable; D2 = transport reaches `dc` (FAIL, not SKIP, if it stays relay — include the `[mesh][rtc]` log excerpt from both node-a and the remote hub in `out/direct-logs-*.txt` and in the report row evidence column, e.g. last `ice failed` line); D3 = marker round-trip while transport=dc.
2. **Scenario H (direct interruption, no data loss)**: with transport=dc between node-a (entry) and the remote hub node, start a terminal stream and run in the remote pane a numbered producer, e.g. `for i in $(seq 1 400); do echo SEQ_$i; sleep 0.02; done`; mid-way inject an interruption of the direct path only. Use the least invasive mechanism that kills the DataChannel but leaves the hub uplink alive: inside node-a container drop UDP with `iptables -I OUTPUT -p udp -j DROP` (needs `cap_add: [NET_ADMIN]` on node-a/node-b in `docker-compose.local.yml` and `iptables` present in the image — check the Dockerfile's apt list; if iptables is absent, use `nft` if present, else fall back to `docker network disconnect`+`connect` of node-a's own bridge and document that it also bounces the uplink) — hub uplink is TCP/WSS so it survives. Assert: (a) `transport` falls back to `relay` within 30s, (b) after the producer finishes, the terminal capture contains SEQ_1…SEQ_400 contiguous with no gaps (use `terminal.ts` new mode `--capture-seq --expect-count 400` reading the pane scrollback via the existing tmux tree/stream API or by `docker exec ... tmux capture-pane -p -S -` on the remote through `rssh` — pick the more reliable; the point is the entry-side stream output, so prefer what the *browser* would have seen: the WS stream received by the driver), (c) remove the drop rule; transport returns to `dc` within 90s. Report rows H1–H3.
3. **Scenario I (file bulk over DC)**: with transport=dc, create a 8 MiB random file on the remote hub container, read it via entry node-a `/n/<hubId>/api/files/*` content endpoint (see `files.ts`), compare sha256; then with the UDP drop active, read again and assert it still succeeds (REST fallback) and sha256 matches. Check `apps/gateway/src/mesh/rtc/bulk.ts` and `forwarder.ts` only to learn which endpoint/headers use the bulk path and how to tell (response header or log line) whether bulk went over dc — record that in the evidence column; if the bulk path is browser-only (not usable from a plain fetch), say so in the doc and assert only correctness + fallback.
4. Update the 分体拓扑 scenario table and the 运行 notes for the new env/caps.

Verification: `bash -n` on the shell scripts, `bun test scripts/hub-e2e/driver/` (existing tests must pass; add tests for pure helpers such as sequence-gap detection), `scripts/hub-e2e/build-driver.sh` regenerates `driver-dist`, biome clean on changed TS.

Result file: `/Users/konata/code/tmex-enhanced-wt-merge/prompt-archives/2026082802-hub-live-e2e/sub/grok-harness-direct-result.md`
