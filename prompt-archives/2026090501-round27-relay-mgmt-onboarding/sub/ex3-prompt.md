You are exploring the tmex monorepo (Bun + React + TypeScript). READ-ONLY: do not modify files. Output the COMPLETE report as your final message (you cannot write files). Be precise: cite file paths with line numbers, quote the relevant i18n keys (packages/shared/src/i18n/locales/zh_CN.json is the source language; en_US/ja_JP mirror it; packages/shared/src/i18n/resources.ts and types.ts are generated, ignore them). Keep the report structured and actionable for engineers who will implement changes without having read the code.

Topic: Settings → Relay tab ("中继", apps/fe/src/pages/settings/relay/**: relay-tab.tsx, relay-cards.tsx, tenant-table.tsx, default-quota-card.tsx, password-dialog.tsx, quota-fields.tsx, relay-metrics-*.tsx, tenant-*.tsx, relay-format.ts) and the settings page tab registry/order (find where settings tabs are declared and ordered, incl. settings-tab-gating and the sidebar/route names).

Report:
1. Tab registry: file:line where tabs are declared, their i18n label keys, ordering, gating conditions; what is needed to rename "中继" to "中继管理" and place it immediately to the right of "多节点互联".
2. relay-format.ts: how rates (inbound/outbound rate, member/node rates) are formatted and where they show many decimals; propose formatting to at most 2 decimals.
3. Tables: describe apps/fe/src/pages/settings/components/wide-table.tsx, tenant-table.tsx, relay-metrics-members.tsx, nodes-table.tsx (management/) — do they share a primitive? Is there any sorting/filtering/search support today? What UI table primitives exist in packages/ui (Table components based on @base-ui/react + tailwind)? Is @tanstack/react-table or antd present anywhere in the lockfile? Recommend the least invasive way to add search + column sort + filter to the relay member ("接入管理") table with matching styles, default sort by node name.
4. "接入口令" card (password-dialog.tsx / relay-cards.tsx): all i18n keys and copy incl. "令牌下限" (token floor / minimum token) — explain what that setting actually does in the backend (packages/app relay password join, relay-store) so we can rewrite the copy in plain language or drop it.
5. Tenant table + default quota card: data shape, actions, how members relate to tenants (tenant id on member rows?), so that we can (a) move the tenant table above the members table, (b) selecting a tenant row filters/shows that tenant's members below, (c) move "接入口令" and "默认配额" into overflow (three-dot) menus that open dialogs. Point at existing DropdownMenu usage patterns in apps/fe (note: DropdownMenuLabel must be inside DropdownMenuGroup — a Base UI crash happened before).
6. Tests covering these files.
