## Shared rules for every coding agent (read fully)

- Repo: /Users/konata/code/tmex-r27 (git worktree, branch feat/round27-relay-mgmt-onboarding). Bun monorepo: apps/gateway (Bun HTTP/WS gateway), apps/fe (React 19 + Vite + Tailwind v4 + @base-ui/react wrapped in packages/ui), packages/app (npm `tmex-cli`: installer CLI + runtime that assembles gateway + relay), packages/api-client, packages/shared (i18n + types).
- Other agents are editing the SAME worktree in parallel. Touch ONLY the files listed in your scope. Do NOT run git add/commit/stash/checkout/reset. Do NOT run formatters over directories; `bunx biome check --write <your files>` only.
- Runtime is Bun only. Tests: `bun test <path>` (in apps/fe use `bun test src/<path>`; never bare `bun test` there — it picks up Playwright specs). Type check: `bunx tsc --noEmit -p <package dir>`. Do not run e2e (Playwright).
- i18n: zh_CN is the source language at packages/shared/src/i18n/locales/zh_CN.json; en_US.json and ja_JP.json must get the same keys. NEVER edit packages/shared/src/i18n/resources.ts or types.ts (generated); after changing locale JSON run `bun run build:i18n` from the repo root (it regenerates them; that is expected). Edit only the sub-objects you own; other agents edit other sub-objects of the same JSON files — keep edits surgical (no reformatting, no key reordering).
- Copy style (very important, user demand): concise, professional, easy to understand, like copy in large mature software (macOS System Settings / GitHub / Tailscale). No chatty explanations, no second person (no 你/您/your), no filler. One short sentence per hint. Chinese uses full-width punctuation. Terms: 本机 (this machine), Hub, 中继 (relay role), 租户 (tenant that joins a relay), 运营者 (relay operator), 加入码 (join code), 接入密码 (relay access password). Long explanations go into tooltips, not inline text.
- Code style: no unnecessary comments (only for genuinely non-obvious logic, in Chinese like the existing code), standard English identifiers, keep functions small (complexity gate: cyclomatic ≤ 15, files ≤ ~400 lines preferred). Follow existing patterns in neighboring files (Card/Badge/Button/DropdownMenu from @tmex/ui, data-testid attributes, `useTranslation`).
- Base UI gotcha: `DropdownMenuLabel` must be inside `DropdownMenuGroup` or the page crashes at runtime. Menus/Dialogs render in a portal: static render tests cannot see their content, so export the menu/dialog content as a plain component and test it directly (see existing `RelayActionsMenuList`, `BulkActionsMenuList`).
- Tests: keep existing tests green; update tests that assert on things you intentionally changed; add focused tests for new pure logic (sorting/filtering/formatting/state mappings). Do not delete tests to make them pass.
- When done, write a result report (Markdown) to the absolute path given in your task: what changed (files), tests run with counts, anything left undone or uncertain. Then exit.
- Never touch the production tmex install (~/Library/Application Support/tmex, port 9883) or the tmux session named `tmex`. Do not start dev servers.

# Task T3 — Settings "中继管理" (Relay Management) tab restructure (frontend, Opus)

Result file (write when done, then exit): /Users/konata/code/tmex-enhanced/prompt-archives/2026090501-round27-relay-mgmt-onboarding/sub/T3-result.md

## Your file scope (exclusive)
- apps/fe/src/pages/SettingsPage.tsx and apps/fe/src/pages/SettingsPage.test.tsx
- apps/fe/src/pages/settings/relay/** (everything in this dir; create new files here as needed)
- apps/fe/src/pages/settings/components/wide-table.tsx (only if needed)
- Locale JSON sub-objects `relay.admin.*`, `relay.metrics.*`, `settings.tabGroup.*` in zh_CN/en_US/ja_JP. Do NOT edit other sub-objects (`relay.tenant.*`, `nodes.*`, `connectDevices.*` belong to other agents).
- Do NOT touch packages/api-client/src/format.ts (shared); round rates inside apps/fe/src/pages/settings/relay/relay-format.ts.

Baseline before you start: `cd apps/fe && bun test src/pages/settings/relay src/pages/SettingsPage.test.tsx` → 138 pass / 0 fail; `bunx tsc --noEmit -p apps/fe` → 0 errors. Keep both at 0 fail / 0 errors.

## Context (from a code exploration; verify against the code)
- Tab registry: apps/fe/src/pages/SettingsPage.tsx — `SETTINGS_TAB_BAR` (visible order, ~line 122) and `RELAY_TAB_ITEM` (~line 137) appended last when `showRelay`. `OPTIONAL_SETTINGS_TABS = ['relay']` gates deep-link; keep it, and keep `relay` OUT of `SETTINGS_TABS` (chunk preload).
- Relay tab layout: apps/fe/src/pages/settings/relay/relay-tab.tsx (header + RelayMetricsPanel + password card + default quota card + tenants card). Members table lives inside relay-metrics-panel.tsx (owner of `useRelayMetrics`). tenant-table.tsx is a plain `<table>` inside `WideTableScroll`. No table library exists (no antd / tanstack-table); do NOT add one — extend the existing HTML tables with matching Tailwind styles.
- Rates: `formatBytesPerSec` → `formatRate` → `formatBytes` (packages/api-client/src/format.ts) prints raw floats below 1024 B (e.g. `12.345678 B/s`).
- Password copy keys: `relay.admin.password.*` (title 接入口令, epoch 口令 = "第 N 代", minTokenEpoch 令牌下限, …). `minTokenEpoch` is an internal epoch floor (tokens with epoch < floor are rejected after "作废旧令牌"). It is a diagnostic value, not a user setting.
- Members (`RelayMetricsMember`) carry `tenantId` + `nodeId` + `name` (currently always null from the backend; a backend agent is adding real names in parallel — write the sort as `name ?? nodeId` via `memberTitle`, it will pick up names automatically). Tenants (`RelayTenantSummary`) come from `/api/relay/status`.

## Requirements
1. Rename the tab label `relay.admin.tabLabel` to 中继管理 / Relay Management / 中継管理, and place the relay tab immediately to the RIGHT of 多节点互联 (`nodes`) in the visible tab bar (before 通知). Update tests that assert it is last.
2. Rates: 入站/出站/接入节点速率 and any B/s value in this tab must show at most 2 decimals (e.g. 12.35 B/s, 0 B/s). Fix in relay-format.ts; add tests.
3. Page structure (top → bottom):
   a. Header: title + description + refresh button + a NEW three-dot (Ellipsis) overflow menu at the top-right. Menu items: 「修改接入密码」opens the existing PasswordDialog; keep any other reasonable operator actions there if they already exist. Menu content must be an exported plain component so tests can assert the element tree (portal caveat). If the relay has NO password set, show a compact warning Notice under the header (任何人都能接入本中继) — the password card itself is removed.
   b. Metrics overview (tiles + trends) as today, but WITHOUT the members card inside it.
   c. 租户 card (tenant table) with its own three-dot menu at the card's top-right containing 「默认配额…」which opens a NEW DefaultQuotaDialog (reuse QuotaFields + parseQuotaDraft; saving calls the existing controller.submitDefaultQuota and closes on success). Remove DefaultQuotaCard from the page (delete the file if unused). Tenant rows become selectable (a clear selection affordance: clickable row with `aria-selected`/highlight ring, plus keyboard support; make sure clicking action buttons/label editor inside the row does not toggle selection). Selecting a tenant shows only that tenant's nodes in the card below; clicking again (or a 「全部」chip) clears the selection. Clear selection automatically if the tenant disappears after refresh.
   d. 接入节点 card (members table) below the tenant card: filtered by the selected tenant; header shows the count and, when a tenant is selected, the tenant label/short id. Add: search input (matches node name, nodeId, tenantId), column sorting (click header toggles asc/desc with a small arrow indicator; sortable: node, state, rtt, streams, rate, reconnects, connected), and a state filter (全部 / 在线 / 离线) using a DropdownMenu or segmented buttons consistent with the existing UI. DEFAULT sort: node name ascending (`memberTitle`), stable by nodeId. Put the pure functions (`filterMembers`, `sortMembersBy`, sort key types) in relay-metrics-model.ts with tests. Show a distinct empty state for "no matches" vs "no members".
   Both cards need the single `useRelayMetrics` owner — lift it to RelayTabBody (or a small hook wrapper) so metrics are fetched once; keep the `api` injection working for tests.
4. Copy: rename 接入口令 → 接入密码 everywhere in `relay.admin.password.*` (title, dialog title, buttons, hints, toasts) in all three languages; the epoch row label 口令 → 密码代次 if still shown; DROP the 令牌下限 (`minTokenEpoch`) row entirely from the UI (the key may be deleted if no longer used). Password dialog: keep 保留现有租户 / 作废旧令牌 options; if a hint needs a longer explanation, use the existing `IconTooltip` (packages/ui/src/components/icon-tooltip.tsx) rather than inline text. All copy must be short and professional (see rules).
5. Tests: update relay-tab.test.tsx, relay-metrics-ui.test.tsx, settings-tab-gating.test.tsx, SettingsPage.test.tsx as needed; add tests for menu contents, DefaultQuotaDialog open/save, tenant selection filtering, members search/sort/filter pure functions, rate rounding. Run `bunx biome check --write` on the files you touched, `bun run build:i18n` from the repo root after locale edits, then the tests and tsc.
