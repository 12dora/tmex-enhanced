You are exploring the tmex monorepo (Bun + React + TypeScript). READ-ONLY: do not modify files. Output the COMPLETE report as your final message (you cannot write files). Be precise: cite file paths with line numbers, quote the relevant i18n keys (packages/shared/src/i18n/locales/zh_CN.json is the source language; en_US/ja_JP mirror it; packages/shared/src/i18n/resources.ts and types.ts are generated, ignore them). Keep the report structured and actionable for engineers who will implement changes without having read the code.

Topic: backend side of relay member "last error" and per-quota live usage.

1. In packages/app (relay runtime: assemble-relay.ts, relay-store.ts, relay-session.ts, lib/relay-*, runtime/relay-*-routes.ts, metrics-types.ts, metrics collection) and apps/gateway (mesh relay client / uplink: search for connect-failed, lastError, recentError, relayStatus), find every place that records a member or client "last error" such as "connect-failed": who writes it, the full set of possible error codes/strings, whether it is cleared on successful connection, and which API responses expose it (list JSON field paths). Include the node-side (client) status endpoint used by the local machine card (e.g. /api/local/relay/status or /api/mesh/relay/status).
2. Quotas: list every quota field the relay supports (tenant quotas, default quotas, member quotas: e.g. maxStreams/concurrent streams, bandwidth, members count...), where each is enforced (file:line), and whether a live counter for the current usage exists (in LinkMux stats, member stats, /api/relay/metrics). For each quota without a live counter, describe the minimal backend change to expose current usage in /api/relay/metrics and in the node-side status endpoint (the local card should be able to show usage vs quota for every quota).
3. Relay switching: does a node enrolled in multiple relays exist (multi-relay membership)? Find the data model (relay list on node side: uplink/relays table or config), the "active relay" concept, and any existing API to switch the active relay (or re-enroll into another). If none exists, describe the minimal backend endpoint to switch the active relay.
4. Tests covering these areas.
