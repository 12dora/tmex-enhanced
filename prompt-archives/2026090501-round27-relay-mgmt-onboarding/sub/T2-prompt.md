## Shared rules for every coding agent (read fully)

- Repo: /Users/konata/code/tmex-r27 (git worktree, branch feat/round27-relay-mgmt-onboarding). Bun monorepo: apps/gateway (Bun HTTP/WS gateway), apps/fe (React 19 + Vite + Tailwind v4 + @base-ui/react wrapped in packages/ui), packages/app (npm `tmex-cli`: installer CLI + runtime that assembles gateway + relay), packages/api-client, packages/shared (i18n + types).
- Other agents are editing the SAME worktree in parallel. Touch ONLY the files listed in your scope. Do NOT run git add/commit/stash/checkout/reset. Do NOT run formatters over directories; `bunx biome check --write <your files>` only.
- Runtime is Bun only. Tests: `bun test <path>` (in apps/fe use `bun test src/<path>`; never bare `bun test` there — it picks up Playwright specs). Type check: `bunx tsc --noEmit -p <package dir>`. Do not run e2e (Playwright).
- i18n: zh_CN is the source language at packages/shared/src/i18n/locales/zh_CN.json; en_US.json and ja_JP.json must get the same keys. NEVER edit packages/shared/src/i18n/resources.ts or types.ts (generated); after changing locale JSON run `bun run build:i18n` from the repo root (it regenerates them; that is expected). Edit only the sub-objects you own; other agents edit other sub-objects of the same JSON files — keep edits surgical (no reformatting, no key reordering).
- Copy style (very important, user demand): concise, professional, easy to understand, like copy in large mature software (macOS System Settings / GitHub / Tailscale). No chatty explanations, no second person (no 你/您/your), no filler. One short sentence per hint. Chinese uses full-width punctuation. Terms: 本机 (this machine), Hub, 中继 (relay role), 租户 (tenant that joins a relay), 运营者 (relay operator), 加入码 (join code), 接入密码 (relay access password). Long explanations go into tooltips, not inline text.
- Code style: no unnecessary comments (only for genuinely non-obvious logic, in Chinese like the existing code), standard English identifiers, keep functions small (complexity gate: cyclomatic ≤ 15, files ≤ ~400 lines preferred). Follow existing patterns in neighboring files (Card/Badge/Button/DropdownMenu from @tmex/ui, data-testid attributes, `useTranslation`).
- Base UI gotcha: `DropdownMenuLabel` must be inside `DropdownMenuGroup` or the page crashes at runtime. Menus/Dialogs render in a portal: static render tests cannot see their content, so export the menu/dialog content as a plain component and test it directly (see existing `RelayActionsMenuList`, `BulkActionsMenuList`).
- Tests: keep existing tests green; update tests that assert on things you intentionally changed; add focused tests for new pure logic (sorting/filtering/formatting/state mappings). Do not delete tests to make them pass.
- When done, write a result report (Markdown) to the absolute path given in your task: what changed (files), tests run with counts, anything left undone or uncertain. Then exit.
- Never touch the production tmex install (~/Library/Application Support/tmex, port 9883) or the tmux session named `tmex`. Do not start dev servers.

# Task T2 — Settings → 多节点互联 → 本机 card: connection section restructure (frontend, Opus)

Result file (write when done, then exit): /Users/konata/code/tmex-enhanced/prompt-archives/2026090501-round27-relay-mgmt-onboarding/sub/T2-result.md

## Your file scope (exclusive)
- apps/fe/src/pages/settings/nodes/*.tsx|ts (card-parts, connection-details, local-machine-*, machine-status, network-section, relay-service-section, nodes-tab and tests) EXCEPT the subfolders management/, setup/, https/ (not yours).
- apps/fe/src/pages/settings/nodes/relay/**, apps/fe/src/pages/settings/nodes/uplink/**, apps/fe/src/pages/settings/nodes/membership/** (only if needed).
- apps/fe/src/node/mesh-relay.ts (+ test) for hook/type plumbing.
- Locale JSON sub-objects `nodes.machine.*`, `nodes.hubs.*`, `relay.tenant.*` in zh_CN/en_US/ja_JP. Do NOT edit `relay.admin.*`, `relay.metrics.*`, `connectDevices.*`, `settings.*` (other agents).
- Do NOT edit packages/api-client (the types you need are already there — read packages/api-client/src/relay/tenant-api.ts: `RelayLinkStatus.lastErrorCode: RelayLinkErrorCode`, `RelayQuotaView.usage: RelayQuotaUsage | null`, `RelayTenantApi.switchRelay(url)`; `normalizeRelayStatus` already nulls lastError/lastErrorCode/lastErrorAt when `online === true`). A backend agent is implementing the server side in parallel; code against these types. Note `usage.bandwidthBytesPerSec` may be added by the backend as an extra optional number on `RelayQuotaUsage` — treat it as optional: if present, show it as the bandwidth usage; else fall back to max(bytesInPerSec, bytesOutPerSec).

Baseline: `cd apps/fe && bun test src/pages/settings/nodes src/node` and `bunx tsc --noEmit -p .` — run them first and record the numbers (another agent is concurrently editing apps/fe/src/pages/settings/relay/**, so a few transient tsc errors/test failures in THAT folder are not yours; everything in your scope must be 0 fail / 0 errors).

## Current UI (what the user sees; screenshot description)
Card "本机" → section 连接: a big rounded box per relay row containing `127.0.0.1:19863` pill, an "在线" badge, "延迟 6 ms", a link icon + "当前挂载于此中继"; below: buttons 追加中继 / 中继操作 (menu: 重新输入口令, 轮换元数据密钥, 移除 …) / 离开中继; hint "须先离开中继才能改回 Hub"; collapsible "连接详情" with rows 租户编号 (+hint), 元数据密钥代数, 经中继可见节点, 节点配额 (used / max + progress), 并发流上限, 密钥日志 (已追平 / 卡在第 N 条), 本机节点编号, Hub 明细. Relevant files: relay/relay-rows.tsx, uplink/relay-uplink-panel.tsx, uplink/relay-targets.ts, relay/relay-notices.ts, relay/use-relay-actions.ts, relay/relay-dialogs.tsx, connection-details.tsx, machine-status.ts.

## Requirements
1. **Error display.** Show an error line under a relay row ONLY when the relay is offline and `lastErrorCode` (or `lastError`) is present. Label it 错误 (not 最近错误). Map `lastErrorCode` to i18n under `relay.tenant.linkErrors.<code>` (connect-failed 无法连接中继; connect-timeout 连接超时; auth-timeout 认证超时; auth-rejected 中继拒绝了本机凭据; heartbeat-lost 心跳中断，正在重连; kicked 令牌已失效，须重新输入接入密码; dns 域名无法解析; refused 连接被拒绝; tls TLS 握手失败; protocol 协议不兼容，请升级中继或本机; unknown 连接失败) with English/Japanese equivalents; when only a raw `lastError` exists fall back to `unknown` text (never print raw strings). `relayFailing()` = kicked || (!online && has error). Update relay-ui.test.tsx accordingly (it currently asserts raw 'ECONNRESET').
2. **Remove the technical rows/actions.** Delete 元数据密钥代数 and 密钥日志 rows from 连接详情 and the 轮换元数据密钥 menu item (and its confirm dialog path + i18n keys if unused elsewhere; the backend/API stays untouched). Keep the automatic "meta key pending" Notice but reword it plainly: zh 「成员密钥更新尚未送达（{{count}} 条）」+ 重试; adjust en/ja. Rename 中继操作 menu label to 更多 if it only holds 重新输入接入密码 / 移除 items (also rename 口令 → 接入密码 within `relay.tenant.*` copy: e.g. 重新输入口令 → 重新输入接入密码).
3. **Quotas with live usage** in 连接详情 (all three, each as `used / max` with a Progress bar when max > 0): 节点 (`currentNodes` or `usage.currentNodes` / `maxNodes`), 并发流 (`usage.currentStreams` / `maxStreams`), 带宽 (`usage.bandwidthBytesPerSec` formatted via the existing byte-rate formatter in packages/api-client/src/format.ts (`formatRate`/`formatBytes`) rounded to ≤ 2 decimals, versus `bandwidthBytesPerSec` limit or 不限). When `usage` is null show only the limits (old relay). Labels: 节点 / 并发流 / 带宽.
4. **Copy.** 经中继可见节点 → 可访问节点 (en "Reachable Nodes", ja 「到達可能ノード」); 本机节点编号 → 本机编号 and move it directly BELOW 租户编号 (order: 租户编号, 本机编号, 可访问节点, 节点, 并发流, 带宽, Hub 明细). Update connection-details.test.tsx.
5. **Connection rows redesign.** Remove the outer ring box. Each relay is one compact line: an address pill (rounded, mono, covering ONLY the host text) + a single status badge: online → `延迟 6 ms` (green dot/`default` variant; if rttMs unknown show 在线) / offline → `离线` (outline). Remove the 当前挂载于此中继 text. When there are ≥ 2 relays: rows are selectable — the attached one is highlighted (pill in primary ring + a small check icon, `aria-current="true"`), the others are buttons (hover affordance) whose click opens an AlertDialog 「切换到 {{host}}？」description 「本机将改为经此中继连接，其余节点不受影响。」confirm → `relay.switchRelay(url)` (api-client) → refresh status; handle failure with the existing toast/Notice pattern and i18n error mapping (`RELAY_UNKNOWN`, `RELAY_KICKED`, `RELAY_ALREADY_ATTACHED`, `RELAY_SWITCH_FAILED`). With exactly 1 relay nothing is selectable (no button semantics). Keyboard accessible. Keep data-testids stable where tests use them, add new ones for the switch flow.
6. Keep the header badge logic (machine-status.ts) consistent: it already merges relay online + rtt.
7. Tests: update connection-details.test.tsx, relay/relay-ui.test.tsx, uplink/relay-uplink-panel.test.tsx, local-machine-card.test.tsx; add tests for error code mapping, quota rendering with/without usage, switch dialog (export dialog content component for static assertions) and single-relay non-selectable case. Run `bun run build:i18n` from repo root after locale edits, biome on touched files, then tests + tsc.
