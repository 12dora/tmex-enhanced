## Shared rules for every coding agent (read fully)

- Repo: /Users/konata/code/tmex-r27 (git worktree, branch feat/round27-relay-mgmt-onboarding). Bun monorepo: apps/gateway (Bun HTTP/WS gateway), apps/fe (React 19 + Vite + Tailwind v4 + @base-ui/react wrapped in packages/ui), packages/app (npm `tmex-cli`: installer CLI + runtime that assembles gateway + relay), packages/api-client, packages/shared (i18n + types).
- Other agents are editing the SAME worktree in parallel. Touch ONLY the files listed in your scope. Do NOT run git add/commit/stash/checkout/reset. Do NOT run formatters over directories; `bunx biome check --write <your files>` only.
- Runtime is Bun only. Tests: `bun test <path>` (in apps/fe use `bun test src/<path>`; never bare `bun test` there — it picks up Playwright specs). Type check: `bunx tsc --noEmit -p <package dir>`. Do not run e2e (Playwright).
- i18n: zh_CN is the source language at packages/shared/src/i18n/locales/zh_CN.json; en_US.json and ja_JP.json must get the same keys. NEVER edit packages/shared/src/i18n/resources.ts or types.ts (generated); after changing locale JSON run `bun run build:i18n` from the repo root (it regenerates them; that is expected). Edit only the sub-objects you own; other agents edit other sub-objects of the same JSON files — keep edits surgical (no reformatting, no key reordering).
- Copy style (very important, user demand): concise, professional, easy to understand, like copy in large mature software (macOS System Settings / GitHub / Tailscale). No chatty explanations, no second person (no 你/您/your), no filler. One short sentence per hint. Chinese uses full-width punctuation. Terms: 本机 (this machine), Hub, 中继 (relay role), 租户 (tenant that joins a relay), 运营者 (relay operator), 加入码 (join code), 接入密码 (relay access password). Long explanations go into tooltips, not inline text.
- Code style: no unnecessary comments (only for genuinely non-obvious logic, in Chinese like the existing code), standard English identifiers, keep functions small (complexity gate: cyclomatic ≤ 15, files ≤ ~400 lines preferred). Follow existing patterns in neighboring files (Card/Badge/Button/DropdownMenu from @tmex/ui, data-testid attributes, `useTranslation`).
- Base UI gotcha: `DropdownMenuLabel` must be inside `DropdownMenuGroup` or the page crashes at runtime. Menus/Dialogs render in a portal: static render tests cannot see their content, so export the menu/dialog content as a plain component and test it directly (see existing `RelayActionsMenuList`, `BulkActionsMenuList`).
- Tests: keep existing tests green; update tests that assert on things you intentionally changed; add focused tests for new pure logic (sorting/filtering/formatting/state mappings). Do not delete tests to make them pass.
- When done, write a result report (Markdown) to the absolute path given in your task: what changed (files), tests run with counts, anything left undone or uncertain. Then exit.
- Never touch the production tmex install (~/Library/Application Support/tmex, port 9883) or the tmux session named `tmex`. Do not start dev servers.

# Task T5 — 接入设备 side panel: rewrite the "服务器或电脑" onboarding steps (frontend, Opus)

Result file (write when done, then exit): /Users/konata/code/tmex-enhanced/prompt-archives/2026090501-round27-relay-mgmt-onboarding/sub/T5-result.md

## Your file scope (exclusive)
- apps/fe/src/components/side-panels/connect-devices/** (all files incl. tests; create new files here)
- Locale JSON sub-object `connectDevices.*` in zh_CN/en_US/ja_JP ONLY.
- Read-only for everything else. If you need a hook/type from elsewhere, import it; do not modify other folders. If something you need in packages/panels (add-device dialog opening) cannot be reached without edits, describe it precisely in the report instead of editing.

Baseline: `cd apps/fe && bun test src/components/side-panels` and `bunx tsc --noEmit -p .` (other agents concurrently edit apps/fe/src/pages/settings/**; transient errors there are not yours; your folder must end at 0 fail and 0 tsc errors in your files).

## Current structure (verified by an audit)
connect-devices-panel.tsx → tabs 移动设备 / 服务器或电脑 → computer-guide.tsx: step 1 安装 tmex (CommandBlock with INSTALL_COMMAND + PATH hint), step 2 选择接入方式 tabs `join` (让新机器加入 → computer-join-guide.tsx: UplinkStep/PasswordStep/TokenAdvanced) and `host` (把本机设为 Hub → HostSteps: entry/hub/invite). Primitives: guide-step.tsx (GuideStep index/title/description/state done|todo, GuideNote, GuideLink), guide-tabs.tsx (GuideTabList), command-block.tsx, join-token.tsx (useJoinEnrollment + fields + confirm status), host-status.ts, join-command-preview.ts (passwordJoinCommand / relayJoinCommand). Data: `useSharedAuthMode()` (/api/auth/mode: mode, hubPublicUrl, nodeId), `useMeshRelay()` (/api/mesh/relay/status: relayMode, attached/ordered relays, tenantId, writable), tunnel status query (TUNNEL_STATUS_QUERY_KEY). Local roles (standalone / node / hub,node / relay / relay,node) come from `/api/local/status` (`defaultLocalApi`, `LocalStatusResponse.role`, `relay.publicUrl`, `relay.hasPassword`) — see apps/fe/src/pages/settings/nodes/use-local-status.ts for how the card reads it (import, don't copy blindly; it is 404 on dev gateways, handle absence gracefully).
Authoritative CLI syntax (packages/app/src/cli/help.ts): `tmex init [--role …]`, `tmex hub join <https-url> --password [--name <name>]`, `tmex hub join <url> --token <token> [--name]`, `tmex relay join <url> --tenant <tenant-id> [--password] [--name]`, `tmex relay passwd`. Install command constant: `INSTALL_COMMAND` from @tmex/shared. The Hub vs Relay facts: Hub = trust center that holds accounts, node membership and approvals (centralized management); 中继 = blind public relay that only forwards encrypted traffic, ideal when nodes sit behind NAT and you just need a public meeting point; 本机直连 (SSH) = the new machine does not run tmex at all — an existing node connects to it over SSH as a device (existing feature: Devices page → add device → type SSH, `POST /api/devices`; opening mechanism: apps/fe/src/pages/devices/add-device-menu.tsx and packages/panels/src/device-management/device-management-events.ts — find the public way to open the add-device dialog for a node, or navigate to `/devices` with whatever query/state the page supports).

## Requirements
Rewrite the 服务器或电脑 guide as:
1. Step 1 安装 tmex — keep (install command + PATH hint). NOT shown for the SSH path (SSH needs no tmex on the new machine) — reorder so the path choice comes first if that reads better; keep step numbering continuous per path.
2. Step 选择接入方式 — three top-level choices (segmented GuideTabList): 「经中继」「经 Hub」「SSH 直连」. Directly under the choice show a compact explainer of the three (one line each, ≤ 40 CJK chars; a short "何时选" per option, e.g. 中继：节点在 NAT 后、只需一个公网汇合点；Hub：需要集中管理账号与节点；SSH 直连：新机器可直接 SSH 到达，无需安装 tmex). Use `IconTooltip` from @tmex/ui for anything longer. Default choice: derived from this machine (relay,node or attached to a relay → 经中继; hub,node or attached to a hub → 经 Hub; standalone → 经中继).
3. 经中继 — sub-choice 「加入已有中继」/「本机自建中继」 (second-level GuideTabList inside the step, or two radio cards; keep it visually lighter than the top level).
   - 加入已有中继: steps 准备接入信息 (if this machine is attached to a relay: show 中继地址 + 租户编号 CommandBlocks; else note 向中继运营者索取地址与接入密码, and if the machine is standalone also a link to 设置 → 多节点互联) → 在新机器上加入 (UI path 「设置 → 多节点互联 → 加入中继」+ CLI `tmex relay join <url> --tenant <id>`) → optional 高级：加入码 (existing TokenAdvanced, only when this machine can issue relay join codes).
   - 本机自建中继: steps 本机设为中继 (state done if role includes relay; show 中继地址 from local status; else GuideLink to /settings?tab=nodes with description 选择「把本机设为中继」) → 设置接入密码 (done if `relay.hasPassword`; else link to /settings?tab=relay with 「中继管理 → ⋯ → 修改接入密码」) → 让新机器加入 (button switching to 加入已有中继 sub-path, like the existing gotoJoin).
4. 经 Hub — sub-choice 「加入已有 Hub」/「本机设为 Hub」: reuse existing JoinSteps (hub variant: Hub 地址 + 密码, CLI `tmex hub join <url> --password`, advanced join code) and HostSteps (entry/hub/invite), respectively, adapted to the new copy style.
5. SSH 直连 — one short explanation + step 添加 SSH 设备: a button that opens the add-device flow for the local node (or navigates to /devices and opens the dialog; if a public API to open the dialog with type=SSH is not reachable from this folder, navigate to `/devices` and describe in the report what would be needed) + a note 「设备保存后即出现在设备列表」.
6. Copy: rewrite ALL `connectDevices.computer.*` strings in the concise professional style (see rules); keep `connectDevices.mobile.*` and `connectDevices.tabs.*` as they are unless a key rename is needed. Delete keys that are no longer used; add new ones in all three languages; do not touch other sub-objects. Run `bun run build:i18n` from the repo root after edits.
7. Tests: update connect-devices-panel.test.tsx / computer-join-guide.test.tsx / host-status.test.ts to the new structure (they assert on i18n keys since i18n is not initialised in static renders) and add tests for default-path derivation and each path's steps/state (done/todo) given mocked local status. biome on touched files; final `bun test src/components/side-panels` at 0 fail; tsc 0 errors in your files.
