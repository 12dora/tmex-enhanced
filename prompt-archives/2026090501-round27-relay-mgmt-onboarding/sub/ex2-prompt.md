You are exploring the tmex monorepo (Bun + React + TypeScript). READ-ONLY: do not modify files. Output the COMPLETE report as your final message (you cannot write files). Be precise: cite file paths with line numbers, quote the relevant i18n keys (packages/shared/src/i18n/locales/zh_CN.json is the source language; en_US/ja_JP mirror it; packages/shared/src/i18n/resources.ts and types.ts are generated, ignore them). Keep the report structured and actionable for engineers who will implement changes without having read the code.

Topic: Settings → Nodes ("多节点互联") → local machine card ("本机") and its relay membership section (apps/fe/src/pages/settings/nodes/**, especially local-machine-card.tsx, local-machine-header.tsx, local-machine-body.tsx, connection-details.tsx, network-section.tsx, relay-service-section.tsx, membership/, uplink/, relay/), plus the backend endpoints they read (packages/app/src/runtime local routes, apps/gateway mesh/relay status routes, packages/api-client/src/local + relay).

User complaints to address (report the code behind each):
A. The local machine is connected to the relay and shown online, yet the card still shows "最近错误 connect-failed". Find where lastError/recentError comes from (backend field, when it is set, whether it is ever cleared on successful connect), where the UI renders it, and confirm the error string is raw and not i18n'd. Propose: show only a CURRENT error (cleared when connected) and map error codes to i18n keys (list all error codes that can appear).
B. "元数据密钥代数" (metadata key generation) and "密钥日志" (key log) rows and the "中继操作 → 轮换元数据密钥" action: locate their rendering, i18n keys, data source, and whether anything else depends on them (tests). We intend to remove them from the UI.
C. Quotas shown on the card: which quota fields exist (concurrent streams, bandwidth etc.), which have live usage counters available from the backend (relay metrics endpoint /api/relay/metrics, member stats, tenant quotas) and which do not; identify what backend data would be needed to show live usage for every quota.
D. The text "经中继可见节点" — where is it, what does it actually count?
E. "本机节点编号" row and "租户编号" row — where rendered, ordering.
F. The top "连接" section: the big outer box below the connection heading, the address pill, "在线"/"延迟" badges, the "当前挂载于此中继" text, and the relay list (how multiple relays are represented; is there an API to switch the active relay / re-attach to another relay? find the endpoint or the closest existing operation, e.g. uplink switch / relay re-enroll). Describe the component tree with file:line so a developer can restructure it.
G. List the existing tests covering these components (file names) so they can be updated.
