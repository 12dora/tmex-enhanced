You are a senior code reviewer for a Bun + React 19 + dnd-kit monorepo (worktree cwd). Review the diff in `prompt-archives/2026083000-sidebar-devices-settings-round2/sub/batch2.diff` (commit db74fb28). Read any repo file for context (read-only).

Scope: devices management page rework — `packages/panels/src/device-folders/*` (drag preview overlay with a local snapCenterToCursor modifier, whole-tree root droppable, tiered collision detection, in-drag optimistic layout `previewDrop` moving nodes between containers, removal of the root drop bar / move-to-root button), `packages/panels/src/device-management/*` (card layout, tooltip, auto-fill grid, dedupe), `apps/fe/src/pages/devices/*` (folders hook pinned to self runtime/QueryClient, snapshot dedupe).

Focus on real bugs: dnd-kit correctness (collision tiers returning wrong `over`, drag end with `over === null`, `previewDrop` causing the dragged node's React subtree (a `NodeRuntimeScope` with websocket runtime) to remount mid-drag, stale closures between onDragOver/onDragEnd, `MeasuringStrategy.Always` interaction with layout changes, keyboard sensor), layout persistence correctness (`applyDrop`/`resolveDrop` with implicit root nodes, duplicate placements, rebase), the self-pinned `useDeviceFolders` (explicit QueryClient passed to useQuery/useMutation; invalidation/query keys still coherent with GlobalDeviceProvider), tooltip trigger accessibility, and tests that assert the wrong thing. Rank by severity with file:line and concrete failure scenario; skip style nits. Also list what you verified as fine. Write the report (Chinese, zh-CN) to the output file.
