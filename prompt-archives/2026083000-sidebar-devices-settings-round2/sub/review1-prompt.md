You are a senior code reviewer for a Bun + React 19 + react-router 7 + dnd-kit + Base UI monorepo (worktree cwd). Review the diff in `prompt-archives/2026083000-sidebar-devices-settings-round2/sub/batch1.diff` (3 commits on top of 07fd162a). You may read any file in the repo for context (read-only).

Changes under review:
1. Route/shell restructure so the sidebar never remounts when navigating between nodes (`apps/fe/src/main.tsx`, `apps/fe/src/node/node-runtime-boundary.tsx`, `app-sidebar.tsx`); sidebar sortable collision detection change (`packages/panels/src/device-tree/device-tree-dnd.tsx`).
2. Right-side slide-over panels driven by `?panel=nodes|security` (`apps/fe/src/components/side-panels/*`), removal of the standalone `/account/security` page, six-box OTP input (`packages/ui/src/components/otp-input.tsx`, used in LoginPage and the security panel), brand block (`apps/fe/src/components/brand.tsx`).
3. Live language switching in settings (`apps/fe/src/pages/settings/*`, `apps/fe/src/i18n/index.ts`).

Focus on real bugs: remount/regression risks (react-query observers bound to a QueryClient, providers keyed by runtime, login gate ordering, socket/connection lifecycle when the route node changes, mobile sidebar behaviour), URL/history handling of the panel param across `/n/:nodeId` routes and the `/account/security` redirect, OTP input edge cases (IME, autofill, paste, backspace, controlled value drift), i18n preview/revert race conditions (unmount during pending changeLanguage, refetch overwriting), accessibility regressions, and anything that would break existing e2e flows (`apps/fe/tests/*.spec.ts`). Rank findings by severity with file:line and a concrete failure scenario; skip style nits. Also list what you verified as fine. Write the report (Chinese, zh-CN) to the output file.
