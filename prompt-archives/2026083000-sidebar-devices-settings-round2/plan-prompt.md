# Prompt 存档

## 2026-08-30 初始 prompt

继续开发, 请你分批commit并在最后push, 然后替换本机tmex为最新版Think in English, Send English prompt.
任务：
    1. 左侧菜单栏
        1. 增加拖动排序节点东南, 例如konata-mac可以自由拖动到中间或最下方
        2. 切换不同节点终端时, 左侧菜单栏会闪烁, 你应该保持左侧不变(仅改变高亮条目)
    2. 管理设备
        1. 点按拖动按钮时应该在原地形成可拖动设备小卡片, 目前点按挪动后只剩1个小文字条,且位置不正确(在非常右侧)
        2. 设备卡片连接/断开按钮右侧大片空白 ,消除这些无用空白, 目前留给左侧显示节点名的空间不足, 请你适当拉长设备卡片, 实在显示不下应该省略并采用hover tooltip显示
        3. 设备卡片点击右上箭头图标进入中断, 有概率会复制出多个重复的设备卡片并进入不正常的界面(该不正常界面除了有重复设备卡片, 顶上还有个"测试"文件夹)
        4. 去掉移到最外层的拖动层,改为用户拖动到不属于任何文件夹虚线范围内,自动来到最外层(并且拖动时其他设备卡片要智能退避, 就像ios移动app图标一样的效果)
    3. 设置中切换语言无效, 需要点击保存后刷新才能切换
    4. 设置-点击多节点互联,账号安全, 不应跳转到新页面, 而是在右侧滑出一个覆盖层, 允许替换这个新页面(并删除原来的独立页面), 覆盖层进入, 退出需要有优雅动画(目前新页面存在点击totp页面内容拉长后不能scroll的问题, 卡片里你需要修复该问题)
        1. totp验证码输入框你应该为6个独立的输入框, 并支持连续输入, 删除, 黏贴
    5. 只能提在没有选择pane时, 提示语存在没有i18n的情况, lieu请在Panes标签中, Panes应该翻译成终端, 且第二个pane首字母没有大写, 请修复
    6. 左上角tmex你应该始终固化显示为产品名(tmex), tmex下方增加小字显示本节点名, 例如konata-mac
注:
1. cursor（grok 4.6, high)担任后端编码
2. opus5(high)担任前端编码
3. codex（gpt-5.6-luna,  xhigh)探索代码
4. codex（gpt-5.6-sol,  high)担任code reviewer, codex存在过度防御的问题, 你应该自行判断问题是否修复
