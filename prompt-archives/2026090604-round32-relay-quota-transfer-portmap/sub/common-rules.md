# Common rules for all round-32 coding agents (tmex monorepo)

Worktree: /Users/konata/code/tmex-r32 (branch feat/round32-relay-quota-transfer-portmap). Runtime is Bun only (bun is on PATH; if missing, source ~/.zshrc PATH).
Archive folder: /Users/konata/code/tmex-r32/prompt-archives/2026090604-round32-relay-quota-transfer-portmap/ — read plan-00.md, plan-01-transfer-portmap.md and the sub/EX*.md report(s) named in your task BEFORE coding. They contain exact file:line pointers; verify against the code, never guess APIs.

Hard rules:
- Several agents edit this worktree concurrently. Touch ONLY the files/directories in your assigned scope. For shared files listed as "append one line" (apps/gateway/src/api/index.ts route registration, apps/gateway/src/db/managed-migrations.ts, packages/api-client/src/index.ts exports, locale JSON subtrees) make minimal targeted edits and never reformat the whole file.
- NO git operations (no add/commit/stash/checkout/reset). The commander commits.
- Never touch the production tmex (launchd service on port 9883, ~/Library/Application Support/tmex/) and never touch the tmux session named `tmex`. Tests use NODE_ENV=test automatically via bunfig; do not start long-lived servers on production ports. If you need a temp instance, use TMEX_TMUX_SOCKET=tmex-e2e-<yourid>, GATEWAY_PORT/TMEX_PEER_PORT in the 2xxxx/4xxxx range, 127.0.0.1 bind.
- Never run lint/format on generated files (packages/shared/src/i18n/resources.ts, types.ts, locales/generated/*, resources/fe-dist/*, dist/*). i18n: edit the three source locale JSONs (zh_CN is the source language; en_US, ja_JP must have identical key sets) under packages/shared/src/i18n/locales/, then run `bun run build:i18n` from the repo root (it regenerates resources.ts; locale-consistency tests fail otherwise). UI copy must follow /Users/konata/code/tmex-copy-guidelines.md: terse, professional, like a mature product; no chatty phrasing; no second person in Chinese.
- Code comments: only where logic is genuinely non-obvious, written in Simplified Chinese like the surrounding code. Variable names in English.
- Complexity gate (`bun scripts/complexity/gate.ts` from repo root): CC ≤ 15 per function, ≤ 120 lines per function, ≤ 600 lines per file. Put new logic in new files rather than growing files already near 600 lines. Do not add allowlist entries unless truly unavoidable.
- Quality gates before you finish (run in your package dirs): `bunx tsc --noEmit -p <pkg>` must be 0 errors (baseline is 0 for every package); `bun test` in each package you touched must have no new failures. Baseline: apps/gateway 4913 pass / 10 fail (pre-existing: 9 "mesh phase-2 integration" tests + 1 flaky DataChannel 8 MiB test); apps/fe `bun test src/` 2693 pass; packages/app 937; packages/shared 795; api-client 252; stores 435; panels 1039; ws-client 413; ui 414; notifications 23 — all 0 fail. `bunx biome check <your files>` must pass. macOS has no `timeout` command; bun test summary lines contain ANSI colors (strip with sed 's/\x1b\[[0-9;]*m//g').
- No laziness: no TODOs, no "simple version first", no skipping tests, no disabling failing tests. Finish the whole scope. If something in the plan turns out impossible or a clearly worse design after reading the code, pick the best alternative, implement it fully, and explain the deviation in your report.
- Write a final report to the absolute path given in your task (Markdown, English, concise: what was built, files touched, deviations, test results with numbers, anything the commander must wire up or verify). Then also return the report as your final message.
