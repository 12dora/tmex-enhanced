# EXA — Why would a remote (non-self) mesh node "drop and auto-recover" on desktop after v1.1.8?

Read-only exploration of the repo at HEAD (v1.1.8). Output the complete report as your final message (Markdown, English, < 1500 words, cite `path:line`). Do not write files.

Symptom (user report, desktop browser, entry node = local machine on 1.1.8, remote nodes still on 1.1.7): remote nodes' sections/terminals randomly go "disconnected/offline" and then recover by themselves. Did not happen (or was rarer) on 1.1.7.

The frontend changes between v1.1.7 and v1.1.8 are `git diff v1.1.7..v1.1.8 -- packages apps/fe/src` (run it). Three items: (1) keep-alive pool cold subscription (`packages/panels/src/device-console/terminal-keep-alive.ts`, `terminal-stage.tsx`, `packages/terminal-ui` `subscribe` prop); (2) mesh node list: 5-min fallback poll + event-driven refresh + `onAuthRequired` → `markLoggedOut` (`apps/fe/src/node/mesh-nodes.ts`); (3) ws-client heartbeat 30 s/60 s while `document.visibilityState === 'hidden'` (`packages/ws-client/src/{client.ts,heartbeat-controller.ts}`).

Trace, with code evidence, every path by which each change can make a *remote node runtime* disconnect and reconnect, or make its sidebar section render as offline / "sign in to this node" and then recover. Specifically:

A. `markLoggedOut` path: `apps/fe/src/node/mesh-nodes.ts` subscribes `onAuthRequired` (`packages/api-client/src/auth/session-interceptor.ts`) and flips the node row to `loggedIn:false`. Follow what consumes `loggedIn`: `apps/fe/src/auth/use-node-login.ts` (`useNodeLoginGate` → `needsLogin` → `useSilentLogin`), `apps/fe/src/node/node-runtime-boundary.tsx`, `apps/fe/src/components/page-layouts/components/sidebar-node-section.tsx`, devices page. Does a flip to `loggedIn:false` unmount the `NodeRuntimeScope` / device console subtree (the code comment says pulling the subtree remounts terminals and WS)? Then enumerate **every request that can produce a 401 `{code:'NODE_LOGIN_REQUIRED'}` for `/n/<id>/...` while the user IS logged in to that node**: look at the gateway proxy `apps/gateway/src/mesh/**` (node-scoped routes, `viaNodeId` session binding, how the entry forwards to the remote node and what happens when the remote node is unreachable / relay is reconnecting / the remote node restarts — does any of these produce 401 instead of 502/503?), the WS upgrade path for `/n/<id>/ws` (a 401 on WS upgrade — is it routed to the interceptor? code 4401?), and any client code that calls `/n/<id>/api/*` before or during silent login (files roots, agent sessions, mesh connection/rtc-config/authorize, site settings, capabilities). Also check the interceptor itself: how it derives `nodeId` and `scope` for a 401 (body vs URL), and whether a 401 from `/n/<selfEntryId>/...` or from a stale/removed node id could map onto an unrelated row.
B. Heartbeat path: for a remote node runtime, the browser WS goes browser → entry gateway `/n/<id>/ws` → mesh link → node. Where are PINGs answered (entry or node)? What keepalive/idle timeouts exist on the relay/uplink/direct-carrier hops (`apps/gateway/src/mesh/**`, `packages/ws-client/src/direct/**`)? Could the hidden-tab 30 s cadence exceed any of them (and note the desktop app / minimized window also reports `hidden`)? Also verify `setCadence` + the visibility handler cannot produce a spurious PONG timeout on hidden→visible (old pongTimer armed with 60 s, new PING at 5 s cadence, `clearPongTimeout` ordering).
C. Keep-alive path: can `subscribe={false}` / cold select on a remote node's pane cause a device-level disconnect or a stream-interrupted state (`applyKeepAliveStreamState`) that the UI shows as "disconnected"?
D. Mesh refresh path: with the 5-min fallback, how is a remote node's `online` derived between polls (NODE_EVENT `status` patch vs REST projection `online = self || hubPresence || reachable`)? Can a `NODE_EVENT` flap (e.g. transport switch direct↔relay, link re-establish) now show "offline" where the 30 s REST poll used to mask it? Does `node-offline.ts` / sidebar treat `online:false` by unmounting the runtime?

Rank the candidate root causes by likelihood given the symptom ("desktop", "remote nodes only", "random", "auto-recovers"), state what log line or DevTools observation would confirm each, and propose the minimal fix for the top candidate(s).
