You are exploring the tmex monorepo (Bun runtime, TypeScript) at the current directory, READ-ONLY. Output your complete report as your FINAL MESSAGE (you cannot write files). Write in Simplified Chinese (zh-CN), technical, concise, exact file paths + line numbers, and give exact file lists so coding tasks can be dispatched with non-overlapping file sets.

Goal of this exploration: produce the implementation map for a new feature — **joining an existing mesh with a password instead of a join token**, for both uplink modes:

(A) Relay mode ("租户编号 + 密码"). Design already decided:
  - The relay keeps, per tenant, an opaque "sealed pack" uploaded by the tenant: `seal(KEK, {K_log, tenant token, key-log head hash, head seq})` where `KEK = HKDF(root seed or root secret, "tmex-relay-pack/v1")`, plus the tenant's public KDF params (salt etc). The relay cannot decrypt it.
  - A joining machine: gets KDF params by tenant id → derives the root key from the password (`packages/shared/src/auth/root-key.ts` deriveSeed/rootKeyFromSeed) → signs the existing `tmex/relay-enroll/v1` proof → relay verifies with the tenant's current root public key and returns the sealed pack (and does NOT reissue/kick tokens, unlike today's repeat-enroll path in `apps/gateway/src/relay/relay-routes.ts` ~L102-106) → the node unseals K_log + token, downloads the key log, replays it to reconstruct user state, then **self-admits**: self-issues its node certificate, appends a root-signed `admit-node` record, then a `meta-key` record (epoch+1, fresh K_meta wrapped to every admitted node incl. itself), then connects `relay/v1` uplink with the `member` sidecar.
  - The writer must refresh the sealed pack whenever the head changes (on every local append in relay mode) and after root rotation (new KEK, new root).
(B) Hub mode ("Hub 地址 + 密码"): the hub verifies a root-key-signed proof (same shape as relay proof, different domain string) and issues a normal enrollment/join token, so the existing join path continues unchanged after that.

Report:
1. **Root key & user state**: exactly how the root seed/keys are derived and where kdfParams live (`/api/auth/mode` response, the key log's bootstrap record, `packages/shared/src/auth/*`), whether anything besides password+salt enters the derivation, and how `rotate-root` / `rotate-root-keep` / `reset-root` change the seed (do they re-derive from the new password with a new salt?). Where kdfParams are stored on a node after joining (tables, replay code).
2. **Existing r3 relay join path end-to-end** (node side): `packages/app/src/lib/relay-*.ts`, `packages/app/src/cli-auth*.ts` (hub join r3 branch), `apps/gateway/src/mesh/relay*`, `/api/mesh/relay/*` routes (`apps/gateway/src/mesh/relay-routes*.ts`?), `join-material`, `performHubJoin` in `packages/app/src/runtime/setup-service.ts` and its relay counterpart — list the steps and the functions so the new password path can reuse them from the "we now hold K_log + token + root key" point onward. Identify the CA-fingerprint pinning code (`packages/app/src/lib/relay-ca.ts`) and whether the web-side join (fe `relay-dialogs.tsx` enroll mode → which API) shares code with the CLI.
3. **Self-admit bootstrap**: `bootstrapUserWithSelfAdmit` (apps/gateway/src/auth or packages/shared) — what it produces (user row, root pk, node cert, admit-node record), and whether it can be reused for "join as an additional node using an existing root key + existing key log" (i.e., append admit-node for a new node id to an existing log rather than bootstrapping a new log). Find how certificates are issued/signed (`packages/shared/src/auth/enrollment.ts`, certificate schema, who signs: root vs hub), how `admit-node` records are built (`packages/shared/src/auth/key-log.ts`, record builders), and the key-log replay/verify code used after a join (`keylog-passkey-replay.ts`, `makeReplayPasskeyVerifier`).
4. **Relay side storage & routes**: `relay_tenants` schema (migration `0039_relay.sql`), `relay-tenant-store.ts`, enroll route & `relay-enroll-limiter.ts`, `relay-password.ts`; where to add: `kdf_params_json`, `sealed_pack` columns, `GET /api/relay/tenants/:id/kdf` (unauthenticated), `POST /api/relay/tenants/:id/pack` (write, token-authenticated) and `POST /api/relay/enroll` extension (`mode: 'join'` returning the pack without reissuing). Also list the relay-side test harness files (`relay-test-harness.ts`, `relay-test-tenant.ts`, `integration/*`) so tests can be extended.
5. **Hub side**: how join tokens/enrollments are created today on the hub (`apps/gateway/src/hub/*`, `/api/hub/enrollments*`, `packages/shared/src/auth/enrollment.ts`), what authentication the create route requires (node-session), and where a new unauthenticated-but-proof-verified route (`POST /api/hub/enrollments/by-password` or similar) would go, plus rate limiting primitives available (`relay-enroll-limiter.ts`, any hub login limiter).
6. **CLI**: `packages/app/src/lib/args.ts` / `args-relay.test.ts` / `cli-auth-entry.ts` dispatch tables — where `tmex relay join <url> --tenant <id> [--password]` and `tmex hub join <url> --password` would be added; how password is read (`packages/app/src/lib/password.ts`, `prompt.ts`).
7. **FE**: the standalone setup wizard (`apps/fe/src/pages/settings/nodes/setup/*`: `join-hub-form.tsx`, `submit.ts`, `use-hub-setup-submit.ts`, `validation.ts`), the relay enroll dialog (`apps/fe/src/pages/settings/nodes/relay/relay-dialogs.tsx`, `use-relay-actions.ts`), the api-client modules (`packages/api-client/src/local/*`, `packages/api-client/src/relay/tenant-api.ts`, `packages/api-client/src/auth/*`), and the "接入设备" side panel guide (`apps/fe/src/components/side-panels/connect-devices/*`: which steps/commands it shows, i18n keys under which namespace) that must be updated to describe password join. Where the tenant id could be displayed for copying (relay strip / local card).
8. Risks you see in the design (e.g. rollback of the key log by the relay, KEK derivation vs root rotation, sessions/limiters, standalone-with-existing-user conflicts like `user_exists` in `becomeHub`), each with the code location that enforces the relevant invariant.
