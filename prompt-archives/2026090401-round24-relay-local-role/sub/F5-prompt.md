# Task F5 — Fix the frontend review findings (R2)

Result file (write LAST): /Users/konata/code/tmex-r24/prompt-archives/2026090401-round24-relay-local-role/sub/F5-result.md

READ FIRST: `prompt-archives/2026090401-round24-relay-local-role/sub/R2-report.md` (file:line refs against the current branch) and the result files of F1–F4, F3a, F3b for context. The BLOCKER (join-material only returning the attached relay) is ALREADY fixed by the commander: `GET /api/mesh/relay/join-material?scope=all` returns every relay and `refreshRelayPack` already requests `scope:'all'` — do not redo it.

Fix ALL of the following (each with tests):
1. `apps/fe/src/node/relay-pack.ts`: treat the upload as successful only if every requested pack has `ok:true` in the response `results`; otherwise keep the pack debt (make the debt store URL-granular if cheap; at minimum keep the boolean debt and retry all). Also fix the K_log zeroization gap (enter `try` right after decoding `logKey`, nullable token zeroed in `finally`; `normalizeJoinMaterial()` validates base64url lengths).
2. Root-signed append coverage: the admit flow uses the reusable signer from `request()` (not `withSigner`) — after admit-node + meta-key succeed, refresh the pack explicitly; replace the permanent `WeakSet` dedupe with an in-flight/same-head dedupe (key = head seq at start; a new head always triggers a refresh).
3. `setup/become-relay-form.tsx` + `nodes-tab.tsx`: the restored intent role (`{role:'relay'}`) must win — remount the form with a `key` derived from the intent/initial role (or sync `initialRole` while untouched).
4. `membership/leave-dialog.tsx` (+ i18n `nodes.membership.*`): `relay,node → relay` needs its own warning: the web UI disappears after restart, management is CLI-only (`tmex relay …`), and how to get the web back (`tmex init --role relay,node` / re-join). Route it through the same confirmation strength as `PureRelayConfirm`.
5. `setup/standalone-relay-setup.tsx` + `use-hub-setup-submit.ts` (+ wizard): lift the setup-transition state to the common parent so that once any form submitted successfully / is waiting for restart, every sibling form and the role selector are disabled; map backend `setup_committed` / `setup_in_progress` to localized messages (`setup/error-messages.ts`).
6. `management/use-create-enrollment.ts` (+ `computer-join-guide.tsx` if needed): the inner `useMeshRelay()` must be `enabled: mode?.mode === 'mesh'` (standalone must never call `/api/mesh/relay/status`); prefer passing the already-resolved relay snapshot in.
7. `apps/fe/src/node/relay-meta-key-retry.ts`: arm key must include attached relay URL + an online generation; reset backoff and retry immediately on offline→online and on attached-URL change.
8. `pages/settings/relay/password-dialog.tsx`: reset the draft immediately when `open` becomes false; hand the parent a resolved request body and drop the local password reference right after submit.
9. `setup/validation.ts`: Hub URL validation reuses `canonicalHubUrl()` from `@tmex/shared/auth` (reject credentials/query/fragment like the backend); `relayPassword` field-level rule: empty allowed, otherwise trimmed length ≥ 8 with a field error next to the input.
10. Copy: relay password-join failure must not say 「Hub 拒绝…」 — split the setup error keys (`nodes.setup.errors.*`) by uplink kind; `side-panels/connect-devices/join-token.tsx` and `apps/fe/src/node/enrollment-engine.ts:~601` pick relay wording when `relayMode`.
11. `packages/ws-client/src/websocket-transport.ts`: version-too-old dedupe becomes a `Set<side:nodeId:version>` for the transport lifetime; a successful canonical READY clears only the gateway entry; a node entry clears only when that node negotiates successfully or reports a different version. Update tests in `packages/ws-client` and `packages/stores` if the contract shifts.

Copy rules: /Users/konata/code/tmex-copy-guidelines.md. i18n: three locales + `bun run build:i18n`.

## Scope
Everything under apps/fe/src/**, packages/ws-client/src/**, packages/api-client/src/relay/** (append only), i18n locale JSONs. No other agent is editing the frontend right now; a live-test agent is only READING the repo and building `apps/fe/dist` — keep the build green (`bun run --cwd apps/fe build` must succeed whenever you pause). Do NOT touch apps/gateway/**, packages/app/**, packages/shared/src/** except the i18n locale JSONs.
Baselines: fe 2029, ws-client 396, stores 415, api-client 208 (+5 pre-existing tsc errors).
## Common rules (apply to every task)

- Repo: Bun-only TypeScript monorepo at /Users/konata/code/tmex-r24 (git worktree, branch feat/round24-relay-local-role, base = main 1.1.23). Bun is /opt/homebrew/bin/bun (if not on PATH, read ~/.zshrc). Runtime code runs on Bun 1.3.x; only packages/app's install CLI stays Node-compatible.
- OTHER AGENTS ARE EDITING THIS SAME WORKTREE IN PARALLEL. Touch ONLY the files listed in your scope (plus new files you create inside your scope directories). Do not reformat or "clean up" files outside your scope. If a change outside your scope is unavoidable, do NOT make it — describe it precisely in your result file under "需要指挥官处理".
- NO git operations at all (no add/commit/stash/checkout/reset/worktree). The commander commits.
- Do not run `bun install`, do not edit lockfiles or package.json dependencies unless your scope says so.
- Do not touch generated files: packages/shared/src/i18n/resources.ts, packages/shared/src/i18n/types.ts, packages/shared/src/i18n/locales/generated/*, resources/fe-dist/*, dist/*. i18n keys live in packages/shared/src/i18n/locales/{zh_CN,en_US,ja_JP}.json (all three must be updated together, same key set) and are regenerated with `bun run build:i18n` at the repo root (you MAY run that if your scope includes i18n changes). Edit only the sub-objects named in your scope; other agents edit other sub-objects of the same JSON files.
- Copy (UI text) rules: read /Users/konata/code/tmex-copy-guidelines.md before writing any user-facing text. Key points: "Hub" means hub role; "中继" means the relay role ONLY; "本机" not "这台机器"; no second person; full-width Chinese punctuation.
- Never touch the production tmex service (port 9883, ~/Library/Application Support/tmex) and never touch a tmux session named `tmex`. Any temporary gateway instance must set TMEX_TMUX_SOCKET to an isolated socket (e.g. tmex-r24-<task>) and use ports other than 9883/9663/19883/19663. Never run `tmux kill-server` on the default socket.
- Do not run the Playwright e2e suite (apps/fe/tests) — the commander runs it. In apps/fe, unit tests are `bun test src/`.
- Code style: TypeScript, biome. No unnecessary comments; comments only for genuinely complex logic, written in Simplified Chinese. Identifiers in English. Follow patterns in neighbouring files. Keep every file under 600 lines (complexity gate; `bun run lint` at repo root runs biome + the gate). Do not add entries to the complexity allowlist. When a file you must touch is already near 600 lines, split it (moving code into new files inside your scope is fine).
- Verify before finishing: (1) `bunx tsc --noEmit -p <package>` for each package you touched must not add errors versus baseline (baseline: packages/stores 1, packages/theme 9, packages/api-client 5 pre-existing errors; everything else 0); (2) `bun test` inside each touched package (apps/fe: `bun test src/`) must pass — baselines: gateway 4141, shared 621, app 798 (+1 known env failure in scripts/build-runtime.test.ts), fe 1883, ws-client 392, stores 411, panels 911, ui 370, api-client 201, terminal-ui 394, theme 52; (3) `bunx biome check <your files>`. macOS has no `timeout` command; bun test summary lines carry ANSI colours.
- Write tests for new behaviour (bun test, colocated `*.test.ts(x)`). Deterministic; no sleeps over 100 ms.
- Never hardcode credentials in scripts or tests beyond obvious fixture values.
- When completely done, write your result report (Simplified Chinese, concise, technical) to the ABSOLUTE result path given in your task — list files changed, tests added, verification output summary, anything the commander must handle — and only then exit. The result file must be the LAST thing you write.
