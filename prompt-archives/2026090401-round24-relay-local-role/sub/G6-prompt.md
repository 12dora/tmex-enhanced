# Task G6 — Fix the confirmed findings of the backend security review (R1)

Result file (write LAST): /Users/konata/code/tmex-r24/prompt-archives/2026090401-round24-relay-local-role/sub/G6-result.md

READ FIRST: `prompt-archives/2026090401-round24-relay-local-role/sub/R1-report.md` (the review; file:line refs are against the current branch), then the files it names. Context docs: `docs/relay/2026090304-relay-role.md` §5b/§8, `sub/P1-result.md`, `sub/P2-result.md`, `sub/G5-result.md`.

The commander triaged the findings. Fix these (behaviour + tests), in this priority:

1. **R1-#1 kick / token-epoch TOCTOU** (`apps/gateway/src/relay/relay-uplink-server.ts`, `relay-runtime.ts`, `relay-pack-http.ts` HTTP write routes): (a) in `finishAuth()` re-read the tenant right before registering the live connection and reject if kicked / token hash changed / `tokenEpoch < minTokenEpoch`; (b) the per-message recheck in `dispatchAuthenticated()` must also compare the connection's token epoch against the current global `min_token_epoch` and tenant `token_epoch`; (c) HTTP `/pack` and `/keylog` write routes re-authenticate (re-read tenant) inside the write transaction after the body has been read. Add a race test that performs `passwd --kick` / token reissue between precondition check and registration.
2. **R1-#2 join commit ordering** (`packages/app/src/lib/relay-password-join.ts` + `relay-password-join-flow.ts`): append the two records to the relay FIRST (admit-node with the `admit` member sidecar, then meta-key), each with the relay's CAS on seq; on `SEQ_MISMATCH` re-download the log, rebuild both records on the new head and retry (bounded, 4 attempts like the CLI hub join); treat `member_ignored: true` on the admit append as a hard failure (abort, nothing committed locally). Only after both remote appends succeed: commit locally (user/root state, certs, secrets, relays) and then upload the pack + write env. If the second append fails after the first succeeded, abort without local commit and surface a clear error; document the orphan pending node id in the doc §5b. Tests for: head race → retry succeeds; first append rejected → no local state; second append fails → no local state; `member_ignored` → failure.
3. **R1-#3 unauthorized relay URL** (`relaysForPersist` in the flow module): after replay, the normalized `(url, tenantId)` the user typed MUST exist in the root-signed `set-relays` projection; otherwise fail with `RELAY_NOT_AUTHORIZED` (`join_failed` code + message 「该中继不在根签名的中继列表里」). Replace the token of the matching row with the pack token; never `unshift` an unlisted URL. Test: pack/log copied to another host is rejected.
4. **R1-#5 per-relay packs** (`packages/app/src/lib/relay-pack-upload.ts`, `apps/gateway/src/mesh/relay-pack-routes.ts`, `docs §5b`): each relay has its own tenant id + token, so the pack must be sealed per relay. CLI: seal N packs (KEK info = that relay's tenant id, plaintext token = that relay's token) and upload each to its own relay. Node route `POST /api/mesh/relay/pack`: body becomes `{ kdf_params, root_epoch, head_seq, packs: [{ url, sealed_pack }] }` — forward each pack to its own relay (by url match against `mesh_relays`), keep accepting the old single-pack body for one release (treat as the attached relay's pack). The FE (another agent, F3b) is being told to seal per relay and send `packs[]`. Tests: two relays with different tenant ids both receive a pack they can serve to a joiner.
5. **R1-#6 hub by-password via standby** (`apps/gateway/src/hub/hub-password-enroll.ts`, `hub-runtime.ts` `dispatchForwardedWrite`): the standby verifies the proof against ITS OWN host + runs the limiter, then forwards to the writer over the existing authorized hub→hub write channel with a marker that the proof was verified by hub `<id>`; the writer's `dispatchForwardedWrite` gains the route and, for forwarded frames, skips the host-bound proof check (still verifies the enrollment authorization) and uses the forwarded client IP only for logging. Test with the existing writer-forward harness: standby → writer → 201 with the same material as direct.
6. **R1-#8 relay-join route env** (`packages/app/src/runtime/relay-join-routes.ts`): after `performRelayPasswordJoin` succeeds write env via the staged-env/promote path used by `joinHub` (`setup-shared.ts` helpers): `TMEX_ROLES=node` (or `relay,node` when the machine already runs the relay role), `TMEX_HUB_URL=''`, `TMEX_HUB_PUBLIC_URL=''`, then schedule the restart. Verify what the CLI path (`runRelayPasswordJoin`) does and make both share one helper. Test asserts env file contents.
7. **R1-#9 KDF param bounds** (`packages/shared/src/relay/relay-pack.ts` `kdfParamsFromWire`, `packages/app/src/lib/relay-password-join-flow.ts`, `packages/app/src/lib/hub-password-join.ts`, and the FE-facing shared helper if any): before calling Argon2 reject params outside a fixed budget: `memory_kib ≤ 262144`, `iterations ≤ 10`, `parallelism ≤ 4`, salt exactly 16 bytes; export one shared validator `assertKdfParamsWithinBudget` from `packages/shared/src/auth/root-key.ts` and use it in both join paths. Tests.
8. **R1-#7 limiter hardening** (`apps/gateway/src/hub/hub-enroll-limiter.ts`, `apps/gateway/src/relay/relay-enroll-limiter.ts`): separate buckets for IP and for UID/tenant (a request must pass both); synchronous reservation for the success cap (reserve before the async persist, release on failure); eviction never removes a bucket that is still within its window or currently limited. Skip the proxy-CIDR part (TMEX_TRUST_PROXY policy is a settled round-20 decision).
9. **R1-#10 zeroization on failure paths** in the join flow: hoist the sensitive buffers (pack.log_key, pack.token, generated K_meta, seed) and zero them in a single `finally`. Test with an injected failure after unpack.
10. **R1-#11 relay password policy**: server-side minimum 8 chars when non-empty (empty = no password stays allowed) in `packages/app/src/runtime/relay-setup-service.ts` and `apps/gateway/src/relay/relay-admin-routes.ts` (shared constant in `apps/gateway/src/relay/relay-password.ts`); tests.

NOT to fix (commander decision, do not touch): R1-#4 (passkey admit sidecar — documented boundary §13), R1-#7 proxy CIDR validation.

## Scope (files you own)
- apps/gateway/src/relay/** (except relay-routes.ts only where needed for #1/#10), apps/gateway/src/mesh/relay-pack-routes.ts (+test), apps/gateway/src/hub/hub-password-enroll.ts, hub-enroll-limiter.ts, hub-runtime.ts (dispatch only), their tests, writer-forward.test.ts
- packages/app/src/lib/relay-password-join.ts, relay-password-join-flow.ts, relay-pack-upload.ts, hub-password-join.ts, packages/app/src/commands/relay-password-join.ts, packages/app/src/runtime/relay-join-routes.ts, relay-setup-service.ts, setup-shared.ts (helpers only) and their tests
- packages/shared/src/relay/relay-pack.ts, packages/shared/src/auth/root-key.ts (validator only) and tests
- docs/relay/2026090304-relay-role.md §5b (pack per relay, ordering, orphan note)
Do NOT touch: apps/fe/**, packages/api-client/** (F3b is editing tenant-api.ts — describe the new `packs[]` body for it under 需要指挥官处理), packages/shared/src/auth/** other than root-key.ts.
Baselines: gateway 4202, app 841, shared 646. Keep the complexity gate clean (no allowlist changes).
## Common rules (apply to every task)

- Repo: Bun-only TypeScript monorepo at /Users/konata/code/tmex-r24 (git worktree, branch feat/round24-relay-local-role, base = main 1.1.23). Bun is /opt/homebrew/bin/bun (if not on PATH, read ~/.zshrc). Runtime code runs on Bun 1.3.x; only packages/app's install CLI stays Node-compatible.
- OTHER AGENTS ARE EDITING THIS SAME WORKTREE IN PARALLEL. Touch ONLY the files listed in your scope (plus new files you create inside your scope directories). Do not reformat or "clean up" files outside your scope. If a change outside your scope is unavoidable, do NOT make it — describe it precisely in your result file under "需要指挥官处理".
- NO git operations at all (no add/commit/stash/checkout/reset/worktree). The commander commits.
- Do not run `bun install`, do not edit lockfiles or package.json dependencies unless your scope says so.
- Do not touch generated files: packages/shared/src/i18n/resources.ts, packages/shared/src/i18n/types.ts, packages/shared/src/i18n/locales/generated/*, resources/fe-dist/*, dist/*. i18n keys live in packages/shared/src/i18n/locales/{zh_CN,en_US,ja_JP}.json (all three must be updated together, same key set) and are regenerated with `bun run build:i18n` at the repo root (you MAY run that if your scope includes i18n changes). Edit only the sub-objects named in your scope; other agents edit other sub-objects of the same JSON files.
- Copy (UI text) rules: read /Users/konata/code/tmex-copy-guidelines.md before writing any user-facing text. Key points: "Hub" means hub role; "中继" means the relay role ONLY; "本机" not "这台机器"; no second person; full-width Chinese punctuation.
- Never touch the production tmex service (port 9883, ~/Library/Application Support/tmex) and never touch a tmux session named `tmex`. Any temporary gateway instance must set TMEX_TMUX_SOCKET to an isolated socket (e.g. tmex-r24-<task>) and use ports other than 9883/9663/19883/19663. Never run `tmux kill-server` on the default socket.
- Do not run the Playwright e2e suite (apps/fe/tests) — the commander runs it. In apps/fe, unit tests are `bun test src/`.
- Code style: TypeScript, biome. No unnecessary comments; comments only for genuinely complex logic, written in Simplified Chinese. Identifiers in English. Follow patterns in neighbouring files. Keep every file under 600 lines (complexity gate; `bun run lint` at repo root runs biome + the gate). Do not add entries to the complexity allowlist. When a file you must touch is already near 600 lines, split it (moving code into new files inside your scope is fine).
- Verify before finishing: (1) `bunx tsc --noEmit -p <package>` for each package you touched must not add errors versus baseline (baseline: packages/stores 1, packages/theme 9, packages/api-client 5 pre-existing errors; everything else 0); (2) `bun test` inside each touched package (apps/fe: `bun test src/`) must pass — baselines: gateway 4141, shared 621, app 798 (+1 known env failure in scripts/build-runtime.test.ts), fe 1883, ws-client 392, stores 411, panels 911, ui 370, api-client 201, terminal-ui 394, theme 52; (3) `bunx biome check <your files>`. macOS has no `timeout` command; bun test summary lines carry ANSI colours.
- Write tests for new behaviour (bun test, colocated `*.test.ts(x)`). Deterministic; no sleeps over 100 ms.
- Never hardcode credentials in scripts or tests beyond obvious fixture values.
- When completely done, write your result report (Simplified Chinese, concise, technical) to the ABSOLUTE result path given in your task — list files changed, tests added, verification output summary, anything the commander must handle — and only then exit. The result file must be the LAST thing you write.
