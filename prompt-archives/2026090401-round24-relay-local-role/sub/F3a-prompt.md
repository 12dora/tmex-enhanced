# Task F3a — Web password join forms (Hub / 中继), tenant id display, 接入设备 guide update, relay-mode rename via rename-node

Result file (write LAST): /Users/konata/code/tmex-r24/prompt-archives/2026090401-round24-relay-local-role/sub/F3a-result.md

## Background (all already committed on this branch — read the result files)
- `sub/P2-result.md`: `POST /api/setup/join` now takes `method: 'token' | 'password'` (+ `password`); new `POST /api/setup/relay-join { relayUrl, tenantId, password, name, caFingerprint?, directEnable? }`; api-client `packages/api-client/src/local/setup-api.ts` has `joinHub()` (method) and `relayJoin()`. CLI: `tmex hub join <url> --password`, `tmex relay join <url> --tenant <id>`.
- `sub/P1-result.md` + `docs/relay/2026090304-relay-role.md` §5b: relay password join protocol; the tenant id is what a joiner types.
- `sub/G3-result.md`: new key-log record `rename-node` (`buildRenameNodePayload` / `encodeRenameNodePayload` in `@tmex/shared/auth`), appended via `AuthApi.appendKeyLog({bytes,sig}, {hubSync:true})` like `set-relays`; relay-mode nodes have no hub rename API.
- `sub/F1-result.md`, `sub/F2-result.md`: 本机 card tabs (`apps/fe/src/pages/settings/nodes/uplink/*`), setup wizard now has three path cards, `BecomeRelayForm`, `PasswordFieldWithGenerate` (`apps/fe/src/components/forms/password-field-with-generate.tsx`).
- Copy rules: /Users/konata/code/tmex-copy-guidelines.md. Password join is now the PRIMARY path for users; join tokens/`r3.` strings become the advanced/fallback path.

## Requirements
1. **Join Hub form** (`setup/join-hub-form.tsx`, `validation.ts`, `submit.ts`, `use-hub-setup-submit.ts` + tests): default mode 「用密码加入」 — fields Hub 地址、密码（账号密码）、本机名称、直连开关; a toggle/link 「改用加入码」 switches to the existing token fields. Submit with `method:'password'`. Keep existing test ids for token mode; add `setup-join-method-password` / `setup-join-password-input` etc.
2. **Join relay form** — new `setup/join-relay-form.tsx`: 中继地址、租户编号、密码、本机名称、CA 指纹（高级，可选）、直连开关 → `setupApi.relayJoin()` → same restart-wait flow. Render it in the relay tab's standalone slot (`relay-uplink-panel.tsx` currently renders `BecomeRelayForm` there via `nodes-tab.tsx`): the standalone relay tab should offer two clearly separated choices — 「加入已有中继」 (this form, first) and 「本机作为中继」 (existing `BecomeRelayForm`, second); also add a wizard path card 「加入中继」 (`setup-path-join-relay`) and `SetupIntent` `'join-relay'` (extend `membership/intent.ts` + `role-transition.ts` only where the intent enum is exhaustively matched; do not change the role matrix semantics).
3. **Tenant id display**: in relay mode show the tenant id (from `useMeshRelay` state — `apps/fe/src/node/mesh-relay.ts` exposes `tenantId`; check) as a copyable value in `RelayStrip` (`relay/relay-strip.tsx`, use `CopyableValue` from `../copy-feedback`) with label 「租户编号」 and a hint that another machine joins with 中继地址 + 租户编号 + 密码. Test id `nodes-relay-tenant-id`.
4. **接入设备 side panel** (`apps/fe/src/components/side-panels/connect-devices/*`, i18n `connectDevices.*`): the computer guide must describe password join as the primary path, chosen by the current uplink mode: Hub mode → 「在新机器上安装 tmex，然后在设置 → 节点 → 本机 → 接入 Hub 选择『用密码加入』，填 Hub 地址 + 密码」 plus the CLI equivalent `tmex hub join <hub-url> --password`; relay mode → same with 中继地址 + 租户编号（可复制）+ 密码 and `tmex relay join <relay-url> --tenant <id>`. Keep the join-token flow as a collapsed 「使用加入码（高级）」 section (existing `join-token.tsx` + `join-command-preview.ts`). Update `connect-devices-panel.test.tsx` and the guide tests. The mobile guide is unaffected unless it references tokens.
5. **Relay-mode rename**: today renaming another node calls the hub API (`apps/fe/src/node/hub-api.ts` `rename`, used by `pages/settings/use-node-rename-channel.ts`, `use-site-settings-save.ts`, `nodes/management/use-node-row-actions.ts`, node detail dialog). In relay mode (`useMeshRelay().relayMode`), sign a `rename-node` record with the credential prompt (`withSigner`, same as `set-relays` flows in `use-relay-actions.ts`) and append with `hubSync:true`; self-rename via site settings in relay mode goes the same way. Hub mode keeps the HTTP path. Tests.
6. i18n: you own `nodes.setup.*` (add keys), `connectDevices.*`, `relay.tenant.strip.tenantId*`, `nodes.rename*` keys you need; three locales; `bun run build:i18n`.
7. Tests + `bunx tsc --noEmit -p apps/fe` clean for your files; fe baseline 1968.

## Scope (files you own)
- apps/fe/src/pages/settings/nodes/setup/** (incl. tests), membership/intent.ts (+test), membership/role-transition.ts (+test, intent enum only)
- apps/fe/src/pages/settings/nodes/uplink/relay-uplink-panel.tsx (+test) — only the standalone slot composition; apps/fe/src/pages/settings/nodes/nodes-tab.tsx (+test) — slot wiring
- apps/fe/src/pages/settings/nodes/relay/relay-strip.tsx, relay-ui.test.tsx
- apps/fe/src/components/side-panels/connect-devices/**
- apps/fe/src/pages/settings/use-node-rename-channel.ts (+test), use-site-settings-save.ts, nodes/management/use-node-row-actions.ts (+test), nodes/management/node-detail-dialog.tsx / node-detail-types.ts if needed; new `apps/fe/src/node/rename-node.ts` (+test)
- packages/api-client/src/auth/* only if a helper is missing (append only)
- i18n sub-objects listed above
Do NOT touch: apps/fe/src/pages/settings/nodes/relay/relay-dialogs.tsx, use-relay-actions.ts, apps/fe/src/auth/account-security-actions.ts, apps/fe/src/node/relay-enroll.ts (a follow-up task F3b handles pack refresh there); apps/fe/src/node/relay-meta-key-resident.tsx, mesh-nodes-resident.tsx, main.tsx, nodes/management/enrollment-section.tsx, use-create-enrollment.ts, pages/settings/relay/** (F4 is editing them right now); packages/app/**, apps/gateway/**.
## Common rules (apply to every task)

- Repo: Bun-only TypeScript monorepo at /Users/konata/code/tmex-r24 (git worktree, branch feat/round24-relay-local-role, base = main 1.1.23). Bun is /opt/homebrew/bin/bun (if not on PATH, read ~/.zshrc). Runtime code runs on Bun 1.3.x; only packages/app's install CLI stays Node-compatible.
- OTHER AGENTS ARE EDITING THIS SAME WORKTREE IN PARALLEL. Touch ONLY the files listed in your scope (plus new files you create inside your scope directories). Do not reformat or "clean up" files outside your scope. If a change outside your scope is unavoidable, do NOT make it — describe it precisely in your result file under "需要指挥官处理".
- NO git operations at all (no add/commit/stash/checkout/reset/worktree). The commander commits.
- Do not run `bun install`, do not edit lockfiles or package.json dependencies unless your scope says so.
- Do not touch generated files: packages/shared/src/i18n/resources.ts, packages/shared/src/i18n/types.ts, packages/shared/src/i18n/locales/generated/*, resources/fe-dist/*, dist/*. i18n keys live in packages/shared/src/i18n/locales/{zh_CN,en_US,ja_JP}.json (all three must be updated together, same key set) and are regenerated with `bun run build:i18n` at the repo root (you MAY run that if your scope includes i18n changes). Edit only the sub-objects named in your scope; other agents edit other sub-objects of the same JSON files.
- Copy (UI text) rules: read /Users/konata/code/tmex-copy-guidelines.md before writing any user-facing text. Key points: "Hub" means hub role; "中继" means the relay role ONLY; "本机" not "这台机器"; no second person; full-width Chinese punctuation.
- Never touch the production tmex service (port 9883, ~/Library/Application Support/tmex) and never touch a tmux session named `tmex`. Any temporary gateway instance must set TMEX_TMUX_SOCKET to an isolated socket (e.g. tmex-r24-<task>) and use ports other than 9883/9663/19883/19663. Never run `tmux kill-server` on the default socket.
- Do not run the Playwright e2e suite (apps/fe/tests) — the commander runs it. In apps/fe, unit tests are `bun test src/`.
- Code style: TypeScript, biome. No unnecessary comments; comments only for genuinely complex logic, written in Simplified Chinese. Identifiers in English. Follow patterns in neighbouring files. Keep every file under 600 lines (complexity gate; `bun run lint` at repo root runs biome + the gate). Do not add entries to the complexity allowlist. When a file you must touch is already near 600 lines, split it (moving code into new files inside your scope is fine).
- Verify before finishing: (1) `bunx tsc --noEmit -p <package>` for each package you touched must not add errors versus baseline (baseline: packages/stores 1, packages/theme 9, packages/api-client 5 pre-existing errors; everything else 0); (2) `bun test` inside each touched package (apps/fe: `bun test src/`) must pass — baselines: gateway 4141, shared 621, app 798 (+1 known env failure in scripts/build-runtime.test.ts), fe 1883, ws-client 392, stores 411, panels 911, ui 370, api-client 201, terminal-ui 394, theme 52; (3) `bunx biome check <your files>`. macOS has no `timeout` command; bun test summary lines carry ANSI colours.
- Write tests for new behaviour (bun test, colocated `*.test.ts(x)`). Deterministic; no sleeps over 100 ms.
- Never hardcode credentials in scripts or tests beyond obvious fixture values.
- When completely done, write your result report (Simplified Chinese, concise, technical) to the ABSOLUTE result path given in your task — list files changed, tests added, verification output summary, anything the commander must handle — and only then exit. The result file must be the LAST thing you write.
