You are exploring the tmex monorepo (Bun runtime, TypeScript) at the current directory, READ-ONLY. Output your complete report as your FINAL MESSAGE (you cannot write files). Write the report in Simplified Chinese (zh-CN), technical, concise, with exact file paths and line numbers. Give exact file lists per item so coding tasks can be dispatched with non-overlapping file sets.

Context: version 1.1.23 added a public "relay" role (blind multi-tenant relay, roles `relay` / `relay,node`) alongside `standalone` / `node` / `hub,node`. Read `docs/relay/2026090304-relay-role.md`, `docs/hub/2026082700-hub-node-architecture.md`, `packages/app/src/lib/roles.ts`, `apps/gateway/src/api/local*` (or wherever `/api/local/status` and `/api/local/leave` live), `apps/gateway/src/relay/*`, `apps/gateway/src/mesh/relay*`, `packages/app/src/cli/*relay*`, and `prompt-archives/2026090304-round23-relay-legacy-removal/plan-00-result.md` section 五 (leftovers).

Report the implementation map for each item below (backend side only; the FE is covered by another explorer):

### A. Local machine role switching must support relay roles
Today the web "本机" card offers roles standalone / node / hub,node via `/api/local/status` (role field) and role transitions (`/api/local/leave`, hub setup wizard that calls init/join style routes). Map precisely:
1. How `standalone → hub,node` (become hub) and `standalone → node` (join hub) are executed from the web today: which routes, which module writes app.env roles / restarts the gateway, what validation exists (`packages/app/src/lib/roles.ts`, `apps/gateway/src/api/...`, `apps/gateway/src/system/*`).
2. How `tmex init --role relay|relay,node --relay-public-url` works in the CLI (files, what it writes: TMEX_ROLES, TMEX_RELAY_PUBLIC_URL, relay operator password, etc.) and what a web equivalent ("become relay") needs: exact env keys, secrets, restart semantics. Is there any route today that lets the web switch the machine to a relay role? If not, propose the route shape (e.g. `POST /api/local/role` or extend the hub setup route) and list every backend file to touch.
3. Transitions matrix: for each pair among {standalone, node, hub,node, relay, relay,node}: what must happen (leave mesh? keep relay tenants? clear relay operator state?). Note `/api/local/leave` currently maps `relay,node` → `standalone` and refuses pure `relay` (400 not_member); the result doc says keeping `relay` after leaving needs a target-role field. Specify the minimal backend change to support a target role.
4. `/api/local/status` response: does it expose relay-role info (relay public url, tenant count)? List what the FE needs and where to add it.

### B. Round-23 leftovers (backend)
For each, give: files, current code lines, minimal design, tests to add.
1. `peer_cache` lacks a `version` column → relay-mode peers have unknown version; add column + write in `relayListToNodeList`, new migration (find migration numbering under apps/gateway migrations; latest is 0040).
2. Enrollment fan-out to all relays: `relay.enroll.create` only on the attached relay; make it fan out so the r3 join string carries every relay (r3 layout already per-entry `{url, tenant_id16, token32}`); note CA fingerprint per entry.
3. `tmex relay list` / `relay leave` need the local password (node-session). Find round-20's "peer tagging" local no-password gate (search `localAuth`, `peer` tagging in `apps/gateway/src/api/auth*` / `mesh/*`) and how to open `GET /api/mesh/relay/status` to local callers without a password.
4. `GET /api/mesh/relay/status` `rttMs` always null; `/api/relay/status` lacks current node count (for maxNodes preflight in the add-node form). Where to measure RTT (uplink ping) and where to add counts.
5. Relay-mode rename of OTHER nodes needs a `rename-node` keylog record type: map how hub rename works (`nodes` table + broadcast), how relay-mode metadata (`meta-key`, `set-relays`) records are defined in `packages/shared/src/auth/*` and applied on nodes, and what a `rename-node` record would need (signer, minVersion, apply path, FE hook).
6. `join-material` top-level `tenantId`/`token` compat fields kept only for the integration harness: find the harness and list what to change to drop them.

### C. Canonical v1.1 version gate mismatch (bug found today)
`packages/shared/src/ws-borsh/canonical-version.ts` sets CANONICAL_V11_MIN_PEER_VERSION = '1.1.22', but the browser also requires capability `canonical-state-v1.1` (packages/shared/src/capabilities.ts) which only 1.1.23 advertises, and 1.1.22 browsers send clientVersion '0.1.0'. So the real interop floor is 1.1.23. List every place that would change if the constant becomes '1.1.23' (tests, docs, e2e helpers, changelog), and find where the 'server-too-old' transport event is turned into the toast (`packages/stores/src/tmux-event-router.ts`, i18n `websocket.serverTooOld`) so the message can say which node and which version is too old (the entry's `peerNodeTooOldMessage` includes the node's version; check whether the browser can learn the node's id/version — e.g. the per-node runtime already knows nodeId).
