# Task F2 — 本机 card: five roles in the role selector, 「本机作为中继」 setup form, password fields with a 「生成」 button, leave/switch semantics

Result file (write LAST): /Users/konata/code/tmex-r24/prompt-archives/2026090401-round24-relay-local-role/sub/F2-result.md

## Background
- F1 (already committed on this branch) moved the uplink UI into the 本机 card as two tabs (`apps/fe/src/pages/settings/nodes/uplink/*`; read `sub/F1-result.md`). The relay tab exposes a `relaySetup` ReactNode slot for standalone content (currently `null` from `nodes-tab.tsx`).
- G1 (already committed) added the backend: `POST /api/setup/relay` `{ role:'relay'|'relay,node', relayPublicUrl, relayPassword?, username?, password?, directEnable? }` → `{ ok, role, relayPublicUrl, hasPassword, restarting:true, fingerprint? }`; `POST /api/local/leave` now accepts `targetRole?: 'standalone'|'relay'` (`relay,node → relay` keeps the relay operator state; `node|hub,node` with `targetRole:'relay'` is 400 `invalid_target`); `/api/local/status` has `relay: null | { publicUrl, hasPassword, tenantCount, nodesOnline, currentNodes }`. api-client: `packages/api-client/src/local/{types,setup-api,local-api}.ts` (`setupRelay()`, `LocalLeaveRequest.targetRole`). Read `sub/G1-result.md` and those files.
- Exploration maps: `sub/EX2-report.md` §A2, §C (role transitions, wizard, intent), `sub/EX1-report.md` §A3 (transition matrix).
- Copy rules: /Users/konata/code/tmex-copy-guidelines.md (Hub = hub role; 「中继」 = relay role only; 「本机」; no second person).

## Requirements
1. **Role selector** (`local-machine-card.tsx` `RoleRow`, `SELECTABLE_ROLES`): offer all five roles: 单机 / 节点 / Hub 兼节点 / 中继兼节点 / 纯中继 (reuse `ROLE_LABEL_KEY`; check the zh/en/ja labels read well — `nodes.machine.roleRelay` = 「中继」 today; make it 「纯中继（无网页）」 style so the consequence is visible). Selecting 纯中继 must go through a confirmation dialog explaining the web UI disappears after restart and management is CLI-only (`tmex relay …`).
2. **Transitions** (`membership/role-transition.ts` `classifyRoleChange` — rewrite as the full 5×5 matrix, with tests):
   - standalone → relay / relay,node: `setup` path `'become-relay'` (new `SetupIntent`), the form preselects the role.
   - node / hub,node → relay / relay,node: `switch`: leave to standalone (existing `/api/local/leave`, `targetRole` omitted) with intent `'become-relay'` (+ the preselected role) so the wizard reopens on the relay form after restart. `LeaveDialog` copy must say the machine leaves the current mesh first.
   - relay,node → relay: `leave` with `targetRole:'relay'`; dialog copy: 本机退出所属 mesh，中继服务与已接入租户保留.
   - relay,node → standalone: `leave` with `targetRole:'standalone'`; copy: 中继服务、租户、日志全部清除，不可恢复.
   - relay,node → node / hub,node: `switch` via leave (targetRole standalone) + intent join-hub / become-hub; copy mentions the relay service will be removed.
   - relay → anything: not reachable from the web (pure relay has no page) — leave `classifyRoleChange` defined (treat as `setup`-like no-op with a `kind:'unsupported'`) and make the UI never offer it.
   Extend `membership/intent.ts` (`SetupIntent` union: `'become-hub' | 'join-hub' | 'become-relay'`; optionally carry `{ role }` payload for become-relay — keep the sessionStorage record backwards compatible) and `use-leave-mesh.ts` / `leave-api.ts` / `leave-controller.ts` for `targetRole`.
3. **`BecomeRelayForm`** (`setup/become-relay-form.tsx`, rendered into the relay tab's `relaySetup` slot from `nodes-tab.tsx` when standalone; also reachable from the wizard path cards — add a third card 「本机作为中继」 next to 做 Hub / 加入 Hub in `hub-setup-wizard.tsx`, and when the intent is `become-relay` open it directly): fields
   - 公网地址 `relayPublicUrl` (required; same validation as the CLI `normalizeRelayUrl` in `@tmex/shared` — https, loopback http allowed in non-production; reuse the precheck pattern of `become-hub-form.tsx` if a precheck endpoint fits, otherwise client-side validation only),
   - 接入口令 `relayPassword` — **PasswordFieldWithGenerate, pre-generated by default** (see 4), with a one-line explanation: 租户接入本中继时须出示；留空则任何人都可接入,
   - 角色 toggle 「本机也作为节点」 (relay,node, default ON) — when ON, show 用户名 / 密码 / 确认密码 (account bootstrap, same rules as become-hub; the password field gets the generate button but NOT pre-filled) and the direct-connect switch like the other forms,
   - submit → `setupRelay()` → existing restart-wait state machine (`use-hub-setup-submit.ts`, `submit.ts`), then for relay,node write a `SetupIntent`-like follow-up marker `'enroll-self-relay'` so that after the restart the relay tab shows a 「接入本机中继」 call-to-action which opens the existing `RelayEnrollDialog` (`relay/relay-dialogs.tsx` via the uplink controller's `openEnroll('enroll', <publicUrl>)`) with the URL prefilled from `status.relay.publicUrl` and a hint that the 接入口令 just set must be entered again (do not persist secrets). For pure relay, after submit show the 「网页即将不可用」 notice and stop polling.
4. **`PasswordFieldWithGenerate`** — new reusable component `apps/fe/src/components/forms/password-field-with-generate.tsx`: password input with a 「生成」 button on the right (`data-testid="<id>-generate"`), optional show/hide toggle, copy button when a value is present, `defaultGenerate?: boolean` (generate on mount when the field is empty), generator = 20 chars from an unambiguous alphabet using `crypto.getRandomValues`. Use it for: the relay 接入口令 in `BecomeRelayForm` (defaultGenerate), the account password in `BecomeRelayForm` and `become-hub-form.tsx` (no default), and the operator 改口令 dialog `apps/fe/src/pages/settings/relay/password-dialog.tsx` (defaultGenerate when setting a new password). Keep existing test ids on the inputs.
5. **Relay tab in `relay,node` without uplink**: `uplink/relay-uplink-panel.tsx` — when `status.role` includes relay and `relay.mode === 'none'` (no uplink), render the 「接入本机中继」 CTA (prefilled URL) in addition to the generic 「接入中继」 button. Also show a small 「本机中继」 summary row (public URL copyable, 租户 N / 在线 N / 节点 N, 口令 已设置/未设置) sourced from `status.relay` — put it at the top of the relay tab for relay roles.
6. **Type fixes** left by G1: `LocalStatusResponse.relay` is now required → fix fixtures (`setup/hub-setup-wizard.test.tsx`, any other fe test constructing that type); run `bunx tsc --noEmit -p apps/fe` to find them.
7. i18n: you own `nodes.setup.*`, `nodes.membership.*`, `nodes.machine.role*` (labels only) and new `nodes.machine.relayService*` keys, plus `relay.admin.password*` keys you need for the dialog; three locales; run `bun run build:i18n`.
8. Tests: role-transition matrix, intent round-trip, leave targetRole request shaping, BecomeRelayForm validation + submit payload (mock api), PasswordFieldWithGenerate (default generate / regenerate / manual override), relay tab CTA. fe baseline now 1899 pass.

## Scope (files you own)
- apps/fe/src/pages/settings/nodes/local-machine-card.tsx (+test) — role row only
- apps/fe/src/pages/settings/nodes/nodes-tab.tsx (+test) — slot wiring + intent
- apps/fe/src/pages/settings/nodes/setup/** , membership/**
- apps/fe/src/pages/settings/nodes/uplink/relay-uplink-panel.tsx (+ its test), uplink/local-uplink-tabs.tsx only if the slot/props need extending
- apps/fe/src/components/forms/password-field-with-generate.tsx (+test)
- apps/fe/src/pages/settings/relay/password-dialog.tsx (+ relay-tab.test.tsx if affected)
- i18n sub-objects listed above
Do NOT touch: uplink/local-uplink-controller.ts, uplink/hub-uplink-panel.tsx, management/**, relay/relay-dialogs.tsx / use-relay-actions.ts (another task adds password-join there later), apps/fe/src/components/side-panels/**, packages/*, apps/gateway/**.
## Common rules (apply to every task)

- Repo: Bun-only TypeScript monorepo at /Users/konata/code/tmex-r24 (git worktree, branch feat/round24-relay-local-role, base = main 1.1.23). Bun is /opt/homebrew/bin/bun (if not on PATH, read ~/.zshrc). Runtime code runs on Bun 1.3.x; only packages/app's install CLI stays Node-compatible.
- OTHER AGENTS ARE EDITING THIS SAME WORKTREE IN PARALLEL. Touch ONLY the files listed in your scope (plus new files you create inside your scope directories). Do not reformat or "clean up" files outside your scope. If a change outside your scope is unavoidable, do NOT make it — describe it precisely in your result file under "需要指挥官处理".
- NO git operations at all (no add/commit/stash/checkout/reset/worktree). The commander commits.
- Do not run `bun install`, do not edit lockfiles or package.json dependencies unless your scope says so.
- Do not touch generated files: packages/shared/src/i18n/resources.ts, packages/shared/src/i18n/types.ts, packages/shared/src/i18n/locales/generated/*, resources/fe-dist/*, dist/*. i18n keys live in packages/shared/src/i18n/locales/{zh_CN,en_US,ja_JP}.json (all three must be updated together, same key set) and are regenerated with `bun run build:i18n` at the repo root (you MAY run that if your scope includes i18n changes). Edit only the sub-objects named in your scope; other agents edit other sub-objects of the same JSON files.
- Copy (UI text) rules: read /Users/konata/code/tmex-copy-guidelines.md before writing any user-facing text. Key points: "Hub" means hub role; "中继" means the relay role ONLY; "本机" not "这台机器"; no second person; full-width Chinese punctuation.
- Never touch the production tmex service (port 9883, ~/Library/Application Support/tmex) and never touch a tmux session named `tmex`. Any temporary gateway instance must set TMEX_TMUX_SOCKET to an isolated socket (e.g. tmex-r24-<task>) and use ports other than 9883/9663/19883/19663. Never run `tmux kill-server` on the default socket.
- Do not run the Playwright e2e suite (apps/fe/tests) — the commander runs it. In apps/fe, unit tests are `bun test src/`.
- Code style: TypeScript, biome. No unnecessary comments; comments only for genuinely complex logic, written in Simplified Chinese. Identifiers in English. Follow patterns in neighbouring files. Keep every file under 600 lines (complexity gate; `bun run lint` at repo root runs biome + the gate). Do not add entries to the complexity allowlist. When a file you must touch is already near 600 lines, split it (moving code into new files inside your scope is fine).
- Verify before finishing: (1) `bunx tsc --noEmit -p <package>` for each package you touched must not add errors versus baseline (baseline: packages/stores 1, packages/theme 9, packages/api-client 5 pre-existing errors; everything else 0); (2) `bun test` inside each touched package (apps/fe: `bun test src/`) must pass — baselines: gateway 4141, shared 621, app 798 (+1 known env failure in scripts/build-runtime.test.ts), fe 1883, ws-client 392, stores 411, panels 911, ui 370, api-client 201, terminal-ui 394, theme 52; (3) `bunx biome check <your files>`. macOS has no `timeout` command; bun test summary lines carry ANSI colours.
- Write tests for new behaviour (bun test, colocated `*.test.ts(x)`). Deterministic; no sleeps over 100 ms.
- Never hardcode credentials in scripts or tests beyond obvious fixture values.
- When completely done, write your result report (Simplified Chinese, concise, technical) to the ABSOLUTE result path given in your task — list files changed, tests added, verification output summary, anything the commander must handle — and only then exit. The result file must be the LAST thing you write.
