You are exploring the tmex monorepo (Bun runtime, TypeScript, React 19 frontend in apps/fe) at the current directory, READ-ONLY. Output your complete report as your FINAL MESSAGE (you cannot write files). Write the report in Simplified Chinese (zh-CN), technical, concise, with exact file paths and line numbers. Give exact file lists so coding tasks can be dispatched with non-overlapping file sets.

Context: 1.1.23 added a "relay" uplink mode next to the "hub" uplink mode for a mesh node. The settings page "节点" tab (`apps/fe/src/pages/settings/nodes/`) has: `local-machine-card.tsx` (本机 card: role select with standalone/node/hub,node, current hub rows, direct plugin, domain access), `management/nodes-management.tsx` (node table etc.), `relay/uplink-section.tsx` (HubStrip vs RelayStrip + a `Network` icon dropdown that switches to relay mode / add / reauth / rotate / leave), `relay/*`, `setup/hub-setup-wizard.tsx` (standalone: become hub / join hub), `membership/*` (leave / role transition), and `apps/fe/src/node/mesh-relay.ts` (useMeshRelay hook) and `apps/fe/src/node/mesh-hubs.ts`.

The product decision for this round:
(1) The 本机 card's role selector must also offer the relay roles (`relay`, `relay,node`), with the corresponding setup (public relay URL, operator password) when switching from standalone, and sensible transitions from/to mesh roles.
(2) The "hub vs relay uplink mode" choice must move OUT of the node-management section (no more Network icon menu there) INTO the 本机 card as two tabs: "接入 Hub" / "接入中继". Selecting the Hub tab shows the hub-related UI (current hub, hub list, change hub, become/join hub wizard in standalone); selecting the 中继 tab shows relay-related UI (relay strip, enroll/add/reauth/remove/rotate/leave, quota, kicked/meta-key notices). Node management keeps only the node table & enrollment.

Report:
### A. Current wiring
1. Every component/hook involved in the uplink section and relay actions: props, data sources (`useMeshRelay`, `useMeshHubs`, `useSharedAuthMode`, `useLocalStatus`), where `RelayActionsController` is created (`use-relay-actions.ts`) and which dialogs (`relay-dialogs.tsx`) it drives, the meta-key follow-up hook, and how `nodes-management.tsx` composes them (line ranges). Note the complexity gate: `nodes-management.tsx` is at 595/600 lines.
2. How the standalone wizard (`setup/hub-setup-wizard.tsx`, `join-hub-form.tsx`, `become-hub-form.tsx`, `submit.ts`, `use-hub-setup-submit.ts`) works, including the `SetupIntent` mechanism (`membership/intent.ts`) and `role-transition.ts` (`classifyRoleChange`, `ROLE_LABEL_KEY`). Does the join-hub form already accept an `r3.` relay join string (relay enrollment from standalone)? If not, where does relay enroll from standalone happen today (`relay-dialogs.tsx` enroll mode)?
3. i18n: list the key namespaces used (`nodes.*`, `relay.tenant.*`), which locale files (`packages/shared/src/i18n/locales/*.json`), and how keys are regenerated (`bun run build:i18n`).
4. Tests: list the existing unit tests covering these components (`*.test.tsx`) and the e2e specs under `apps/fe/tests` that touch settings nodes / relay / local machine card (test ids used).
5. The "local machine card multi hub" rows (`MachineHubRows`) and how `useMeshHubs` is enabled.

### B. Proposed restructure
Propose a concrete component split for the 本机 card with two uplink tabs (use `@tmex/ui/tabs` if it exists — check `packages/ui/src` for a Tabs component and its usage elsewhere, e.g. settings page tabs), keeping every file under 600 lines: which existing components move where (file moves/renames are fine since the commander commits), which new files, which props. Specify how the active tab is derived (relay mode → 中继 tab, hub mode → Hub tab, standalone → user choice, remembered where?) and how "migrate from hub to relay" / "leave relay" flows map onto the tabs. Identify every test file and e2e spec that must be updated (test ids that move).

### C. Relay roles in the role selector
Map what the FE needs to offer `relay` and `relay,node`: `LocalRole` type in `packages/api-client/src/local/types.ts`, `ROLE_LABEL_KEY`, `classifyRoleChange`, `LeaveDialog` texts, `HubSetupWizard` paths (a "become relay" form: public URL + operator password + optional also-node), and what `/api/local/status` currently returns for relay roles. List backend routes the FE would call (existing: hub setup/join routes; note if a "become relay" route is missing).
