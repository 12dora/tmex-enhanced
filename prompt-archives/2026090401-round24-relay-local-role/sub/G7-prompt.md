# Task G7 — Fix the two live-test failures: Hub password join never gets admitted; relay password-join errors collapse to 500

Result file (write LAST): /Users/konata/code/tmex-r24/prompt-archives/2026090401-round24-relay-local-role/sub/G7-result.md

READ FIRST: `prompt-archives/2026090401-round24-relay-local-role/sub/LT-result.md` (live test; §7 hub password join FAIL with the exact evidence and file:line pointers, plus the relay-join error-mapping finding), `sub/P2-result.md`, `sub/P1-result.md`, `sub/G6-result.md`.

## Bug 1 (release blocker): a node joined with 「Hub 地址 + 密码」 never connects
`POST /api/setup/join { method:'password' }` / `tmex hub join --password` succeeds (enrollment created via `POST /api/hub/enrollments/by-password`, redeem ok, env written), but the node's uplink is rejected with `unknown-cert` forever: nobody signs an `admit-node` record for it. In the token flow the *browser* that generated the token signs the admit (`apps/fe/src/node/enrollment-engine.ts:~756`); by-password enrollments have `entry_node_id = null` (`apps/gateway/src/hub/hub-password-enroll.ts:~243-252`), so the hub pushes the certificate to no one (`hub-runtime.ts:~992-1001`), and the only writer of `node_certs` on the hub is the admit-node projection (`apps/gateway/src/auth/user-key-persistence.ts:~201-214`). The joiner holds the root key, so it must **self-admit**, exactly like the relay password join does (`apps/gateway/src/auth/user-key-self-admit.ts` `joinSelfAdmitAndPersist`, used by `packages/app/src/lib/relay-password-join-flow.ts`).
Implement: in the hub password-join path (`packages/app/src/lib/hub-password-join.ts` + `packages/app/src/commands/hub.ts` `performHubJoin` password branch, or a post-join step in `packages/app/src/runtime/setup-service.ts` `joinHub` when `method==='password'`), after the local join commit build a root-signed `admit-node` for the node's own certificate on top of the replayed key-log head and publish it to the hub with the same CAS/publish path the token flow's admit uses from the hub's perspective (find how the browser's admit reaches the hub: `POST /api/auth/keylog?hub=sync` on the entry node → hub `key.log.append`; from a joining CLI/standalone process use the hub's HTTP key-log endpoint that `performHubJoin` already talks to, or the uplink's first sync). Requirements: the record must reach the hub before the process restarts (or be persisted locally so the node's first uplink sync pushes it and the hub applies it before the cert check — verify which one is true and test it); on hub conflict (head moved) re-fetch and retry (bounded). Reuse `joinSelfAdmitAndPersist` where possible instead of duplicating.
Tests: extend the gateway hub test harness (`apps/gateway/src/hub/*.test.ts`, `writer-forward.test.ts` style, or an integration test next to `apps/gateway/src/relay/integration/`) with an end-to-end: standalone joins hub by password → node uplink authenticates (`node_certs` has 1 row on the hub, node online). Also a CLI unit test that the password branch appends admit-node.

## Bug 2: relay password-join errors surface as HTTP 500 `internal_error`
`packages/app/src/runtime/relay-join-routes.ts:~47-61` does not translate `RelayPasswordJoinError` into `SetupError`. Map: wrong password / bad proof → `relay_password_invalid` (401), unknown tenant → `relay_tenant_unknown` (404), sealed pack open failure / AAD mismatch / head mismatch → `relay_pack_invalid` (409), relay unreachable → `relay_unreachable` (502), local user exists → `local_user_exists` (409), relay not in the root-signed list → `relay_not_authorized` (403), anything else → `join_failed` (400) with the inner message. Keep the error codes stable and list them in the result file (the frontend maps them in `apps/fe/src/pages/settings/nodes/setup/error-messages.ts`, another agent). Tests for each mapping.

## Scope
- packages/app/src/lib/hub-password-join.ts, packages/app/src/commands/hub.ts, packages/app/src/runtime/setup-service.ts, relay-join-routes.ts, packages/app/src/lib/relay-password-join*.ts (only if the error classes need codes), their tests
- apps/gateway/src/hub/hub-password-enroll.ts, hub-runtime.ts (only if the hub must accept/apply the record on a path it does not today), apps/gateway/src/auth/user-key-self-admit.ts (generalize for hub mode if needed), tests
Do NOT touch apps/fe/**. Baselines: gateway 4211, app 850, shared 649. Keep `bun run lint` green (no allowlist changes).
## Common rules (apply to every task)

- Repo: Bun-only TypeScript monorepo at /Users/konata/code/tmex-r24 (git worktree, branch feat/round24-relay-local-role, base = main 1.1.23). Bun is /opt/homebrew/bin/bun (if not on PATH, read ~/.zshrc). Runtime code runs on Bun 1.3.x; only packages/app's install CLI stays Node-compatible.
- OTHER AGENTS ARE EDITING THIS SAME WORKTREE IN PARALLEL. Touch ONLY the files listed in your scope (plus new files you create inside your scope directories). Do not reformat or "clean up" files outside your scope. If a change outside your scope is unavoidable, do NOT make it — describe it precisely in your result file under "需要指挥官处理".
- NO git operations at all (no add/commit/stash/checkout/reset/worktree). The commander commits.
- Do not run `bun install`, do not edit lockfiles or package.json dependencies unless your scope says so.
- Do not touch generated files: packages/shared/src/i18n/resources.ts, packages/shared/src/i18n/types.ts, packages/shared/src/i18n/locales/generated/*, resources/fe-dist/*, dist/*. i18n keys live in packages/shared/src/i18n/locales/{zh_CN,en_US,ja_JP}.json (all three must be updated together, same key set) and are regenerated with `bun run build:i18n` at the repo root (you MAY run that if your scope includes i18n changes). Edit only the sub-objects named in your scope; other agents edit other sub-objects of the same JSON files.
- Copy (UI text) rules: read /Users/konata/code/tmex-copy-guidelines.md before writing any user-facing text. Key points: "Hub" means hub role; "中继" means the relay role ONLY; "本机" not "这台机器"; no second person; full-width Chinese punctuation.
- Never touch the production tmex service (port 9883, ~/Library/Application Support/tmex) and never touch a tmux session named `tmex`. Any temporary gateway instance must set TMEX_TMUX_SOCKET to an isolated socket (e.g. tmex-r24-<task>) and use ports other than 9883/9663/19883/19663. Never run `tmux kill-server` on the default socket.
- Do not run the Playwright e2e suite (apps/fe/tests) — the commander runs it. In apps/fe, unit tests are `bun test src/`.
- Code style: TypeScript, biome. No unnecessary comments; comments only for genuinely complex logic, written in Simplified Chinese. Identifiers in English. Follow patterns in neighbouring files. Keep every file under 600 lines (complexity gate; `bun run lint` at repo root runs biome + the gate). Do not add entries to the complexity allowlist. When a file you must touch is already near 600 lines, split it (moving code into new files inside your scope is fine).
- Verify before finishing: (1) `bunx tsc --noEmit -p <package>` for each package you touched must not add errors versus baseline (baseline: packages/stores 1, packages/theme 9, packages/api-client 5 pre-existing errors; everything else 0); (2) `bun test` inside each touched package (apps/fe: `bun test src/`) must pass — baselines: gateway 4141, shared 621, app 798 (+1 known env failure in scripts/build-runtime.test.ts), fe 1883, ws-client 392, stores 411, panels 911, ui 370, api-client 201, terminal-ui 394, theme 52; (3) `bunx biome check <your files>`. macOS has no `timeout` command; bun test summary lines carry ANSI colours.
- Write tests for new behaviour (bun test, colocated `*.test.ts(x)`). Deterministic; no sleeps over 100 ms.
- Never hardcode credentials in scripts or tests beyond obvious fixture values.
- When completely done, write your result report (Simplified Chinese, concise, technical) to the ABSOLUTE result path given in your task — list files changed, tests added, verification output summary, anything the commander must handle — and only then exit. The result file must be the LAST thing you write.
