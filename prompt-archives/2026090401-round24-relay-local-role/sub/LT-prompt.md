# Task LT — Live multi-process test of this round (relay password join, hub password join, web relay setup, leave targetRole, version gate)

Result file (write LAST): /Users/konata/code/tmex-r24/prompt-archives/2026090401-round24-relay-local-role/sub/LT-result.md

You do NOT modify repository files (read-only on /Users/konata/code/tmex-r24). All scripts, DBs, logs go under /private/tmp/claude-501/-Users-konata-code-tmex-enhanced/c1f43d39-77fb-4832-b1c4-2ca4e9c12e4e/scratchpad/live/ (already contains last round's driver `live-relay.ts` (876 lines, 4 instances, 24 assertions — reuse its boot/env/session/submitRecord helpers verbatim; read `prompt-archives/2026090304-round23-relay-legacy-removal/sub/LT-result.md` for how it was run) plus `helper-*.ts`).

## Hard safety rules (violating any of these is unacceptable)
- Never touch the production tmex: port 9883 / 9663, `~/Library/Application Support/tmex/`, launchd service. Never touch the tmux session named `tmex` or the default tmux socket; use `TMEX_TMUX_SOCKET=tmex-live24` for every instance and only `tmux -L tmex-live24 kill-server` at cleanup.
- Build process env as a whitelist (do not spread process.env) exactly like the previous driver: `NODE_ENV=test`, `TMEX_BIND_HOST=127.0.0.1`, `TMEX_DIRECT_ENABLED=false`, `TMEX_STUN_SERVERS=`, `TMEX_MIGRATIONS_DIR=<repo>/apps/gateway/drizzle`, `TMEX_FE_DIST_DIR=<repo>/apps/fe/dist`, ports 19993–19999 (check free first), one DB per instance under scratch.
- The web setup routes (`/api/setup/*`, `/api/local/leave`) write an env file: read `packages/app/src/runtime/setup-shared.ts` / `setup-service.ts` `resolveSetupEnvPath()` to see which path is used under NODE_ENV=test; if it would point at the repo's `test.env` or any repo file, override it (env var or by running the instance with a cwd/scratch layout that makes it resolve into scratch) — the repo working tree must stay clean (`git status` clean at the end). Restart semantics in test mode: the process exits 0 after `restarting:true`; your driver must re-boot the instance with the env values the route wrote (read the staged/promoted env file from scratch and merge it into the whitelist).
- First build the frontend once: `bun run --cwd apps/fe build` (another agent is editing apps/fe concurrently; if the build fails, wait 60 s and retry up to 5 times, then continue with the API-only checks and note it).

## Scenarios (each numbered assertion PASS/FAIL with evidence)
1. **Relay R** (`TMEX_ROLES=relay`, `TMEX_RELAY_PUBLIC_URL=http://127.0.0.1:<port>`): health 200; set tenant password ≥ 8 chars via admin API; `GET /api/relay/tenants/<random>/kdf` → 404.
2. **Node A enroll** (`node`, user bootstrapped like last round): `POST /api/mesh/relay/enroll` with the relay password → attached; `GET /api/mesh/relay/status` shows `tenantId`, `rttMs` numeric after a heartbeat, `quota.currentNodes`; **pack upload**: use the CLI path (`bun packages/app/src/cli-auth-entry.ts relay enroll …`? — check how `tmex relay enroll` is invoked from source; the runtime CLI entry is `packages/app/src/cli.ts` or similar) OR call `POST /api/mesh/relay/pack` yourself: build the pack with `sealRelayPack` from `@tmex/shared/relay` using A's root seed (you have the password) and `GET /api/mesh/relay/join-material?scope=all` + `GET /api/auth/keylog/head`; then confirm on R (sqlite read-only) `relay_tenants.sealed_pack IS NOT NULL` and `kdf_params_json` set.
3. **Node B joins by password**: B is a fresh standalone instance (`TMEX_ROLES=standalone`); call `POST /api/setup/relay-join { relayUrl, tenantId, password, name:'B' }` → `restarting:true`; re-boot B with the written env (`TMEX_ROLES=node`); B's `/api/mesh/relay/status` → `mode:'relay'`, attached, `nodesViaRelay ≥ 1`; A sees B in `/api/mesh/nodes` (name 'B', admitted) within 15 s; R registry has B `admitted` (sqlite). Also run the CLI form once: `tmex relay join <url> --tenant <id> --password <p> --name B2 --no-restart` against a third fresh instance DB if the CLI can be pointed at a scratch DB (`DATABASE_URL`), else skip and say so.
4. **Negative**: wrong password → `POST /api/setup/relay-join` fails with a clear code and B's DB has no user; unknown tenant id → 404-derived error; tampered pack (flip a byte in `relay_tenants.sealed_pack` on a COPY of R's DB — do not corrupt the running R; or simply assert the client rejects when you seal with a wrong AAD) → rejected.
5. **rename-node in relay mode**: from A, sign a `rename-node` record for B (root key; `buildRenameNodePayload` + `buildKeyLogRecord` from `@tmex/shared/auth`, submit via `POST /api/auth/keylog?hub=sync` like the previous driver's `submitRecord`) → B's `/api/local/status` or node identity name becomes the new name and A's node list shows it.
6. **Web relay setup** (`relay,node`): fresh standalone S → `POST /api/setup/relay { role:'relay,node', relayPublicUrl:'http://127.0.0.1:<S port>', relayPassword:'<≥8>', username, password }` → restart with the written env → S `/api/local/status.relay` shows publicUrl/hasPassword; then enroll S to ITSELF via `POST /api/mesh/relay/enroll` (self-dial loopback) → attached; then `POST /api/local/leave { expectedRole:'relay,node', targetRole:'relay' }` → env shows `TMEX_ROLES=relay`, relay tables intact (sqlite), mesh tables empty.
7. **Hub password join**: H (`hub,node` via `POST /api/setup/hub` on a standalone instance + restart), C standalone → `POST /api/setup/join { method:'password', hubUrl, password, name:'C' }` → restart → H lists C admitted; wrong password → 401-derived error and the hub limiter counts it (`GET` nothing exposed — just assert the second wrong attempt also fails and a correct attempt after 10 wrong ones from the same IP+uid is rate limited with 429).
8. **Version gate**: raw WS HELLO to A with `clientVersion:'1.1.22'` → ERROR with message prefix `canonical-state-v1.1 required: client 1.1.22 < 1.1.23` and close 1002; with `'1.1.24'` → HELLO_S2C (use `apps/fe/tests/helpers/site-theme.ts` as the template for the raw handshake).
9. **Cleanup**: kill all instances, `tmux -L tmex-live24 kill-server`, verify default socket `tmux ls` output unchanged from before the run, ports freed, `git status` clean.

Write the driver as one Bun script (`live24.ts`) with the same `check()/record()` reporting as before; save full logs; the result file lists topology, every assertion with evidence, bugs found (with file:line and a proposed fix — do NOT fix them), and the safety verification.

## Common rules subset
- Bun at /opt/homebrew/bin/bun. macOS has no timeout command. Never run the Playwright e2e suite. No git operations. Write the result file last.
