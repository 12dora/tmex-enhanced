# Task F3b — Web side of the relay "sealed pack": build & upload the pack whenever the browser holds the root seed

Result file (write LAST): /Users/konata/code/tmex-r24/prompt-archives/2026090401-round24-relay-local-role/sub/F3b-result.md

## Background (committed on this branch)
- `docs/relay/2026090304-relay-role.md` §5b and `sub/P1-result.md`: password join ("租户编号 + 密码") relies on a per-tenant sealed pack stored on the relay: `seal(KEK, {K_log, token, head_seq, head_hash})`, `KEK = HKDF(root_seed, "tmex-relay-pack/v1", tenant_id)`, AAD binds tenant_id / root_public_key / root_epoch. Shared implementation: `packages/shared/src/relay/relay-pack.ts` (browser-compatible). Node-side upload proxy: `POST /api/mesh/relay/pack` (`apps/gateway/src/mesh/relay-pack-routes.ts`; body documented in §5b) — the node forwards to the attached relay (and other reachable relays) with its tenant token. Material for the browser: `GET /api/mesh/relay/join-material` (`logKey` + `relays[{url,tenantId,token}]`), `GET /api/auth/keylog/head` (head seq/hash — verify the exact route in `apps/gateway/src/mesh/auth-key-log-routes.ts`), `/api/auth/mode` (uid, kdfParams, rootEpoch).
- The CLI already uploads the pack after `tmex relay enroll` / `reauth` and after CLI password rotation (`packages/app/src/lib/relay-pack-upload.ts` — mirror its logic in the browser).
- The pack must be (re)built by whoever holds the **root seed** at that moment. The browser holds it inside the credential prompt / root signer flows: `apps/fe/src/pages/settings/nodes/relay/use-relay-actions.ts` (`enrollRelay` for enroll / migrate / add / reauth — these already take the root password), `apps/fe/src/auth/account-security-actions.ts` (rotate-root / rotate-root-keep: the NEW seed is in hand), and the generic root-signed key-log appends via `prompt.withSigner` (admit / revoke / meta-key / set-relays / rename-node): look at `apps/fe/src/node/relay-enroll.ts`, `apps/fe/src/auth/*signer*`, `apps/fe/src/components/**/credential-prompt*` to find where the root key is derived (`deriveRootKey` / `deriveSeed` from `@tmex/shared/auth`) and whether the seed is retained on the RootKey object.

## Requirements
1. New module `apps/fe/src/node/relay-pack.ts` (+test): `refreshRelayPack({ rootSeed, api, relayApi, urls? })` → fetch join-material + keylog head + auth mode → `sealRelayPack(...)` from `@tmex/shared/relay` → `POST /api/mesh/relay/pack` (add the client method to `packages/api-client/src/relay/tenant-api.ts` + test). Zero the seed/KEK buffers after use. Errors are non-fatal for the calling flow: log + surface a small non-blocking toast 「密封包未更新，用密码加入其它机器前请重新输入口令」 only when the pack has never been uploaded (i.e. the node reports none — if the node-side route/status exposes `packUploaded`/`packHeadSeq`; if not, just log).
2. Call it: (a) after a successful relay enroll / migrate / add / reauth in `use-relay-actions.ts`; (b) after rotate-root / rotate-root-keep / reset-root in `account-security-actions.ts` (with the NEW seed, after the rotation record is accepted; in the `rotate-root` case the session is revoked, so queue it alongside the pending meta-key mechanism in `apps/fe/src/node/relay-meta-key-pending.ts` — it will need the password again after re-login: reuse the existing "欠账" UX instead of storing the seed); (c) after every root-signed append in relay mode where the signer is a root key (not passkey) — find the central place (`withSigner` / the shared append helper) and add a hook that runs `refreshRelayPack` when `useMeshRelay` reports relay mode; passkey-signed appends skip it (document why in a comment: KEK needs the seed).
3. Only in relay mode (`relay.relayMode`); hub mode is unaffected.
4. Tests: refresh builds the right body (seq/hash/epoch, AAD via shared open); hook runs on root path and not on passkey path; enroll flow calls it after success; rotation queues when 401.
5. i18n: `relay.tenant.pack.*` keys, three locales, `bun run build:i18n`.

## Scope (files you own)
- apps/fe/src/node/relay-pack.ts (+test), apps/fe/src/node/relay-meta-key-pending.ts (+test, extend for the pack refresh debt), apps/fe/src/node/relay-enroll.ts (+test)
- apps/fe/src/pages/settings/nodes/relay/use-relay-actions.ts, relay-dialogs.tsx (+ relay-ui.test.tsx ONLY the parts about these — F3a also edits that test file for relay-strip; append your own `describe` blocks and re-read before editing)
- apps/fe/src/auth/account-security-actions.ts (+test) and the signer/prompt helper you identify (list it in the result)
- packages/api-client/src/relay/tenant-api.ts (+test)
- i18n `relay.tenant.pack.*`
Do NOT touch: apps/fe/src/pages/settings/nodes/setup/**, membership/**, uplink/**, nodes-tab.tsx, relay-strip.tsx, components/side-panels/**, use-node-rename-channel.ts, use-node-row-actions.ts (F3a, running now); apps/fe/src/node/relay-meta-key-retry.ts / resident (F4 done, leave as is); apps/gateway/**, packages/app/**, packages/shared/src/relay/** (if the shared module lacks something you need, describe it under 需要指挥官处理).
Baselines: fe 1976, api-client 206 (+3 pre-existing tsc errors).
## Common rules (apply to every task)

- Repo: Bun-only TypeScript monorepo at /Users/konata/code/tmex-r24 (git worktree, branch feat/round24-relay-local-role, base = main 1.1.23). Bun is /opt/homebrew/bin/bun (if not on PATH, read ~/.zshrc). Runtime code runs on Bun 1.3.x; only packages/app's install CLI stays Node-compatible.
- OTHER AGENTS ARE EDITING THIS SAME WORKTREE IN PARALLEL. Touch ONLY the files listed in your scope (plus new files you create inside your scope directories). Do not reformat or "clean up" files outside your scope. If a change outside your scope is unavoidable, do NOT make it — describe it precisely in your result file under "需要指挥官处理".
- NO git operations at all (no add/commit/stash/checkout/reset/worktree). The commander commits.
- Do not run `bun install`, do not edit lockfiles or package.json dependencies unless your scope says so.
- Do not touch generated files: packages/shared/src/i18n/resources.ts, packages/shared/src/i18n/types.ts, packages/shared/src/i18n/locales/generated/*, resources/fe-dist/*, dist/*. i18n keys live in packages/shared/src/i18n/locales/{zh_CN,en_US,ja_JP}.json (all three must be updated together, same key set) and are regenerated with `bun run build:i18n` at the repo root (you MAY run that if your scope includes i18n changes). Edit only the sub-objects named in your scope; other agents edit other sub-objects of the same JSON files.
- Copy (UI text) rules: read /Users/konata/code/tmex-copy-guidelines.md before writing any user-facing text. Key points: "Hub" means hub role; "中继" means the relay role ONLY; "本机" not "这台机器"; no second person; full-width Chinese punctuation.
- Never touch the production tmex service (port 9883, ~/Library/Application Support/tmex) and never touch a tmux session named `tmex`. Any temporary gateway instance must set TMEX_TMUX_SOCKET to an isolated socket (e.g. tmex-r24-<task>) and use ports other than 9883/9663/19883/19663. Never run `tmux kill-server` on the default socket.
- Do not run the Playwright e2e suite (apps/fe/tests) — the commander runs it. In apps/fe, unit tests are `bun test src/`.
- Code style: TypeScript, biome. No unnecessary comments; comments only for genuinely complex logic, written in Simplified Chinese. Identifiers in English. Follow patterns in neighbouring files. Keep every file under 600 lines (complexity gate; `bun run lint` at repo root runs biome + the gate). Do not add entries to the complexity allowlist. When a file you must touch is already near 600 lines, split it (moving code into new files inside your scope is fine).
- Verify before finishing: (1) `bunx tsc --noEmit -p <package>` for each package you touched must not add errors versus baseline (baseline: packages/stores 1, packages/theme 9, packages/api-client 5 pre-existing errors; everything else 0); (2) `bun test` inside each touched package (apps/fe: `bun test src/`) must pass — baselines: gateway 4141, shared 621, app 798 (+1 known env failure in scripts/build-runtime.test.ts), fe 1883, ws-client 392, stores 411, panels 911, ui 370, api-client 201, terminal-ui 394, theme 52; (3) `bunx biome check <your files>`. macOS has no `timeout` command; bun test summary lines carry ANSI colours.
- Write tests for new behaviour (bun test, colocated `*.test.ts(x)`). Deterministic; no sleeps over 100 ms.
- Never hardcode credentials in scripts or tests beyond obvious fixture values.
- When completely done, write your result report (Simplified Chinese, concise, technical) to the ABSOLUTE result path given in your task — list files changed, tests added, verification output summary, anything the commander must handle — and only then exit. The result file must be the LAST thing you write.
