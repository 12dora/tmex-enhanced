# Task G2 — Backend: relay leftovers (peer version column, local no-password status, RTT + node counts, self-dial loopback, compat field removal)

Result file (write LAST): /Users/konata/code/tmex-r24/prompt-archives/2026090401-round24-relay-local-role/sub/G2-result.md

READ FIRST: `prompt-archives/2026090401-round24-relay-local-role/sub/EX1-report.md` sections B1, B3, B4, B6 (precise file:line maps and minimal designs — follow them), and `docs/relay/2026090304-relay-role.md` §7–§9, §13.

## Deliverables
1. **B1 `peer_cache.version`**: nullable TEXT column, migration `apps/gateway/drizzle/0041_peer_cache_version.sql` registered in `apps/gateway/src/db/managed-migrations.ts` + `drizzle/meta/_journal.json` (another agent will later append a 0042 entry to the same registry — re-read the registry file right before you edit it and only append your own lines). `PeerCacheRecord` / upsert / `toPeer()` carry `version`; `relayListToNodeList()` writes `blob.version`; cache fallback returns it; hub path (`persistAdmittedPeers` / `persistHubPeer` in uplink-client.ts) writes node version. Then remove the two `KEYLOG_RECORD_COMPAT` exemptions the relay doc §7.3 mentions IF the gate can now judge relay-mode peers (check `apps/gateway/src/auth/*compat*` / grep `KEYLOG_RECORD_COMPAT`); if you remove them, add the test that a relay-mode peer with a too-old cached version is now rejected. Update doc §13 last bullet.
2. **B3 local no-password `GET /api/mesh/relay/status`**: trusted-local gate via `isTrustedLocalClient(req)` (`apps/gateway/src/mesh/client-source.ts`) in `MeshHttpRuntime.localUiGuard()` (mesh-http.ts) and in `RelayRoutes.handle()` for that single GET route; browser-supplied `x-tmex-client-source` must not be trusted; peer-forwarded requests are not local. Then make `tmex relay list` (`packages/app/src/commands/relay.ts` `runRelayList`, `packages/app/src/lib/relay-session.ts`) call the status route WITHOUT opening a node-session/asking for a password when it talks to loopback (fall back to the session path on 401). `relay leave` still needs the password (it signs `set-relays`).
3. **B4 RTT + node counts**: `RelayUplinkClient` measures ping→pong RTT (latest value; reset on reconnect) and `/api/mesh/relay/status` reports it for the attached relay; relay server sends `currentNodes` (= `countActiveNodes(tenantId)`) inside `relay.quota` on auth.ok and whenever a node's status changes (codec change in `packages/shared/src/relay/codec.ts`, backwards compatible: optional field); client stores it; `/api/mesh/relay/status` returns `quota.currentNodes`; `GET /api/relay/status` `totals.nodes` (sum of active nodes) — update `packages/api-client/src/relay/admin-api.ts` and `tenant-api.ts` types.
4. **Self-dial loopback**: new helper `apps/gateway/src/mesh/relay-dial.ts` `resolveRelayDialUrl(url, ctx)` — when this process runs the relay role (`roles.relay`) and `hubHostFromUrl(url)` equals the host of its own `TMEX_RELAY_PUBLIC_URL`, return `http://127.0.0.1:<GATEWAY_PORT>` (ws equivalent for the uplink) so a `relay,node` machine can enroll to and attach to its own relay without hairpin NAT; otherwise return the url unchanged. Wire it into `RelayUplinkClient` (WS dial + any HTTP it does) and into the node-side enroll/redeem HTTP calls in `apps/gateway/src/mesh/relay-routes.ts` (`/api/mesh/relay/enroll`, `join-material`, enrollments). The relay-side auth signature binds `relay_host` from `TMEX_RELAY_PUBLIC_URL` — make sure the client still signs with the *public* host, not the loopback host (read how `relayHost` is set at relay-uplink-client.ts:121). CA pinning must be skipped for loopback. Tests for both branches. Also precedent: `uplink-pool.ts` `fallbackCandidate()` uses `http://127.0.0.1` for the self-hub case.
5. **B6**: remove top-level `tenantId`/`token` from `join-material`; harness reads `relays[0]`.

## Scope (files you own)
- apps/gateway/src/mesh/relay-routes.ts (+test), relay-uplink-client.ts (+test; it is 597 lines — split before adding, e.g. move heartbeat/RTT into `relay-uplink-heartbeat.ts`), relay-node-list.ts, relay-dial.ts (new), uplink-client.ts (+test, only the peer version write), mesh-http.ts (+test)
- apps/gateway/src/db/schema/mesh.ts, apps/gateway/src/auth/user-store.ts (+test), apps/gateway/src/auth/schema.migration.test.ts, apps/gateway/drizzle/0041_peer_cache_version.sql, apps/gateway/src/db/managed-migrations.ts, apps/gateway/drizzle/meta/_journal.json
- apps/gateway/src/relay/relay-uplink-server.ts, relay-uplink-handlers.ts, relay-admin-routes.ts (+tests relay-uplink.test.ts, relay-admin.test.ts), apps/gateway/src/relay/integration/relay-tenant-ops.ts + the two integration tests listed in EX1 B6
- packages/shared/src/relay/codec.ts (+test)
- packages/api-client/src/relay/admin-api.ts, tenant-api.ts (+tests)
- packages/app/src/commands/relay.ts, packages/app/src/lib/relay-session.ts (+tests)
- docs/relay/2026090304-relay-role.md: §7.2 (status fields), §9 (uplink RTT / self-dial), §13 (version bullet) — brief edits only
Do NOT touch: packages/app/src/runtime/** (G1), apps/gateway/src/relay/relay-runtime.ts / relay-config-store.ts / relay-password.ts / relay-tenant-store.ts (G1) — if you need a tenant-store counting helper that does not exist, describe it under 需要指挥官处理 and use an inline query in your own file meanwhile; packages/shared/src/auth/** and apps/gateway/src/auth/user-key-persistence.ts / key-log-store.ts (G3); apps/fe/**.
Baselines: gateway 4141, shared 621, api-client 201 (+5 pre-existing tsc errors), app 798 (+1 known env failure).
## Common rules (apply to every task)

- Repo: Bun-only TypeScript monorepo at /Users/konata/code/tmex-r24 (git worktree, branch feat/round24-relay-local-role, base = main 1.1.23). Bun is /opt/homebrew/bin/bun (if not on PATH, read ~/.zshrc). Runtime code runs on Bun 1.3.x; only packages/app's install CLI stays Node-compatible.
- OTHER AGENTS ARE EDITING THIS SAME WORKTREE IN PARALLEL. Touch ONLY the files listed in your scope (plus new files you create inside your scope directories). Do not reformat or "clean up" files outside your scope. If a change outside your scope is unavoidable, do NOT make it — describe it precisely in your result file under "需要指挥官处理".
- NO git operations at all (no add/commit/stash/checkout/reset/worktree). The commander commits.
- Do not run `bun install`, do not edit lockfiles or package.json dependencies unless your scope says so.
- Do not touch generated files: packages/shared/src/i18n/resources.ts, packages/shared/src/i18n/types.ts, packages/shared/src/i18n/locales/generated/*, resources/fe-dist/*, dist/*. i18n keys live in packages/shared/src/i18n/locales/{zh_CN,en_US,ja_JP}.json (all three must be updated together, same key set) and are regenerated with `bun run build:i18n` at the repo root (you MAY run that if your scope includes i18n changes). Edit only the sub-objects named in your scope; other agents edit other sub-objects of the same JSON files.
- Copy (UI text) rules: read /Users/konata/code/tmex-copy-guidelines.md before writing any user-facing text. Key points: "Hub" means hub role; "中继" means the relay role ONLY; "本机" not "这台机器"; no second person; full-width Chinese punctuation.
- Never touch the production tmex service (port 9883, ~/Library/Application Support/tmex) and never touch a tmux session named `tmex`. Any temporary gateway instance must set TMEX_TMUX_SOCKET to an isolated socket (e.g. tmex-r24-<task>) and use ports other than 9883/9663/19883/19663. Never run `tmux kill-server` on the default socket.
- Do not run the Playwright e2e suite (apps/fe/tests) — the commander runs it. In apps/fe, unit tests are `bun test src/`.
- Code style: TypeScript, biome. No unnecessary comments; comments only for genuinely complex logic, written in Simplified Chinese. Identifiers in English. Follow patterns in neighbouring files. Keep every file under 600 lines (complexity gate; `bun run lint` at repo root runs biome + the gate). Do not add entries to the complexity allowlist. When a file you must touch is already near 600 lines, split it (moving code into new files inside your scope is fine).
- Verify before finishing: (1) `bunx tsc --noEmit -p <package>` for each package you touched must not add errors versus baseline (baseline: packages/stores 1, packages/theme 9, packages/api-client 5 pre-existing errors; everything else 0); (2) `bun test` inside each touched package (apps/fe: `bun test src/`) must pass — baselines: gateway 4141, shared 621, app 798 (+1 known env failure in scripts/build-runtime.test.ts), fe 1883, ws-client 392, stores 411, panels 911, ui 370, api-client 201, terminal-ui 394, theme 52; (3) `bunx biome check <your files>`. macOS has no `timeout` command; bun test summary lines carry ANSI colours.
- Write tests for new behaviour (bun test, colocated `*.test.ts(x)`). Deterministic; no sleeps over 100 ms.
- Never hardcode credentials in scripts or tests beyond obvious fixture values.
- When completely done, write your result report (Simplified Chinese, concise, technical) to the ABSOLUTE result path given in your task — list files changed, tests added, verification output summary, anything the commander must handle — and only then exit. The result file must be the LAST thing you write.
